# Adaptive limiter seeds — LOCAL-LLM-EXTRACTION-V1 (2026-08-29).
# Static numbers are seeds + ceilings; adaptive=true lets the controller
# move the effective limit within [min, max] at runtime (AIMD: +1 per 8
# clean successes, x0.5 on 429/503/timeout, Retry-After honored).
# Keyed per (provider, api_key) — multi-key pools scale linearly.
providers:
  mlx_local:
    kind: concurrency      # local bottleneck = GPU/VRAM concurrency
    init: 2
    min: 1
    max: 4
    adaptive: true
  ollama_cloud:
    kind: rate             # cloud bottleneck = rate (RPM + TPM)
    rpm: 120               # seed bucket; header-sync refines it live
    tpm: 200000
    conc_cap: 18           # CEILING pinned just UNDER the measured provider
                           # edge (~19-20 concurrent triggers 429s). The
                           # 32-era probe policy found the edge but paid for
                           # every probe: burst past 19 -> 429 -> halve to
                           # ~5 -> minutes climbing back (receipts 2026-08-30:
                           # eff 5->19, 21 lifetime decreases). Capping at 18
                           # trades a sliver of peak concurrency for zero
                           # 429 storms. The found value still persists in
                           # llm_controller_state; raise the cap only with a
                           # measured reason (new provider tier, second key).
    min: 2
    max: 18
    adaptive: true
    use_headers: true      # throttle proactively from rate-limit headers
    init: 3                # AIMD starts LOW and climbs to the ceiling on
                           # clean successes — that is what makes it live
  # Per-ENDPOINT seeds (EXTRACTION-POOL): a pool endpoint whose name
  # matches a key here gets its own seed; otherwise it inherits
  # ollama_cloud. AIMD still governs at runtime (x0.5 on 429/503,
  # Retry-After honored, header sync) — these are starting buckets and
  # ceilings, not hard schedules.
  nvidia:                  # owner 2026-08-30: dedicated latent/enrichment
    kind: rate
    family: nvidia
    rpm: 36                # ~35-38 RPM initial per owner directive
    tpm: 150000
    conc_cap: 4            # worker-controlled concurrency; small ceiling
    min: 1
    max: 4
    init: 2
    adaptive: true
    use_headers: true
  openrouter1:             # OpenRouter paid key — mistral-small-2603 (Mistral
    kind: rate             # first-party endpoints); no published RPM: seeded
    family: openrouter     # conservatively, header ceiling adoption grows it
    rpm: 60
    tpm: 500000
    conc_cap: 4
    min: 1
    max: 4
    init: 2
  openrouter2:             # OpenRouter paid key — ministral-14b-2512
    kind: rate
    family: openrouter
    rpm: 60
    tpm: 500000
    conc_cap: 4
    min: 1
    max: 4
    init: 2
  nvidia2:                 # second UNLINKED account — own separate rate
    kind: rate
    family: nvidia
    rpm: 36
    tpm: 150000
    conc_cap: 4
    min: 1
    max: 4
    init: 2
    adaptive: true
    use_headers: true
  gemini1:              # AI Studio account key 1 — own bucket
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 3
    min: 1
    max: 3
    init: 1
  gemini2:              # AI Studio account key 2 — own bucket
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 3
    min: 1
    max: 3
    init: 1
  gemini3:              # AI Studio account key 3 — own bucket
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 3
    min: 1
    max: 3
    init: 1
  gemini4:              # AI Studio account key 4 — own bucket
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 3
    min: 1
    max: 3
    init: 1
  gemini5:              # AI Studio account key 5 — own bucket
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 3
    min: 1
    max: 3
    init: 1
  gemini6:              # AI Studio account key 6 — own bucket
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 3
    min: 1
    max: 3
    init: 1
  gemini1b:            # key 1 @ 3.5-flash-lite — own per-model quota
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 3
    min: 1
    max: 3
    init: 1
  gemini2b:            # key 2 @ 3.5-flash-lite — own per-model quota
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 3
    min: 1
    max: 3
    init: 1
  gemini3b:            # key 3 @ 3.5-flash-lite — own per-model quota
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 3
    min: 1
    max: 3
    init: 1
  gemini4b:            # key 4 @ 3.5-flash-lite — own per-model quota
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 3
    min: 1
    max: 3
    init: 1
  gemini5b:            # key 5 @ 3.5-flash-lite — own per-model quota
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 3
    min: 1
    max: 3
    init: 1
  gemini6b:            # key 6 @ 3.5-flash-lite — own per-model quota
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 3
    min: 1
    max: 3
    init: 1

  openrouter3:             # OpenRouter SECOND key — qwen/qwen3.7-flash (reasoning off);
    kind: rate             # own account = own bucket; seeded like openrouter1
    family: openrouter
    rpm: 60
    tpm: 500000
    conc_cap: 4
    min: 1
    max: 4
    init: 2
  openrouter5:             # OpenRouter THIRD key — mistral-small-24b-2501, enrichment pin only
    kind: rate
    family: openrouter
    rpm: 60
    tpm: 500000
    conc_cap: 4
    min: 1
    max: 4
    init: 2
  compiler1:            # CHAT-COMPILER-LANES-V1: key 1 @ gemini-3.1-flash-lite, chat_compiler pin only
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 2
    min: 1
    max: 2
    init: 1
  compiler2:            # CHAT-COMPILER-LANES-V1: key 2 @ gemini-3.1-flash-lite, chat_compiler pin only
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 2
    min: 1
    max: 2
    init: 1
  compiler3:            # CHAT-COMPILER-LANES-V1: key 3 @ gemini-3.1-flash-lite, chat_compiler pin only
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 2
    min: 1
    max: 2
    init: 1
  compiler4:            # CHAT-COMPILER-LANES-V1: key 4 @ gemini-3.1-flash-lite, chat_compiler pin only
    kind: rate
    family: gemini
    rpd: 500
    rpm: 15
    tpm: 250000
    conc_cap: 2
    min: 1
    max: 2
    init: 1
  compiler_alt:          # COMPILER-CROSS-FAMILY-V1: OpenRouter mistral-small-2603, chat_compiler pin only
    kind: rate
    family: openrouter
    rpm: 60
    tpm: 500000
    conc_cap: 2
    min: 1
    max: 2
    init: 1
  profile_groq1:       # DOCUMENT-PROFILE-V1 pool tier 0: dedicated Groq account 1, groq/compound (provider limits RPM 30 / RPD 250 / TPM 70k). The limiter is per PROCESS (threading locks): with six profile slots each row is ONE slot's budget on ITS key (lane offset) — rpm 2 / conc 1: measured 2026-09-07 with the rate-limit headers, ONE profile request through compound = 12 internal model calls (11 llama-4-scout router steps + 1 gpt-oss-120b answer), 38.5k internal tokens, 40 s, while the key's own TPM window is charged only ~3.7k and RPD 1 — the 429s come from the internal models' budgets, so ~1–2 compound requests per key per minute is the safe cadence (AIMD halves on 429/503).
    kind: rate
    family: groq_acct_1
    rpd: 230
    rpm: 2
    tpm: 60000
    conc_cap: 1
    min: 1
    max: 2
    init: 1
  profile_groq2:       # DOCUMENT-PROFILE-V1 pool tier 0: dedicated Groq account 2, groq/compound (provider limits RPM 30 / RPD 250 / TPM 70k)
    kind: rate
    family: groq_acct_2
    rpd: 230
    rpm: 2
    tpm: 60000
    conc_cap: 1
    min: 1
    max: 2
    init: 1
  profile_groq3:       # DOCUMENT-PROFILE-V1 pool tier 0: dedicated Groq account 3, groq/compound (provider limits RPM 30 / RPD 250 / TPM 70k)
    kind: rate
    family: groq_acct_3
    rpd: 230
    rpm: 2
    tpm: 60000
    conc_cap: 1
    min: 1
    max: 2
    init: 1
  profile_groq4:       # DOCUMENT-PROFILE-V1 pool tier 0: dedicated Groq account 4, groq/compound (provider limits RPM 30 / RPD 250 / TPM 70k)
    kind: rate
    family: groq_acct_4
    rpd: 230
    rpm: 2
    tpm: 60000
    conc_cap: 1
    min: 1
    max: 2
    init: 1
  profile_groq5:       # DOCUMENT-PROFILE-V1 pool tier 0: dedicated Groq account 5, groq/compound (provider limits RPM 30 / RPD 250 / TPM 70k)
    kind: rate
    family: groq_acct_5
    rpd: 230
    rpm: 2
    tpm: 60000
    conc_cap: 1
    min: 1
    max: 2
    init: 1
  profile_groq6:       # DOCUMENT-PROFILE-V1 pool tier 0: dedicated Groq account 6, groq/compound (provider limits RPM 30 / RPD 250 / TPM 70k)
    kind: rate
    family: groq_acct_6
    rpd: 230
    rpm: 2
    tpm: 60000
    conc_cap: 1
    min: 1
    max: 2
    init: 1
  profile_fallback_gemini1:   # DOCUMENT-PROFILE-V1 fallback 1: gemini-3.1-flash-lite on GEMINI_API_KEY_5 (own key)
    kind: rate
    family: gemini
    rpd: 400
    rpm: 8
    tpm: 200000
    conc_cap: 2
    min: 1
    max: 2
    init: 1
  profile_fallback_gemini2:   # DOCUMENT-PROFILE-V1 fallback 1: gemini-3.1-flash-lite on GEMINI_API_KEY_6 (own key)
    kind: rate
    family: gemini
    rpd: 400
    rpm: 8
    tpm: 200000
    conc_cap: 2
    min: 1
    max: 2
    init: 1
  profile_fallback_openrouter:   # DOCUMENT-PROFILE-V1 fallback 2: OpenRouter mistral-small-2603 on OPENROUTER_API_KEY_2 — sees only earlier-tier failures
    kind: rate
    family: openrouter
    rpm: 20
    tpm: 300000
    conc_cap: 1
    min: 1
    max: 1
    init: 1
  map_groq1:       # PARENT-MAP compound-mini lane; SHARES account 1's budget (GROQ_API_KEY_1) with profile_groq1 via groq_accounts (S7a)
    kind: rate
    family: groq_acct_1
    rpd: 230
    rpm: 2
    tpm: 60000
    conc_cap: 1
  map_groq2:       # PARENT-MAP compound-mini lane; SHARES account 2's budget (GROQ_API_KEY_2) with profile_groq2 via groq_accounts (S7a)
    kind: rate
    family: groq_acct_2
    rpd: 230
    rpm: 2
    tpm: 60000
    conc_cap: 1
  map_groq3:       # PARENT-MAP compound-mini lane; SHARES account 3's budget (GROQ_API_KEY_3) with profile_groq3 via groq_accounts (S7a)
    kind: rate
    family: groq_acct_3
    rpd: 230
    rpm: 2
    tpm: 60000
    conc_cap: 1
  map_groq4:       # PARENT-MAP compound-mini lane; SHARES account 4's budget (GROQ_API_KEY_4) with profile_groq4 via groq_accounts (S7a)
    kind: rate
    family: groq_acct_4
    rpd: 230
    rpm: 2
    tpm: 60000
    conc_cap: 1
  map_groq5:       # PARENT-MAP compound-mini lane; SHARES account 5's budget (GROQ_API_KEY_5) with profile_groq5 via groq_accounts (S7a)
    kind: rate
    family: groq_acct_5
    rpd: 230
    rpm: 2
    tpm: 60000
    conc_cap: 1
  map_groq6:       # PARENT-MAP compound-mini lane; SHARES account 6's budget (GROQ_API_KEY_6) with profile_groq6 via groq_accounts (S7a)
    kind: rate
    family: groq_acct_6
    rpd: 230
    rpm: 2
    tpm: 60000
    conc_cap: 1
  # SILICONFLOW-EXTRACTION-LANES-V1 (2026-09-08): 3 INDEPENDENT accounts (own
  # balances), Qwen/Qwen3-8B, firing EXCLUSIVELY into the unpinned `extract`
  # (graph) ring. kind=CONCURRENCY, not rate — MEASURED single-account probes
  # (2026-09-08) found SiliconFlow exposes NO rate-limit headers (use_headers is
  # a no-op) and returns NO 429/Retry-After even at 48 trivial-concurrent; the
  # ONLY failure mode is ReadTimeout under heavy-request latency saturation.
  # Heavy bursts (prod system prompt, max_tokens=2000, enable_thinking:false):
  #     N=16 -> 16/16 clean       N=24 -> 24/24 clean (~32 req/min, highest CLEAN)
  #     N=32 -> 30/32 (2 ReadTimeouts, still no 429) = tail saturates here.
  # So ceiling max=24 (highest measured-clean per account), seed init=12 (climb,
  # don't cold-open at the edge during a slow provider window). AIMD halves on
  # timeout/503 and climbs +1/8-clean back to 24. NO `family` -> the 3 accounts
  # are fully independent lanes (a backoff on one never cools the others), which
  # is the whole point of 3 separate balances. Raise max only with a NEW measured
  # burst. 3 x 24 = 72 aggregate concurrent graph-extraction slots.
  siliconflow1:
    kind: concurrency
    init: 12
    min: 2
    max: 24
    adaptive: true
    use_headers: false
  siliconflow2:
    kind: concurrency
    init: 12
    min: 2
    max: 24
    adaptive: true
    use_headers: false
  siliconflow3:
    kind: concurrency
    init: 12
    min: 2
    max: 24
    adaptive: true
    use_headers: false
  compiler_alibaba_qwen:   # CHAT-COMPILER pool: Alibaba token-plan compatible-mode qwen3.8-flash (shared token-plan account)
    kind: rate
    family: alibaba
    rpm: 15
    tpm: 500000
    conc_cap: 2
    min: 1
    max: 2
    init: 1
    adaptive: true
  compiler_alibaba_deepseek:   # CHAT-COMPILER pool: Alibaba token-plan deepseek-v4-flash-0731 (SHARES token-plan account via family alibaba)
    kind: rate
    family: alibaba
    rpm: 15
    tpm: 500000
    conc_cap: 2
    min: 1
    max: 2
    init: 1
    adaptive: true
  compiler_ollama_gemma:   # CHAT-COMPILER pool: local Ollama daemon gemma4:31b-cloud (OpenAI-compat, no-auth dummy key)
    kind: rate
    family: ollama
    rpm: 30
    tpm: 500000
    conc_cap: 2
    min: 1
    max: 2
    init: 1
    adaptive: true
  map_fallback_openrouter:   # PARENT-MAP fallback: OpenRouter mistral-small-2603 (OPENROUTER_API_KEY_3), MAP DSL, fires only on groq-lane failure
    kind: rate
    family: openrouter
    rpm: 60
    tpm: 500000
    conc_cap: 2
    min: 1
    max: 2
    init: 1
    adaptive: true
