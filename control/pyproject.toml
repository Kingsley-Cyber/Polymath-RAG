[project]
name = "polymath-control"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
    "polymath-shared",
    "psycopg[binary]>=3.1,<4",
]

[tool.setuptools.packages.find]
include = ["control*"]
