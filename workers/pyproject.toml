[project]
name = "polymath-workers"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
    "polymath-shared",
    "psycopg[binary]>=3.1,<4",
]

[project.optional-dependencies]
extraction = ["gliner>=0.2", "spacy>=3.7,<4"]

[tool.setuptools.packages.find]
include = ["workers*"]
