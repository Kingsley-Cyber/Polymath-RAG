[project]
name = "polymath-orchestrator"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
    "polymath-shared",
    "fastapi>=0.115,<1",
    "uvicorn>=0.30,<1",
]

[tool.setuptools.packages.find]
include = ["orchestrator*"]
