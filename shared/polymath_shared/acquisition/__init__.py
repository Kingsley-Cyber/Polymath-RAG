"""Research acquisition: Polymath reads permitted web pages FOR a connected harness (AUTORESEARCH-SOURCES-AND-HARNESS-V1, slice R8).
`service` holds the contract and the policy (owner only, an open research step, a fixed read-only catalog, the action's budget);
`opencli` is the one backend (OpenCLI's browser bridge on the host that holds the owner's browser)."""
