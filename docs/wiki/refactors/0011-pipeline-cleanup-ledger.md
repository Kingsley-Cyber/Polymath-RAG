---
triggered_by: ADR-0016 (lexical-role realignment) + Phase 6 frozen-I4 measurement
status: in_progress
last_reviewed: 2026-08-18
last_touched: 2026-08-18
---

# Refactor 0011: pipeline cleanup ledger (live)

> **Active plan:** `docs/wiki/plans/REFERENTIAL-ADMISSION-V2-PLAN.md`
> — read REVISION 1 first. Rows below feed its phases.

A running list of known defects, stale mechanisms, and unretired
dependencies found while measuring the extraction pipeline. This entry is
**live**: add rows as issues are found, move them to DONE with evidence
when closed. The wiki worm lists this entry until every row is closed.

Scope note: rows here are *observed*, not speculative. Each carries the
evidence that produced it. A row that says AUDIT has not been verified
either way and must not be treated as a finding.

## DONE — closed this cycle

| # | Area | Issue | Evidence | Fix |
|---|---|---|---|---|
| 1 | observability | `trace_event_id` is a content hash and the envelope omitted the relation pipeline, so legacy_v1 and kimi_v1 runs of the same corpus at the same pack produced identical ids; the second arm's events were dropped by `ON CONFLICT DO NOTHING`. An A/B was unreadable from the trace table. | A3 recorded 0 `FACT_ACCEPTED` in the table while its own stage artifact showed 23 | `relation_pipeline` added to `extraction_contracts()`; pinned by `test_envelope_distinguishes_extraction_arms` |
| 2 | observability | `TraceCollector.flush()` returned `len(rows)` (attempted) not the inserted count, so stage artifacts over-reported what they wrote. | artifact claimed `events_written: 148` after dedup dropped most | returns `cur.rowcount` |
| 3 | tooling | `scripts/trace_report.py` filtered only on `run_id`. run_ids are deterministic, so a run_id identifies a DOCUMENT, not one execution — every report silently unioned every configuration ever run against that document. | one run_id's waterfall mixed 5 configs over 2 days (807 events vs 70 for the newest) | scopes to newest extraction contract, prints the scope, `--all` opts into the union |
| 4 | extraction | `trig_head` was assigned only inside `if tokens:` but read unconditionally when building `LexicalSemanticEvidence`. Python loop variables outlive their block, so a token-less sentence silently reused the PREVIOUS sentence's head token — no exception, wrong data. | code inspection during Phase 5 wiring | reset per evidence; reused instead of recomputed |
| 5 | extraction | `binding_source` was a bare `str` on three paths and a `BindingSource` enum on two; both recall branches had identical if/else arms. | `kimi_candidates.py` inspection | normalized to the enum |
| 6 | observability | `TYPE_PRECHECK_IMPOSSIBLE` was emitted but never registered in `ALL_CODES`. | Phase 5 audit | renamed on emission to plan-specified `TYPE_PRECHECK_FAIL`; old code retained in the registry so historical rows still validate |

## OPEN — measured, awaiting a named gate

| # | Area | Issue | Evidence | Lever |
|---|---|---|---|---|
| 7 | chunking | `legacy_v1` packs sentences greedily by character count with no semantic boundary and keeps markdown headings in chunk BODY text, fusing a heading with the first body sentence and breaking trigger localization. | I4R fn-waterfall class B = 4/14 FN | `semantic_v2` exists and fixes it (+2 TP, R 0.462 -> 0.538) but costs precision (0.706 -> 0.560) and regresses the envelope gate 7/8 -> 6/8. NOT qualified. |
| 8 | admission | Every one of A5's 11 FPs is a span-identity failure — 4 are paired FN+FP on the SAME fact differing only in boundary, 3 more are contractions, 2 unresolved definite descriptions, 2 generic plurals. Zero are wrong-predicate or wrong-direction. | A5 `fp_details` | needs a span-boundary/identity admission outcome; no current outcome class can express "relation right, span identity wrong" |
| 9 | admission | The admission contract is real but split across three modules with three vocabularies: `entity_admission.py` (4 classes), `neo4j_eligibility.py` (fact eligibility), compiler decisions (ACCEPT/QUALIFY/REJECT/UNSUPPORTED/AMBIGUOUS). No single object states a candidate's outcome and its reason. | code inspection | unified admission-outcome object |
| 10 | config | `rescue` defaults `off` and `syntax_provider` defaults `disabled`. The published I4 acceptance number (P 0.500) was measured with rescue off. | A2 vs documented I4: rescue on is worth +0.206 P / +0.077 R | promotion decision, not a code change |
| 11 | extraction | With spaCy fully live, only 55.4% of bindings are UD_PRIMARY; 43.2% still fall through to BOUNDED_RECALL. The realigned lane drives half its own traffic. | A3 trace, 691 events | unknown whether spaCy fails to parse or entity->UD-head mapping fails to connect — one query away |
| 12 | extraction | PropBank rolesets are decorative, not operational: 38 candidates, 25 `ROLE_NO_ROLESET`, 11 ARG0, 7 ARG1, 0 ARG2 (~29% engagement). Directive invariant I5 is measurably NOT satisfied. | A3 trace | roleset coverage in the compiled lexicon |
| 13 | vocabulary | GLiNER label-alias mechanism (`semantic-query-policy-v2`) is implemented and preserved but NOT qualified; v1 identity is production. Label DESCRIPTIONS (as opposed to short aliases) are untested, and are constrained by the measured multi-label dilution effect (Kubernetes 0.929 single-label -> 0.672 with two labels). | GLINER-QUERY-VOCAB-v2 report | a v3 vocabulary-DATA gate through `POLYMATH_QUERY_POLICY`, single-label rescue regime only |

## OPEN — defects found, not yet fixed

| # | Area | Issue | Evidence |
|---|---|---|---|
| 14 | observability | `trace.count("candidates_compiled", len(candidates) if 'candidates' in dir() else 0)` reports only the LAST slice's candidate count, not the document total. | artifacts reported 0/0/1/0/2 while the run created 38 candidates |
| 15 | storage | `facts` has no run or corpus column; rows are reachable only through `evidence -> documents` (ON DELETE CASCADE). Orphaned fact rows accumulate and are never collected. | 448 fact rows total, 16 reachable for the live corpus |
| 16 | control plane | A stage ticket that exhausts its attempts (3) is left `failed` with no re-arm path; the run sits in `reconciling` indefinitely. | `run_380c43d6…` stuck ~2h after an extract `NameError`, cleared only by a corpus wipe |
| 21 | canonicalization | The canonicalize stage produced 39 clusters from 39 entities on the I4 corpus — it merged NOTHING. Surface fragments of the same real-world thing stay as separate graph nodes: `corval` + `corval logistics`; `freightnet` (Organization) + `freightnet routing platform` (Technology); `invoicing system` + `quickscale invoicing system`. | `canonical_entities`/`canonical_memberships` 1:1 for corpus `i4-fresh-acceptance-v1` |
| 22 | extraction | Definite-description resolution covers the SUBJECT slot only, so an object-slot back-reference cannot bind. `causes(harbor terminal -> billing delays)` is lost because the sentence says "the delays"; both endpoints were discovered (0.928 / 0.556) and `causes` is in the 28-predicate inventory. | doc 05 single-document audit |
| 23 | eval methodology | `verify_i4.py` writes every run to the SAME `eval/i4/evidence/evidence.json`, overwriting the previous arm. Aggregate scores survive only in whatever a human copied out; per-fact `fp_details`/`fn_details` for earlier arms are unrecoverable, so no arm-over-arm fact-level diff is possible after the fact. | A1-A4 per-fact detail lost; only A5 survives on disk |
| 24 | admission | `dr.` is admitted as a standalone entity under `semantic_v2`, alongside `dr. amara osei`. A bare honorific is never a referable entity. | doc_audit VAR-CHUNK fragment pairs |
| 25 | naming | `arm` means two unrelated things in repo evidence: an EXPERIMENT arm (one full configuration, `gliner_query_vocab_v2/REPORT.md`) and a TRIGGER arm (a predicate's verbs/nouns/multiword lexical list, `i4r/REPORT.md`, capability matrix). A sentence like "the verb arm regressed in both arms" is unparseable. | grep `arm` across `eval/*/REPORT.md` |
| 26 | naming | `POLYMATH_RELATION_PIPELINE` is not a pipeline. It swaps the candidate generator + compiler entry point INSIDE the single `extract` stage; the other seven stages are untouched. The name implies a whole-pipeline replacement and oversells the blast radius of `kimi_v1`. | `extract_worker.py` dispatch at the `compile_relation*` call site |
| 27 | lexical resources | VN/PB/FN/SemLink are vendored, compiled and wired, but sparse at runtime. Of 38 kimi-arm candidates: roleset 13, FrameNet 17, VerbNet 14, **SemLink CONSISTENT only 2** (33 UNAVAILABLE). Compiled lexicon cites a VerbNet class for just 8 of 28 predicates. Legacy `compile_relation` never reads them at all (no reference in its body). Invariant I7 ("VN/PB/FN/SemLink are active compiler inputs") holds only in kimi_v1, and only for ~1/3 of candidates. | doc_audit trace + `compiled_lexical-v1.3.0.json` rule_coverage |
| 28 | lexical resources | Stratified test: lexical evidence changes NO decision at any support level. 38 kimi candidates bucketed by resource coverage — legacy and kimi outcomes differ in 0 of 5 buckets, including the 2 candidates with full PB+VN+FN+SemLink. Expanding coverage is therefore NOT a justified precision lever until the compiler can demonstrably overturn a decision on lexical grounds. | `eval/doc_audit/REPORT.md` Finding 5 |
| 29 | architecture | **I3R typed trigger contract forecloses ADR-0016 converging evidence.** `_trigger_matches` (compiler.py:630-633) admits only the predicate that localized the trigger, so `matches` is length <=1 before stage-2c lexical scoring runs — nothing to re-rank. The only other path, the all-four-negative veto (`score > (-1,-1,-1,-1)`, lexicographic), is unreachable given 8/28 predicates cite VN and SemLink resolves for 2/38 candidates. Empirically: AMBIGUOUS_PREDICATE 0, lexical_evidence_mismatch 0, COMPILER_REJECTED 0. Invariant I7 ("VN/PB/FN/SemLink are active compiler inputs") is FALSE as shipped. Neither decision is wrong on its own; the collision is unreconciled and ADR-0016 did not notice it. | compiler.py inspection + kimi-arm trace |
| 30 | tooling | `repo_guard.IGNORED_NAMES` matched `.venv` exactly, so any sibling virtualenv (`.venv-gliner2`, and the `.venv-relex` convention used in polymath_v3.3) was reported as thousands of undeclared repository files. Fixed with an `IGNORED_DIR_PREFIXES = ('.venv',)` prefix rule in `repo_guard.is_ignored` + matching fix in `agent_preflight.check_python_syntax`. | GLINER-SPEED-V1 install |
| 31 | model choice | `gliner2-base-v1` matches production GLiNER latency exactly (34.2 vs 33.8 ms/slice) while improving entity typing 16/16 vs 13/15 and exact-span recall 16 vs 15 on the 2-doc benchmark. Typing drift was a named I4 FP driver. NOT qualified — needs the full frozen I4 gate under the admission and canonicalization contracts — but it is the first free improvement candidate found. | `eval/gliner_speed_v1/REPORT.md` |
| 32 | admission | **Two-token rule admits descriptive class-nouns.** `_classify` refuses a generic head only when `len(discriminative) <= 1`, then admits anything with `>= 2` content tokens as CORPUS_SCOPED. So `invoicing system`, `container platform`, `pricing model`, `billing delays`, `regional hauls`, `regional dispatchers`, `two new surgeons` all get durable identity. The rule counts TOKENS; it never asks whether any token is a name rather than a kind. | `decide()` over the observed surfaces |
| 33 | admission | `GENERIC_HEAD` is a 32-word hand list, singular-only, no lemmatization. `worker`/`workers`, `delay`/`delays`, `terminal`, `haul` are absent, so the generic-head guard never fires for them — `workers` alone is refused only by the `insufficient_referential_specificity` fallback, and `background workers` is admitted outright. | `'workers' in GENERIC_HEAD == False` |
| 34 | model choice | **Supersedes row 31.** gliner2-base-v1 confidence saturates (`delays` 1.000, `regional` Location 0.966) so threshold 0.5 is inert, and it mistypes core entities (`Corval`->Person 0.998, `Corval Freight`->Product 0.990, `FreightNet` as both Product and Technology). The 16/16 typing figure in the speed report scored MATCHED GOLD SPANS ONLY and did not see this. gliner2 is NOT a free improvement candidate. | `eval/gliner_speed_v1/arms/C.json` raw proposals |
| 35 | admission | **Row 32's proposed fix was measured and REVERTED.** entity-admission-v1.2 (refuse any surface with no proper noun / acronym / version id) gave I4 P 0.706 -> 0.750 with zero recall cost, but scored 35/44 on the frozen admission gold (v1.1 = 44/44), refusing `vector index`, `transactional outbox`, `language model`, `storage layer`, `retrieval system`, `workflow authority` — all SCOPED by design. I4 contains none of that surface class, so I4 alone would have shipped the regression as a win. | `eval/admission/admission_gold.json` scored against both policies |
| 36 | admission | **MENTION_ONLY does not suppress facts.** It blocks Neo4j projection only (parked-in-Postgres by design), so reclassifying entities cannot remove junk facts — `member_of(regional dispatchers [MENTION_ONLY] -> ...)` still emits. The mechanism that DOES suppress is the referential gate in `candidates.py:341`, which abstains when any endpoint is MENTION_ONLY — but `_REFERENTIAL_FRAMES = {"association"}` scopes it to 1 of 18 evidence classes. **Extending that set is the real lever**: per-class, testable incrementally, and it touches admission not at all so the 44/44 gold is unaffected. | `candidates.py:60,272,341` + VAR-ADMIT arm |
| 37 | eval methodology | **CLOSED by PHASE 0** (see work log 2026-08-18-referential-admission-v2). | `eval/admission/qualify_admission.py` imports a STALE FORK (`eval/admission/entity_admission.py`, 194 lines, pinned v1.1) instead of the 228-line production module, so the admission gate's own qualification does not test production code. It also OVERWRITES `artifacts/admission_metrics.json` on every run. Re-running it today yields accuracy 0.773 while the committed artifact records 0.909 under `entity-admission-v1` — the harness no longer reproduces its own recorded evidence. | ran the harness; restored the artifact from git |
| 38 | admission | **PREDICATE QUALIFIED by IDENTITY-PRECISION-V2 (not yet wired to production).** `entity-admission-v1.1` admits pronouns and wh-words as GLOBAL identities on conversational prose.** On a 500-word transcript: `I`, `That`, `What`, `Workers`, `Earlier versions`, `Two documents`, `Exact`, `Every accepted fact` all -> GLOBAL / `proper_name_identity`. 9 of 13 identity admissions were false (69%). The `sentence_initial` guard does not help: it only fires when the first token is in the 32-word `GENERIC_HEAD` list, and none of these are. The policy scores 55/55 on its own gold because that gold is technical noun phrases from one self-referential corpus — it contains no pronoun, no wh-word, and no sentence-initial quantifier/determiner. | `/Users/king/Downloads/e/04_transcript_local_rag_build.md` probe |
| 39 | admission | Boundary defect on the same document: `two John Smith` -> GLOBAL (quantifier absorbed into the name). Related to rows 8/32 but distinct — a leading numeral is not trimmed before proper-name evidence is read. | same probe |
| 40 | admission | Row 38 reproduces on ACADEMIC prose, and worse: on a 500-word psychology text, `Researchers`, `One influential account`, `Several laboratory studies`, `These findings`, `Performance`, `This`, `Worked examples` all -> GLOBAL, and **`When attention shifts` — a subordinate CLAUSE — was admitted as a global entity.** ~10 of 12 identity admissions false. The defect is register-general, not transcript-specific. | `/Users/king/Downloads/e/01_psychology_working_memory.md` probe |
| 41 | concept evidence | **CLOSED by CONCEPT-DEFINITION-COVERAGE-V1.** ~~Misses hedged academic definitions.~~ The copular pattern requires an article IMMEDIATELY after the copula, so `X is the Y used to...` hits but `X is often described as the Y`, `X can be defined as the Y`, `X is generally understood as a Y` all MISS. Result: zero concepts admitted from a document entirely about `working memory` / `cognitive load`. Cross-domain fixtures used clean genus-differentia forms and did not expose this. Extending the pattern set is policy DATA work, not a rule widening. | same probe |
| 42 | discourse | **CLOSED by E4 BOUNDARY REPAIR.** ~~E4 produced a CONFIDENT WRONG resolution — the failure mode the gate explicitly forbids.** Its antecedent regex `\b(?:a|an|the)\s+([a-z][\w\- ]*?HEAD)\b` allows SPACES in the non-greedy span, so it crosses clause boundaries to reach the head word. On a real research-notes document, `the second group` resolved to `'recurring methodological issue is that sleep restriction studies often compare group'` — a 12-word fragment spanning most of a sentence — and was reported ANTECEDENT_RESOLVED, i.e. fabricated identity. The 14 frozen context fixtures never contained a head word appearing mid-clause after an indefinite article, so 14/14 passed while this was live. Owner: `discourse_reference.py` E4. | `/Users/king/Downloads/e/03_research_notes_sleep_and_attention.md` probe |
| 43 | discourse | **CLOSED by SET-PARTITION-REFERENCE-V1.** ~~E6 admitted ordinal set-partitions as bounded participants.~~ After the E4 repair, `the second group` falls through to E6 and is judged DOCUMENT_CONSTITUTED, therefore graph-eligible — 1 of 138 on the research-notes document. But an ORDINAL modifier (`the second group`, `the first case`) partitions a set; it does not name a bounded institutional actor the way `the engineering group` does. E6 requires only "recurs + discriminating modifier" and an ordinal satisfies that. NOT patched: changing E6 exceeds the authorized boundary repair. | `03_research_notes_sleep_and_attention.md` probe |
| 20 | naming | `legacy_v1` names TWO unrelated things on two different axes: a CHUNKER provider (`POLYMATH_CHUNKER`) and a RELATION PIPELINE (`POLYMATH_RELATION_PIPELINE`). A config or report saying "legacy_v1" is ambiguous without knowing which axis is meant. | `settings.chunker` default `legacy_v1`; `active_pipeline()` default `legacy_v1` |

## AUDIT — unverified, do not treat as findings

| # | Area | Question |
|---|---|---|
| 17 | dependencies | Phase H rejected *hybrid resource enrichment*. `polymath_shared/hybrid.py` is imported by `retrieval_modes.py`, but that is the production HYBRID retrieval MODE (green 10/10 in I4), which is a different mechanism with a colliding name. Confirm the rejected enrichment path is gone and rename one of the two. |
| 18 | dependencies | E5 closed with concept code "PRESERVED as non-production research infrastructure". 9 modules reference `concept`. Confirm none is on the production path. |
| 19 | dependencies | `extraction_context.py` — EXTRACTION-CONTEXT-V1 measured NOT QUALIFIED. Confirm it is inert in production. |

## Diagnostic entry points

- `scripts/trace_report.py waterfall|surface|sentence|run <id>` — per-run
  extraction funnel and per-surface causal chain. Contract-scoped as of
  row 3; always read the SCOPE line it prints.
- `eval/measure_layers.py` — layer-wise L1..L6 attribution so a low
  end-to-end number names its own cause.
- `eval/i4/verify_i4.py --phase <phase>` — frozen acceptance gates.
- `scripts/run_i4_arm.sh <pipeline> <rescue> <pack> [chunker]` — restart
  the fleet for one measurement arm.
| 44 | wiring | **CLOSED by S4a/S4b/S4c.** ~~Admission was computed independently at five inline call sites, so two representations of one span could disagree.~~ One authority now interprets each proposed span exactly once at the post-syntax, post-rescue boundary; parse records, candidates, mentions and fact endpoints all read that decision. A consumer with no entry RAISES rather than allocating a second identity. Frozen I4 improved 0.706 -> 0.750 with recall, envelope and must-not unchanged. |
| 45 | wiring | **CLOSED by S4c.** ~~Mentions were persisted from the PRE-rescue snapshot while candidates used post-rescue spans, so a mention row could describe a span no fact endpoint referred to.~~ Mention persistence moved past rescue; the persisted set is now the final proposal set. |
| 46 | identity | **CLOSED during S4c.** ~~Identity was keyed on `referential_surface`, splitting one referent in two: `the CareConnect portal` and `CareConnect portal` produced separate entity rows.~~ The envelope is an interpretation surface, not an identity key. Identity keys on the proposal surface. Locked by `test_identity_does_not_fragment_on_a_determiner`. |
| 47 | semantics | **CLOSED by HEADING-IDENTITY-PRECISION-V1.** ~~ Title-case headings manufacture proper-noun evidence: `Working Memory` admits IDENTITY/GLOBAL on POS=PROPN while `working memory` in prose admits CONCEPT/CORPUS_SCOPED. One concept, two identities, decided by capitalization. Same cause for `Attention`, `Research Notes`. Not actioned — the cutover step was closed to semantic tuning. ~~ Heading context now withdraws PROPN evidence that title-casing fully explains, while acronym/identifier/internal-capital evidence survives. `Working Memory` and `working memory` converge on one DOCUMENT_DEFINED concept. Headings are detected from chunk line structure, never from phrase shape.|
| 48 | semantics | **CLOSED by ANTECEDENT-IDENTITY-INHERITANCE-V1.** ~~ `graph_eligible()` grants durable DOCUMENT_SCOPED ids to ANTECEDENT_RESOLVED local references (`these notes` via E3, `new concept` via E4). This is inherited eligibility, not manufactured identity, so it is within contract — but whether such phrases should hold durable ids is the evaluator's call. ~~ A resolved reference now inherits the antecedent's entity_id and can no longer mint `entd_` from its own surface; with no durable antecedent it is not eligible. same referent -> same identity.|
| 49 | operations | **CLOSED by SEMANTIC-BUNDLE-WORKER-FENCE-V1.** ~~ Long-running workers held stale code across a semantic cutover and nothing detected it; the first V2 run silently measured the previous build. CP2.1 worker supervision is the conspicuous operational gap before qualification. ~~ Workers advertise `semantic_bundle`; claims require equality. The bundle now hashes authority SOURCE (`authority_code_sha256`), because the incident's change bumped no version string and the version-only bundle would not have caught it.|
| 50 | hygiene | **CLOSED by SEMANTIC-RESIDUE-RECONCILIATION-V1.** ~~ 57 orphaned determiner-prefixed `entities` rows remain from the failed intermediate runs, none referenced by any live mention. Not deleted — row deletion was not authorized. ~~ `reconcile_semantic_residue()` in verify_worker removes rows with a broken provenance chain, ordered along evidence -> facts -> entities. Applied: entities 863->728, facts 462->385, residual zero.|
| 51 | identity | **CLOSED by HARBOR-TYPE-IDENTITY-ALIGNMENT-V1.** ~~A third identity-fragmentation vector, distinct from row 47 (typography) and row 48 (reference minting): `core_type` is part of the identity key, so extractor type instability splits one referent. On the psychology document `working memory` yields `entc_d870...` [Concept] and `entc_d994...` [Technology] from identical normalized surfaces. Found during the pre-S5 regression.~~ Canonical type is now named by the qualified authority: a CONCEPT anchor keys on CONCEPT, so a provisional label cannot fragment a document-defined term. IDENTITY deliberately RETAINS provider type so Java/Apple/Mercury homonyms stay apart; type was not removed from the key globally. Psychology went 2 identities -> 1; transcript unchanged; no overmerge in the live corpus. Frozen principle: provider type describes an identity, it does not define it. |
| 52 | coverage | **CLOSED as EXPECTED ABSTENTION.** ~~ `03_research_notes_sleep_and_attention.md` now admits 0 of 40 proposals (was 3). The cascade is individually correct: rows 47/48 withdrew `Attention`, `Research Notes` (title-case headings) and `these notes` (antecedent no longer durable). Whether a document of this kind should yield zero graph entities is a coverage question, not a defect.~~ Ruled expected: a document may yield chunks, embeddings, mentions, syntax and retrieval while yielding zero canonical entities and facts. Forcing every document to contribute graph nodes would violate the precision-first design. |
| 53 | identity | **CLOSED by LAYOUT-EVIDENCE-V1 (option C).** ~~HEADING-IDENTITY-PRECISION-V1 as implemented reads heading regions from chunk text, but production chunk text carries NO newlines (the materializer normalizes them; `documents` has no text column and `source_map` holds only a whole-document entry). An ATX heading at the start of a chunk therefore marks the ENTIRE chunk as a heading region, and identity is withdrawn from every span in it. On I4: eligible 55->13, IDENTITY 48->10, and 15 of 16 facts have a MENTION_ONLY endpoint. Invisible to the I4 fact score, which compares surfaces rather than eligibility — P stayed .750 throughout. Corollary: the rule is also INERT where it was intended to work, so the `Working Memory` convergence demonstrated earlier used probe chunks that preserved newlines and does NOT currently hold in production. Introduced by me in the row-47 fix; surfaced by S5 invariant 3.~~ Layout is now DETECTED at intake on materialized source text (line structure intact), persisted to `document_layout` in source offsets, and projected character-exactly into `chunks.layout_map` by the chunker — the only component holding both coordinate systems. Chunk text, chunk ids, embeddings and Qdrant are untouched. Recovery: I4 eligible 13 -> 53, IDENTITY 10 -> 46, prose identities lost to heading context = 0, and the heading occurrence of `Corval Logistics` is still correctly suppressed while its prose occurrence is IDENTITY. Fact score improved P .750 -> .812. |
| 54 | s5 | **CLOSED by SENTENCE-SLICE-MANIFEST-V1 + S5 completion.** ~~S5 halted at invariant 3. Machinery verified: invariants 1, 2, 4, 5, 6, 9, 10 pass; re-derivation reproduces live extraction byte-identically across mentions/entities/facts/canonical memberships; replay is idempotent; fact classification UNEXPLAINED=0, endpoints unresolved=0. Halted before canonicalization/Neo4j/metrics because row 53 makes the eligibility state wrong. Residue from re-ingestion left unreconciled pending resumption.~~ Extraction now persists the interpreter's exact view (`sentence_slices`); reprocessing CONSUMES it and refuses with `MissingSliceManifest` rather than reconstructing one, because a guessed context loses or invents antecedents. All ten invariants pass. |
| 55 | hygiene | **CLOSED by GRAPH-RESIDUE-RECONCILIATION-V1.** ~~`reconcile_semantic_residue` was Postgres-only, so removing a fact whose provenance chain broke left its relationship standing in Neo4j — the graph asserting a fact the authoritative store no longer held.~~ Graph residue now reconciles from Postgres: 31 orphaned relationships and 29 orphaned entity nodes removed, residual zero. Postgres is the authority in one direction only; the graph is derived and never reconciled back. |
| 56 | qualification | **CLOSED by the ADMISSION CENSUS REGRESSION.** ~~No qualification dimension observed the shape of admission.~~ `eval/census/verify_census.py` freezes per-document mention count, anchor_kind distribution, graph-eligible count, canonical entity and canonical fact counts, with no tolerance band. Permanent principle recorded: **a fact-level P/R metric cannot certify the entity-admission layer** — row 53 parked 42 of 55 identities and left P unmoved at .750. |
| 57 | identity | **CLOSED during S5.** ~~An entity's `admission_class` was written by whichever mention reached it first: a resolved reference that INHERITED an anchor's id (row 48) restamped it with the reference's DOCUMENT_SCOPED scope, so `ent_` ids described themselves as document-scoped. Order-dependent under ON CONFLICT DO NOTHING.~~ Only the anchor that OWNS an identity may describe it; inheriting references are skipped in both writers. Verified: 0 rows where the id prefix disagrees with the class. |
| 58 | diagnosis | **CLOSED by S6A/S6B (diagnostic only).** The canonical-identity recall of .385 is now fully attributed. 52 gold endpoint instances: 38 DURABLE, 7 DISCOVERY_MISS, 4 LOCAL_REFERENCE_UNRESOLVED, 2 HEADING_SUPPRESSED, 1 SPAN_BOUNDARY, UNEXPLAINED 0. Six canonical FPs: 3 WRONG_CANONICAL_IDENTITY, 3 UNSUPPORTED_FACT, UNEXPLAINED 0. Dominant mechanism is span discovery and extent (11 of 20 residual defects); admission is behaving as designed in 6. |
| 59 | binding | **CLOSED as NOT-A-DEFECT.** ~~Argument binding was suspected as the residual precision problem.~~ Refuted by S6B: zero WRONG_ARGUMENT_PAIR, zero WRONG_DIRECTION, zero WRONG_PREDICATE, zero OVERMERGE. Three FPs that first bucketed as wrong-pair are identity-extent mismatches (right argument, shorter identity). BINDING-PRECISION-CLOSURE-V1 is not justified and was not opened. |
| 60 | discovery | **OPEN — the next bottleneck, needs a ruling.** 7 of 14 missing endpoints were never proposed by GLiNER (`Nimbus Cloud`, `radiology review board`, `Nimbus billing service`, `Nimbus Cloud platform`, `load-testing harness`, `shift scheduling model`, `quality database`), verified against the full proposal list per document. Plus 1 SPAN_BOUNDARY and 3 extent mismatches. This is provider recall/extent, upstream of every semantic gate. No action taken. |
| 61 | semantics | **OPEN — ruling, not a defect.** A proper name occurring ONLY in a document heading (`Crestline Automation`) is refused by row 47, so the document's own subject never becomes a node. The rule is correct as written; whether heading-only names should be recoverable by other means is a design decision. |
| 62 | relations | **OPEN — low priority.** 3 canonical FPs are UNSUPPORTED_FACT: relations with no gold counterpart, two of which connect endpoints that hold no durable identity and therefore never reach the graph. Bounded; revisit only if the fresh sealed set shows the same shape. |
| 63 | discovery | **FIXED by RESCUE-SPAN-PRESERVATION-V1 (A); QUALIFICATION FAILED — see row 67.** ~~PRIMARY MECHANISM, corrects row 60.~~ `rescue.py:327` — when boundary rescue speculatively WIDENS a span and GLiNER refuses the wider form, the ORIGINAL provider span is deleted (`if hit is None: continue`). `Nimbus Cloud` at 0.91 Organization is discarded because `Postmortem Review Nimbus Cloud` failed. 13 original spans deleted this way in the last I4 run (13 refused / 2 accepted). A failed speculation about a span's extent is not evidence against the span. Counterfactual (read-only): keeping the original recovers the `Nimbus Cloud` gold endpoint, loses nothing, and adds 2 other spans. NOT FIXED — needs authorization. |
| 64 | discovery | **OPEN — secondary, cross-domain scaling hazard.** Rescue re-query acceptance is coupled to PROFILE VOCABULARY SIZE. With the document's 20-label profile GLiNER returns nothing for all 7 probe phrases at threshold 0.5; with the 12 core labels, `Nimbus billing service` (0.527) and `Nimbus Cloud platform` (0.58) are accepted. Irrelevant domain labels (`Vulnerability`, `AttackTechnique`, `ProgrammingLanguage`…) dilute per-label confidence below a threshold frozen against a different vocabulary size. Rescue recall therefore DEGRADES as a profile activates more domains — the opposite of intended, and it worsens with corpus breadth. NOT FIXED. |
| 65 | discovery | **FIXED by RESCUE-SPAN-PRESERVATION-V1 (B).** ~~Shares the row 53 root cause.~~ The boundary-widening candidates are heading-contaminated noun chunks: `### Brightpath Learning`, `Postmortem Review Nimbus Cloud`, `Line Efficiency Review Crestline Automation`, `### Northvale Health Network`. Sentence splitting merges a heading with the following prose (no newline in chunk text), so spaCy noun chunks straddle the junction and manufacture bogus wide NPs. Persisted layout evidence (row 53) can now bound noun chunks at heading edges; not applied. |
| 66 | diagnosis | **CORRECTION to row 58/60.** S6A's DISCOVERY_MISS bucket measured "absent from `mentions`" but its NAME asserted "the provider never proposed the span". Those are different claims and the second is false for several cases: GLiNER proposes `Nimbus Cloud` (0.91), `load-testing harness` (0.69) and `radiology review board` (0.69); the pipeline then discards them. The attribution was structurally sound but causally overstated, and it aimed row 60 at provider recall when the dominant mechanism is span DELETION inside rescue. |
| 67 | discourse | **PARTIALLY FIXED by E4-ANTECEDENT-TYPE-COMPATIBILITY-V1; STACK STILL FAILS — see rows 69-71.** ~~BLOCKS acceptance of rows 63/65.~~ Preserving refused-widening spans gives the discourse resolver more antecedent candidates, and some resolutions are wrong. 8 identities now span two surfaces via ANTECEDENT_RESOLVED inheritance; 4 are correct (`outage`/`september outage`, `careconnect portal`/`patient portal`, `mentor engine`/`mentor assessment engine`, `invoicing system`/`quickscale invoicing system`) and 4 are not — most clearly `company` -> `raleigh` (a Location inherited by a common noun) and `assembly line` -> `crestline automation`. E4's "exactly one compatible local antecedent" is admitting type-incompatible antecedents. NOT caused by rows 63/65: the span-deletion bug was MASKING this. Removing a mask is not a regression, but the exposed defect must be settled before the gate can be accepted. |
| 68 | qualification | **RECORDED.** RESCUE-SPAN-PRESERVATION-V1 measurement. Objective MET: DISCOVERY_MISS 7->4, HEADING_SUPPRESSED 2->0, DURABLE endpoints 38->41, envelope 7/8->8/8, surface TP 13->14, R .500->.538. Both pinned adversarial cases recovered (`Nimbus Cloud`, `Crestline Automation`). Zero endpoints moved durable->non-durable; zero previously-matched facts lost. Criteria 8 and 9 VIOLATED: WRONG_CANONICAL_IDENTITY 3->6, UNSUPPORTED_FACT 3->4, plus one new OVERMERGE. Surface precision .812->.583. Not accepted; awaiting a ruling on row 67. |
| 69 | discourse | **FIXED by E3-ANCHOR-CONTINUITY-V1.** ~~The dominant wrong-inheritance mechanism, and it is E3, not E4.~~ The surviving bad merges (`assembly line`/`crestline automation`, `regional dispatchers`/`corval logistics`, `analytics team`/`crestline automation team`) all carry `discourse: E3 repeated named anchor`. E3 matches on ANY shared content word between target and anchor, with no type or head constraint. Row 67 constrained E4's co-occurrence branch and left E3 untouched, so it fixed `company` -> `raleigh` and nothing else. Not actioned — the ruling was to STOP if the regressions did not disappear. |
| 70 | discourse | **CLOSED by explicit exclusion.** ~~My E4b wiring fix revived a dead rule and it made things worse.~~ E4b retained in code with its input representation CORRECT; admission-harbor-v2 passes `enable_type_noun_anaphora=False`. Excluded, not dormant; its tests are component qualification only. ~~ Production passed `(surface, entity_id)` as `admitted_anchors` while E4b compares `ct == type_of`, so E4b never fired in production though it passed in tests. Correcting it to `(surface, core_type)` revived E4b, which then resolved `vision system` -> `Siemens PLCs` (both Technology, type-compatible, wrong referent) — a NEW overmerge. Type compatibility cannot separate two same-typed co-occurring entities. Whether to keep the wiring correct and constrain E4b, or leave E4b dormant, is a ruling. |
| 71 | qualification | **RECORDED — combined A+B+67 stack FAILS.** Required vs actual: WRONG_CANONICAL_IDENTITY <=3 -> **6**; UNSUPPORTED_FACT <=3 -> **4**; OVERMERGE =0 -> **2**. Recovery gains held (DURABLE 41, DISCOVERY_MISS 4, HEADING_SUPPRESSED 0, envelope 8/8, TP 14, R .538, zero prior TP or durable endpoints lost, UNEXPLAINED 0) but surface precision fell .812 -> .560. Per the ruling: STOP, no further rescue tuning. A+B+67 retained as unpromoted candidates. |
| 72 | qualification | **RECORDED — final stack FAILS the acceptance bar; discourse/rescue work STOPPED.** A+B+67+69 with E4b excluded. PASS: DISCOVERY_MISS 4, HEADING_SUPPRESSED 0, prior TP loss 0, **OVERMERGE 0**, wrong predicate/direction/argument-pair 0, UNEXPLAINED 0. FAIL: DURABLE 40 (>=41 required), WRONG_CANONICAL_IDENTITY 6 (<=3), UNSUPPORTED_FACT 5 (<=3). Per the ruling, no further discourse or rescue work: no E5, no new matching heuristic, no new model, no exception. |
| 73 | limitation | **KNOWN LIMITATION for this release.** The three failing criteria are NOT discourse-driven. All 6 WRONG_CANONICAL_IDENTITY are "same referent, different extent" (`crestline` vs `Crestline Automation`, `nimbus` vs `Nimbus billing service`) — the row 61 heading-as-alias/extent mechanism that was explicitly deferred, which discourse cannot reach. All 5 UNSUPPORTED_FACT are relation over-generation (row 62), 3 with non-durable endpoints that never reach the graph. DURABLE fell 41->40 because E3 tightening removed `analytics team` -> `crestline automation team`, which was itself one of the WRONG merges — the >=41 target counted a defect as coverage. The deterministic discourse design is sound for continuity and descriptive anaphora; identity EXTENT is the unsolved axis. |
| 74 | process | **CLOSED.** ~~Practical.~~ The repository is not under version control, so the unpromoted candidate stack (A+B+67+69) sits in the working tree with no branch isolating it. Semantics cannot be gated behind a runtime flag (wiring invariant 1), so "candidate, not promoted" is currently a documentary status rather than an enforced one. Resolved: the repository WAS under version control (HEAD 31212bc, branch main) — the environment metadata reporting otherwise was wrong, and the whole cycle sat uncommitted on top of it. The candidate stack is now committed on `candidate/rescue-discourse-v1-failed` (tag `v4-candidate-rescue-discourse-FAILED`) and `main` carries the reconstructed accepted state, verified to reproduce P .812 / TP 13 / FP 3, census with no divergences, and the exact S6A/S6B waterfalls. |
| 75 | robustness | **FIXED on candidate/subtoken-span-admission-v1; qualification PASSED. Not yet promoted.** ~~Latent production blocker for URL-bearing documents.~~ A provider span nested INSIDE one syntax token (`instagram` within a URL token) yields zero covering tokens, and `identity_evidence(require_syntax=True)` raises `RetryableDependencyUnavailable` although syntax IS present — the extract stage fails deterministically and retries cannot succeed. Found by ENTITY-PROVIDER-FORENSICS-V1 on calibration transcripts; no i4/smq1 document triggers it. Layer B_admission (token filter conflates "no covering tokens" with "no syntax"). Fix: the interpreter now distinguishes syntax-outage (still retryable) from a span covering no complete token (settled abstention, UNKNOWN/ABSTAINED/MENTION_ONLY, surface preserved verbatim). Qualified: 6 unit gates; the previously-crashing call completes with `instagram` abstaining; I4 state hash byte-identical (P .812 exact); smq1 stamped hashes reproduced across TWO independent re-ingests; 55-gold 1.0; suite 575. |
| 76 | interaction | **RECORDED — gate dependency discovered during row 75 qualification.** In the CURRENT accepted configuration the ledger-63 rescue deletion MASKS the sub-token crash on the probe documents: boundary widening is attempted on `instagram`/`youtube`, refused, and the original span is DELETED before admission ever sees it (verified: pre-rescue 2+2 spans, post-rescue 0). Two consequences. (1) The row-75 "deterministic production crash" claim was overstated for the current config — exposure is real only for sub-token spans that escape boundary widening. (2) SUBTOKEN-SPAN-ADMISSION-V1 is a PREREQUISITE for ever promoting the rescue-preservation fix (failed candidate A): fixing the deletion without row 75 converts silent span deletions into deterministic stage crashes. Any future revival of `candidate/rescue-discourse-v1-failed` must sequence row 75 first. |

