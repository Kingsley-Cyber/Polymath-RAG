---
change_id: PLAN-AUTHORITY-REGISTER
owner: governance
date: 2026-08-29
status: living
architecture_impact: authoritative detail extraction of the migration plan
last_reviewed: 2026-08-29
---

# PLAN AUTHORITY REGISTER — every normative detail, tagged

> Source: `/Users/king/Downloads/polymath-v4-local-migration-plan.md` rev 4.
> Every normative detail extracted atomically. Status tags:
> **DONE** (implemented + verified) · **DEVIATED** (implemented differently,
> deviation recorded + reason) · **SUPERSEDED** (readiness Directory Contract
> or later owner directive replaces it) · **MISSING** (not built — must be
> scheduled or explicitly waived) · **PARTIAL**.
> This register is the completion contract for goal mode. Update statuses as
> work lands; never delete lines.

## §0 TL;DR / design law

| # | Detail | Status |
|---|---|---|
| 0.1 | Summaries route, vocabulary translates, source chunks prove — three systems joined by IDs | DONE (architecture; vocabulary = query_policy + corpus lexicon stage) |
| 0.2 | Extraction generates vocabulary; deterministic summaries generate routing | DONE (extraction→raw ledger→lexicon stage exists; digest→summaries PARTIAL, see 4.3.6) |
| 0.3 | idempotent stage transactions; kill -9 → resume → zero re-work (P2 acceptance test) | PARTIAL (runtime property + accidental restarts passed; formal drill MISSING) |
| 0.4 | Self-aware control plane, sidecars hold no state | DONE (receipts/artifacts; no sidecar state) |
| 0.5 | Two model windows never concurrent (local); cloud exempt | DONE (only one local engine; rule enforced by design) |
| 0.6 | Providers are data, not code (registry rows + failover) | DEVIATED → limiter config + client lanes; no providers TABLE (superseded by receipts decision). Limiter carries lane/failover seeds. |
| 0.7 | One output gate for every provider | DONE (gate.py; raw-ledger provenance) |
| 0.8 | Shadow lane first; promotion only after beating baseline | DEVIATED (canary-E2E promotion gate adopted instead of full benchmark; owner to ratify) |

## §1 key architectural decisions

| # | Detail | Status |
|---|---|---|
| 1.1 | Postgres is the control plane (no new infra) | DONE |
| 1.2 | Model = process: 4B pinned sidecar `/infer_batch` (load→batches→release); 35B oMLX lifecycle | DEVIATED (batch_generate server built 2026-08-29; window lifecycle automation MISSING — manual start/stop) |
| 1.3 | Model proposes; Python validates; refuse unattested | DONE — and since 2026-08-30 (owner): in `llm_live` the validated relations ARE the facts (LLM-DIRECT-FACTS-V1, `workers/llm_direct.py`); the GLiNER-era predicate compiler / admission-harbor chain is bypassed for LLM proposals (it admitted 3 of 286 attested relations). Identity: entities by (core_type, normalized surface), facts by (predicate, subject, object) → cross-document aggregation by id; mentions carry corpus_id + doc_id. |
| 1.4 | Model sees evidence neighborhoods, never parent summary rows | DONE (build_neighborhoods) |
| 1.5 | Summaries route / vocabulary translates / chunks prove | DONE (same as 0.1) |
| 1.6 | Legacy compat: passage_concepts five buckets; extractor_version separates generations | PARTIAL (buckets ✓; **extractor_version does NOT distinguish LLM-era facts — MISSING stamp**) |
| 1.7 | Two-tier extraction: 4B dense volume runs WHOLE pass; quality tier re-extracts flagged | **SUPERSEDED by owner decision 2026-08-29: single-lane adopted** (>300KB→cloud whole-doc, ≤300KB→local; measured 6m34s). Two-tier flagged/dense machinery will NOT be built. Three-layer graph (1.17) also IGNORED by owner. Scope: wave = cyber books already in corpus (original 12); the 14 backfilled non-cyber books are held out of extraction. |
| 1.8 | Shadow lane before promotion (benchmark gate) | DEVIATED (see 0.8) |
| 1.9 | Rejected claims locked (no concurrent local windows; no parent rows to model; no LLM→Neo4j) | DONE (respected) |
| 1.10 | Cloud quality lane routed by file size; >300KB→cloud; no cap at launch | DONE (selection+dispatch enforced) |
| 1.11 | Output gate sanitize→validate→normalize→write for every provider | DONE |
| 1.12 | One self-aware entity graph; provenance per chunk (provider, model, tokens, wall, cost) | PARTIAL (tokens/wall/lane per call in artifacts ✓; cost MISSING — no price table) |
| 1.13 | Borrow, don't build: Instructor ADOPT; KGGen clustering BORROW; Docling-via-fork CANONICAL; LightRAG REJECT; thinking strip ours | PARTIAL (**Instructor: DEVIATED to direct client**; **KGGen clustering MISSING**; thinking strip ✓; Docling superseded by REQ-003) |
| 1.14 | Stateless per call; 4bit load; corpus_entities + entity_links merge ladder; 850w default volume unit; 15k spike | PARTIAL (stateless ✓; 4bit ✓; **merge ladder MISSING**; volume unit = 7,000-char neighborhoods DEVIATED from 850w; 15k spike run 2026-08-29 at batch 4 only) |
| 1.15 | Single-pass full schema: e/m/r + L latent + ps prompt seeds; open-vocabulary types; latent namespace | PARTIAL (e/r/digest ✓; **m by chunk:sentence DEVIATED to quotes**; **L latent MISSING**; **ps prompt seeds MISSING**; open vocabulary ✓ with raw labels preserved) |
| 1.16 | Canonical chunker = 3.3 tier_chunker fork; tables atomic; ChunkKind noise skip; <15w drop | **OWNER DIRECTIVE 2026-08-30: v3.3 `tier_chunker` (Docling fork, no OCR) IS the canonical chunker; swap scheduled as LATENT-TRANSFER-LAYER-V1-PLAN Phase 0 (re-ingest).** Previously: DONE at the neighborhood layer (chunk_kind.py ported from v3.3 section_classifier; noisy kinds skip LLM extraction; <15w stub skip; tables atomic — children never split) |
| 1.17 | Three-layer graph L0/L1/L2; query modes local/bridge/propagation; latent miner | **REJECTED (owner 2026-08-30: never blessed; removed from all plans)** |
| 1.18 | Generation lock-in: max_tokens=2500, rep_penalty=1.15, rep_ctx=400, thinking off; truncated-JSON salvage | DONE (locked config + salvage; spike showed clean at 3000 out on 15K-in) |
| 1.18 | D1 "deterministic parent summary / retrieval_summaries / chunker UNCHANGED" — DEVIATED by owner (2026-08-30 "include it in slice 2"): `retrieval_summaries` is now compiled by SUMMARY-COMPILER-V1 (`retrieval-summary-v3`, migration 0041 dual slot); the chunker stays unchanged | DEVIATED (work-log 2026-08-30-summary-compiler) |

## §2 current-state audit → §2.5 memory

| # | Detail | Status |
|---|---|---|
| 2.1 | Memory budget table honored; BGE-M3 stand-down during 4B window | SUPERSEDED-lite (no 20.6GB window exists; embedder+4B co-resident ≈ 4GB — measured safe) |
| 2.2 | Batch cap 32GB = 40 (56 max, 80 only on 36GB+) | DEVIATED (client micro-batch 4 neighborhoods/call; batched server MAX_BATCH=40 env) |

## §3 target architecture

| # | Detail | Status |
|---|---|---|
| 3.1 | Per-doc state machine staged in doc_stages | SUPERSEDED (stage_tickets/attempts/receipts are the authority) |
| 3.2 | Model lifecycle rules 1–7 (load only if needed; ready probe /infer; offload triggers incl idle>10min; OOM guard ≥8GB; crash pid adoption; no concurrent local windows; embedder stand-down) | PARTIAL (load-on-demand ✓ via worker claims; **offload automation MISSING**; **OOM guard MISSING**; **pid adoption MISSING**; probe ✓) |
| 3.3 | CLI ingest_cli add/run/status/resume/cancel/report with --tier/--max-docs/--keep-model | **MISSING** (using scripts/ingest.py + SQL bumps) |
| 3.4 | Self-aware SQL: status/report as queries; provider reliability rollup | PARTIAL (SQL possible; **no rollup view/report command**) |

## §4.0 stage contracts
| 4.0.1 | Build rule: live shape wins over plan; P0 verifies line-by-line | DONE (readiness audit + this register) |

## §4.1 chunking

| # | Detail | Status |
|---|---|---|
| 4.1.1 | merge_volume_chunks 850w target / 1400w hard max | DEVIATED at present (neighborhood packing over v4 children); becomes DONE with the tier_chunker swap (1.16, owner 2026-08-30) |
| 4.1.2 | parent_neighborhood = children concatenated with [chunk_id] markers, never parent row | DONE |
| 4.1.3 | embedding_text [Parent][Header] content | DONE (untouched intake) |
| 4.1.4 | old rows is_superseded on swap; CHUNKER flag | SUPERSEDED (no chunker swap) |
| 4.1.5 | <15w stubs dropped; ChunkKind structural skip | DONE (chunk_kind.py: full v3.3 taxonomy — TOC/bibliography/index/appendix/front/back matter skipped from LLM extraction; body/code/table/output/caption kept) |
| 4.1.6 | tables atomic; split tables repeat header | SUPERSEDED (intake chunker authority) |

## §4.2 model runtime

| # | Detail | Status |
|---|---|---|
| 4.2.1 | contracts/extraction versioned packet | DONE (polymath-extraction-v1) |
| 4.2.2 | sidecar /infer_batch + OpenAI-compatible /v1, pinned release, batch 40 cap 56 | PARTIAL (batched_server has /infer_batch + micro-batch /v1; **client does not use /infer_batch yet**; pin ✓) |
| 4.2.3 | LocalExtractorClient typed, Instructor-driven | DEVIATED (direct httpx client; Instructor installed unused) |
| 4.2.4 | provider selection gliner/local_llm_shadow/local_llm | DONE (llm_shadow/llm_live/gliner) |
| 4.2.5 | runtime_budget.yaml window profile | MISSING (no budget entry for 4B window; measured 6.4GB batch in report) |
| 4.2.6 | quality_router per file; no cap at launch | DONE (policy.py + limiter) |
| 4.2.7 | .env OLLAMA key; no key → fall back | SUPERSEDED (daemon auth; no key handling needed) |
| 4.2.8 | model_events + window management | DEVIATED (window receipts in artifacts; no table, no automation) |
| 4.2.9 | one request may batch multiple neighborhoods; provenance never mixes | DONE (NEIGHBORHOODS_PER_CALL=4; items per neighborhood) |
| 4.2.10 | ADR-0008 `evidence_proposal_mode` ('lexical' = pass 2 abstains) and the router deprioritization skip apply to the LLM evidence lane | DEVIATED (recorded 2026-08-29 audit): in `llm_shadow`/`llm_live` the gated LLM relation proposals merge with lexical anchors regardless of `evidence_proposal_mode` (the mode governs the GLiNER pass-2 only) and regardless of `scientific_lane_prioritized` (that skip saved a GLiNER call; there is no call to save). Both inputs stay in the contract hash. |

## §4.3 extraction contract

| # | Detail | Status |
|---|---|---|
| 4.3.1 | compact index-encoded entities (e/m/r) | DEVIATED (flat surface+quote; offset arithmetic in Python; rationale recorded) |
| 4.3.2 | verbatim attestation every entity/relation | DONE |
| 4.3.3 | L latent namespace (confidence, never auto-admitted) | MISSING |
| 4.3.4 | ps prompt seeds | MISSING |
| 4.3.5 | digest central_claim/main_mechanism/retrieval_uses hard-capped | DONE |
| 4.3.6 | digest consumed by summary workers (no second pass) | MISSING (digests in artifacts only) |
| 4.3.7 | open-vocabulary types; unknown falls through, raw preserved | DONE |
| 4.3.8 | per-item output budget scaling with input (15k-in→3k-out) | **SUPERSEDED by measurement 2026-08-30**: the input-scaled cap (484 tokens per parent) truncated the local lane (finish=length, 3 relations) where the locked 2,500 cap self-terminated at 841 tokens with 9 relations. Cap = decision-18 `max_tokens=2500` per neighborhood (+700 per extra neighborhood on cloud calls); batches are budgeted by expected output (~900), not the cap; `finish_reason` recorded per call. |
| 4.3.9 | self-flag dense/low-confidence per item → quality tier | MISSING (no dense flag; whole-doc lane instead) |
| 4.3.10 | profiles volume/quality (volume lean: no L/ps; quality full) | PARTIAL (profile field exists; single profile used) |
| 4.3.11 | tier-2 supersedes tier-1 on same keys | N/A until two-tier decision |
| 4.3.12 | P3 cross-genre probe (one non-exam doc) | MISSING |
| 4.3.13 | EXTRACTION-COVERAGE-V1 (owner 2026-08-30 "checks are mandatory, grounded in the control plane"): every neighborhood sent has a durable disposition; incomplete/missing/quarantined re-issued once singly; `dropped`/`unaccounted` block `query_ready` in the census (run → `degraded`, reasons in `runs.metadata`); soft coverage floor `POLYMATH_CONTROL_EXTRACTION_COVERAGE_FLOOR` reports only | DONE (measured trigger: 118/181 CySA+ parents empty, pattern X...X...; work-log 2026-08-30-extraction-coverage-hardening) |
| 4.3.14 | INTERROGATIVE-ATTESTATION: relations attested only by a question stem are rejected at the gate; prompt rule 8 tells the model the same | DONE (owner delegated the call; narrow rule — declarative "X is not Y" untouched) |
| 4.3.15 | REGION-ROLE-V1: chunker-independent `chunks.region_role` (noise_ocr/index/toc/legal/stub/question_bank/output/code/body); noise never enters a neighborhood or a routing summary | DONE (calibrated on 1,024 live chunks; thresholds hashed into the extract contract) |
| 4.4.8 | SUMMARY-COMPILER-V1 (owner spec 2026-08-30): one model-free compiler for section/document routing cards — verbatim sentences with offsets, triple-aware ranking + relation capsule, TF-IDF salience against the document background, coverage-first over children / ordered regions, Jaccard dedupe, source order, hard bound, keywords, one serialized embed text; the extractor's digest is the LLM adapter (active variant when clean, deterministic card always persisted; `retrieval_summaries.active`); S2 parent summaries consume the card; verifier gates on starved children and missing cards | DONE (17 tests; live dry run 206/206 cards, 61 llm_digest active) |
| 4.3.16 | ONTOLOGY-DURABLE-CHECK-V1: verifier proves ledger predicates ⊆ ontology, `unknown_predicates == 0`, every llm_live ledger relation has an `evidence` row; off-enum/fact-less relations degrade the run | DONE (pre-hardening runs reported, not judged) |
| 4.3.17 | TERM-SURFACE-GATE (owner 2026-08-30): entity surfaces and relation endpoints must be TERMS — ≤8 words, no sentence punctuation, strengthened by exact-token clause-aux/clause-opener tests (case-sensitive; "IS NOT NULL"/"The Open Group" survive); rejection classes `NON_TERM_SURFACE`/`NON_TERM_ENDPOINT` | DONE (measured pre-landing: SQL 10/128, CySA+ 118/2624 caught, 0 false positives; known misses pinned in test_term_surface_gate; work-log 2026-08-30-control-plane-hardening) |
| 4.3.18 | TRANSPORT-RETRY-500-V1: a single transient 500 from the lane daemon retries once with backoff instead of failing the whole extract stage; repeat still fails closed | DONE (measured trigger: one Ollama 500 burned a 6-min cloud stage; receipt 7d46676d) |
| 4.3.19 | require_slices relaxation (llm_live): the evidence bundle for llm_live documents carries NO sentence-slice manifest (`write_bundle(require_slices=False)`) — the bundle is the raw ledger over chunks; the GLiNER-era "required evidence — may not be reconstructed" contract does NOT hold for the LLM era | DEVIATED (recorded 2026-08-30; either the LLM era grows its own slice-equivalent interpreter view or this stays the documented contract — owner call, see §10.2) |

## §4.4 summaries + vocabulary

| # | Detail | Status |
|---|---|---|
| 4.4.1 | summary hierarchy deterministic + digest enrichment | PARTIAL (4.3.6) |
| 4.4.2 | lexiconed stage inline refresh | DONE (existing stage; fed by extraction) |
| 4.4.3 | indexed AFTER lexicon (ordering fix) | DONE (existing DAG) |
| 4.4.4 | corpus_entities table + merge ladder + raw_types | MISSING |
| 4.4.5 | entity_links global join layer | MISSING |
| 4.4.6 | latent_links table + confirmation flow | MISSING (with 4.3.3) |
| 4.4.7 | prompt_seeds table | MISSING (with 4.3.4) |
| 4.4.8 | relations accumulate with provenance, never merge in place | DONE (facts + evidence append-only) |

## §4.5 retrieval
| 4.5.1 | RetrievalConfig kd/ks/λ; RETRIEVAL_MODE flat|hierarchical; eval gate MRR+0.05 | MISSING (P4; retrieval untouched) |

## §4.6 graph RAG
| 4.6.1 | L0/L1/L2; query modes; PPR Connect-4; latent miner; community deferred | **REJECTED (owner 2026-08-30: never blessed; removed from all plans)** |

## §4.7 control-plane schema
| 4.7.1 | ingest_jobs/doc_stages/model_events/extraction_proposals/provider_calls tables | SUPERSEDED (receipts/ledger/artifacts) |
| 4.7.2 | state-transition table: one txn per transition; idempotent keyed; filtering never destroys evidence | DONE (runtime invariants pre-existing + raw ledger durable dispositions) |
| 4.7.3 | long-stage rule: claim_depth=1 + keeper renewal ≤claim_ttl/2, heartbeat OUTSIDE txn | DONE (existing in-flight keeper; observed renewing) |
| 4.7.4 | extraction_proposals keyed (parent_id, extractor_version) → per-file resume | PARTIAL (ticket-level resume ✓; parent-level skip MISSING) |
| 4.7.5 | shadow = same edge, different write target; promotion flips provider, no state-machine edit | DONE |
| 4.7.6 | CENSUS-DIRTY-SIGNAL-V2: incremental-census dirtiness tracks `stage_tickets.updated_at` as well as `stage_attempts` (summary stages write no attempts); gap verdicts are NEVER cached; per-run `failed` (not the global fail list) gates promotion | DONE (measured trigger: both cysa runs pinned at `reconciling` with 24/24 tickets done; unstuck in 6 s by the forced full pass; pinned by test_census_dirty_signal) |
| 4.7.7 | HASH-FENCE-V2: the worker code fence fingerprints file CONTENT (sha256, stat-cached), not `size:mtime_ns` — a byte-identical rewrite (pytest restoring the ontology yaml) can no longer quarantine the fleet | DONE (replaces the "tests trip the stale-code fence" CLAUDE.md trap with a structural fix) |

## §4.8 provider layer

| # | Detail | Status |
|---|---|---|
| 4.8.1 | providers/provider_keys/provider_calls tables + lane failover by priority | SUPERSEDED (limiter registry + receipts) |
| 4.8.2 | Instructor = single client across providers | DEVIATED (direct; see 4.2.3) |
| 4.8.3 | KGGen-style entity clustering in NORMALIZE | MISSING |
| 4.8.4 | thinking-token strip at gate | DONE |
| 4.8.5 | per-provider reliability accumulation drives failover | PARTIAL (receipts accumulate; **failover not wired to reliability**) |

## §4.9 dense 4B I/O

| # | Detail | Status |
|---|---|---|
| 4.9.1 | uniform-size packing (straggler control) | DONE (c7e98b9: balanced k-bucket packing per parent + <15w stub skip) |
| 4.9.2 | one shared system prompt (prefix reuse) | DONE (single system prompt; batched server reuses it per request) |
| 4.9.3 | per-file resume (skip already-extracted) | PARTIAL (ticket resume; neighborhood-level skip MISSING) |
| 4.9.4 | input-size curve spike 850/1400/2000 @ batch 40 + 15k @ 16/8/4; LOCK volume size at max passing ~28GB gate | **MISSING** (only 15k@4 and 8k@4 batches measured) |
| 4.9.5 | BGE-M3 stand-down during window | N/A (no large window; revisit if batch-40 dense adopted) |
| 4.9.6 | true batch_generate runtime (owner report: batch 40, 6.4GB, 241 tok/s-class) | DONE (batched_server + client extract_batched wired; 6×7.5K-token neighborhoods in ONE batch = 151s, 6/6 schema-valid after per-item shape tolerance; curve points: 15k@4=1.7x, 7.5K×6@1=151s) |

## §5 retrieval quality control
| 5.1 | eval_queries.jsonl (20 queries, seeded from manual_retrieval_needed) | MISSING |
| 5.2 | eval_mrr harness + search_evals rows | MISSING |
| 5.3 | shadow benchmark gate metrics (name accuracy first-class, P/R, attestation, valid-output ≥95%, wall, peak mem) | PARTIAL (attestation sampler ✓; **semantic metrics MISSING**) |
| 5.4 | P4 acceptance gate MRR_doc ≥ flat+0.05 before flag flip | MISSING |

## §6 retirements
| 6.1 | GLiNER stays as rollback provider until 2-week clean run, then deleted | PARTIAL (rollback path ✓; deletion pending) |
| 6.2 | spaCy sidecar kept until P5 then deleted | PARTIAL (in use for syntax) |
| 6.3 | Gemini legacy never auto-submitted; --llm local option | N/A (legacy untouched) |
| 6.4 | v1 flat chunker deleted P5 | SUPERSEDED (chunker unchanged) |
| 6.5 | CrossEncoder device cpu fix | MISSING (P4) |

## §7 phases / §8 risks / §9 open questions
| 7.1 | P0 eval set | MISSING (waived for first slice; still owed) |
| 7.2 | P0 011_ingest_control.sql | SUPERSEDED |
| 7.3 | P5 4,500-doc corpus over nights | MISSING (future) |
| 7.4 | P6 thin UI over tables | MISSING (P6) |
| 8.1 | Risk mitigations table | PARTIAL (boundary fail-closed ✓; cost visibility = tokens only, **no cost cap/price**; straggler lock missing per 4.9.1) |
| 9.2 | Literal objects storage shape: typed value nodes (recommended) | MISSING (contract allows literal objects; storage still entity-endpoint-only) |
| 9.8 | query lane slot reserved | PARTIAL (limiter lanes reserved; no query-time LLM) |

## §10 Concept & procedure migration with the LLM lane (rev 5, 2026-08-30)

Authoritative section added to the migration plan
(/Users/king/Downloads/polymath-v4-local-migration-plan.md §10). Register items:

| # | Detail | Status |
|---|---|---|
| 10.1 | Wire `_persist_knowledge_artifacts` into llm_live (deterministic CONCEPT/PROCEDURE compilers, model-free, opportunity accounting preserved) | DONE (2026-08-30 KNOWLEDGE-ARTIFACT-LLM-V1: wired before the llm_live artifact write, gate-admitted surfaces as durable_surfaces, counts + `knowledge_artifacts_s` in the artifact; e2e receipt = next ingest's concepts/procedures counts) |
| 10.2 | Parent Semantic Compiler: one bounded forward pass per parent → summary + abstraction(1) + mechanisms(≤3) + affordances(≤3) + pseudo_queries(≤4); qual budget 1/2/2/3 = 8 latent vectors | MISSING (Adapter 2 contract; supersedes the digest adapter once active) |
| 10.3 | summary_vector stays PURE; enrichment = separate object keyed parent_id + source_content_hash + version | design fixed |
| 10.4 | Latent Qdrant namespaces first; Neo4j unchanged in phase 1 | design fixed (owner, Adapter 2) |
| 10.5 | Latent retrieval = additive seed generator → parent_ids → EXISTING evidence pipeline; fail-open, 100–300ms budget, per-channel flags, retrieval_mode baseline|baseline_plus_latent | design fixed |
| 10.6 | Per-channel attribution + P6 LATENT_TRANSFER_RECALL suite (LatentRecall@K, CrossDomainRecall@K, FalseAnalogyRate, AnswerLift ±latent) — separate from ordinary retrieval eval | MISSING |
| 10.7 | LLM-native extract.procedures (Adapter 1) DEFERRED unless opportunity accounting proves deterministic compiler gaps | decided |

## §11 One authority, three projections — storage model + governing principle (owner 2026-08-30)

Governing principle (owner, verbatim intent): **the model proposes;
deterministic Python owns truth.** The LLM never writes a store, never
assigns a canonical id, never becomes a survival dependency. Identity,
admission, the predicate contract, projections, and the control plane are
deterministic; the model is swappable, the guarantees are not.

### §11.0 Contract audit — is the governing design embedded in the control plane? (verified against code 2026-08-30, session 4)

| Claim | Where enforced | Verdict |
|---|---|---|
| Lane from durable `documents.byte_length`, ≤300 KB local; enforced at selection AND dispatch | `policy.py` `CLOUD_MIN_BYTES=300_000` clamped floor; `select_lane` + `require_cloud_eligible` raises `CloudBoundaryViolation` immediately before the socket | CONTRACTED |
| Local = GPU window: batched decode, OOM-halving, concurrency 1–4 | `client._infer_batch_call` (500→halve after slot release, budget `record_oom`), `limiter.yaml mlx_local max:4` | CONTRACTED |
| Cloud = rate problem: RPM/TPM buckets before send, AIMD, header sync, breaker | `AdaptiveLimiter` (closed→open→half-open one-probe breaker, `use_headers`), `ollama_cloud` seeds init 3 | CONTRACTED (ceiling raised 16→32 this session — it saturated with ZERO 429s, masking the discoverable limit) |
| Parent unit: model reads child chunks concatenated, never the stored parent row | extract neighborhoods built from `tier='child'` rows; gate `ChunkView`s are children | CONTRACTED |
| Noise never enters an LLM call | `chunks.region_role` (REGION-ROLE-V1) + neighborhood packing filters | CONTRACTED (4.3.15) |
| Output budget scales with input; generation config locked | `output_budget_for`; `GENERATION_CONFIG` = temp 0, rep_penalty 1.15, ctx 400, local `enable_thinking:false`, cloud `reasoning_effort:none` | CONTRACTED |
| Closed predicate enum, exact→alias→RELATED_TO, every fallback recorded | 74-entry `PREDICATE_ALIASES` + patterns; gate counts `predicate_fallbacks`; `predicate_raw`+`predicate_method` persisted on every evidence row AND in facts.provenance | CONTRACTED |
| Off-enum can never become a stored predicate | gate normalizes; `llm_direct` guards `pred not in RELATION_ONTOLOGY` (counted, never expected); verifier `ontology{}` degrades off-enum runs (4.3.16) | CONTRACTED |
| No-think guaranteed twice | engine-layer per lane (above) + `strip_thinking` at the gate | CONTRACTED |
| One control plane: tickets→leases→receipts→census, idempotent, kill-9 resume, lease keeper | `stage_transaction`/receipts, `_lease_keeper`, ticket DAG; census stuck-run class killed by CENSUS-DIRTY-SIGNAL-V2 (4.7.6) | CONTRACTED |
| Coverage accounted, not assumed | EXTRACTION-COVERAGE-V1 dispositions + census promotion barrier (4.3.13) | CONTRACTED |
| Shadow before trust | `llm_shadow` admits nothing; promotion = provider flip (4.7.5) | CONTRACTED |
| Graph hits ground to chunks; identity-admitted facts only | `Fact→Evidence→Chunk` rows written by identity in `llm_direct`; projector reads facts table only | CONTRACTED |
| Deterministic floor: summaries, concepts, procedures model-free | SUMMARY-COMPILER-V1 (4.4.8) + compile_objects stage (11.4) | CONTRACTED |
| Entities carry generation + open vocabulary (de-flattening) | was MISSING — closed by 11.1 | BUILT 2026-08-30 |
| Graph extractions first-class in FAST routing | was MISSING — closed by 11.2 | BUILT 2026-08-30 |
| Exact-name lexical recall (BM25) | was MISSING — 11.3 | BUILT (projection side) 2026-08-30 |

### §11 register items

| # | Detail | Status |
|---|---|---|
| 11.1 | GENERATION-STAMPING-V1 (closes 1.6): migration 0042 — `entities.extractor_version/generated_by_bundle_hash/raw_types(jsonb set-union, containment-guarded for observable idempotency)`, `facts.extractor_version` (indexed); `llm_direct` writes `llm-direct-v1` | DONE (2026-08-30 s4) |
| 11.2 | ROUTING-ENTITY-CARDS-V1: one content-addressed card per (entity, corpus) — surface + aliases + core type + predicate capsule; `representation_kind='routing_entity'`, payload `{entity_id, corpus_id, doc_ids}`; shared id derivation `projection_contracts.entity_card_id` (projector writes, verifier reconciles — one derivation); full receipt/incremental/reconcile parity with the other routing lanes | DONE (2026-08-30 s4) |
| 11.3 | SPARSE-BM25-V1: named `bm25` sparse vector (server-side IDF) on the routing collection; deterministic shared tokenizer `polymath_shared/sparse_bm25.py` (index side and query side MUST import the same function); children index attested entity surfaces + parent head; MEASURED: qdrant 1.13.4 cannot add sparse to an existing collection → new collections sparse-native, `scripts/migrate_routing_sparse.py` (copy-out/recreate/copy-back, dense preserved, owner-gated `--apply`) migrates legacy; legacy collections log `SPARSE_LANE_SKIPPED_LEGACY_COLLECTION`, never fail | DONE projection-side (2026-08-30 s4); query-side fusion = §4.5 work, PARTIAL |
| 11.4 | COMPILE-OBJECTS-STAGE-V1: concept/procedure compilers as their own non-blocking ticket stage (`compile_objects.v1`, after verify, before summaries) — own contract hash, attempts, receipts, artifact, opportunity accounting; supersedes the llm_live bolt-on (removed); legacy GLiNER inline call stays frozen behind the seam; supervisor slot + pipeline/converge profiles | DONE (2026-08-30 s4) |
| 11.5 | L0 authority hardening: `predicate_raw`/`predicate_method`/`raw_type` were already persisted per row (audit); remaining L0 item = none | DONE by audit |
| 11.6 | Query-side: FAST reads `routing_entity` cards; HYBRID fuses the `bm25` sparse lane (same tokenizer import) | DONE (2026-08-30 RETRIEVAL-FULL-FIX-V1: cards are a fused RRF lane in `pass1-retrieval-v2`; sparse fused in HYBRID + breadth lanes) |
| 11.7 | RETRIEVAL-FULL-FIX-V1 (audit F2/F6/F7/F8/F10/F11/F12): card lane fused into RRF; chunk lane children-only + 65 parent points retired; AI-designed breadth/depth caps (BREADTH-V2/DEPTH-V2) + synthesis budget 2000×48; multi-corpus FAST; sparse-tokenizer contract test; OBJECT-NAME-CONTRACT-V2 naming gate (compile + serve) | DONE (2026-08-30, work-log 2026-08-30-retrieval-full-fix.md) |
| 11.8 | EXTRACTION-POOL-V1 + LANE-AFFINITY-STEAL-V1 (owner 2026-08-30): extract slots get lane affinity (extract=local, extract2+=cloud) with a counted steal pass when the home lane is dry; cloud lane generalized to a deterministic multi-provider router pool (POLYMATH_LLM_CLOUD_EXTRA_ENDPOINTS, blake2b(doc_id) sharding, per-endpoint AIMD limiter lanes, roster in contract_identity). 300KB exfiltration boundary UNTOUCHED — providers widen throughput, never eligibility | DONE (2026-08-30, work-log 2026-08-30-extraction-pool.md) |
| 11.9 | CLOUD-ASSIST-V1 (owner rule v2, 2026-08-30, SUPERSEDES the 2026-08-29 exfiltration framing): byte threshold = throughput router — big docs always cloud; small docs prefer local but ride the cloud pool as an explicit ASSIST when a cloud-affinity worker holds them (own lane dry). Dispatch guard verifies assist INTENT (still fail-closed on caller bugs). Lane is operational not replay-deterministic; artifact records lane+reason+endpoint | DONE (2026-08-30, work-log 2026-08-30-cloud-assist.md) |
| 11.10 | MULTI-PROVIDER-AUTH-V1: committed provider registry config/cloud_providers.json (Groq LIVE on openai/gpt-oss-120b, extraction canary GREEN; NVIDIA pre-configured, parked until NVIDIA_API_KEY in .env). Auto-gate: key drop = activation, logged never silent. Bearer auth per endpoint, per-endpoint payload quirks, keys excluded from fingerprints/repr. extract3 cloud-affinity slot. Preflight scripts/probe_cloud_endpoints.py | DONE (2026-08-30, work-log 2026-08-30-multi-provider-auth.md) |
| 11.11 | NVIDIA-LATENT-PIN-V1: NVIDIA LIVE (key in .env) + DEDICATED to parent_enrichment (STAGE-PIN-V1). Model nemotron-3.5-lightning-30b-a3b, reasoning_effort "none" (verified: only knob that stops NIM thinking-burn; /no_think fails), temp 0, JSON mode, limiter seed 36 RPM/conc 4 (limiter.yaml per-provider blocks; groq seeded 30 RPM). Canary through the pinned path GREEN. Phase B must re-canary the enrichment schema | DONE (2026-08-30, work-log 2026-08-30-nvidia-latent-pin.md) |
| 11.12 | NVIDIA-DUAL-LANE-V1: second unlinked NVIDIA account live (NVIDIA_API_KEY_2); stage pins are GROUPS — parent_enrichment shards deterministically across [nvidia, nvidia2] (measured 103/97 over 200 docs), each with its own 36 RPM AIMD bucket (~72 RPM combined). One dark = reduced capacity logged; all dark = loud fail | DONE (2026-08-30, work-log 2026-08-30-nvidia-dual-lane.md) |
| 11.13 | GROQ-EXTRACTION-FLEET-V1: baseline extraction cloud = primary + groq1..groq5 (5 unlinked owner accounts, qwen/qwen3.8-27b, reasoning none, temp 0, STRICT JSON SCHEMA level-1 — live-canaried; 30 RPM AIMD each). DEDICATED-V1: nvidia lanes excluded from extraction sharding, enrichment-only. Old hermes groq key dropped from the pool | DONE (2026-08-30, work-log 2026-08-30-groq-extraction-fleet.md) |
| 11.14 | CROSS-PROVIDER-FAILOVER-V1: nvidia2<->groq5 role swap (extraction shards groq1-4+nvidia2+primary on super-120b extraction-canaried; enrichment group [nvidia, groq5]) + LANE-FAILOVER-V1 deterministic ring (transport-dead or limiter-refused batches retry once on the next lane, logged EXTRACTION_LANE_FAILOVER; pinned stages ring within group). Whole-provider outage stalls nothing | DONE (2026-08-30, work-log 2026-08-30-cross-provider-failover.md) |
| 11.15 | UI-V3-PRESENTATION-V1: PRD executed — presentation fields on every bundle item + FAST evidence (source_name bug dead, live-verified), citations human_locators, sections-tree endpoint + F13 query_enabled toggle, frontend Sources panel (human name › section + quote, provenance expander) + document section tree. Zero raw chunk ids in default view (browser-verified). All additive | DONE (2026-08-30, work-log 2026-08-30-ui-v3-presentation.md) |
| 11.16 | LATENT-PHASES-A-D-V1 + §0a buttons: latent package (contract/prompt/gate/compiler/runtime/projection/rescue), migration 0043, owner-triggered enrichment stage on the pinned cross-provider group, latent projection + verifier reconciliation, HYBRID rescue lane + latent flag through every surface, P6 harness. LIVE: 24 READY enrichments, 48 latent points, cross-domain reach proven (3 parents/9 children per query, 2 unique evidence gains per P6 case). latent default OFF until owner P6 GO | DONE (2026-08-31, work-log 2026-08-31-latent-phases-a-d.md) |
| 11.17 | ENRICHMENT-CONCURRENCY-V1: transport parallelized to the pinned endpoint limiter conc_cap (4/account, ~4x measured); HTTP_429 retry ladder (backoff → same lane → cross-lane → backoff) after a conc-4 burst 429-killed 30/40 AWS parents; INVALID rows upgrade in place on retry (re-click = recovery, proven 40/40 READY, 14 failovers); verify.v1 era-exempt (receipts-only) | DONE (2026-08-31, work-log 2026-08-31-enrichment-concurrency.md) |
| 11.18 | CENSUS-WEDGE-RESTORATION-V1: three stacked wedges fixed after latent projection took the corpus to corpus_not_ready — (1) DAG-less ticket KeyError killed advance_tickets fleet-wide; (2) 1C successor carry-gap (per-run artifact reads) parked + originals re-pinned; (3) the F6 children-only want-set had THREE copies (verify/census/tickets) — all aligned. query_ready restored, latent reach live on full corpus | DONE (2026-08-31, work-log 2026-08-31-census-wedge-restoration.md) |
| 11.19 | AUTO-ENRICH-UI-V1: enrichment auto-mints at run promotion (census tick = timer; retrieval-first rationale; enrichment_auto gate); ONE shared mint path (latent/trigger.py); /documents per-doc parents/enriched/failed; FilesView enrichment badges + conditional ✨ buttons + ＋Add-files + ＋new-corpus flow. Browser-verified | DONE (2026-08-31, work-log 2026-08-31-auto-enrich-ui.md) |
| 11.20 | SESSION-A-RELIABILITY-V1: projection_want = the ONE want-set authority (3 consumers rewired + import pin); reconciliation E2E proves drift→successor→query_ready with NO manual re-pin (1C carry-gap REFUTED, chip dismissed; outage was census bugs); SEMANTIC-FAILOVER-V1 (gate-reject cross-lane retry, one only, source conditions never) + ROW-TRUTH-DONE. Corpus 65/65 READY zero INVALID | DONE (2026-08-31, work-log 2026-08-31-session-a-reliability.md) |
| 11.21 | SESSION-B-QUERY-PATH-V1: Pass1Result.qvec (single embed); latent survival diagnostics in engine trace + chat frame + UI chip ("✨ 2/3 · 2" browser-verified); HYBRID presentation joins; GRAPH latent silent-drop bug FIXED (now truly inherits, 7 facts intact); ✨ toggle in the retrieval bar | DONE (2026-08-31, work-log 2026-08-31-session-b-query-path.md) |
| 11.22 | SESSION-C-P6-V1: 20-case latent qualification run — SURVIVAL 78% (47/60 nominated parents kept >=1 original child through rerank), +3.0 unique evidence/case, both kinds alive (abstraction 55/transfer 27), latency delta ~20 ms, 0 failures. GO/NO-GO awaits owner; enable = latent_retrieval_enabled=true | MEASURED (2026-08-31, work-log 2026-08-31-session-c-p6.md, results in eval/v5/latent_transfer/) |
| 11.23 | LATENT-GO-V1 (owner 2026-08-31): latent_retrieval_enabled default TRUE — HYBRID/GRAPH run the latent rescue by default (live-verified: nominated 3/survived 2 with no flag), FAST frozen baseline, per-request latent:false opts out | DONE (settings.py, continuity session pack updated) |
| 11.24 | TIER-CHUNKER-V3 (latent plan D15 Phase 0, owner GO 2026-08-31): native chunk-structure-v3 — heading-bounded parents with REAL section text + level-aware heading_path, byte-exact offsets (D15 amended: native on v4 regions, NOT a v3.3 port — the module rewrites text and would break the §8 offset contract), page-scaffold merge (v3.3 OCR-lane rule), hard 1,400 w cap, generation purge on re-ingest; re-ingest of HEALTHY corpora needs the owner trigger scripts/reingest_corpus.py (REINGEST-TRIGGER-V1 — reconciler rescues stranded runs only, refuted-live claim recorded in the work-log) | DONE (2026-08-31, work-log 2026-08-31-tier-chunker-v3.md) |
| 11.25 | POLYMATH-MCP-V1 (owner 2026-08-31): v4 MCP server :8930 (streamable-http, stateless, bearer-gated, host allowlist) — 3 thin tools over the orchestrator API (list_corpora/retrieve/ask, no MCP-side retrieval logic); LaunchAgent com.polymath.mcp; mcp.kingsleylab.xyz revived on the live tunnel (v33 origin found dead, 530); Hermes polymath entry → local :8930 | DONE (2026-08-31, work-log 2026-08-31-polymath-mcp-v1.md) |
| 11.26 | GEMINI-FLEET-V1 (owner 2026-09-01): 6 AI Studio lanes on gemini-3.1-flash-lite (owner re-pin "its cheaper"; owner-linked 2.5-flash-lite retired upstream) — gemini1-4 join the extraction shard (now 10 lanes), gemini5-6 dedicated enrichment (pin group = nvidia+groq5+gemini5+gemini6); strict-schema canary green, all-lane preflight green, AIMD seeds rpm 12/conc 3 | DONE (2026-09-01, work-log 2026-09-01-gemini-fleet.md) |
| 11.27 | SMART-PIPELINE-V1 (owner 2026-09-01, design owner-reviewed): enrichment lane per PARENT (all pin lanes churn on one doc); enrichment mints at INTAKE-done (overlaps the chain; promotion mint = backstop; rescue clause re-opens consumed-event/open-ticket strands — found live); GET /fleet + Fleet tab (lanes/AIMD/workers/queue/coverage); depth-aware extraction spread (affinity when queue deep, ring spread only when lanes would idle; unknown depth never spreads). Central scheduler brain explicitly rejected. | DONE (2026-09-01, work-log 2026-09-01-smart-pipeline.md) |
| 11.28 | EVIDENCE-UTILITY-V1 (owner 2026-09-01, outside design reconciled): marginal-utility evidence-SET composition for HYBRID/GRAPH, flag-gated (default OFF; per-request `utility`) — bounded-lookahead cut (requirement coverage, parent saturation, redundancy veto; original order = relevance tier; rescue+latent seat FLOORS keep candidacy) + post-G3 latent competition against the cross-encoder's own scores ("guaranteed access to the competition, not guaranteed final seats"). NOT score fusion — set composition; cross-encoder stays sole relevance authority. Grounding discoveries: pipeline is cut-then-rerank; baseline probe refuted the redundancy premise (fact/entity novelty deferred) and confirmed parent saturation (3.6/8 worst). Qualified: T1 sat 3.00/3→2.00/2 std-profile, T2 survival 70% 0pt delta + gain 2.7/case, T3 −27ms, T4 byte-identical. 13-test diagnostic suite. Default-on = owner GO. | DONE (2026-09-01, work-log 2026-09-01-evidence-utility.md) |
| 11.29 | ENRICH-HARD-CASE-V1 (owner design 2026-09-01): A→B→one cross-family MINIMAL escape (ring+2; {abstraction,transfer} only, aggressively gated, provenance compiler_contract=minimal — enum rejected as 6-filter churn)→typed terminal ENRICH_HARD_CASE (INELIGIBLE = sweeps stop). Persist upgrade-path bugs found live and pinned: DO UPDATE dropped minimal provenance; DO NOTHING made terminal dispositions no-op. LIVE: all 7 hard cases recovered on the escape — corpus 67/67 READY (60 full + 7 minimal), 0 INVALID | DONE (2026-09-01, work-log 2026-09-01-enrich-hard-case.md) |
| 11.30 | DIVERGENT-RETRIEVAL-V1 / WILDCARD (owner-blessed 2026-09-01): TRUE fourth mode — frontier retrieval (reward latent similarity, punish ordinary similarity), two-hop validation (query↔latent; latent↔source child via the cross-encoder), novelty damp, WildcardValue = hop1×hop2×novelty, ≤3 bridges in a SEPARATE lane that never displaces evidence. §0b CARVE-OUT owner-blessed: bridges display enrichment text as labelled DERIVED INSIGHT with the real source child attached — never source evidence, never graph truth. UI picker = VECTOR/HYBRID/GRAPH/WILDCARD (ASK hidden per owner; endpoint stays). Live receipts: cross-domain grounded bridges at support 0.92-0.99; doc-exclusion small-corpus bug found live → same-doc = damp not kill | DONE (2026-09-01, work-log 2026-09-01-divergent-retrieval.md) |
| 11.31 | EXTRACTION-THROUGHPUT-V2 (owner-blessed after the 4-book post-mortem: 84% wire failure, collision-idled fleet, concurrency ≈1, whole-doc re-buys): rank-sliced disjoint lane allocation (ring//active per doc; lone doc = whole fleet); size-budgeted packing vs per-lane request_char_budget (413 impossible); per-batch content-addressed receipts (migration 0044 — retries replay raw through the same gate, byte-equivalent); 413 = split → cross-HOST escape, never an AIMD rate signal; slice-external transport failover. 8-test suite + live benchmark = ecom-meta-v1 V2 re-extraction | DONE (2026-09-01, work-log 2026-09-01-extraction-throughput-v2.md) |
| 11.32 | EXTRACTION-FLEET-V3 (owner-blessed, incl. owner's find-each-limit directive): header-declared ceiling adoption (grow-only, seed×4 clamp, persisted — providers' own 429 headers raise our seeds, zero probing); RPD daily budgets (gemini 2000); provider-family circuit (8 fails/30s → 45s family cooldown, store-shared, fail-open); semantic escape for quarantined extraction calls (one cross-host try, PARSED ≠ ACCEPTED); receipt accepted_count (0045); provider-equivalence bench post-gate (tiering + family interleave = owner gate on its numbers). Live receipt: 195 receipt replays completed the benchmark books 40–90s after the deploy bounce | DONE (2026-09-01, work-log 2026-09-01-extraction-fleet-v3.md) |
| 11.33 | ENRICH-MICROBATCH-V1 (owner-blessed; amends one-parent-per-call): 6-8 item-isolated parents/call (per-item gate = the per-parent gate; split ladder to the single-parent floor; partial acceptance); eligibility gate (noise parents never reach an LLM); groq5 OUT of the pin (declared 8K TPM); pin = nvidia+gemini5/5b/6/6b; GEMINI VARIANT LANES: 12 lanes from 6 keys (per-model quotas; 3.5-flash-lite strict-schema PASS; non-lite flashes parked); groq limits set to provider-declared truth (TPM 8000, request budget 7000) | DONE (2026-09-01, work-log 2026-09-01-enrich-microbatch.md; AMENDED same day by 11.35 per-batch persist) |
| 11.34 | FLEET-HARDENING-0901 (cold-retest defects, standing "production ready" order): RANK-STABILITY — rank/active from the timing-stable OPEN ticket set (pending/ready/leased of live runs, ordered by ticket_id), replacing the leased-set race that convoyed every doc onto one lane (429 storm → strike-outs); gemini seeds to owner-dashboard truth (15 RPM/250K TPM/500 RPD); ZOMBIE PURGE — 111 fixture tickets + 13 fixture runs (reconv-*, d7-h1-test) purged (they inflated active_docs → 1-lane slices); fixtures-must-rollback = recorded debt; GROQ PARKED from the extraction ring (8K TPM declared = a slice that cannot carry a packet) — dedicated:true, escape-reachable only. ac0a128 message overstated the seed edit; real edit e91c5de, recorded | DONE (2026-09-01, work-log 2026-09-01-fleet-hardening.md) |
| 11.35 | PER-BATCH PERSIST (owner-directed "FIX THE PER BATCH PERSIST"; amends 11.33's persist grain): `on_compiled` callback seam in compile_parents_microbatched (fires per gated batch + at the single-parent floor; callback exceptions swallowed); worker `_persist_ready_now` lands each READY parent in its OWN committed tx (ensure-job + persist + summary_jobs COMPLETE, `_persisted` dedupe, tail-loop skip). Crash cost ≤ one microbatch, was one document (4 bounces that day each discarded a full doc's compiled work). 11 microbatch tests green incl. per-batch callback + callback-error survival; live: rows accumulated across the 05:10:39Z bounce with zero loss | DONE (2026-09-01, commit 534cccb, amendment in work-log 2026-09-01-enrich-microbatch.md) |
| 11.36 | PRODUCTION-SPEED-SWEEP-0901 (owner: "bugs identified, optimized for production, true speed rag"): ANSWER-ADMISSION-V2 (hyphen-compound coverage, relation words never required, >=75% quorum; nonce regressions unchanged; live: habits+JTBD abstention → supported/14 citations/3.3s); FAIL-FAST-BREAKER-V1 (refused = no backoff sleeps + 15s per-host breaker; busy/5xx keep P0-C patience — kills the 97s degraded-query anatomy); `serve` runtime profile (pipeline+reranker+orchestrator, 15.75/18.5 GB ceiling-checked) + fleet rebuilt as one supervised tree (launchd auto-boot still TCC-blocked = owner action); MICROBATCH-CONCURRENCY-V1 (batches concurrent across the 5-lane pin, default enrichment_batch_concurrency=5; sequential loop measured 5.3 parents/min → 2h49m tail). MCP battery green (0.01/0.04/2.42/2.90s); GRAPH "no evidence" = probe artifact, rejected | DONE (2026-09-01, work-log 2026-09-01-production-speed-sweep.md) |
| 11.37 | AUTOPILOT-TAIL-DEMAND-V1 (live upload test caught it): query_ready froze the tail — _open_work's run-status filter dropped promoted runs, parent_enrichment mapped to no lane, compile_objects had no lane/slot mapping at all (its worker had NEVER been demand-woken). Fixes: filter += query_ready; summary lane += parent_enrichment; new compile lane. 4 tests green + live drain receipt: compile_objects (open since 13:29Z) done 14:26:40 seconds after the fixed fleet booted; whole tail drained by 14:33:48, enrichment 112/112 READY | DONE (2026-09-01, work-log 2026-09-01-autopilot-tail-demand.md) |
| 11.38 | PROVIDER-POOL-CAMPAIGN-0901 (owner: cheap OpenRouter pool, production rule, "429 has a reason" correction): 14 models canaried on both production gates; standing tool eval/v5/fleet/provider_canary.py (v2: capacity events paced + counted separately from quality, 180s budget, PASS = 6/8 + 6/8). Survivors: mistral-small-2603 (25.7 f/1Kw, 83.4 e/1Kw, 7/8 enrich, 4.5s, 4 providers), ministral-14b-2512, gemma-3-4b (extraction only, single provider), gpt-oss-20b on groq only. Lessons: /api base (no /v1), pool-is-the-product, json_schema narrows routing, sub-10B envelope floor, :nitro picks garbage hosts. NO lane wired — owner gate | DONE (2026-09-01, work-log 2026-09-01-provider-pool-campaign.md) |
| 11.39 | FAMILY-INTERLEAVE-V1 + OPENROUTER-LANES-V1 (owner: "bless interleave and wire the two openrouter lanes"): cloud_ring() = smooth-weighted-round-robin over provider families (family = URL host) so every rank slice mixes families (live 12-lane ring: two-doc slices {gemini,primary,openrouter}/{gemini,nvidia,openrouter}); closes FLEET-V3's conditional item on the 0.01–0.10 agreement finding. Lanes openrouter1 = mistral-small-2603, openrouter2 = ministral-14b-2512 (ring + enrichment pin → 7 lanes; family openrouter, rpm 60/tpm 500K/conc 4; key in .env only). 5 interleave tests + throughput/limiter suites green; ops lesson: key rotation requires a fleet bounce (env snapshot) — first receipt run exposed it; LANE-AUTH-QUARANTINE-V1 shipped from that finding (401/403 lane = dead for the run + cross-host escape; a stale-key 401 had struck a document's extract to FAILED); TRANSPORT-FAILOVER-CROSS-HOST (lone doc: lane_i+n_lanes wrapped onto itself, one gemini 503 failed the attempt → walk to a different host); EXTRACT-SCALE-OUT-V1 (autopilot wakes one extract worker per open ticket, cap 3 — GLiNER-era single-worker rule retired; second book no longer waits); RERANKER-DURING-INGEST-V1 (extract demand no longer parks the reranker — GLiNER-era ceiling rule; every query during an ingest waited the full 90s wake budget, measured 91–95s; breaker verified 0.02s). 12 throughput + 6 autopilot tests green | DONE (2026-09-01, work-log 2026-09-01-family-interleave-openrouter-lanes.md; live receipt recorded there) |
| 11.40 | CENSUS-UNCACHED-DIRTY-V1 (owner's double-check pass): incremental census re-evaluates every ACTIVE run with no cached verdict — gap verdicts are never cached, and a run whose last ticket closed just before a sibling advanced the GLOBAL watermark was never re-evaluated (Netnography: chain complete, pinned at reconciling while its sibling promoted 5s later). One clause, no watermark semantics changed; 5 census tests green | DONE (2026-09-02, work-log 2026-09-02-census-uncached-dirty.md) |
| 11.41 | PROVIDER + KEY SCRUB 2026-09-02 (owner: which models are worth keeping, at model@host / host / key level): eval/v5/fleet/PROVIDER-SCRUB-2026-09-02.md — KEEP qwen3.5-397b (weight up), mistral-small-2603 + ministral-14b @ OpenRouter, gemini flash-lites as bulk carrier (all 6 keys), nvidia both; CUT groq host to one escape key, all campaign FAILs; DEMOTE local Qwen3.5-4B (76% quarantine on its only run vs 0–5% cloud) — owner decisions: local-lane floor rule, groq key count, gemma-3-4b; receipt accepted_count found dead (0 on 495 receipts) | ANALYSIS DONE (2026-09-02); decisions = owner gate |
| 11.42 | RECEIPT-ACCEPTED-COUNT-FIX (scrub finding): extraction_call_receipts.accepted_count was 0 on 495/495 receipts — the dispatch summed packet fields that do not exist (flat entities/relations/digests vs items[]); now sums proposals per item. Semantics corrected: the field is the PROPOSED count per packet (pre-gate, so replay stays byte-equivalent); gate outcomes live in the extract artifact. 13 throughput tests green; 11.32's "accepted_count" wording is superseded by this row | DONE (2026-09-02, work-log 2026-09-02-receipt-accepted-count-fix.md) |
| 11.43 | GAMBLING-E2E-0902 + COVERAGE-DROP-TOLERANCE-V1 + DEGRADE-IDEMPOTENCY-FIX: single-book from-zero test (47 KB) landed on the LOCAL lane — extraction 166s, 16/18 quarantined, 8/10 neighborhoods dropped, degraded (same bytes on cloud a day earlier: 30s, 0 quarantined, 69 ents/26 rels) → small-doc lane choice is worker-affinity luck; CLOUD-FIRST-V1 staged = owner gate. Fixes: dropped neighborhoods block promotion only above 10% of sent (Alchemy was held forever over 1/106; coverage floor stays soft); apply_degrades re-marks a run whose status was reset even when reasons match. SiliconFlow Qwen2.5-7B canary FAIL (150–158s/call). Retrieval on the corpus: HYBRID/CHAT 2.6–3.3s, 13–16 citations | DONE (2026-09-02, work-log 2026-09-02-gambling-e2e-coverage-tolerance.md) |
| 11.44 | CLOUD-FIRST-V1 (owner-blessed 2026-09-02) + residuals: floor 300_000→0 (policy/settings/.env) so every document rides the cloud ring, local lane no longer size-selected (small-doc lane choice was worker-affinity luck; retires the 2026-08-29 ≤300 KB rule, register 1.10/4.x deviated). STATUS-MONOTONE-V1 = the unknown writer root-caused: every chain worker's run_status('reconciling') on stage completion overwrote verdicts; now a progress write applied only over intake/reconciling. ENV-OVERLAY-ON-SPAWN-V1: every spawn overlays current .env (key rotation needs no fleet restart). GRACEFUL-LEASE-HANDBACK-V1: fence restarts hand leases back without consuming attempts. extraction_drop_tolerance setting = 0.10. DOCUMENT-DELETE purges extraction_call_receipts. SPLIT-KEEPS-PARTIAL: V2 output split back-fills from the truncated partial (data loss pin). Legacy coverage/audit harnesses made hermetic (they had been hitting real endpoints). SUMMARY-JOB-IDEMPOTENCY-V1: summary_jobs pkey collision on re-ingest of identical bytes (parent_summary failed 3/3) — stale same-ticket job superseded; delete cascade purges summary_jobs | DONE (2026-09-02, work-log 2026-09-02-residuals-cloud-first.md; receipts appended there) |
| 11.45 | STALL-TRACER-V1 (owner law 2026-09-02: any unit stuck > 3 min is a defect to trace, never waited out): control tick phase `trace_stalls` diagnoses every stage ticket / run / summary job older than `control.stall_threshold_s` (180) with the scheduler's own predicates — 15 deterministic diagnosis codes (READY_NO_CLAIM_EVENT, LEASED_OWNER_GONE, PENDING_ON_PREDECESSOR, RUN_NO_TICKET_CHAIN, …) — persisted one row per episode in `stall_traces` (migration 0046), resolved the tick the unit moves; evidence only, never a mutation; phase fenced in a savepoint. Operator view `scripts/trace_stalls.py`. First live collect: 6 phantom `intake` runs of deleted corpora (CORPUS-DELETE cascade gap) — owner-authorized delete 09:09Z, episodes resolved next tick. + CONTROL-HEARTBEAT-WATCHDOG-V1: supervisor restarts a control.main that completes no tick for stall_threshold_s (probe every 30 s, boot grace = threshold; activates at the next supervisor restart). | DONE (2026-09-02, work-log 2026-09-02-stall-tracer.md; 8 tests) |
| 11.46 | SILICONFLOW-DECOMPOSITION + CANARY-LIMITER-ISOLATION + CANARY-CALL-DECOMP (owner 2026-09-02: "150 s per call — something ain't right"): streaming replay of the client's exact payload shows SiliconFlow serverless = ~30 s queue + 16–18 tok/s + degenerate output to the 2,500 cap, while the same Qwen2.5-7B weights on OpenRouter answer in 4–10 s (TTFT 0.5 s, 53–55 tok/s, valid JSON) → endpoint DROPPED; the same model PASSED the production canary at OpenRouter (8/8 in 6.4 s mean, 56 tok/s, 0 limiter wait, enrichment 6/8, 97 s total; density 13.4/1Kw = bulk tier). The canary had shared production's PRIMARY AIMD row (`llm_cloud[default]`, 23 decreases) — now keyed `canary:<host>`; every canary call prints limiter wait / attempts / tokens / tok/s / finish_reason. | DONE (2026-09-02, work-log 2026-09-02-siliconflow-decomposition.md; owner REMOVED Qwen2.5-7B from evaluation after the weakness A/B — never wired; SiliconFlow key removed from .env) |
| 11.47 | PROVIDER-REMOVALS-0902 (owner orders 2026-09-02): Qwen2.5-7B (endpoint + weights), gemma-3-4b-it and the entire groq host removed from the fleet — groq1–5 out of config/cloud_providers.json, groq family + groq1–5 blocks out of limiter.yaml, GROQ_API_KEY(_1–5) + SILICONFLOW_API_KEY out of .env, llm_controller_state rows llm_cloud[groq1–4] deleted. No lane was live (groq parked/dedicated, the other two never wired), so no pipeline behavior changed; the gpt-oss-20b escape rep is gone with the host. | DONE (2026-09-02, work-log 2026-09-02-provider-removals.md) |
| 11.48 | QUICK-MODEL-GRADE-V1 (owner 2026-09-02: "quick test… 2 chunks with an answer key… all in 5 mins"): standing tool eval/v5/fleet/quick_model_grade.py + hand-authored key (2 chunks, 14 gold entities, 9 gold relations, 8 must-cover terms; sha256-pinned text; canonical predicates) grading extraction (recall/precision/relation/hallucination) and enrichment (envelope/coverage/gist) through the production client+gate+compiler, all models concurrently, 120 s budget each; pre-registered rubric A/B/C/F. Seven models in 76 s: reference A; qwen3.7-flash B only with thinking off (reasoning tokens ate the budget as-is); granite-4.0-h-micro B (enrichment-only); llama-3.1-8b, granite-4.1-8b F (capability); ling-3.0-flash F (upstream 429 + 130 s/chunk); inkling-small:free F (403, harness-only). Follow-up same day: failures removed; qwen3.7-flash canary PASS with reasoning off (70 s, 118 tok/s, 8/8 enrichment); LFM-2.5 F (mandatory reasoning); Gemma-4 :free route broken upstream on OpenRouter; Gemma-4 direct on Google = best extraction measured (0.78, 0 hallucination) but enrichment blocked by undisableable inline thinking → HOLD for a native adapter. Hardened same day: enrichment graded on two parents (easy 2-child + hard 8-child) packed in ONE microbatch call (production shape) after ministral-3b passed the single easy parent but failed the canary — it now grades C with ENRICH_NO_RESPONSE. | DONE (2026-09-02, work-log 2026-09-02-quick-model-grade.md; results eval/v5/fleet/QUICK-GRADE-2026-09-02.md) |
| 11.49 | OPENROUTER-LANE-3 (owner 2026-09-02 "which ones work well and should be wired"): qwen/qwen3.7-flash wired as `openrouter3` on the owner's second OpenRouter key (OPENROUTER_API_KEY_2), `reasoning_effort: none` mandatory, structured json, in the extraction ring (13 lanes) and the parent_enrichment pin (8). Evidence: production canary PASS 70 s (8/8 at 5.3 s mean, 103–130 tok/s, enrichment 8/8 gist 1.00), quick grade B. Not wired with reasons on record: Gemma-4 direct on Google (best extraction, enrichment blocked by undisableable inline thinking → HOLD for a native adapter), granite-4.0-h-micro (slow, extraction weak); everything else removed by the owner. Receipt run pending. | DONE (2026-09-02, work-log 2026-09-02-openrouter-lane-3.md) |
| 11.50 | OPENROUTER-ENRICHMENT-LANES-V1 (owner 2026-09-02 "both will be for enrichment lane since enrichment is slow", third OpenRouter key): mistral-small-24b-2501 wired as `openrouter5`, dedicated (enrichment pin only, 9 lanes), json — canary on that key PASS 121 s (8/8 + 8/8, 24.7 f/1Kw). ministral-3b-2512 NOT wired: canary enrichment 4/8 then 1/8 on 8-child parents (the 2-chunk quick grade had passed it — tool gap recorded). | DONE (2026-09-02, work-log 2026-09-02-openrouter-enrichment-lanes.md) |
| 11.51 | ENRICHMENT-CONCURRENCY-SETTING: `enrichment_batch_concurrency` was read via getattr with a hard 5 and never declared on WorkerSettings — no env value could size it to the pin (8 lanes pinned, 5 batches in flight). Declared (default 5, 1–32, env POLYMATH_WORKER_ENRICHMENT_BATCH_CONCURRENCY); .env = 9 = pin size; 4 tests. The actual lever for "enrichment is slow". | DONE (2026-09-02, same work-log) |
| 11.52 | POLYMATH-MCP-V2 (owner 2026-09-02 "test the mcp hermes will use to upload, query, check statuses"): the V1 server was running KEYLESS (launchd bash denied ~/Documents by TCC → `. .env` failed since 08-31) and its `if API_KEY` gate therefore served tools/call to anyone, including on the public mirror; it also had no upload/status tools. V2: fail-closed gate (503 without key, 401 wrong bearer), tools list_corpora/list_documents/upload_document/upload_text/document_status/corpus_status/retrieve/ask (corpus_id required — unscoped ask measured 20 s + abstention vs 3 s + 16 citations scoped), orchestrator GET /status (run, stages, enrichment, open stall traces), MCP now a supervised ALWAYS slot (launchd agent disabled). 5 tests; live: 401 no-auth local+public, 8 tools with key. | DONE (2026-09-02, work-log 2026-09-02-polymath-mcp-v2.md) |
| 11.53 | ENV-INLINE-COMMENT-OUTAGE (postmortem, 2026-09-02): an inline comment on a .env value line (`…=9   # …`) is kept as the value by pydantic-settings → WorkerSettings invalid → orchestrator crashed on every respawn → supervisor quarantine → API down ~2 min. Fixed (comment on its own line, fleet bounce). Law: no inline comments on .env values; settings smoke test after every .env edit. | DONE (2026-09-02, same work-log; memory feedback_env_and_launchd_gotchas) |
| 11.54 | ENRICH-IDENTITY-V2 + ENRICH-OWN-DOC-FIRST (traced from the owner's MCP upload test, 2026-09-02): the enrichment identity hashed `<lane>:<model>`, so every pin-group change re-sharded parents and re-enriched the corpus (1,309 rows/day for 1,374 parents; 188 parents redone with identical source_hash) while a fresh upload waited behind the nine-book sweep. Identity now = source + prompt + contract/bounds (`enrichment_contract_id`); one-time re-key of 2,506 rows (batched, SKIP LOCKED, idempotent); the sweep starts with the run's own document. Live: re-enrichment stopped at once, new book enriched first. 6 tests. Open: outer sweep transaction holds row locks for 20+ min; lease/re-mint interplay. | DONE (2026-09-02, work-log 2026-09-02-enrich-identity-v2.md) |
| 11.55 | ENRICH-BUDGET-V2 + ENRICH-CALL-VISIBILITY (2026-09-02, traced with the new per-call log): verbose lanes hit finish=length at 700×n+200 (gemini-lite 3,300+ chars at 700 tokens; qwen3.7/nemotron at 2,300), truncated envelopes failed the gate, the ladder split into more truncated calls — 29/54 parents ground for 20 min. Enrichment identity = output SHAPE (budget is a serving knob; profiles share identity, no re-enrichment on switch); per-call budget 1.3×/parent+300 (cap 8000); one doubled retry on a likely-truncated envelope before splitting; production profile (900) on; one INFO line per enrichment call + split/gate lines. 2,543 rows re-keyed. First 2 min: stop 5/length 1, 37→45/54. | DONE (2026-09-02, work-log 2026-09-02-enrich-identity-v2.md postscript) |
| 11.56 | SIDECAR-READINESS-GATE-V1 + TICKET-TRANSIENT-RELEASE (traced 2026-09-02 from the MCP upload test): the autopilot woke the embedder and the qdrant worker in one tick; the worker failed the latent re-projection ticket 3× in 8 s against the still-booting embedder (breaker open) and froze the summary tail behind it. Now: `SidecarClient.wait_ready` (bounded /ready poll, clears the breaker), the projection worker gates before its first call, and `worker_runtime` hands a ticket back WITHOUT consuming an attempt on SidecarUnavailable (+15 s backoff). Stuck run recovered with retry_failed_stage; all 14 stages done 12:18:41Z. 4 tests. | DONE (2026-09-02, work-log 2026-09-02-sidecar-readiness-gate.md) |
| 11.57 | SUMMARIES-SCALE-OUT-V1 (owner "fix it", 2026-09-02): the autopilot wakes a second summaries worker (`summaries2`) when ≥ 2 summary-lane tickets are open (one worker had serialized enrichment at 45–105 s/call and every summary stage; parent_summary sat READY_UNCLAIMED behind enrichment). Fleet slot + budget profiles + test; safe by lease exclusivity + idempotent enrichment identity. launchd auto-boot remains an OWNER action (TCC denies launchd bash access to ~/Documents): grant Full Disk Access to /bin/bash or relocate the checkout. | DONE (2026-09-02, work-log 2026-09-02-summaries-scale-out.md) |
| 11.58 | READINESS-SWEEP-0902 + LLM-DIRECT-PRONOUN-GATE-V1 (owner: "bug free and 100% ready?"): full suite → 12 failures classified: 2 same-day regressions fixed (readiness gate getattr; breaker key getattr); 1 LIVE defect fixed — the llm-direct fact path wrote 13 ACCEPT facts with pronoun endpoints ("me IS_A british luxury designer"), now dropped+counted at materialize and the 13 retired to REJECT; census-test probe leak + stale embed-batching pin fixed; 7 pre-existing/stale failures chip-tracked with causes (sval ×3, killchain, llm_controller fake, relation_candidates GLiREL-era table, re-ingest orphan concept artifacts) + syntax_readiness DB-state flake. No "100 %" claim made. + GRAPH-ELIGIBILITY-DECISION-V1: REJECT facts are graph-ineligible at the shared predicate (projector/census/verify); reconcile left 0 edges on any REJECT fact. | DONE (2026-09-02, work-log 2026-09-02-readiness-sweep.md) |
| 11.59 | QUERY-RECEIPTS-V1 (owner 2026-09-02: "i need to ensure everything is good. even query retrieval tracking"): every served /chat, /ask, /retrieve writes ONE durable row to `query_receipts` (migration 0047: kind, client, corpus_ids, scope, mode, latent, question sha256 + 200-char head, wall_ms, status ok/abstained/error, verdict, citations, claims, evidence, source_docs, meta, error) from a thin timed wrapper around the unchanged handler body (`_<name>_impl`), on the success AND exception path, in its own short transaction (`clock_timestamp()`), failures logged QUERY_RECEIPT_FAILED and swallowed — a receipt can never fail a query. Read surfaces: `GET /queries?corpus_id&kind&limit&since_h` (per-(kind, mode) count / p50 / p95 / max / abstained / errors / avg citations + recent rows), MCP tool `recent_queries` (fail-closed on empty corpus_id, advertised in /health), `scripts/query_log.py`. Live 2026-09-03: 8-request battery → 8 receipts incl. an `abstained` nonce chat and an `error` row carrying QUERY_SCOPE_UNKNOWN; MCP session lists 9 tools. Tests: test_query_receipts.py 6. Work-log 2026-09-03-query-receipts-and-release-evidence. | complete (live receipts 2026-09-03) |
| 11.60 | QUERY-PATH-RESIDENT-V1 + REGISTRATION-RETENTION-V1 (same log): the autopilot parked the embedder/reranker sidecars when the pipeline idled, so the first query after idleness paid a cold start and `verify_product_readiness` failed on /ready → `fleet_autopilot.ALWAYS` += sidecar_embedder, sidecar_reranker (test_fleet_autopilot_demand 7 green; readiness PASS 8/8 after the bounce, sidecars up in 11–13 s). Worker registrations were never deleted (3,145 dead rows beside 1 live) and `verify_live_build.check_workers` reported them as STALE workers on old builds → `worker_supervisor.prune_dead_registrations` (24 h retention, in `sweep`) and the fence ignores registrations past the heartbeat window — only a LIVE worker on the wrong build fails it. | complete |
| 11.61 | RUN-SCOPED-RECEIPTS-V1 + STALL-TRACER-V1.2 + RELEASE-EVIDENCE producers (2026-09-03, work-log 2026-09-03-query-receipts-and-release-evidence): the tracer's first multi-document watch found the advance predicate CORPUS-scoped for every stage — document B (query_ready 09:33Z) held `parent_summary` PENDING ~17 min because siblings C/D were still extracting (evaluated live: attempts ok, artifacts ok, qdrant receipts run=True / corpus=False). Fix: `CORPUS_STAGES=(corpus_summary, vocabulary)`, `receipt_scope_for(stage)`, `_run_doc_ids`, `projection_want.missing_chunk_receipts_for_docs` (same want rule as the bulk check: qdrant child tier only), cache-first `_receipts_present(scope)` with a run-scoped verdict key; advance picks the scope per stage; tracer mirrors it and exempts only CORPUS-stage tickets waiting on live sibling work. Live: control bounced 09:52:41Z → B `parent_summary` READY 34 s later while D was `extract leased` and the corpus still MISSING. Tests: test_run_scoped_receipts 4, test_stall_tracer 10, verdict-store/lock-contention/projection-want green. Evidence producers: `eval/v5/retrieval/record_fast_hybrid_evidence.py` → FAST_HYBRID PASS (12 probes, 0 foreign-corpus hits, p50 2.36 s); `eval/v5/measure_incrementality.py` → INCREMENTALITY PASS (43 changed → 125 projected; identical re-upload = 0 new work; mid-projection SIGTERM resumed: the 64 receipted chunks skipped, 985 embeds vs 1,067 uninterrupted); `replay_full.py --record-evidence` → EXACT_REPLAY not applicable to llm-direct corpora (sentence_slices = 0 rows; release-books-v1 has 0 docs) — stays UNPROVEN by design. Open: census promotion (`missing_chunk_receipts_for_run`) still corpus-scoped; BOOT_RECOVERY (TCC, owner) and SEALED_HOLDOUT (owner) unproven; probe corpus probe-incr-2026-09-03-7760 left in place. | complete (live receipt 2026-09-03) |
| 11.62 | LLM-DIRECT-CANON (owner 2026-09-03: "fix it so it's declared llm direct canon"; ADR-0017; plan `docs/wiki/plans/LLM-DIRECT-CANON-PLAN.md`): the LLM extractor was running inside the GLiNER pipeline's shape — measured on 20 documents: 46 % relation survival (UNATTESTED_RELATION_ENDPOINT 787 = the anchor-chunk substring rule), 2,415/2,680 type coercions to Concept, replay and retrieval validation grading the interpreter view / fact tuples, `extraction_provider` default still `gliner`, CLAUDE.md still prescribing spaCy/rescue/rule-pack knobs. Dependency map confirmed with graphify (gate → materialize is the only fact writer; nothing on the query path imports the tagger modules). LANDED P0–P2 (4a4b67d): ADR-0017 + architecture canon section + CLAUDE.md law + `llm_live` default; ATTESTATION-LEVELS-V1 (endpoint attestation = recorded level quote › anchor › neighborhood › document › abstract-with-token-support; junk/unattested-quote/interrogative stay hard gates; `POLYMATH_EXTRACTION_ATTESTATION=strict` rollback; levels in stage stats, evidence offsets and fact provenance `gate_version`); endpoint type by normalized surface (the old fallback routed the SURFACE through the TYPE mapper); Neo4j projects `raw_types`, `display_type`, `predicate_raw`. Tests: test_attestation_levels 6 + extraction/term-gate/pronoun/eligibility/llm_direct_facts green (42). Historical sval fixtures skip-marked. P3 `eval/v5/replay_llm_direct.py` (raw-response ledger replay, EXACT_REPLAY contract), P5 `eval/v5/holdout/` (answer-level grader + dev seed set; sealed set owner-supplied) — receipts in the work-log. Canary + P4/P6 status: see work-log 2026-09-03-llm-direct-canon. | P0–P3 + P5 complete 2026-09-03 (canary: relation survival 52 → 82 %; ledger replay IDENTICAL 103/103 → EXACT_REPLAY PASS; RECEIPT-COMPLETENESS-V1 f34ddb6); P4 complete (agent retired, 29 tests deleted, harnesses → eval/historical); P6 awaits owner go |
| 11.63 | LLM-DIRECT-CANON round 2 (owner 2026-09-03: "I agree with all 3 decisions, fix the chunker bug, findings need to be resolved"): P6 mechanism — the gate (version + attestation policy) is part of `contract_identity()` so a gate change is contract drift and `reingest_corpus.py` re-extracts; receipts carry `contract_ident` (migration 0049, 88 pre-change receipts stamped) and the ledger replay translates keys per era. CHUNK-GAP-ACCOUNTING-V1: `tier_chunk_layout()` records every dropped span (heading / dropped_stub / dropped_empty) as layout evidence, intake persists it, layout contract v2, fidelity gate measures the UNEXPLAINED gap; rows/ids unchanged. Census promotion run-scoped. Cloud-floor finding RETRACTED (CLOUD-FIRST-V1 floor 0 is owner-blessed 2026-09-02; the 450 KB change was reverted within the hour). Local extraction lane = supervised `local_extractor` slot (wakes with the extract lane) inside a 29 GB budget with a readiness wait. Abstention finding closed as data/question grounding (dev holdout 90 % supported, 0 wrong, 1 open). apple-ml agent retired; 29 interpreter-path tests deleted. P6 launched on cysa-study-v1 then ecom-meta-v1 — receipts in the work-log. | DONE (P6 converged 2026-09-03: cysa 2 + ecom 10 query_ready) |
| 11.64 | RETIREMENT-DELETE-V1 (owner 2026-09-03: "delete the retired code and do the blue/green reingest"; work-log 2026-09-03-llm-direct-canon item 12): the GLiNER/spaCy/rule-pack path is DELETED from the tree — `extract_worker.py` rewritten LLM-direct only (1,986 → 324 lines), both sidecar runtimes, 9 workers, `polymath_shared/rulepack/`, `syntax_readiness`/`discourse_bridge`/`fact_admission`, their settings + execution-contract keys + the S3 syntax claim gate removed; `compound_heads.py`, `verb_inventory.py` (190 verbs frozen as data, no import-time fallback) and `workers/knowledge_artifacts.py` (the compile_objects persister) carry what production still uses; 18 harnesses moved to `eval/historical/`. `bundle_integrity` pins the gate + `llm_direct.py`, census requires a production caller of the GATE, lock re-frozen `v5-production-002-llm-direct`. Tests retargeted where the invariant survives (compatibility on chunker/query_policy, staleness on semantic_bundle, authority pins → 7b7fbcd284b47850, lane-affinity at the policy boundary, summary idempotency retries the SAME ticket — a P23-era test drift). P6 converged (cysa 2 + ecom 10 query_ready). Phase B GENERATION-SWAP-V1 designed (plan "Phase B"); it owns the post-P6 lifecycle findings (orphan Neo4j Chunk/Evidence nodes, stale concept supporting_chunks). | DONE (deletion) / Phase B next |
| 11.65 | GENERATION-SWAP-V1 — blue/green re-ingest (owner 2026-09-03: "do the blue/green reingest"; work-log 2026-09-03-llm-direct-canon item 13): `reingest_corpus.py --blue-green` mints a shadow successor beside the serving run (predecessor untouched), intake keeps both chunk generations (migration 0050: uniqueness per generation), readers hide in-flight generations (`polymath_shared.generation.chunk_visible_sql` + Qdrant must_not; chunk points now carry `chunk_contract_version`), promotion swaps atomically (`control.generation_swap`: supersede, purge old generation, remove ungrounded derived artifacts, sweep Neo4j/Qdrant best-effort). Persister upserts refresh `supporting_chunks`. CONTRACT-DRIFT BLIND SPOT fixed: `extraction_gate` (gate version/attestation policy) is an execution-contract key and an extract dependency (with `ontology_file_sha`). Post-deletion canary: 1-doc corpus query_ready in 117 s, 63 llm_direct facts, FAST 9 citations. Live drill on cysa-study-v1 (2 docs, 294 KB): mint 18:41:33Z → swapped 18:45:08Z, 31/31 probes HTTP 200 (0 × 502), predecessors served throughout, answers identical pre/post (4 citations). Era-fence lesson: old-era runs cannot be re-armed; `sweep_orphan_derivatives.py` re-grounds their artifacts. | DONE 2026-09-03 (drill green) |
| 11.66 | RETRIEVE-EVIDENCE-ROWS-V1 — retrieval returns evidence, not ids (owner 2026-09-03: "the rag needs to return evidence not ids & make it transcript aware… a ideation mode is important to have for abstract pull and breadth"; work-log 2026-09-03-retrieve-evidence-rows): `POST /retrieve` `evidence: true` / `mode: EXPLORE` adds `evidence_rows` (`contracts/retrieve/v1/evidence_row.schema.json`): chunk rows with timecodes split out, document-summary rows, graph facts only with attesting chunks, EXPLORE graph hops; source = title · channel · date · timecode, never a path. Frontmatter parsed once (`polymath_shared.frontmatter`), stamped at intake (migration 0051) and backfilled (`scripts/backfill_frontmatter.py`). MCP `retrieve(..., explore=True)`. Live: 14 precision rows / 38 EXPLORE rows over 11 docs incl. 11 attested facts. Ranking untouched. | DONE 2026-09-03 (live-probed) |
| 11.67 | CORPUS-PLAN-V1 + CAPABILITIES-V1 — Polymath as a detectable agent layer (owner 2026-09-03: "make this polymath native while allowing the skill to also work in any new versions or work in others"; work-log 2026-09-03-corpus-plan-capabilities): `GET /capabilities` advertises contracts (additive), `POST /retrieve/plan` compiles 3–5 reformulations from one signal (byte-for-byte port of TRAIL's compiler, parity fixture pinned in both repos) and returns merged evidence rows with `query_ids`; MCP `capabilities` / `compile_plan` / `retrieve_evidence`; frozen sample response shared with the consumer. Consumers switch on contracts, never on the name; `--generic` = control arm. | DONE 2026-09-03 |
| 11.68 | FIELD-EVIDENCE-CORPUS-V1 (owner 2026-09-03: "go build 3 and 4"; work-log 2026-09-03-typed-claims-field-evidence): `scripts/ingest_field_evidence.py` turns a TRAIL OS run's curated observations into one document per community thread (FIELD_OBS paragraphs with author/roles/freshness/gap + provenance frontmatter) under `field-evidence-v1`; `/capabilities` detects the corpus from `runs`; evidence rows always carry `document {source_name, frontmatter}`; consumer re-materializes rows as observations with the original identity. 283 obs → 43 docs ingested. | DONE 2026-09-03 (Arm 2: 17/18 gaps with corpus support, fresh threads 30 → 11) |
| 11.69 | TYPED-CLAIMS-V1 (same owner instruction; same work-log): extractor labels lived claims — `claim_kind ∈ friction | behavior | workaround | purchase_language` on RelationProposal (schema + prompt rule 9 + LEAN 5th element), gate passthrough, persisted in `facts.qualifiers` with an ON CONFLICT merge so re-extraction enriches existing facts; graph_fact evidence rows carry `claim_kind`; `/capabilities.typed-rows` lists kinds present. Bundle re-frozen `v5-production-004-typed-claims`. Canary + blue/green receipts in the work-log. | REVERTED 2026-09-03 (owner: RAG extraction unchanged; consumers ASK the RAG instead — see work-log reversal) |
| 11.70 | CHAT-EVIDENCE-ROWS-V1 (owner 2026-09-03: "for this workflow i want my full power rag system to be used for my agent ideation"): `POST /chat` accepts `evidence: true` and returns the grounded answer (hybrid + rerank + graph/latent lanes + admission + synthesis with citations) PLUS the same chunks/documents/facts as RETRIEVE-EVIDENCE-ROWS-V1 rows — one call, one contract, no second retrieval, extraction untouched. `/capabilities.chat-evidence: v1`; MCP `ask(..., evidence=True)`. Consumer: TRAIL OS corpus lane `--via chat` (default in native mode) records `corpus_answers` (authority CORPUS_SYNTHESIS, abstentions kept) and cites the rows. Corpus names: `ingest_field_evidence.py --corpus-name` (id minted, display name via PATCH /corpora/{id}); consumers resolve names via GET /corpora. | DONE 2026-09-03 |
| 11.71 | RESEARCH-PACKAGE-V1 (owner 2026-09-03: "it just needs to be together in one repo … in its own folder"): TRAIL OS lives in this repo as `research/` (its own harness 396 checks, docs, worklog, registry); no cross-imports, contracts stay the seam; Hermes skill dir → symlink; next: MCP `research_*` tools so the controlled run is a Polymath capability. Work-log 2026-09-03-research-package. | DONE 2026-09-03 |
| 11.72 | LIVED-WORLD-V2 (owner 2026-09-04: "real communities generate the lived-world nouns; the corpus supplies deeper mechanisms and analogies; θ bridges them; Python decides what is allowed to count"): POPULATION_DISCOVERY before ideation in `research/` — leads (authority LEAD, never demand) from corpus / registry / signal / prior field rows / open field; external `field_records` instantiate them; ParticipantEvidenceCards + LivedEvidenceClusters (THIN / ANCHOR by 5 records · 2 threads · 3 independent voices, tighten-only); lived situations keep unknowns (biographies rejected); hypotheses name `lived_anchor_ids` or CORPUS_ONLY; corpus asked at friction/mechanism level (`corpus_mechanisms`, `--questions`); CORPUS_EXAMPLE tagging + provenance verdicts (GROUNDED / CORPUS_ECHO_UNGROUNDED) replace category blacklists; cited-contribution receipt; VOI work queue with round / stagnation / wall-clock ceilings (no fan-out); seven schemas enforce authority labels; `tests/calibration_acceptance.py` (six semantic receipts). Harness 454 checks. Work-log 2026-09-04-lived-world-v2. | DONE 2026-09-04 (calibration run pending) |
| 11.73 | DOCUMENT-SCOPED-RETRIEVE-V1 (owner 2026-09-04: "add document-scoped retrieve support if practical, but do not confuse forced document diversity with meaningful cited contribution"): optional `document_ids` on `POST /retrieve` (default lane + EXPLORE) and `POST /retrieve/plan` — applied at the stores before `limit` (SQL `doc_id = ANY`, Qdrant payload filter, graph seed/authorization on `ev.doc_id`, EXPLORE hop kept inside the filter); FAST/HYBRID/GRAPH/WILDCARD answer a typed 422 `document_filter_unsupported`; `GET /capabilities.contracts.document_ids = true`; absent field = byte-identical statements. 17 pure tests (fakes apply the clause as the stores would). Not wired into the research adapter on purpose (contribution is measured by cited rows, docs/25 §6). Work-log 2026-09-04-document-scoped-retrieve. | DONE 2026-09-04 |
| 11.74 | RESEARCH-FINAL-CORRECTNESS-V2.1.2 (owner 2026-09-04 execution directive: "verified defect → owning file → failing regression → minimum change → freeze"): population canary split (open-field vs LATENT resolution), explicit SOURCE_AGNOSTIC_CALIBRATION mode, CorpusPresenceReceipt via existing /documents + /retrieve, deterministic field origin (FIELD_NAMED / WORKAROUND_DERIVED / NOT), fail-closed document scope, receipt terminology, controller admission fixes found live (hop refs admit latent structures, empty submitted lists, terminal verdict), transitions law (all bridges dead → NO_DEFENSIBLE_BRIDGE), L4 dossier lanes; novel mislabeled as 'Alchemy (Sutherland)' re-ingested as Always Alchemy (Hart). Harness 555. Receipts: books regression + novel-seeded run 02 (NO_DEFENSIBLE_BRIDGE, source-agnostic pass). Work-log 2026-09-04-final-correctness-pass. | DONE 2026-09-04 |
| 11.75 | SUMMARY-SWEEP-SERIALIZATION-V1 (owner 2026-09-05 "start the deadlock fix"): two summary workers sweeping one corpus deadlocked across processes on summary_jobs (stage, input_hash) — outer ticket transaction held uncommitted upserts while the peer's short transaction upserted the same key; Postgres saw no cycle. Fix: `_ensure_job` bounds its lock wait (`SET LOCAL lock_timeout`, POLYMATH_SUMMARY_LOCK_TIMEOUT_MS, default 60 s, raises); every sweeping handler takes a transaction-scoped advisory lock per (stage, corpus) BEFORE writing (`_sweep_lock`, waits outside any row lock, POLYMATH_SUMMARY_SWEEP_WAIT_S); `run_worker` registers faulthandler on SIGUSR1. Regression tests/determinism/test_summary_sweep_serialization.py (real Postgres). Work-log 2026-09-05-summary-sweep-serialization. | DONE 2026-09-05 |
| 11.76 | CI-DEPS-V1 (owner 2026-09-05 "fix the determinism and contracts CI too"): both workflows install the workspace members (+ pytest, jsonschema, mcp); determinism runs a postgres:16 service with all 51 migrations applied; 3 data-dependent tests skip on an empty store; 1 stale test fake fixed; migration 0052 closes the schema drift the first migrated-schema run exposed (stage_tickets.last_error_note). GitHub Actions on 73d571b: determinism 33941188754 success, contracts 33941188785 success, repo-governance 33941188832 success, agent-preflight 33941188765 success. Work-log 2026-09-05-ci-deps. | DONE 2026-09-05 |
| 11.77 | DOCUMENTS-LIST-SUBQUERY-V1 (owner 2026-09-05 "in the front end I don't see any files"): GET /documents cross-joined chunks × parent_enrichments per document — 79.7 s on cinema (67 docs / 79,787 chunks), Files view empty. Correlated subqueries on the doc_id indexes: 1.9 s live, identical numbers; regression test_documents_list_query.py. Work-log 2026-09-05-documents-list-subquery. | DONE 2026-09-05 |
| 11.78 | PROVIDER-CAPACITY-IS-TRANSIENT-V1 + TRANSIENT-HOLD-V1 (owner 2026-09-05 "5 hrs … a whole lot of errors"): HTTP 429 / lane-refused transport errors and TransientStageHold hand the ticket back READY without an attempt (60 s backoff for capacity); the summary sweep lock yields after 30 s instead of blocking a lane slot (parent_summary had sat READY_UNCLAIMED 4.2 h). 12 tests; 3 failed extractions re-armed and completed. Work-log 2026-09-05-transient-capacity-and-hold. | DONE 2026-09-05 |
| 11.79 | MEDIC-V1 + ORCHESTRATOR-LIMBO-V1 + PIPELINE-DEGRADED-V1 (owner 2026-09-05 "I need this auto-healing and correcting itself when I leave or go to sleep"): control tick gains a receipted `medic` phase — capacity-failed tickets re-armed by id (20/tick, 5/ticket/day), lock waiter blocked by an idle-in-transaction session ≥120 s freed by terminating the blocker; table medic_actions (0053); /health/pipeline DEGRADED from open stall episodes + recent medic actions; orchestrator slot readiness 20 s × 3 (was 120 s × 5). 5 tests incl. a real two-session deadlock freed live. Work-log 2026-09-05-medic. | DONE 2026-09-05 |
| 11.80 | ENRICH-DOC-PARTITION-V1 + ENRICH-LADDER-FANOUT-V1 (owner 2026-09-05 "what is the concurrency setting for enrichment, it's too slow"): parent_enrichment claims documents with a per-document advisory try-lock instead of the corpus-wide sweep lock (second summaries worker had yielded SUMMARY_SWEEP_BUSY 315× in 2 h); semantic-failover and hard-case ladders fan out on the lane-cap pool instead of one call at a time (50–94 s serial gaps between waves). Measured before: 12 parents/min, lanes ~95 % idle. 3 new tests; structural law updated. Addendum 14:20Z: ENRICH-OUTCOME-DURABILITY-V1 — INVALID/terminal outcomes were written on the never-committing outer ticket transaction (cinema: 8,370 READY, 0 INVALID rows after 5 h) so every sweep re-ran the same failing parents; now persisted per parent in committed transactions; ladder calls receipted (ENRICH_CALL_LADDER); nvidia retired from the enrichment pin (82–114 s wall, 51 % INVALID). Work-log 2026-09-05-enrichment-throughput. | DONE 2026-09-05 |
| 11.81 | PG-SHM-V1 (found while proving 11.80): Docker's 64 MB /dev/shm made parallel queries fail with "could not resize shared memory segment" 192× in 24 h (routing projection ×90, facts/evidence joins ×89, chunks scan ×10; project_neo4j retried 11 attempts). `max_parallel_workers_per_gather = 0` via ALTER SYSTEM + reload (verified 0, no restart); `shm_size: 1gb` on the postgres service in compose.yaml for the next recreate, then reset parallel. Endpoint latencies unchanged (0.36 s / 0.2 s). Work-log 2026-09-05-pg-shm. | DONE 2026-09-05 |
| 11.82 | HTML-STRUCTURE-V1 + TIER-CHUNKER-V3.1 (owner 2026-09-05 "fix the html list chunking and reingest handbook.html"): materializer 1.1.0 keeps HTML lists/tables/pre/headings as Markdown-shaped blocks (was one paragraph per block tag → 37 % stub children); tier chunker v3.1 merges sub-floor heading sections forward under shared ancestry (handbook: 3,167 parents avg 98 w → 802 avg 392 w; VES .md 1,322 → 704) and sends prose lead-ins into the block they introduce (stubs 37 % → 7 % handbook, 2 % → 0 % markdown); sub-stub/title sections still drop. Fixture structured.html; 9 new tests + exit gate restated. handbook.html deleted and re-ingested on the new contract. Work-log 2026-09-05-html-structure. | DONE 2026-09-05 |
| 11.83 | ENRICHMENT-ROW-TRUTH-V2 (owner 2026-09-05 "the file hasn't begun enrichment"): a sweep persisted 184 enrichment rows against deleted chunks 3 min after handbook.html was deleted/re-ingested (UI: "enriched 103 / failed 81" before the first real call; 922 orphans corpus-wide). Done-ness and EXISTING reuse now require a live parent chunk; orphans under an identity are deleted, not reused; INVALID→READY upgrades re-point the row; both persistence sites skip a deleted document (ENRICH_PERSIST_SKIPPED_DOC_GONE); job state is never proof. 922 orphan rows removed (pinned by id). 5 real-Postgres tests. Work-log 2026-09-05-enrichment-row-truth. | DONE 2026-09-05 |
| 11.84 | CHAT-QUERY-COMPILER-V1 (owner 2026-09-05 analysis: "Polymath's retrieval is fairly sophisticated; its conversational query compiler is essentially missing"): plan-of-record for a conversation-aware LLM query compiler (CHAT-INTENT-PLAN-V1: resolved request, task_type, retrieval/no-retrieval, 1–4 typed queries, must_answer dimensions), task-authority vs factual-authority synthesis contract, used-evidence-only carry, receipts on /chat/stream, ChatRuntime unification, hygiene (exam framing, /chat default HYBRID + truthful meta.mode, reranker OOM). Rev 2 adds: three explicit budgets (retrieval candidates → merged pool 80–120 → selection 20–30 → synthesis 12–18; breadth raised upstream only), stage-by-stage funnel receipts (retrieved → dedupe → RRF → rerank → selected → cited) as P0.0 before any cap moves, provenance-normalized RRF for subqueries, versioning (pass1-retrieval-v3 / CHAT-RETRIEVAL-PLAN-V1) with query_shape.depth_plan bound to the same policy, no similarity thresholds, carry v2 before breadth, AGENTS.md §5 layering. Rev 3 adds CHAT-RETRIEVAL-V2 (owner part 1): three independent retrieval experts — hierarchical route, global dense child, global sparse child — fused at CHILD level with full `arrivals`/`found_by_queries` provenance; cross-encoder as the only cross-lane judge; composition after relevance (8 pure → 6 diverse → 4 coverage, or post-rerank MMR 0.8/0.2); bounded multi-lane agreement boost; query-aspect coverage (primary runs A+B+C, subqueries B+C); rescue caps retired; final 15–20 (was 10/12); old document-level MMR stays off. Rev 4 (FINAL, owner part 2): five primitives A/B/C/G/W with modes as compositions (VECTOR=A+B, HYBRID=A+B+C default, GRAPH=HYBRID→bounded G, WILDCARD=core ∥ W); enforced latency architecture — one serial compiler stage, one embedding, concurrent lanes, one rerank per turn, deterministic composer (8/4/3/3 slots), wall-clock budgets with `degraded` receipts; rules: HYBRID ≤ VECTOR + 0.5 s, GRAPH ≤ HYBRID + 1.5 s, WILDCARD ≤ HYBRID + 2 s, more recall = more candidates never more serial calls; §9 defaults for the four open decisions; §11 unattended execution protocol. Rev 5 (owner part 3, migration protections, all verified in code): query-time migration only — no re-ingest; 18 protections incl. duplicate sparse paths (FastSearcher companion probe + _lexical_search), the Postgres lexical fallback banned from interactive chat, the Hybrid→Pass1 field-copy seam removed, one CandidateBudget authority, graph over canonical CandidateEvidence (no unassigned_rescue), one qvec shared by graph/wildcard, no function-attribute vectors, immutable SearchContext instead of concurrent mutable FastSearcher, corpus/generation filters centralized, exact_terms + semantic_queries in the compiler output, plan_for_query on the resolved request, wildcard baseline = final normal neighbourhood, interactive embedder wake budget; contracts candidate-retrieval-v1 / chat-retrieval-plan-v1 with ADR; 16-test pre-promotion suite. Phases P0.0–P1.g with numeric gates fixed in advance; 18 + 16 acceptance tests incl. the owner's exact video-gen conversation. docs/wiki/plans/CHAT-QUERY-COMPILER-PLAN.md | PLANNED 2026-09-05 |
| 11.85 | P0.0 RETRIEVAL-FUNNEL-V1 + baseline set B (CHAT-QUERY-COMPILER-PLAN §4 P0.0): engines expose per-lane candidates + pre-truncation union; every /chat/stream and /chat turn writes a receipt with a six-stage funnel (retrieved → union → pre_rerank → post_rerank → selected → cited), phase_ms, used_evidence, legend; receipt meta JSON-safe to 64 KB (old 8 KB slice dropped rows); `scripts/chat_funnel.py`, `scripts/chat_baseline.py`. BASELINE (HYBRID, deterministic synth, n=30 cinema+ecom): gold retrieved 0.733 · in union 0.600 · pre-rerank 0.433 · hit@10 0.433 · MRR 0.371 · cited 0.433 · wall p50 6.29 s; deaths CITED 13 / LOST_AT_UNION_TRUNCATION 9 / NEVER_RETRIEVED 8. Video-prompt follow-up: 47 retrieved → 10 selected → 9 cited on a turn needing no retrieval. Work-log 2026-09-05-p00-funnel-baselines. | DONE 2026-09-05 |
| 11.86 | P0.a chat hygiene (CHAT-QUERY-COMPILER-PLAN §4 P0.a): CORPUS-STYLE-V1 (study/exam layer out of the core prompt; per-corpus style, default neutral, cysa-study-v1 study), CHAT-DEFAULT-HYBRID-V1 + CHAT-MODE-TRUTH-V1 (/chat default HYBRID, executed mode stamped, requested mode recorded, no invented label), RERANK-BATCHING-V1 (batched scoring, OOM back-off, typed 503). Gates: stream receipts 34/34 with funnel; 40-doc rerank 200; single-turn + full suite green; /chat no-mode answer 22 claims / 1.9 KB (was 12,732 / 437 KB). 6 tests. Work-log 2026-09-05-p0a-chat-hygiene. | DONE 2026-09-05 |
| 11.87 | P0.b CHAT-INTENT-PLAN-V1 in shadow (CHAT-QUERY-COMPILER-PLAN §4 P0.b): shared/polymath_shared/chat_plan.py (contract validation, law-1 task-class guard, CHAT-PLAN-CORRECTIONS-V1, soft/hard budgets, fallback = today's behavior); stage pin chat_compiler = compiler1–4 (Gemini keys 1–4 @ 3.1-flash-lite) + compiler_alt (OpenRouter mistral-small), own limiter keys; family-diverse attempt order + 120 s lane cooldown + 6 s HTTP timeout; POLYMATH_CHAT_COMPILER off|shadow|on (default shadow, compile beside retrieval, receipted as meta.chat_plan). GATE (final canary): B fallback 0.0 % (< 5 %), task verb kept 6/6 fixtures + 30/30 B, p50 2.01 s (≤ 2.5 s), fixtures 6/6 as expected. Attempt 1 measured 6.7 % (Gemini timeouts) → timeout/cross-family/cooldown; attempt 2 exposed a task-type swap → corrections. 11 tests. Work-log 2026-09-05-p0b-compiler-shadow. | DONE 2026-09-05 |
| 11.88 | P0.c compiled retrieval + no-retrieval routing (CHAT-QUERY-COMPILER-PLAN §4 P0.c): POLYMATH_CHAT_COMPILER default `on` — the plan is stage 0 of /chat/stream; engines search `retrieval_text_for(plan)` (PRIMARY compiled query + verbatim exact_terms the rewrite dropped); `retrieval_required=false` (TRANSFORM / CONTINUE / CREATE-without-corpus / GENERAL) skips retrieval (`retrieve_skipped` phase, empty funnel, `chat_plan.retrieval_skipped`); fallback plan = raw message = pre-P0.c behavior; per-request `compiler` override; correction C `corpus_scope_term` (corpus id is scope, never a query word); follow-up fixtures (`chat_baseline.py --followups --reference`). GATE: follow-ups hit@10 0.000 (compiler off) → 0.947 on the single-turn-retrievable subset (≥ 0.8; all-30 0.600, single-turn ceiling 0.633, 11 items unretrievable in either form); TRANSFORM/CONTINUE fixtures 0 retrievals; B hit@10 0.433 → 0.633 (≥ 0.383), gold retrieved 0.733 → 0.833, cited 0.433 → 0.633; 0 compiler fallbacks; wall p50 6.29 → 9.18 s (serial compile). Attempt 1 (subset 0.882, corpus id injected in 8–9/30 queries) → correction C + prompt rules → attempt 2. 26 chat tests + 1310 offline. Work-log 2026-09-05-p0c-compiled-retrieval. | DONE 2026-09-05 |
| 11.89 | P0.d SYNTHESIS-V2 (CHAT-QUERY-COMPILER-PLAN §4 P0.d / §3.4): `_LLM_GROUNDING` replaced — the plan's authority block verbatim (USER INTENT HAS TASK AUTHORITY. CORPUS EVIDENCE HAS FACTUAL AUTHORITY), evidence absolutism removed, citation/completeness rules kept; request block = request as written + RESOLVED request + TASK · EVIDENCE POLICY · RESPONSE TYPE + MUST COVER + CONSTRAINTS + antecedent summary + PRIOR ARTIFACT verbatim (§9.3; POLYMATH_PRIOR_ARTIFACT_CHARS=16000, past the 4,000-char history cut); `_grounded_messages(plan=)` on both generators; `result.meta` += prompt_contract/task_type/evidence_policy/response_type/retrieval_required/compiler_fallback (LLM + deterministic paths); shadow/off keep the v1 block byte-for-byte; CITATION-PRECISION-V1 in chat_baseline.py. GATE (LLM, compiler on): video_prompt_final → 1,150-char final prompt, retrieval skipped, no abstention; B citation precision 1.000 → 1.000 (369/369 → 445/445), gold cited 0.633 → 0.633; 0/3 artifact fixtures say 'evidence doesn't contain'; absent-term probe names the missing premise then teaches adjacent material. Answer p50 2.4k → 3.0k chars, wall p50 11.3 → 14.7 s (LLM length). 32 live chat tests + 1313 offline. Work-log 2026-09-05-p0d-synthesis-v2. | DONE 2026-09-05 |
| 11.90 | P0.e CARRY-V2 (CHAT-QUERY-COMPILER-PLAN §4 P0.e / §3.5): frontend carries only USED evidence (used_evidence × legend, chunk_id, newest first, cap 8) — never the retrieved inventory; backend `_carry_candidates` → `_admit_carry` (normalise/dedupe/drop already-retrieved, re-hydrate via the assembler's `_resolve_chunk`, one batched rerank against the RESOLVED request, floor POLYMATH_CARRY_ADMISSION_FLOOR=0.25 on the sidecar's raw scores, cap POLYMATH_CARRY_CAP=8, survivors join the evidence bundle as `carried` with [S#] tags); raw carried prompt block removed; CARRY-ACCOUNTING-V1 (`retrieval.carry`, `result.meta.prompt`, receipts; reranker outage → cap-only + `degraded`); `scripts/chat_carry_probe.py`; frontend dist rebuilt. GATE (3-turn probe, turn 1 off-topic): turn-1 chunks in turn 3's prompt 5 → 0 (also 0 for v1 client inputs through the v2 backend); prompt chars T2 43,081 → 35,623, T3 47,321 → 38,731 (T1 carry-free, +23 chars); floor separation on-topic +1.1…+6.9 vs off-topic −3.25…−11.9; 0 reranker degradations. 36 live chat tests + 1316 offline. Work-log 2026-09-05-p0e-carry-v2. | DONE 2026-09-05 |
| 11.91 | P1.a CHAT-RETRIEVAL-V2 (CHAT-QUERY-COMPILER-PLAN §4 P1.a / §3.14 / §3.21 / §3.22): shared engine CANDIDATE-RETRIEVAL-V1 (`shared/polymath_shared/candidate_engine.py`: CandidateBudget = one budget authority, immutable SearchContext, CandidateEvidence with arrivals[]/query_ids[]/per-lane ranks; lanes HIERARCHICAL_ROUTE 16/24/6×2×3, GLOBAL_DENSE_CHILD K50, GLOBAL_SPARSE_CHILD K40 fused at child level by provenance RRF, region demotion kept, merged cap 120, one bounded rerank prefix 20 → final 15; lane C searches the plan's exact terms alone when present); chat path (/chat/stream + /chat HYBRID) on `chat_retrieve_v2` (plan `chat-retrieval-v2`) behind POLYMATH_CHAT_RETRIEVAL (default v2; per-request `retrieval`), latent/utility stay on v1. Seams closed: #1 no bm25 companion probes (ONE sparse search per turn), #2 no Postgres lexical fallback on the chat path (sparse lane DEGRADED instead), #3 no plan copy, #4 one budget, #11 dense+sparse share `FastSearcher._filter_for` (hidden generations incl.), #14 budget shaped on the resolved request; rescue caps retired. GATE (v1 → v2, back-to-back): B gold in union 0.667 → 0.900 (≥ 0.9), hit@10 0.600 → 0.700, MRR 0.433 → 0.477; L (30 acronyms/identifiers, df ≤ 2) gold in union 0.833 → 1.000 (= 1.0), hit@10 0.200 → 1.000; arrivals on 100 % of final candidates (30/30 turns, 0 missing, both fixtures); /retrieve /ask TRAIL untouched (1326 offline tests). Wall p50 B 11.25 → 9.87 s, L 6.25 → 13.46 s (rerank prefix 10–12 → 20 at ~240 ms/pair — reranker throughput is the open ceiling; v33 sidecars share the GPU). Three L attempts: hyphen tokenizer (fixture rule) → compiler expansion burying the identifier (exact-term lane C) → 30/30. FOUND: v1's sparse lane 404s on every HYBRID turn (`for collection in collections` over a dict) → Postgres scan was the running state. 48 live chat tests. Work-log 2026-09-05-p1a-chat-retrieval-v2. | DONE 2026-09-05 |
| 11.92 | P1.b decomposition + aspect coverage (CHAT-QUERY-COMPILER-PLAN §4 P1.b / §3.10 / §3.16): typed subqueries (≤ 3) run lanes B (K20) + C (K15) on their own vectors — ONE batched embedder call per turn (`fast._embed_queries`); lane A once on the primary; provenance-normalised fusion (`query_scores` per compiled query; per (subquery, document) one full-weight chunk; fused = best + bounded agreement ≤ 2×); ASPECT SEATS (≥ 3 judged prefix seats per subquery; weak = no candidates or best judge sigmoid < 0.5, with reason; one final seat per non-weak aspect); CHAT-PLAN-CORRECTIONS rule D (`compare_sides`: one query per side of a compare request) + prompt rule; per-aspect coverage (`aspects`, `weak_aspects`, `weak_reasons`, `final_detail`) in trace / answer event / `retrieve_done` / the synthesis request block (EVIDENCE COVERAGE BY ASPECT); one targeted second pass; fixture M (`--build-multi`) with strict (gold / own document) and system-honest readings; `retrieval: v2-single` A/B switch; `retrieval.latency_ms` in the answer event. GATE (clean pair, same code, fresh sidecars): M dims ✓-or-flagged strict 0.783 → 0.883 (silent 13 → 7), system-honest 0.85 → 0.983; wall p50 9.92 → 14.57 s (+4.65 s — latency gate MISSED under OOM contention; attempt-2 pair before aspect seats +0.6 s); one-vote-per-document-per-subquery by construction + unit test; B union 0.900 → 0.900, hit@10 0.700 → 0.700, MRR 0.477 → 0.516. Four measured attempts (0.667 → 0.867 → 0.883 → 0.883 strict); the literal 1.0 is not reached — residual = B's unretrievable heading anchors (system-honest ✓). FOUND: MPS OOM thrash when latent-enrichment embed batches coincide with chat reranks (both sidecars log OOM + split) — the 'sidecar drift'; recycle restores, root cause is the shared-GPU budget (owner / P1.d). Work-log 2026-09-05-p1b-decomposition. | IMPLEMENTED — QUALIFICATION OPEN (2026-09-05; reconciled 2026-09-06): dimension gate 0.883 strict / 0.983 system-honest against 1.0 and wall +4.65 s against +3 s MISSED after four measured attempts (0.667 → 0.867 → 0.883 → 0.883); cause hypotheses: PRIMARY-only heading anchors never flagged weak, MPS OOM contention with enrichment embeds. Not DONE until the R1 requalification records QUALIFIED or FAILED |
| 11.93 | R1 P1.b requalification (goal 2026-09-06; CHAT-QUERY-COMPILER-PLAN §4 P1.b): `select_evidence` judges the PRIMARY by the same weak floor as every subquery — when its best judged candidate is below `aspect_weak_floor` q0 is flagged `below_floor` (receipt + EVIDENCE COVERAGE line; selection unchanged); deterministic test of the recorded residual (M #5 "FACE OFF", named only by the primary, every candidate rejected, silently shown as covered). Measurement contract CHAT-M-REPLAY-V1: compiled plans FROZEN from the query receipts (`eval/fixtures/chat_{multi_M,baseline_B,lexical_L}_plans.json`) and replayed in-process through `chat_retrieve_v2` by `scripts/chat_m_replay.py` (arms interleaved; strict + system-honest + pre-R1 legacy reading from ONE run; per-stage latency; OOM events per turn; clean-turn subset). GATE (frozen plans, 30 questions / 60 dimensions): frozen-plan pair: v2 (P1.b) system-honest 1.0 (60/60) / strict 0.917 vs v2-single 0.9 / 0.817; wall p50 10.35 → 10.76 s (+0.41 s; clean turns +1.08 s) against +3 s; the primary was flagged below_floor on the recorded residual (best 0.18) — coverage gate MET as written, latency delta gate MET; strict reported with its ceiling (5/60: 3 outranked in-union golds, 2 heading anchors outside the union). Live production confirmation: live v2 system-honest 1.0 / strict 0.9, wall p50 18.38 s vs v2-single 0.933 / 0.783, 12.07 s (+6.31 s, sequential arms — the v2 window logged 29 embedder OOM events vs 6, embed p90 25.3 s; lanes + seats add 0.45 s at the median: contention, not decomposition). Sidecars recycled before measuring (fresh: embed 0.35 s, 20 pairs 4.3–5 s); contention is systemic — enrichment 8-item embed batches coincide with chat reranks (OOM 7 embedder / 10 reranker events during the run) and a 20-pair rerank stretches to 27–52 s. Work-log 2026-09-06-r1-p1b-requalification. | QUALIFIED 2026-09-06 — coverage gate met as written (system-honest 1.000 frozen-plan pair), latency delta gate met on the interleaved pair; absolute wall is GPU contention → P1.d. CI: bc051f6 preflight + governance FAILED (`missing declared path: …r2-hybrid-sparse-lane.md` — the scaffold declared the R2 work-log one commit early); the same tree plus the file is 88a6bda: preflight, contracts, governance success, determinism success |
| 11.94 | R2 hybrid-retrieval-v1 sparse lane (goal 2026-09-06): `_sparse_lexical_search` iterated `_corpus_collections(...)` — a dict corpus_id → collection name — and queried the KEYS as Qdrant collections (`'cinema'` → 404), so every v1 HYBRID turn (/retrieve HYBRID, /ask consumers, TRAIL, chat `retrieval: v1`) silently fell back to the in-memory Postgres `lexical_score` scan with `meta.degraded == []`. Fixed in the versioned authority (`.values()`); the scan stays the degradation path but is now counted: `note_sparse_fallback('sparse_empty' | 'sparse_error:<Exception>')` → `degradations()` component `sparse_lexical` in `meta.degraded` + `sparse_fallback` warning. Tests `test_hybrid_sparse_lane.py` (3: mapped collection queried and no chunk scan when BM25 answers; outage → scan + receipt; empty → scan + `sparse_empty`; state resets per request). Before (live 01:05Z): key → `UnexpectedResponse 404`, mapped name → 3 sparse hits (top 82.09) for "chroma keyer". After: three live /retrieve HYBRID calls on cinema after the orchestrator restart: meta.degraded [], lexical lane live, 0 sparse_fallback warnings. Contracts /retrieve, /ask, hybrid-retrieval-v1 unchanged; chat v2 never used this path. Work-log 2026-09-06-r2-hybrid-sparse-lane. | DONE 2026-09-06 |
| 11.95 | P1.c judge + composition (CHAT-QUERY-COMPILER-PLAN §4 P1.c / §3.17; EVIDENCE-COMPOSER-V1): after the one cross-encoder judgement over the fusion prefix (+ P1.b aspect seats), `compose_evidence` fills `synthesis_max` in deterministic, metadata-only slots — 8 pure relevance → ≤ 4 source diversity (soft max 3 per document unless the gap to the best unrepresented document ≥ 0.1 on the judge sigmoid) → ≤ 3 sparse winners (lane C arrivals) → ≤ 3 aspect seats (one per non-weak compiled query not yet represented, displacing the lowest non-sole representative) → fill in judge order with a DOMINANCE GUARD (one document holds ≤ 60 % of the set while ≥ 3 documents are within the gap; the cap lifts only when nothing else is left). Ordering key = sigmoid(judge logit) + bounded multi-lane agreement (0.02/lane, cap 0.05; near-ties only; no bonus when the judge is degraded — fusion order stands). Slots 2–4 never promote judge-rejected chunks (sigmoid < 0.5). Receipts: `trace/meta/retrieval.composition` {slots, doc_counts, doc_share_top, docs_within_gap, dominance, aspect_seats, agreement_reordered} + `final_detail`; query receipt whitelist += composition; chat_baseline adds survival_selected_given_union / dominance_violations / dominance_eligible_turns / doc_share_top_mean. Rerank prefix 20 → 24 by measurement (frozen-plan B, composer on: 20 → 0.815 survival, 24 → 0.852, 28 → 0.889 for +0.9 s / +2.6 s rerank p50; 24 clears the ≥ 0.85 gate at the smallest cost, 28 recorded as P1.d's deadline-aware ceiling). GATE: funnel survival given union 0.852 (gate ≥ 0.85), MRR 0.578 (baseline 0.519), avoidable dominance 0 / 8 eligible (gate 0; literal 0 reported), citation precision 1.0 (P0.d 1.000), M system-honest 1.0 / strict 0.933 unchanged, L hit@10 1.0; frozen-plan replays + live LLM run, 2026-09-06. Work-log 2026-09-06-p1c-composition. | DONE 2026-09-06 — all four P1.c gates met |
| 11.96 | P1.d latency architecture (CHAT-QUERY-COMPILER-PLAN §4 P1.d / §3.16 / §3.21 #7–#9, #18): CONCURRENCY-DEADLINES-V1 — `retrieve_candidates` runs every independent lane at T=0 on a bounded per-turn pool (fixed-order fusion → byte-identical output when nothing times out), fans the hierarchical deepening out per section, one core budget `lane_deadline_s` 3.0 s (pending lanes dropped + `<lane>_timeout` receipts), route starts BM25 before the single embedding (`prestarted`), exactly ONE judge call under `rerank_deadline_s` 3.0 s (expiry → fusion order + `rerank_timeout`), `embed_deadline_s` 2.5 s receipted, `lanes=` (VECTOR = A+B, HYBRID = A+B+C), per-stage `latency_ms`; METAL-LEASE-V1 — cross-process fcntl lease on the one Metal device with interactive priority (chat requests `X-Polymath-Priority: interactive` outrank enrichment batches; bounded waits; fail-open; `queued_ms` receipts) in both sidecars, clients and the chat path; enrichment untouched. Spy tests: 1 embedding per distinct text set, 1 rerank call per turn. GATE: spies 1 embedding / 1 rerank call per turn (unit) MET; forced deadlines → 10/10 degraded receipts with complete answers MET; lanes 373 ms in parallel, BM25 prestarted 25–30 ms; HYBRID p50 − VECTOR p50 = +3.41 s (clean turns +0.51 s) against ≤ +0.5 s MISSED — entirely judge timeouts (HYBRID 10 vs VECTOR 1 of 30 at the 8 s deadline); contention: after per-attempt leasing + threaded sidecars, B embed p50 5.8 → 1645.8 ms, embedder OOM 31 → 0, wall p50 11.75 → 8.39 s, p90 49.8 → 18.0 s, but judge timeouts 9/30 on B and 14/30 on M (M system-honest 0.933 on this code under load); the §3.16 3 s rerank budget benchmarked unreachable (30/30 timeouts) → default 8 s. Work-log 2026-09-06-p1d-latency-architecture. | IMPLEMENTED — GATE MISSED 2026-09-06; DEVIATION ACCEPTED BY THE OWNER 2026-09-06 ("accept relative gates under contention and finish P1.e–P1.g") — the gate numbers stand as measured: HYBRID ≤ VECTOR + 0.5 s missed after diagnosis + one corrective attempt (+3.41 s p50 / +0.51 s clean); embedder contention controlled (OOM 0, embed p50 1.6 s), judge contention not (24 pairs at ~240 ms/pair on a shared fp32 sidecar; 30–47 % of turns exceed 8 s under enrichment). Owner decisions listed in the work-log |
| 11.97 | P1.e mode recomposition (CHAT-QUERY-COMPILER-PLAN §4 P1.e / §3.15 / §3.18 / §3.19 / §3.21 #5–#9, #16; MODE-COMPOSITION-V1): `chat_retrieve_mode` owns the compositions on the v2 engine — VECTOR/FAST = A+B, HYBRID = A+B+C (byte-identical to `chat_retrieve_v2`), GRAPH = HYBRID → bounded hop-1 over the FINAL evidence (≤ 8 seeds, ≤ 2 when the compiled plan says the question is not relational, ≤ 20 facts, entity-card seeds from the primary vector without re-embedding, fail-open `graph_degraded`), WILDCARD = HYBRID core ∥ latent sweep on the same per-turn pool from the one embedding (`on_context` seam), `divergent_finish` after the core against the final evidence neighbourhood, ≤ 3 bridges never in the evidence list, frontier bounded by `wildcard_deadline_s`; `/chat/stream` dispatches FAST/VECTOR, GRAPH, WILDCARD, HYBRID through it under the v2 flag (v1 engines behind `retrieval: v1` / `latent`), `/chat` FAST → VECTOR; truthful `meta.mode`; harness arms HYBRID/GRAPH/WILDCARD; `tests/determinism/test_chat_modes.py`. GATE: GRAPH − HYBRID p50 +0.53 s (clean +0.42 s) against ≤ +1.5 s MET, facts p50 20.0 / max 20 (cap 20), seeds p50 8.0 (cap 8); WILDCARD − HYBRID p50 +5.89 s (clean +4.82 s) against ≤ +2.0 s MISSED with the deadline-aware finish at the 2.5 s default (bridges p50 1.0 / max 2, bridges in evidence 0, degraded turns 13/30); first measurement at the same default returned 0 bridges on 30/30 turns (frontier abandoned at the deadline) and an 8 s deadline still 0/30 — eight sequential validations behind a contended reranker; mode-equivalence tests green; frozen plans, arms interleaved with alternating order, under enrichment contention (owner-accepted). Work-log 2026-09-06-p1e-mode-recomposition. | IMPLEMENTED — GRAPH GATE MET, WILDCARD LATENCY GATE MISSED 2026-09-06; DEVIATION ACCEPTED BY THE OWNER 2026-09-06 ("accept relative gates under contention and finish P1.e–P1.g") — GRAPH − HYBRID +0.53 s p50 with ≤ 8 seeds / ≤ 20 facts and no degradation (met); WILDCARD bounded and separated (≤ 3 bridges, 0 in evidence on 30/30, budget-honest partial finish) but WILDCARD − HYBRID +5.89 s p50 (clean +4.82 s) against ≤ +2.0 s after diagnosis + one corrective attempt (deadline-aware finish; 17/30 turns with bridges, 13/30 receipted `wildcard_timeout:finish`); every validation is a reranker round trip behind the enrichment-saturated judge; not written as DONE |
| 11.98 | P1.f runtime unification (CHAT-QUERY-COMPILER-PLAN §4 P1.f / §3.7; CHAT-RUNTIME-V1): `chat_events(req)` is the one chat runtime (validation, scope, compiler, mode composition, bundle, CARRY-V2, SYNTHESIS-V2, receipts) yielding the unchanged SSE frames; `chat_stream` streams it, `run_chat(req)` drains it into the `/chat` JSON (answer `result` + the same `retrieval` block + phases + `runtime: chat-runtime-v1`, error frames → the same HTTP status); `/chat` builds a StreamChatRequest (CHAT-REQUEST-MAP-V1) and calls `run_chat` — its old dispatch deleted; MCP `ask` inherits via `/chat`; both routes write the same query receipt (`meta.route` = transport tag); the engine's deadline receipts (`rerank_timeout`, `<lane>_timeout`, `embed_deadline`, graph / wildcard) ride `retrieval.degraded` on both routes and in the receipt (integrator fix — they were only in the retrieve_done phase event, so a timed-out judge was receipted `degraded: []`). `/chat` changes: LEGACY → 422, multi-corpus FAST → 422, FAST reports mode VECTOR, deterministic synthesizer default (machine route) unless given. GATE: same plan + same evidence ids on all routes — live probe 10 pairs under enrichment contention: mode mismatches 0, plan mismatches 0, evidence-id mismatches 0 on the 2 clean pairs and 2 explained by a receipted judge deadline / sidecar OOM split on one side (MET); 101 offline tests green — test_chat_runtime (27 pure parity tests, including the new degraded-receipt test) + test_chat_hygiene + test_chat_modes + 45 contract tests (-k 'not live', 2026-09-06 10:40Z); 93 + 45 right after the cherry-pick; live parity test PASSED 2026-09-06 10:42Z on the respawned orchestrator (pid 80880): identical runtime tag, mode, engine, chat_plan, legend chunk ids, chunk locators, used_evidence and citations on /chat and /chat/stream (the 10:29Z attempt skipped because /ready exceeded the test's 3 s probe right after a respawn; the 10:35Z rerun also passed). Work-log 2026-09-06-p1f-runtime-unification. | DONE 2026-09-06 — parity gate met on the live fleet and in the offline suite; `/chat` behaviour changes (LEGACY 422, multi-corpus FAST 422, FAST → VECTOR, deterministic synthesizer default) documented |
| 11.99 | P1.g regression manifest (CHAT-QUERY-COMPILER-PLAN §4 P1.g / §5 / §5b; CHAT-REGRESSION-MANIFEST-V1): `eval/regression/chat_regression_manifest.json` freezes the 16 pre-promotion cases as floors over committed recordings (JSON pointer + select / field / reduce / minus / per-check file, one bound min / max / equals, the recorded value, optional pre-plan baseline); `scripts/chat_regression.py --check | --refresh | --table`; `tests/determinism/test_chat_regression_suite.py` runs the same evaluation in CI (determinism.yml) — floor regression, drifted recording or deleted instrument fails CI; cases 11 / 12 / 13 recorded from the 2026-09-06 acceptance run (mode invariants, forced deadlines, route parity); floors never moved by --refresh (the one floor change — case 10 turn-3 prompt 47,321 → 55,000 — is the owner's reviewed decision of 2026-09-06); ACCEPTANCE FINDING A1 fixed in the same commit: aspects a timed-out judge never scored are flagged `unjudged` (engine receipt + prompt line) instead of reading as covered. GATE: suite in CI (determinism.yml) with floors not exacts: tests/determinism/test_chat_regression_suite.py: 138 passed, 1 skipped (the empty pending parametrization), 0 failed — 2026-09-06 12:45Z; CHAT-REGRESSION-MANIFEST-V1: 16 cases (recorded-floor 14 / offline-test 2 / pending 0), 131 rows, 0 pending, 0 failing; 78 recorded checks + 53 instruments; pending 0 (MET). Work-log 2026-09-06-p1g-regression-suite. | DONE 2026-09-06 — 16/16 cases frozen (14 recorded-floor, 2 offline-test, 0 pending), CI fails on a floor regression, drifted recording or deleted instrument; acceptance table in the work-log; finding A1 fixed and re-measured |
| 11.100 | JUDGE-FAST-PATH-V1 (owner decision 2026-09-06): the cross-encoder judge (Qwen3-Reranker-0.6B, sentence-transformers CrossEncoder on Metal) runs fp16 (`POLYMATH_RERANKER_DTYPE`, fp32 self-test fallback receipted), truncates every pair to 384 tokens (`POLYMATH_RERANK_MAX_LENGTH`; the owner's 256 measured at top-1 agreement 0.8 → 384 by the sweep), scores a request in ONE forward pass (`POLYMATH_RERANK_BATCH` 64 = wire cap; client `RERANK_BATCH_SIZE` 16 → 64 so a 24-pair judge call is one request; OOM halving kept as the safety net), under `torch.inference_mode`, with a bounded cross-request memo of (query, document) scores (`POLYMATH_RERANK_MEMO_MAX` 20000; scope = model revision + dtype + max_length); receipts per response (dtype, max_length, batch, memo_hits/misses, nonfinite, dtype_fallback) and cumulative on /ready. Scoring semantics unchanged (same model, raw scores, rank-based use). Instrument `scripts/rerank_judge_bench.py` (real pairs, interleaved configs, ranking agreement). GATE: judge+select p50 B 5469.8 → 3072.9 ms (M not re-measured — owner stopped the replay); judge timeouts B 10 → 6/30; ranking agreement vs fp32 untruncated on 10×24 real pairs: Spearman 0.976, top-8 overlap 0.95, non-finite 0; quality B hit@10 0.667 → 0.633 / MRR 0.565 → 0.558 / survival 0.815 → 0.815 (MET). Work-log 2026-09-06-judge-fast-path. | DONE 2026-09-06 — fp16:384 shipped; judge p50 5469.8 → 3072.9 ms on B with ranking agreement Spearman 0.976 and no quality regression |
| 11.101 | CHAT-MODEL-CATALOG-V1 (owner decision 2026-09-06): the chat model dropdown offers OpenCode Zen's FREE models (provider row `opencode-free`, LiteLLM `openai` on https://opencode.ai/zen/v1, api_key `env:OPENCODE_API_KEY` resolved from .env at call time — hidden until the key is set) and Ollama's FREE cloud tier only (fixed allowlist of six `-cloud` names, `available` per daemon registration; local and paid cloud models no longer offered); new-chat default `litellm:openai/glm-5-free` (POLYMATH_DEFAULT_SYNTHESIZER), else the first offered; `scripts/chat_models_setup.py` (row upsert from `config/chat_models/opencode_free.json`, Ollama pulls, catalog print); providers endpoint shows key NAMES and readiness, never values; + Alibaba Cloud Model Studio row `alibaba-model-studio` (Bailian token plan, ap-southeast-1, Anthropic-messages app via LiteLLM `anthropic`, key `env:ALIBABA_MODEL_STUDIO_API_KEY`, nine plan models, `--alibaba`). GATE: only free models offered (live: 6 Ollama free-tier entries, paid LiteLLM `ollama` row disabled, OpenCode's 31 free models hidden until OPENCODE_API_KEY is set); 37 tests; live generation: Ollama free tier returned its weekly-limit error as a typed 502 (path intact, error observable), OpenCode pending the key. Work-log 2026-09-06-chat-model-catalog. | DONE 2026-09-06 (catalog + wiring) — live generation through the free providers pending the owner's OpenCode key and Ollama's weekly quota reset; Alibaba proven live 2026-09-06 (deepseek-v4-flash-0731 16–23 s with citation tags; qwen3.7-plus 39 s with tags; qwen3.8-max 42 s without tags → fast citing models ordered first, default = first offered of a preference list); OpenCode PROVEN LIVE 2026-09-06 after the owner's key (big-pickle 26.1 s, 8 tags) — with the served-id reconcile of 11.108 |
| 11.102 | CHAT-UI-SURGICAL-V1 (owner request 2026-09-06): frontend-only pass — assistant output as a document (transparent `.answer`, 72ch, no card; user bubbles kept), PhaseStream as a compact process rail (rotating disclosure, 'Working · N steps' + current step, ✓/spinner rows with right-aligned detail, delayed 1.8 s collapse to 'Worked for X s · N steps', click/keyboard reopens, reasoning pane kept), follow-tail scrolling with a sticky 'Jump to latest' pill, narrow-viewport and reduced-motion rules; streaming contract, App.tsx, api.ts, types.ts and every backend package untouched except one fallback line (`_default_synthesizer`: an empty synthesizer resolves to the first OFFERED model, never a hidden provider — found by this pass). dist rebuilt and re-declared. GATE: npm run build clean (tsc + vite); 41 offline tests; live checklist in the in-app browser on the final bundle: chats reload, sends, token + reasoning streaming, live rail with details, 1.8 s delayed collapse to 'Worked for 60s · 9 steps', reopen/close, evidence panel (32 rows), citations, follow-tail (viewport held during streaming, pill, jump), 375 px viewport without overflow, no console errors (MET); GRAPH/WILDCARD/degradation/code-block renderers unchanged but not exercised by the HYBRID turns. Work-log 2026-09-06-chat-ui-surgical. | DONE 2026-09-06 — presentation/state only, streaming contract and backend untouched except the empty-synthesizer fallback the pass exposed |
| 11.103 | READING-HIERARCHY-V1 (owner design contract 2026-09-06): the answer as a knowledge document — typography tokens (`--pm-*`) in app.css: answer prose Rubik 15.5 px / 1.62 / 78ch, headings 21 / 18 / 16 at 600 with 1.55 rem above and a tight 0.3 em to the first unit, paragraphs 0.78 em, lists 1.35 rem / 0.28 rem, bold 600; machine layer in Roboto Mono and smaller (process rail 12.5 px, metadata 11 px, badges 10.5 px at 500, code 12.5 px); evidence as footnotes (transparent panel, 2 px rule, 13 px title with the locator at 600, 13 px / 1.5 quote, 11 px mono provenance behind the expander); prose monochrome; model markdown at white-space normal. CSS only; dist rebuilt. GATE: computed styles on the live UI match the contract (answer 15.5 px / 25.1 px / 768 px; h2 18 px 600 with 24.8 / 6.4 px margins; lists 21.6 / 4.5 px; rail and metadata in the mono stack at 12.5 / 11 px; evidence 13 px prose title + 13 px quote in a transparent footnote panel); build clean, guards ok (MET). Work-log 2026-09-06-reading-hierarchy. | DONE 2026-09-06 — the synthesis-prompt presentation contract is the proposed follow-up |
| 11.104 | LEGIBILITY-V1 (owner request 2026-09-06: text hard to read and to separate from the UI): per-theme `--text-dim` / `--accent` raised so secondary text measures ≥ 5.2:1 on `--bg` and ≥ 4.6:1 on `--bg-3` in all nine themes (light themes were 3.8–4.0:1; light accents now ≥ 4.5:1); the answer column at full text colour, secondary tone reserved for machine information, evidence quote at 90 %; the occasional highlight — `strong` in an answer renders as a 13 % accent wash under full text (clone-boxed across wraps), `::selection` 32 % accent, inline code / blockquote faint accent tint; rail rows 74 % with the active row full. CSS only; dist rebuilt and re-declared. GATE: computed contrast on the live bundle ≥ 4.5:1 in every theme × surface cell (table in the work-log); paper theme rendered with the warm anchor wash; build clean, guard ok (MET). Work-log 2026-09-06-legibility. | DONE 2026-09-06 — the whole-sentence bold the model emits is a prompt matter (PRESENTATION-V1, 11.105) |
| 11.105 | PRESENTATION-V1 (owner design contract 2026-09-06, "the LLM's answer-generation instructions should cooperate with the renderer"): the information-presentation contract appended to the synthesis system message AFTER the v3.3 style layer and declared to win over it on display shape only — short information-dense paragraphs (2–5 sentences) as the default unit, no one-sentence paragraph spam, conclusion → mechanism → evidence, headings only for real sections (never a bold line as a heading), lists only for parallel items, tables only for comparisons, bold only for semantic anchors (a few words, never a sentence), KVP / ASCII maps / "at least one visible structure" as options not requirements, citation tags at the end of the sentence; authority, citation and completeness rules untouched (`prompt_contract` stays synthesis-v2); every answer receipt names `presentation_contract: presentation-v1`. GATE (same 10 fixture-B questions, same model, before → after): words per paragraph 38.5 → 89 (band 40–110, MET); one-sentence paragraphs 2 → 1 (MET); headings 1.5 → 0 at the median, max 6 → 4 (MET at the median; one long enumeration over the cap of three); bold pseudo-headings 4 → 0 standalone (MET; run-in lead-ins remain); bold share 0.060 → 0.030 (MET), max 0.172 → 0.084 (target 0.08, MISSED by 0.004); longest bold span 38 → 40 words (target ≤ 5, MISSED: the bold thesis sentence survives on some turns); tags on 10 / 10 answers; answer chars 2,822 → 2,699 (MET); LLM citation check (before 30 questions; after the first 10 of the same fixture — owner: "30 questions is too much"): precision 1.0 → 1.0, tagged answers 28 / 30 → 10 / 10, answer chars p50 3,133 → 2,488 (MET); GENERATION-BOUND-V1 (11.106) was found by this run. Work-log 2026-09-06-presentation-contract. | IMPLEMENTED 2026-09-06 — shape gates met at the median, citation gate met; residual = bold thesis sentences / run-in bold lead-ins on some turns (a v3.3 style-layer habit; retire that line from polymath_style.py if the owner wants it gone) |
| 11.106 | GENERATION-BOUND-V1 (found 2026-09-06 while proving 11.105: a live artifact test got a 66-character answer with no error): LiteLLM sends Anthropic-format providers its DEFAULT_MAX_TOKENS 4096 when the caller sets no bound, and deepseek-v4-flash (Alibaba Model Studio) spends most of it on reasoning, so long answers ended mid-sentence, unreceipted (1 of 10 before-probe answers cut; a 5,773-char artifact cut at "which is what separates"). The chat path now sends `POLYMATH_CHAT_MAX_TOKENS` (default 16000; 0 = none), records the provider's `finish_reason` in `result.meta.generation` and the query receipt, appends `generation / cut / truncated:max_tokens` to the degraded list when the provider stops at the bound (answer event, /chat JSON, receipt, UI degraded note), and retries ONCE without the bound when a provider rejects the number (`generation / bound refused` receipted); other provider errors stay the typed `litellm_error`. UI degraded note shows the state ("generation cut — …"). GATE: 10 offline tests (bound sent + finish receipted, `length` named, rejection → one unbounded retry + receipt, other errors typed, 0 disables, bad value → default, wording matrix); live after the respawn: the brainrot artifact fixture (the 66-character failure) now returns 6,981 characters ending in a full sentence after 26,119 reasoning characters, `meta.generation = {finish_reason: stop, max_tokens: 16000}`, no degraded generation entry; both artifact live tests PASS; the JSON route with the LLM synthesizer carries the same receipt (412-character answer, `stop`, 16000); a short stream turn shows `finish_reason: stop`; the shipped-wording probe answered 10 / 10 with 0 cut answers where draft A without the bound had 3 empty.. Work-log 2026-09-06-generation-bound. | DONE 2026-09-06 — the bound is sent, the finish reason is receipted, a cut answer is marked degraded, and the artifact fixture that failed at 66 characters completes at 6,981 |
| 11.107 | MODEL-PICKER-V1 (owner request 2026-09-06): the chat model dropdown becomes a button + popover of collapsible provider sections — `/synthesizers` rows carry `provider` / `provider_label` / `model` (grouping by data, ids and defaults unchanged); the section holding the selected model opens by default, open state per provider persists in localStorage, a filter box appears above twelve models, `not pulled` / `default` tags from the row flags, popover docks to the bottom under 640 px. GATE: catalog tests 9 green incl. the boundary test; build clean; live 15 models in 2 groups after the respawn (OpenCode's 31 join as a third group once its key is in .env); in-app browser: sections collapse / expand with persisted state and a "current" marker, selection updates the button, the filter narrows 15 → 5, Escape closes. Work-log 2026-09-06-model-picker. | DONE 2026-09-06 |
| 11.108 | OPENCODE-RECONCILE-V1 (2026-09-06, the moment the owner's key went live): models.dev's zero-cost list (31 ids) ≠ what https://opencode.ai/zen/v1 serves (GET /models: 70 ids, 8 of them free) — the first-preference default `glm-5-free` answered "Model … is not supported", breaking every new chat. `scripts/chat_models_setup.py --reconcile` (automatic with `--opencode-free` when the key is set) keeps snapshot ∩ served (browser User-Agent: Cloudflare 403 / 1010 on Python's default UA), records `unserved` / `served_total` / `reconciled` / `models_snapshot`; default order now proven-first: Alibaba deepseek-v4-flash-0731, then OpenCode big-pickle / mimo-v2.5-free / nemotron-3.5-lightning-free, then Ollama gemma4. GATE: catalog 46 → 23 models (OpenCode 8); canary 4 / 8 served free ids answer right now (the rest 400 / 503 / 500 upstream — typed errors, not hidden); live RAG turn via big-pickle 26.1 s, 2,562 chars, 8 tags, finish stop (the 11.101 live proof); 10 catalog tests green. Work-log 2026-09-06-opencode-reconcile. | DONE 2026-09-06 |
| 11.109 | STYLE-BOLD-RETIRE-V1 (owner decision 2026-09-06, "yes retire it"): the v3.3 style layer's "bold thesis" / "short bold summary sentence" / "bolded headers" / "one sentence" bold-stroke clauses (nine places) are retired — the opening thesis stays as plain prose, sections use small Markdown headings, bold is an anchor (a key term or a decision label), never a whole sentence — so the style layer and PRESENTATION-V1 (11.105) give the model ONE instruction about bold; everything else in the owner-protected file is unchanged and the edit is dated in its docstring. Pin test: retired phrases absent from the style prompt and the assembled system message. GATE (same 10 fixture-B questions, deepseek-v4-flash, shipped contract → bold retired): longest bold span 40 → 6 words (target ≤ 5: two six-word phrases remain), bold share 0.030 → 0.010 (max 0.084 unchanged), bold pseudo-headings 3 → 0, openings plain 10 / 10, headings 0 → 1 at the median (max 4 → 3), paragraphs 89 → 75.5 words, one-sentence paragraphs 1 → 1, 10 / 10 tagged, chars 2,699 → 2,674, 0 cut, wall 36.3 → 37.4 s. Work-log 2026-09-06-style-bold-retire. | DONE 2026-09-06 — one bold instruction reaches the model; residual = two six-word anchors and a heading median of 1 on 4–6-paragraph answers (recorded) |
| 11.110 | SIDEBAR-COLLAPSE-V1 (owner request 2026-09-06): the side panel collapses to a 52 px rail — brand dot (expands), new chat, the five views as inline-SVG icons (tooltips, `role=tab`), toggle at the bottom; state per viewer in localStorage; ⌘B / Ctrl+B toggles; under 640 px the collapsed panel is one short row. Frontend only; dist rebuilt and re-declared. GATE: build clean; in-app browser — collapse / expand, rail actions reachable, persisted across reload, shortcut, a 45 px single-row rail at 375 px without overflow; no console errors (MET). Work-log 2026-09-06-sidebar-collapse. | DONE 2026-09-06 |
| 11.111 | BACKLOG B2 (owner order 2026-09-06): the near-identical Sound Design copy ("… Cinema (1).md", 8 bytes apart, both duplicate-guard layers blind to it) removed from `cinema` through DOCUMENT-DELETE-V1 with typed confirmation; receipt: 1,975 chunks, 2,682 Qdrant points, 1,061 evidence rows, 987 orphan facts, 255 parent summaries, 1 run, 1 document. GATE: 68 → 67 documents (DB + `/corpora` card, query_ready), surviving copy intact (255 sections / 1,975 chunks), no re-ingest (MET). Work-log 2026-09-06-b2-sound-design-twin. | DONE 2026-09-06 |
| 11.112 | BACKLOG B7 (owner order 2026-09-06): `generation` (finish_reason, max_tokens — GENERATION-BOUND-V1) added to the stored query receipt's meta whitelist in `query_receipts.summarize_response`; pure test (key kept with both fields, unknown keys still dropped). GATE: live on both routes after the respawn — stored receipts for `chat/stream` and `chat` carry `generation = {finish_reason: stop, max_tokens: 16000}`; offline suites green (MET). Work-log 2026-09-06-b7-generation-receipt. | DONE 2026-09-06 |
| 11.113 | BACKLOG B11 EVIDENCE-DIET-V1 (owner order 2026-09-06): (1) document- and section-summary rows out of the prompt, the [S#] legend, the Sources count and the funnel's `selected` (bundle unchanged for the deterministic synthesizer and /retrieve); (2) each passage's legend line carries a cleaned breadcrumb "book › section" (heading_path; no hash / extension / link syntax); (3) `judged_prefix`: the judge's 24 seats filled document-fairly — round 1 seats every surfaced document's best fused candidate, then the fusion order under a per-document cap (8), then fill; set fair, order = fusion order; receipts `prefix_policy` / `judged_docs` / `capped_out` / `prefix_docs`; `rerank_round_robin=False` = the old slice. GATE steps 1–2 (10 fixture-B questions, deepseek-v4-flash): prompt chars p50 41,613 → 28,899, legend rows 31 → 15 (passages only), 10 / 10 tagged on the probe, citation precision 1.0 with 9 / 10 tagged + 1 correct abstention on the LLM check, wall 37 → 30 s (MET). GATE step 3 (frozen fixtures, retrieval-only, before → after): B gold-in-union 0.900 → 0.900, hit@10 0.667 → 0.633 (floor 0.60), MRR 0.581 → 0.566 (floor 0.45), survival|union 0.852 → 0.778; L 1.000 → 1.000, hit@10 1.000 → 0.933 (floor 0.90), MRR 0.902 → 0.843 (floor 0.80), survival 1.000 → 0.933; documents judged per turn 5 → 9 (p50), documents in the final set 4 → 6; the 24-seat first attempt was worse (B hit@10 0.600 / MRR 0.505) and 32 seats is the one corrective attempt. Work-log 2026-09-06-b11-evidence-diet. | IMPLEMENTED 2026-09-06 — steps 1–2 MET; step 3: every manifest floor held and breadth doubled, survival-given-union below its P1.c value (B 0.852 → 0.778, L 1.0 → 0.933) — recorded, rollback = POLYMATH_CHAT_RERANK_ROUND_ROBIN=0 |
| 11.114 | BACKLOG B2 follow-up (found 2026-09-06 by `test_no_derived_node_outlives_its_postgres_row` after B2): DOCUMENT-DELETE-V1 pruned Chunk nodes by a `doc_id` property they do not carry (0 deleted) and never touched Document / Fact / Evidence nodes; its optional `projection_receipts` delete was receipted as a bare `skipped`, so the removed chunks stayed receipted and the verify reconciler kept their nodes. Fix: the endpoint prunes Chunk / Evidence / orphan Fact / REL / Document by the ids Postgres released with per-kind receipts, and a skipped optional table carries its reason; pin test on the five Cypher patterns. Cleanup on the live graph: reconciler run (Document −1, Fact −987, Evidence −1,061) + 7,512 stale chunk receipts removed + reconciler again (Chunk 98,238 → 96,263 = PG). GATE: lifecycle test green; Neo4j Chunk count equals the Postgres chunk count (MET). Work-log 2026-09-06-b2-sound-design-twin (Follow-up). | DONE 2026-09-06 |
| 11.115 | BACKLOG B8 GRAPH-EVIDENCE-HYGIENE-V1 (owner order 2026-09-06): (a) the legend skips the assembler's `claim` items — a graph fact's provenance passage is no longer an unjudged [S#] row (the fact still rides in the tagless `[fact:…]` block); (b) one tag per chunk however many bundle items carry it; (c) a conservative lexical structural-noise filter in the candidate engine drops back-of-book index pages and page-number lists from the fused union before truncation, receipted (`noise_dropped` / `noise_reasons` / `noise_sample`) — the role-based demotion that already existed only sees chunks with a region_role, which the VES index chunks lacked. GATE (10 fixture-B questions in GRAPH mode, before → after): legend rows p50 21.5 → 15 (the judged set only), duplicate rows 41 → 0, unjudged rows 61 → 0, index-page rows 5 → 0, tags p50 10.5 → 12, prompt chars 33.7k → 27.3k, 10 / 10 answered; frozen-plan replay HYBRID + GRAPH (10 turns): graph_facts_max 20, graph_seeds_max 8, graph_degraded 0, errors 0, GRAPH − HYBRID wall +0.46 s p50 (≤ 1.5 s), gold-in-union equal on both arms (MET). Work-log 2026-09-06-b8-graph-evidence-hygiene. | DONE 2026-09-06 — GRAPH's prompt carries judged passages only; bounds held |
| 11.116 | BACKLOG B12 LATENT-COMPOSITION-V1 (owner order 2026-09-06, "it should work … I like that I can toggle it with other retrieval"): (1) WILDCARD's finish gets its own budget (`wildcard_finish_budget_s` 4 s) and ≤ 8 children per parent per judge call; when the budget is missed the top unvalidated parents ship as bridges labelled `verified: false` (best routing child without the judge, `source_support: null`), receipted `verified_bridges` / `unverified_bridges` and a `wildcard / unverified` degradation — never a silently empty lane; UI labels them "unverified". (2) ✨ (`req.latent`) no longer drops the turn to the v1 engine: it sets `budget.latent_enabled` and the v2 engine runs lane D `LATENT_RESCUE` — abstraction / transfer vectors nominate parents, their ORIGINAL children enter the union, fused / judged / composed like any lane, receipted in `lane_sizes`, `funnel_lanes`, `trace.latent`; fail-open. GATE (frozen fixture B, 10 turns, HYBRID + WILDCARD replay, before → after): turns with ≥ 1 bridge 9 / 10 (calm) → 10 / 10 (under load: 6 finish timeouts, 5 verified + 25 unverified bridges), 0 bridges in the evidence, forced 0.05 s budget → 30 / 30 unverified (mechanism proven); WILDCARD − HYBRID wall +4.6 s p50 under load (the finish budget is now spent, not abandoned); ✨ turns stay on `chat-retrieval-v2` with a `latent_rescue` lane in the receipts and B floors held: live ✨ turn on chat-retrieval-v2 with `lane_sizes.latent_rescue = 15`; HYBRID+LATENT arm: lane-D candidates 16 / turn on 10 / 10 turns, gold-in-union / hit@10 / survival identical to HYBRID (0.7 / 0.6 / 0.857), wall +2.9 s p50 under load (MET). Work-log 2026-09-06-b12-latent-composition. | DONE 2026-09-06 — WILDCARD always delivers (verified or labelled unverified); ✨ composes on v2; latency costs recorded, knobs named |
| 11.117 | BACKLOG B9 BREADTH-V1 (owner order 2026-09-06): the compiler's ADJACENT aspect — at most one per plan, only for GROUNDED_SYNTHESIS / CREATE_FROM_KNOWLEDGE — restates the question's underlying principle in domain-neutral words other books would use (weight 0.6); it rides the existing aspect machinery (own vector on lanes B + C, reserved judged seats, coverage line, per-aspect receipts); `validate_plan` drops it for other tasks and demotes extras; the judge-prefix per-document cap landed with B11. GATE (10 probe questions, live compiler, HYBRID, before → after): factual questions: ADJACENT never emitted (0 / 10), precision 1.0, 10 / 10 tagged, compiler 1.99 → 2.06 s; synthesis questions (fixture M): ADJACENT on 0 → 6 of 9 plans (a permissive first wording got 0 of 18 — the rule was made prescriptive with a JSON example), queries per plan 2 → 3, documents in the final set 7.5 → 9 per turn and 33 → 43 distinct over ten turns, citation tags 8.5 → 15, compiler +0.3 s, no fallback (MET); frozen-plan floors unchanged by construction. Work-log 2026-09-06-b9-breadth. | DONE 2026-09-06 — breadth arrives on synthesis questions through a judged aspect; residual: the model still skips it on 3 of 9 plans |
| 11.118 | BACKLOG B10 SUMMARY-ABLATION-V1 (owner order 2026-09-06): measurement only. Ablation 1 (summary rows out of the prompt) = B11 steps 1–2: prompt chars 41.6k → 28.9k, citation precision 1.0, tags held, 0 summary-row citations lost (there were none in 1,529 offered). Ablation 2 (frozen fixture B, 10 plans, arms ABC vs BC interleaved): gold-in-union 0.7 = 0.7, gold in judged 0.6 = 0.6, hit@10 0.6 = 0.6, MRR 0.417 vs 0.414, survival 0.857 = 0.857, wall 3.80 vs 4.48 s (contention: 1 vs 2 degraded turns) — the summary-routed lane found nothing the child lanes did not. Recommendation: summaries earn nothing measurable at retrieval time (as rows or as a lane on these plans); confirm on the recorded 30-plan B fixture and on L before retiring the lane; producing summaries is the owner's enrichment ledger. Owner decides. Work-log 2026-09-06-b10-summary-ablation. | DONE 2026-09-06 (measurement) — decision with the owner |
| 11.119 | BACKLOG B3 CORPUS-SHAPE proposal (owner order 2026-09-06, proposal only): per-document competition table over 514 cinema turns (union / judged / final / cited, survival flat at 0.12–0.22 across document sizes; the handbook = 3,127 union candidates, cited share 0.55 vs books 0.60–0.80; FACS manuals cited 0.08 / 0.17 of their final rows); three shapes costed — (A) handbook in its own corpus (quick; needs multi-corpus scope in the UI), (B) document-kind tag + scope filter (durable; a profile field, a lane filter, a chip; no re-ingest), (C) status quo. Recommendation B; nothing implemented until the owner picks. Work-log 2026-09-06-b3-corpus-shape-proposal. | CLOSED 2026-09-07 — owner chose C: the handbook stays in `cinema` untouched ("leave my handbook alone"); nothing implemented |
| 11.120 | B13 SECTION-ROUTING-V1 (owner decision 2026-09-07): the hierarchical lane routes by section summaries (+ child and entity-card votes) only; the document-summary search is opt-in (`hierarchy_route_documents`, `POLYMATH_CHAT_HIERARCHY_ROUTE_DOCUMENTS=1`); receipt `route_kinds`; document summaries are now out of both the prompt (B11) and chat routing (this), still produced and used by /retrieve. GATE (frozen fixtures B and L, 10 plans, section routing vs the old document vote, interleaved): B recall identical (gold-in-union 0.7, hit@10 0.6, MRR 0.417, survival 0.857), documents in the final set 5.0 → 5.5, wall 3.41 → 3.35 s; L recall identical (gold 1.0, hit@10 0.9, survival 1.0), MRR 0.742 → 0.775, judged / final documents 32 / 15 both ways (MET). Work-log 2026-09-07-b13-section-routing. | DONE 2026-09-07 — sections carry chat routing; the document vote is one env line away |
| 11.121 | B1 NEAR-DUPLICATE-GUARD-V1 (owner go 2026-09-07: "polymath 3.3 implemented it and you can see the design"): the v3.3 containment dedup ported as DUPLICATE-DOCUMENT-GUARD layer 3 — exact content-word 5-gram shingles, Jaccard gate 0.10, refuse at containment ≥ 0.95 of the INCOMING text with a typed `NEAR_DUPLICATE_DOCUMENT` receipt naming the match (no rows land); below that the document lands with `materialization.near_duplicate` (likely / review) + the intake artifact; `config.allow_near_duplicate` ("keep both" in the Files tab) overrides layer 3 only; a landed document is never re-judged on an intake replay; env `POLYMATH_INTAKE_NEAR_DUPLICATE_{GUARD,CONTAINMENT,JACCARD,SCAN_DOCS}`. Files tab: `already in corpus (same bytes / same text / near-duplicate of X — N % contained)` instead of `failed: 409`, amber `duplicate` pill, folder drops enumerated with a summary line. PROOF: 10 tests (pure core + intake against Postgres incl. override and replay); live — the real Sound Design twin rebuilt from the spool (8 bytes apart) refused at 100.0 % containment / Jaccard 0.9999, cinema stays 67; throwaway corpus: new book accepted, twin refused 99.9 %, keep-both accepted with the record; 2.7 s to compare all 67 cinema documents. Finding fixed before commit: the scheduler's intake replay re-judged a landed document after its overridden twin arrived → replay exemption. Work-log 2026-09-07-b1-near-duplicate-guard. | DONE 2026-09-07 — the twin cannot come back; the owner can keep both on purpose |
| 11.122 | B16 COMPILER-CORPUS-CONTEXT-V1 (owner design 2026-09-07: "titles of the books, not summaries … top 40, dynamic"; refinement: "top section summaries per document, backward-mapped to the top-k documents"): the query compiler now sees the library's TITLES for the message — documents ranked by content through lane A's own vote (`aggregate_documents_n` over section-summary + document-summary hits, RRF_K 60), ranked first then the rest A→Z up to `POLYMATH_CHAT_COMPILER_TITLES_TOP_N` 40 (0 = off); default ranker = dense (one message embedding, ranks by meaning), `_RANK=sparse` = the 80 ms BM25 route; fail-open to question-word overlap; prompt rule: use the library's terminology, never a title in a query, PRIMARY stays the message; receipt `plan.compiler.titles`; per-request `titles_rank` override (off | sparse | dense). GATE: factual fixture B (10) — plans unchanged, no regression, sparse 73 ms / dense 1.7 s; the owner's punch question — off: no Laban; sparse: no Laban; dense: the compiler wrote "Laban effort and shape for physical force and reaction in fight scenes", the Laban Workbook reached union → final → CITED, tags 15 → 23 (the 2026-09-06 miss closed); synthesis fixture M — sparse distinct final docs 37 → 42, distinct cited 24 → 31, compiler +0.4 s of prompt; dense arm 6 / 10 (owner stopped it to test by hand), no citation gain on compare-questions. Latency gate (≤ +0.3 s) MISSED with dense. Hygiene: 2,754 orphan routing points + 2 stale summary rows of a pre-B2 delete removed. Work-log 2026-09-07-b16-compiler-corpus-context. | IMPLEMENTED 2026-09-07 — dense default (the ranker that reaches Laban), sparse one env line away; owner testing by hand |
| 11.123 | REGION-EXCLUSION-V1 (owner report 2026-09-07: a Timing for Animation table of contents was S1 on the stick-figure turn): noisy materializer roles (front matter, marketing, toc, index, bibliography, OCR noise — now incl. the chunker's `noise_ocr` spelling) are dropped at the union and receipted (`region:<role>`), not merely sunk — demotion was undone by the document-fair judged prefix (a document whose only candidate is its contents page got the seat) and the judge had missed its deadline; document-metadata questions keep the old behaviour; the lexical filter learns `toc_links` (chapter-file link lists). B16 follow-up: the title ranker fuses the child passages' own vote (16 / 20 overlap with the summary route; found the Laban Workbook where summaries did not) — no dependence on summaries. Summary analysis for the owner: 11,638 active section summaries, 69 % grounded, 6 / 10 distinctive terms named, 0 boilerplate — well made, and in chat unnecessary (B10 / B13 / B11 / this). GATE (frozen B and L, 10 plans, HYBRID): B identical (gold 0.7, hit@10 0.6, MRR 0.417, survival 0.857); L gold 1.0, hit@10 0.9 unchanged (one turn gained, one lost — the lost gold was displaced from the 32 fair seats by two documents the removed noise had been keeping out), MRR 0.775 → 0.825, survival 1.0 → 0.9; all floors held (MET). Work-log 2026-09-07-region-exclusion. | DONE 2026-09-07 — a contents page cannot be cited for a subject question; live at the next orchestrator respawn (owner testing by hand) |
| 11.124 | INTERACTIVE-RELIEF-V1 (owner reports while testing 2026-09-07): (1) `rerank_timeout` on 8 / 40 turns traced to the embedder OOM-splitting every batch (309 in an hour at the 3.5 GiB Metal cap) under a corpus-wide re-projection (24 `project_qdrant` tickets since 09-05) — embedder batch caps 8 / 16 k → 4 / 8 k, judge deadline 8 → 12 s while the lane drains; (2) "retrieves nothing" = transform / continue turns skipped retrieval AND dropped the carried evidence — CARRY-ARTIFACT-V1 keeps the previous answer's cited passages (no relevance gate, cap 16, receipt `carry.mode`); (3) "too long" — presentation-v2 length rule (≤ ~200 / ≤ ~450 words / artifact + 2 paragraphs; no padding, no closing summary), ceiling 16 000 → 6 000; (4) measured: standing instructions 17,037 chars ≈ 4,300 tokens per turn (style layer 10,921) vs ≈ 6,500 chars of passages — LEAN-PROMPT queued. Code + config committed with unit proof (36 green); NOT live until the owner's one-minute window (embedder recycle + orchestrator respawn + the .env deadline line) — the owner is testing by hand; the after-measurement lands in the work-log then. Work-log 2026-09-07-interactive-relief. | IMPLEMENTED 2026-09-07 — live at the owner's window |
| 11.125 | DOCUMENT-PROFILE-V1 step 1 (owner architecture 2026-09-07; plan of record DOCUMENT-PROFILE-V1.md): the owner's profile compiler ported as `rag-profile-v3` — THEORY and CONCEPT as their own surfaces (aliases FRAMEWORK / MECHANISM → THEORY, CONCEPT / PRINCIPLE → CONCEPT; CONCEPT no longer folds into TERM), counts TOPIC 3–5 / TERM 3–5 / Q 3–7 / SEARCH 3–7 / THEORY 1–5 / CONCEPT 1–5 / SEEALSO 2–5 as advisory targets (info, never error), hard caps, `emit()` carries ATOMIC `representations` (identity, theme, questions[], searches[], theories[], concepts[], seealso[]) beside the legacy pooled keys, `semantic_artifact()`, `profile_valid()` = semantic core (ONE or SUMMARY) + query hook (Q or SEARCH) — the compiler's half of the tolerant readiness contract; prompt `doc-profile-v3`. 7 pins. Invariants: chunk vectors, chunk ids, parent/child identity and graph receipts untouched. | DONE 2026-09-07 — step 1 of 6; no fleet change yet |
| 11.126 | DOCUMENT-PROFILE-V1 steps 2–4 (owner architecture 2026-09-07): the lean context builder (~500 tokens: identity, de-furnitured heading structure with an even stride, opening / ending / sampled sections, known terms; deterministic `input_hash`), the `doc_profile` stage worker (isolated pool `profile1` / `profile2` / `profile_fallback` via `stage_pins.doc_profile`; rag-profile-v3 compile; artifact `doc_profile` + `doc_profile_qdrant` carrying content → input → raw → compiled → projection hashes; dark pool = transient hold; invalid profile = failed attempt), and the projection (own collection `polymath_document_profiles_<contract>`, named dense title / identity / theme + MaxSim multivectors questions / searches / theories / concepts / seealso, one vector per atomic unit, `projection_key`). Control plane: last, NON-blocking DAG entry (rollout phase A), autopilot lane, fleet slot. 17 tests incl. the worker against Postgres with fakes. Invariants held. Not live yet: no LLM call made, no point written — step 5 (backfill: tickets for existing runs + fleet boot with the new slot, owner go on spend) and step 6 (retrieval lane), then phase B (QUERY_READY requires the profile). Work-log 2026-09-07-document-profile-stage. | IMPLEMENTED 2026-09-07 — dormant until the fleet is booted with the `doc_profile` slot |
| 11.127 | DOCUMENT-PROFILE-V1 follow-up (owner 2026-09-07): profile aims raised to TOPIC 10 / TERM 10 / Q 15 / SEARCH 15 / THEORY 10 / CONCEPT 10 / SEEALSO 10 (advisory floors 5 / 5 / 8 / 8 / 4 / 4 / 5; hard caps aim + 6; counts still never required), prompt `doc-profile-v3.1` ("aims are ceilings to reach when the document supports them, never quotas"), worker output budget 900 → 2,400 tokens; pool attempt order = the PRIMARY lanes rotated by a hash of the run (six dedicated keys spread evenly), then the fallback lanes in pin order (Gemini fallback 1, OpenRouter fallback 2), ≤ 4 attempts. The six dedicated lanes are wired when the owner names their provider / model and `.env` variable names (keys never in the repo). 22 tests. | DONE 2026-09-07 — config for the six keys pending the owner's provider details |
| 11.128 | DOCUMENT-PROFILE-V1 step 5 — the live backfill (owner go 2026-09-07: "go ahead and backfill"; additive to the summary layer). What the first live calls found and what changed: Groq 400 on JSON mode → the nine `profile_*` lanes are `structured: "text"`; the model sometimes writes a label once and lists bare lines under it → `rag-compiler-v3.1` treats an unlabeled line under a list tag as a NEW item (`ITEM_SPLIT`) unless the previous item is visibly open, and splits `Q: a? B?` (owner: improve the compiler, the model is capable); prompt `doc-profile-v3.2` (label on every line) as the secondary guard; `attempt_lanes` = 2 rotated primaries + fallbacks ≤ 4 (with six primaries the fallbacks were unreachable); only transient pool errors hold the ticket, the rest fail the attempt; the embedder's batch cap (4 texts) 422'd the ~63-text projection → sliced; artifact carries `doc_id`. `scripts/backfill_document_profiles.py` (mint / status) + `scripts/document_profile_gate.py` (self-retrieval gate). DOC-PROFILE-SCALE-OUT-V1 (owner mid-backfill: six accounts, not one document at a time): `FLEET` + `doc_profile2..6`, the autopilot wakes one profile worker per open ticket capped at six (one in flight per key); fleet booted mid-backfill. 35 tests, repo_guard ok. Measured: 67/67 cinema documents profiled (0 failed, 0 dead) in 20 min (one slot 14:43–14:54Z ≈ 1 doc/min, six slots 14:54–15:03Z ≈ 6 docs/min); quality p50 1.0 (min 0.13), LLM p50 7.7 s/doc; lanes fallback_gemini1 2, fallback_gemini2 1, groq1 11, groq2 11, groq3 11, groq4 10, groq5 11, groq6 10; 67 points in the profile collection; self-retrieval (own questions + searches → profile lane, RRF) top-1 85.8 %, top-3 99.5 %, median rank 1; punch question top-5: Fight Choreography: The Art of Non-Verbal Dialogue · The Screen Combat Handbook · Stage Combat Arts · How to Draw Manga: Martial Arts and Combat · Grammar of the Shot · … The Laban Workbook for Actors at 6 and Your Move at 15 — the Laban case reached through the profile lane alone. Work-log 2026-09-07-document-profile-stage (step 5 section). | DONE 2026-09-07 — step 6 (retrieval lane) and phase B (QUERY_READY requires the profile) next |

| 11.129 | DOCUMENT-SEMANTIC-INDEX-V1 slice S0 — plan / bootstrap admission (owner plan of record 2026-09-07: `docs/wiki/plans/DOCUMENT-SEMANTIC-INDEX-V1-PLAN.md`, original `POLYMATH_NEXT_PHASE_IMPLEMENTATION_PLAN_FINAL_2026-09-07.md`): the owner's next-phase plan admitted to the living docs and hooked from every bootstrap surface (AGENTS.md current-state, CONTINUITY-REPORT read-order + a new latest checkpoint with the START-HERE ladder, the dated handoff README, UNFINISHED_WORK, DEPENDENCY_MAP, this register) so a fresh / context-compacted session discovers it without chat history; both plan files carry `last_reviewed` and are declared in the scaffold `TREE`. Scope: a two-scale semantic index — the global document profile vNext (adds research-index surfaces LATENT-PATTERN / ANCHOR / RECALLQ / TENSION / BRIDGE / INVERSION / BOUNDARY) + ONE deterministic `ParentSkeleton` and compact routing `MAP` per eligible parent produced in PACKED Compound-Mini calls (never one LLM call per parent; exact identifiers extracted deterministically in Python, not from model hooks) + a deterministic Vocabulary Bridge (no extra LLM). Postgres proves completeness; Qdrant is projection; retrieval is document → parent map → child evidence; Wildcard deferred. Slice ledger S0–S16 (plan §40) — one register row lands per slice as it does, as DOCUMENT-PROFILE-V1 did across 11.125 / 11.126 / 11.128. Frozen measured Compound-Mini baseline (do not overwrite with guesses): 40 / 40 aliases, 0 missing / invented / duplicate, injection-resistant, identifiers preserved; input 135.7 tok/parent, billed completion 87 tok/parent (visible ~29 — use billed for capacity), latency 8.1 s, finish stop, tools disabled. The prior handoff's U1 folds into slice S12 (runtime profile / map lane), U2 into S15 (QUERY_READY promotion). No runtime code; no chunk / graph / projection identity or schema change. Work-log 2026-09-07-document-semantic-index-admission. | DONE 2026-09-07 — S0 admission; S1 deterministic ParentSkeleton next |

| 11.130 | DOCUMENT-SEMANTIC-INDEX-V1 slice S1 — deterministic ParentSkeleton (plan §7-§10 / §36.1): `shared/polymath_shared/document_profile/parent_skeleton.py` — pure CPU policy (re / Counter / math.log; no summary model, no local LLM, no scikit-learn, no API). `build_parent_skeletons(parents)` returns a `SkeletonManifest` of one `ParentSkeleton` per RETRIEVAL-ELIGIBLE parent (alias P0001 → real parent_id in the manifest; opaque ids never enter the prompt) with a salient EXTRACTIVE sentence (not first-30-words), 3-5 TF×IDF key terms (`idf = log((N+1)/(df+1))+1`), and exact identifiers extracted DETERMINISTICALLY from source (CVE / RFC / dotted version / letter+digit code / zero-padded / acronym, span-masked most-specific-first so no CVE substring leaks) — independent of model hooks (plan §9 / §43.3). Furniture/noise parents are ACCOUNTED FOR as `ExcludedParent`, never dropped; eligibility defers to the single region-role authority `document_region.is_noisy` (no competing furniture policy). `text_hash` / `skeleton_hash` / `manifest_hash` give content identity; `SKELETON_BUILDER_VERSION = parent-skeleton-v1` (versioned per §32). 13 determinism pins incl. 1/80/1000-parent scale (1000 deterministic and < 5 s), identifier fidelity, hash stability, alias stability under reorder, and a static AST purity pin. Additive: no worker / API / persistence / schema / chunk-graph change; nothing consumes it yet (S2 map compiler, S9 map worker next). Work-log 2026-09-07-parent-skeleton. | DONE 2026-09-07 — S1 exit met (deterministic, cheap); S2 map compiler next |

| 11.131 | DOCUMENT-SEMANTIC-INDEX-V1 slice S2 — parent-map compiler (plan §9-§11 / §18-§19 / §30-§33 / §36.2): `shared/polymath_shared/document_profile/map_compiler.py` — deterministic `compile_maps(raw_response, manifest)` for the `MAP\|alias\|signature\|hook1;hook2;hook3` DSL. TOLERANT FORMAT, STRICT IDENTITY: alias drift (`P17`, `MAP \| P0017 \|`, `map\|p3`) resolves by numeric value to a known S1 alias; unknown / ambiguous / invented aliases and empty signatures are rejected and receipted, NEVER attached to a look-alike parent. PARTIAL OUTPUT IS DURABLE (§18.4): 73 of 90 valid lines keep 73, `missing_aliases` is the exact sorted repair set, successful lines never re-run; duplicates keep the first valid record (pinned rule) and receipt the rest. Every `CompiledMap` carries the skeleton's Python `exact_identifiers` verbatim — independent of model hooks (§9). Search text normalized on the repo NFC contract + the measured Compound-Mini non-breaking-hyphen/space fold (identifiers exempt); raw model text hashed FIRST so `raw_response_hash != completeness_hash` (§10/§33). Generic signatures/hooks flagged not fatal (§19.2); an injected instruction inside a MAP line compiles as the signature string — the compiler executes nothing (§30). `MAP_COMPILER_VERSION = map-compiler-v1` (versioned per §32). 15 determinism pins composed with real S1 manifests (40/40, exact partial recovery, identity safety, Unicode fold, deterministic identifiers, injection-as-data, purity). Additive: no worker / API / persistence / schema change (S9 worker calls it, S4 tables store it). Work-log 2026-09-07-parent-map-compiler. | DONE 2026-09-07 — S2 exit met (40/40 + partial recovery + identity safety); S3 token packer next |

| 11.132 | DOCUMENT-SEMANTIC-INDEX-V1 slice S3 — token packer / capacity model (plan §12-§14 / §21-§22 / §36.3): `shared/polymath_shared/document_profile/map_batches.py` — deterministic capacity + batch planner. `DensityModel` seeded from the measured 40-parent Compound-Mini test (135.7 input / 87 billed / 29 visible tokens per parent), refined by an EMA `update` from receipts — NOT a permanent constant (§43.9); capacity uses BILLED density, never the visible ~29 (§43.2). `mapping_only_capacity` = min(target 60, floor((6500 envelope − 512 safety)/billed)) — 40 proven, the ~74 edge never packed to. `combined_capacity` sizes the one-call global-profile+MAP fast path from the global profile's MEASURED billed output (§13.2). `token_feasible_rpm` is the §14.4 guard (4 RPM is a ceiling, only under the 15k-token/request cap). `plan_batches(manifest, density)` cuts eligible parents into deterministic `MapBatch` manifests (optional combined first batch + mapping-only overflow), each with token estimates + a hash, plan hash over them. At baseline 40 parents = 8908 tokens / 60 = 13362 (matches §14.5); a 150-parent doc → [60,60,30]. 12 determinism pins (baseline fit, target pack, guard never exceeded, EMA, determinism, purity). Additive: no worker / API / persistence / schema change (S9 mints tickets from these, S7 meters them). Work-log 2026-09-07-map-batches. | DONE 2026-09-07 — S3 exit met (40 proven, 60 target, combined formula tested); S4 SQL durability next |

| 11.133 | DOCUMENT-SEMANTIC-INDEX-V1 corrective checkpoint — S2/S3 durable-identity + guard fixes (owner corrective goal 2026-09-07, BEFORE S4 persists the contracts): five defects closed in the deterministic core. (A) `MapBatch.batch_hash`/`BatchPlan.plan_hash` now bind SOURCE identity (planner contract + `manifest_hash` + per-alias `skeleton_hash` + combined flag) — two documents that both alias P0001..P0060 can never share durable batch identity. (B) `token_feasible_rpm` drops the ceiling to 3 above the 15k/request cap (the old TPM-only floor left 15,001–17,500-token requests silently at 4); boundary pins added. (C) durable parent identity is the parent chunk's `chunk_id` (content PK `chunk_<sha256(doc_id\|idx\|text)>`); `chunk_index` (positional, `UNIQUE(doc_id,chunk_index)`, unstable across re-ingest) is REJECTED loudly, never a map key — the S9 worker's parent-load query must select `chunk_id`. (D) `completeness_hash` → `map_completeness_hash`, strengthened to bind manifest + expected + valid + missing (§33) so a 73/90 partial, a different 73/90, and a complete 90/90 all differ. (E) guarded regression pins the owner's REAL 40-parent Compound-Mini output the moment it is supplied (`tests/determinism/fixtures/parent_map_mini_40.txt`), synthetic fixtures supplemental. No migration/backfill cost (nothing persisted yet — the reason the checkpoint precedes S4). 44 passed / 1 skipped; guards ok. Work-log 2026-09-07-map-contract-corrections. | DONE 2026-09-07 — S1-S3 contracts corrected; S4 inherits `chunk_id` keys + source-bound batch hash + `map_completeness_hash` |

| 11.134 | GROQ-ROUTING-POLICY-V1 + slice S7a routing decision core (owner corrective goal 2026-09-07, Part 3; policy `docs/wiki/plans/GROQ-ROUTING-POLICY-V1.md`): the six Groq accounts are six CAPACITY DOMAINS; each account's two models (`groq/compound`, `groq/compound-mini`) share ONE account RPD/TPM/RPM budget — NOT independent quota pools (the correction); per-model latency/density/error EWMAs; compound = global profile / mini = overflow mapping; tools DISABLED during ingestion; dynamic (account, model) selection by remaining RPD/TPM/rolling-RPM/Retry-After/latency/billed-tokens/429/in-flight; NO round-robin, NO key burning; durable shared rate state via the existing `ControllerStore`. Landed the pure decision core `shared/polymath_shared/document_profile/groq_router.py` (`choose(work_class, accounts, now, est_total_tokens) -> RouteDecision`) — a SELECTION layer above the existing per-lane limiter (which stays enforcement; no second scheduler, §21) that keys budget on the account, respects the corrected §14.4 token guard, and returns typed waits (all_locked/soonest-unlock, rpd_exhausted, no_capacity, empty_pool). 13 determinism pins. Additive: no config/fleet/provider-spend change. Build remainder (S7 wiring: compound-mini lanes in cloud_providers.json, per-account family budgets in limiter.yaml, `_FamilyGate`→account budget + ControllerStore in limiter.py, capacity-aware selection replacing `_ring_pick` in pool.py) is gated behind the reindex canary (Part 4). Work-log 2026-09-07-groq-routing-policy. | DONE 2026-09-07 (policy + decision core) — S7 live wiring + reindex canary gated next |

| 11.135 | DOCUMENT-SEMANTIC-INDEX-V1 slice S4 — parent-map SQL durability (plan §20 / §36.4): migration `stores/postgres/migrations/0054_document_parent_maps.sql` — Postgres as workflow truth for the parent map (Qdrant stays a rebuildable projection). `document_parent_map_batches` (resumable substage; `batch_id` = the S3 source-bound `batch_hash`, so re-running the same document's mapping yields the same batch_id → `ON CONFLICT DO NOTHING` means restart/retry NEVER duplicates completed API work — a Part-4 canary gate met at the schema layer); `document_parent_maps` (final authority; `map_id` = the S2 `map_hash`, `parent_id` = the parent chunk_id, PARTIAL unique index `(doc_id, parent_id, map_contract) WHERE active` = exactly one active map, supersede flips `active` never deletes); `document_parent_exclusions` (furniture accounted for). Keyed on the corrected 11.133 contracts (that checkpoint ran BEFORE this migration by design). Idempotent (applied + re-applied clean on dev Postgres); 5 durability pins pass on real PG (idempotent batch, partial state, one-active-map + supersede via UniqueViolation, restart-safe map, exclusion) and skip without a DB (CI-safe, `to_regclass` gate). No worker (S9). Additive; no existing table/stage touched. Work-log 2026-09-07-parent-map-sql. | DONE 2026-09-07 — S4 exit met (idempotent insert/update, partial state, one active map/parent/contract); S5 profile vNext + fingerprint next |

| 11.136 | DOCUMENT-SEMANTIC-INDEX-V1 live parent-map contract canary + identifier fix (owner ask 2026-09-07 "have you tested groq… the compiler… the contract?"): FIRST live end-to-end proof — real cinema parents → S1 `build_parent_skeletons` → map prompt → live `groq/compound-mini` (`GROQ_API_KEY_1`, temp 0, tools not used) → S2 `compile_maps`. Two docs: "How to Draw Manga: Martial Arts" 22 parents (200, 7.8 s, stop, 7143/2825 tok) → 22/22 complete; "A Multistage Pipeline for Character-Stable" 11 parents (200, 4.2 s, stop, 2845/1708 tok) → 11/11 complete; both `unknown/duplicate/rejected` empty; signatures distinguish neighbouring sections (caught `arXiv:2304.07193`), OCR parents honestly mapped as garbled (no hallucination). Contract live: `parent_id` = durable `chunk_id` (Fix C), deterministic `exact_identifiers` attached independent of model hooks (§9). Injection (P0031) PASS — attack mapped as untrusted content, not obeyed, format held 11/11, no system-prompt leak. FIX from the finding: the zero-padded-code identifier rule now requires ≥3 digits (`\b0\d{2,6}\b`) — `021`/`0217` kept, 2-digit table cells `02`/`03` dropped (+1 pin). Measured density 129–155 billed tok/parent (verbose probe prompt; S8 tightens; feeds the S3 EMA, not a new constant). Probes scratchpad-only — no fleet/config/persistence change. Work-log 2026-09-07-live-map-contract-canary. | DONE 2026-09-07 — Groq + compiler + contract + injection proven live on real documents; S8 must still set tools-off explicitly |

| 11.137 | DOCUMENT-SEMANTIC-INDEX-V1 ParentSkeleton v2 — `lead_excerpt` framing for headingless parents (owner-directed 2026-09-07, before the MAP/profile backfill): the smallest repo-native fix for the transcript/flat-window weakness — a headingless parent's only framing today is `select_salient_excerpt` (strongest sentence ANYWHERE), which loses the window's opening orientation. Adds ONE deterministic field `ParentSkeleton.lead_excerpt` = first 2 substantive sentences (reusing `_sentences`/`_is_boilerplate`/`_truncate_words`; `LEAD_EXCERPT_MAX_WORDS` 50), emitted ONLY when `heading_path` is empty (structured parents keep the heading as framing → `lead_excerpt=""`, unchanged). `skeleton_prompt_tokens` counts it (bounded, density envelope intact). `SKELETON_BUILDER_VERSION` v1→v2; `lead_excerpt` enters `skeleton_hash` → propagates to `batch_hash` via `manifest_hash` (the 11.133 correction) — derived MAP input changed so identity changes, as designed; nothing persisted yet so no backfill. LOCKED OUT of scope: NO chunker / materializer / transcript-detection / map_compiler change, no new dependency. 60 pins pass / 1 skip; demonstrated live-deterministically on the owner's essay (headingless variant: LEAD = the opening thesis vs SALIENT = a mid-doc criterion). Work-log 2026-09-07-parent-skeleton-lead-excerpt. | DONE 2026-09-07 — skeleton v2; the MAP input now carries opening framing for flat windows |
| 11.138 | RETRIEVAL-MIGRATION-DEPENDENCY-V1 admitted as the living migration ledger (owner /goal 2026-09-07): the owner-supplied FINAL RETRIEVAL MIGRATION / DEPENDENCY PLAN is now repository truth at `docs/wiki/plans/RETRIEVAL-MIGRATION-DEPENDENCY-V1.md` (was `~/Downloads` only). Admitted VERBATIM (the §1-§30 body, classification vocabulary KEEP/BRIDGE/DUAL-RUN/RETIRE/DELETE-LATER/TRACE-BEFORE-CUTOVER, GAP-01..GAP-12, S0-S18) with a prepended **REPO EXECUTION STATUS LEDGER** maintained in place: each slice keyed by capability, cross-mapped to the repo build-slice numbers (DOCUMENT-SEMANTIC-INDEX-V1 repo S0-S16 differ from this plan's S0-S18), state-tracked (NOT STARTED/IN PROGRESS/IMPLEMENTED/VERIFIED/LANDED/GATED/BLOCKED/SUPERSEDED). Reconciliation recorded: this file is the migration/dependency & retirement authority (gates, generation invariant, GAP closure); DOCUMENT-SEMANTIC-INDEX-V1-PLAN.md stays the build-slice detail; both consistent. Discrepancy recorded: the migration plan's S1 dependency census is only PARTIAL in-repo (`scripts/semantic_lane_census.py` = concept/procedure lane liveness; the full legacy-symbol census is unbuilt), and GAP-04's live reader is `doc_profile_worker.py:102`. Docs-only; no code/fleet/spend change. Work-log 2026-09-07-retrieval-migration-ledger. | DONE 2026-09-07 — migration plan is repo-authoritative + status-tracked; S5 profile vNext fingerprint (GAP-04) next |
| 11.139 | DOCUMENT-SEMANTIC-INDEX-V1 slice S5 — profile vNext DocumentFingerprint (plan §5-§6/§18/§31; migration RETRIEVAL-MIGRATION-DEPENDENCY-V1 §18/§S7/GAP-04): `shared/polymath_shared/document_profile/fingerprint.py` — the deterministic vNext successor to `context.py`'s `lean-context-v1` ~500-tok block. ADAPTIVE 500-2000 tok (2000 a CEILING, small docs stay small — pinned); SIX surfaces identity/structure/framing/**coverage**/synthesis/vocabulary with COVERAGE the largest, computed LAST so it absorbs slack and total never exceeds budget (pinned `used_total <= budget`); FULL-STRUCTURE scan via even-stride sampling across ALL eligible parents (no first-400 bias — pinned first/75%/last section markers all present at 2000, incl. a 1000-parent doc reaching ZZMARK999 in < 5 s); SELF-SUFFICIENT `vocabulary` derived from source (cross-parent TF·IDF key terms + exact identifiers) so the vNext profile no longer NEEDS `document_summaries.major_concepts` — **closes GAP-04** (the live reader `workers/workers/doc_profile_worker.py:102` is dropped in the S8 worker refactor, not here). Pure stdlib + `document_region` + REUSED `context`/`parent_skeleton` helpers (no parallel infra; no `chunk_id` required — the fingerprint keys nothing on a parent). Exports the vNext OUTPUT-contract tag vocabulary (RESEARCH_INDEX_TAGS LATENT-PATTERN/ANCHOR/RECALLQ/TENSION/BRIDGE/INVERSION/BOUNDARY, hyphenated LATENT-PATTERN distinct from the compiler's PATTERN→CONCEPT alias; SOURCE_ANCHORED vs ROUTING_INFERRED split) as the single source of truth for the (owner-gated, canary) prompt/compiler change. `FINGERPRINT_BUILDER_VERSION = fingerprint-v1`; `input_hash` mirrors the receipt chain. 17 determinism pins (surfaces, budget clamp+bound, monotonic coverage, coverage-largest, no-first-400-bias, self-sufficient vocab, identifier fidelity, determinism+order-independence, adaptive small-doc, 1000-parent scale, headingless framing, furniture exclusion, tag vocabulary, empty/furniture-only, AST purity). Additive: no worker/API/prompt/compiler/persistence/schema change; the live profile compiler is untouched (T1828 handoff) until S8 qualifies the switch behind the 500/1000/1500/2000 quality canary (owner-gated spend). Work-log 2026-09-07-profile-vnext-fingerprint. | DONE 2026-09-07 — S5 deterministic core met (adaptive, full-structure, self-sufficient vocab closes GAP-04); GATED: the live switch + quality canary (spend). Next unblocked: profile-atom OUTPUT contract (additive tolerant parsing) then S9/S10/S11 worker/projector/verifier CODE |
| 11.140 | RETRIEVAL-MIGRATION-DEPENDENCY-V1 slice S1 — legacy dependency census (plan §S1/§12 GAP-01/§22/§S16): `scripts/legacy_dependency_census.py` — the automated, drift-proof code-dependency census the retirement half gates on ("eliminate readers before stopping writers"; "no unknown runtime before retirement"). Read-only over repository SOURCE (no DB/model/network/fleet/spend; scripts/ is fence-free), complementary to `semantic_lane_census.py` (durable-state LANE liveness). Word-boundary scan of the legacy symbol groups (summaries / representation kinds / enrichment+latent / compile_objects+artifacts / compiler title context); each occurrence tagged owner-layer + deterministic KIND-by-path (migration/test/eval/doc/config/runtime/script/frontend/other) + heuristic runtime ROLE (writer>producer>reader>reference). Snapshot at admission: 3,877 total, **332 runtime** (reader 72, writer 43, producer 3, reference 214 = the human-review queue), top runtime files ui.py/summary_worker_impl/verify_worker/candidate_engine/summary_runtime/corpus_mapping — empirically confirms BE-AWARE §10 (summaries+enrichment still-read). 8 determinism pins incl. the GAP-04 reader (`doc_profile_worker.py:102` → reader), the enrichment writer (`latent/runtime.py:90` → writer), migration-kind under stores/migrations, self-exclusion, role-precedence unit, CLI smoke. Discrepancy recorded + amended in ledger: the plan §3 `chat/*.py` query-chain paths are `shared/polymath_shared/*.py` in the repo (no top-level `chat/`). Additive report-only; no runtime/schema/prompt/chunker change. Work-log 2026-09-07-legacy-dependency-census. | DONE 2026-09-07 — S1 census IMPLEMENTED (VERIFIED pending human classification of the 214 references before S15–S18 retirement); ledger §S1 → IMPLEMENTED. Next unblocked additive: S9 doc_parent_map worker CODE / report-only readiness verifier |
| 11.141 | RETRIEVAL-MIGRATION-DEPENDENCY-V1 slice S11 (report-only) — vNext readiness report (plan §2/§16/§19): `scripts/vnext_readiness_report.py` — the availability-neutral parent-map backfill-completeness VIEW. Read-only over durable state (documents/chunks/runs + the 0054 tables); no writes/models/network; scripts/ is fence-free. Per corpus: legacy `runs.status='query_ready'` count + eligible parents (chunks tier='parent' minus `document_region.NOISY_ROLES` — the skeleton's eligibility rule) + active maps + exclusions + batch status, classifying each document NOT_STARTED / MAP_PARTIAL / MAP_COMPLETE where COMPLETE == every eligible parent resolved (mapped OR excluded) = the §19 floor; preserves the generation invariant (a partially-mapped doc is never complete). Complementary to `semantic_readiness.py` (legacy semantic-lane verdict); both are VIEWS not schedulers; does NOT redefine query_ready. 3 pins (generation-invariant classification, no-DB no-op exit, live-DB report shape — DB pin skips without PG). LIVE baseline (dev store): cinema 67 docs/11,993 eligible parents, ecom-meta-v1 10/1,346, d7-h1-test 3/78 — ALL NOT_STARTED (0 vNext maps; substrate empty by design, S9 RUN Groq-gated) = the concrete backfill scope. Additive report-only; no runtime/schema/prompt/chunker change. Work-log 2026-09-07-vnext-readiness-report. | DONE 2026-09-07 — S11 report-only verifier IMPLEMENTED + VERIFIED live; ledger §S11 → IMPLEMENTED. The safe non-gated migration runway (ledger, S5 fingerprint, S1 census, S11 report) is complete; remaining slices are owner-GATED (LLM spend / fleet wiring / live-reader switch / backfill / canary / cutover) |
| 11.142 | DOCUMENT-SEMANTIC-INDEX-V1 slice S9 / migration §S5 — durable doc_parent_map worker: `workers/workers/doc_parent_map_worker.py` — the durable ORCHESTRATION over the migration-0054 tables tying S1 skeletons → S3 batch plan → [INJECTED inference] → S2 compile_maps → persist active maps + exclusions. Enforces the three load-bearing durability properties: never holds a DB tx across inference (§28: PREPARE tx → lease → infer OUTSIDE tx → persist tx), partial output is durable + only missing aliases re-inferred (§18.4), and restart/retry is idempotent (§36.5: batch_id=batch_hash + map_id=map_hash ON CONFLICT). Store ops: `prepare_batches` (ON CONFLICT DO NOTHING idempotent re-prepare + exclusions), `claim_batch` (lease pending/partial/expired; done never re-claimed; dead-worker recovery), `persist_maps` (supersede-then-upsert = exactly one active per (doc_id,parent_id,contract), superseded retained not deleted), `active_parent_ids` (restart/repair skip set), `record_batch_result` (release lease). The inference boundary is INJECTED (`Infer` type) — NO provider call, NO spend; the module holds NO fleet wiring and is NOT registered in STAGE_DAG/the supervisor, so no live ingestion changes (dormant like S1-S4 were before their consumers). 5 durability pins on real dev PG (full map 6/6 + 1 excluded; partial→repair re-infers only the 1 missing, infer.calls==2; restart re-infers NOTHING + active set stable; supersede one-active; lease unexpired-blocks/expired-reclaims); skips cleanly without PG (CI-safe). repo_guard forbidden-import check passes (imports only polymath_shared.*). Work-log 2026-09-07-doc-parent-map-worker. | DONE 2026-09-07 — S9 durable worker IMPLEMENTED + VERIFIED (dev PG); ledger migration §S5 → IMPLEMENTED. GATED to RUN: live Groq infer closure + map prompt + S7 shared budget + STAGE_DAG registration (spend + fleet). Next additive: S10 parent-map Qdrant projector contract + verifier/purge coverage |
| 11.143 | DOCUMENT-SEMANTIC-INDEX-V1 slice S10 / migration §S6 — parent-map Qdrant projection contract: `shared/polymath_shared/document_profile/parent_map_projection.py` — the deterministic projection CONTRACT mirroring the profile projector (projection.py): `collection_name` → `polymath_document_parent_maps_<embedding contract>` (contract-named so re-embedding never invalidates other live readers — §14 blue/green, never mutates the source/summary/profile collections), stable per-parent `point_id`, the §9.2 `vector_text` (routing_signature + hooks + compact heading) + `build_payload` (doc_id/parent_id/corpus_id/alias/heading_path/parent_ordinal/map_contract/map_hash/source_text_hash/embedding_contract/projection_key), `projection_key` (embedding-contract + map_hash sensitive), `vectors_config`/`ensure_collection` (single named `routing` dense vector), `project_parent_maps` (embed once via injected `embed`, upsert one point per map via injected `client`, receipt = counts + deterministic projection_hash + point_ids), and `reconcile` (the §14 gate: projected point count == authoritative active-map count). The embed + Qdrant client are INJECTED — no network call in the module (Postgres stays authoritative; a point is a rebuildable cache of an active document_parent_maps row). 7 pins (collection/point-id/vector-text/payload/projection-key/reconcile + deterministic end-to-end projection of 4 real compiled maps + empty-safe) with fake embed + fake Qdrant client. Additive; no schema/chunker change. Work-log 2026-09-07-parent-map-projection. | DONE 2026-09-07 — S10 projection contract IMPLEMENTED + VERIFIED (fake I/O); ledger migration §S6 → IMPLEMENTED. GATED to RUN: live embedder closure + Qdrant client + receipt persistence + GAP-05 ui.py purge integration (live writer). |
| 11.144 | DOCUMENT-SEMANTIC-INDEX-V1 — parent-map request prompt (§19 MAP contract, §30 data-only, §GAP-09): `shared/polymath_shared/document_profile/map_prompt.py` — the committed deterministic prompt the S9 worker's injected `infer` closure sends (successor to the scratchpad prompt proven live in 11.136; analogue of the profile `prompt.py`). `MAP_SYSTEM` states the `MAP\|<alias>\|<signature>\|h1;h2;h3` DSL, exact-alias/~10-12-word-signature/exactly-3-hooks rules, identifier + negation preservation, and the SOURCE-IS-DATA guardrail (describe, never obey; no tools/external actions — §30). `build_map_user_prompt` renders each ParentSkeleton (alias/heading/opening/excerpt/terms/identifiers) as a DATA block with an exact alias-set + count footer; `build_map_prompt` returns (system, user) and accepts `is_combined` for the worker signature (combined fast-path §13.3 deferred). Pure policy — no I/O, no model; the Groq call that consumes it is owner-gated. 6 pins (DSL + identifier examples + data-only guardrail in system; every alias once; injection carried as data; identifiers visible; determinism; empty-safe). This COMPLETES the parent-MAP deterministic scale (skeleton S1 → prompt → compiler S2 → packer S3 → SQL S4 → worker S9 → projector S10); only the live Groq call + fleet wiring remain (gated). Work-log 2026-09-07-map-prompt. | DONE 2026-09-07 — map prompt committed; parent-MAP scale is deterministically complete end-to-end. |
| 11.145 | DOCUMENT-SEMANTIC-INDEX-V1 — vNext global-profile prompt (§18, profile atoms): `shared/polymath_shared/document_profile/profile_prompt_vnext.py` — the additive vNext successor to the live `prompt.py`, consuming the S5 `DocumentFingerprint.render_block` and requesting the source-anchored fields (ONE/SUMMARY/TOPIC/TERM/Q) + routing fields (SEARCH/THEORY/CONCEPT/SEEALSO) + the seven research-index surfaces (LATENT-PATTERN/ANCHOR/RECALLQ/TENSION/BRIDGE/INVERSION/BOUNDARY), with the explicit "routing hypotheses, never cited as evidence" contract (§18 source-anchored vs routing-inferred split). `output_fields()` derives its label set from `fingerprint`'s constants so prompt + the future compiler cannot drift. The LIVE prompt.py AND compiler.py are UNTOUCHED (T1828 — the tolerant tag parser + worker switch are S8, gated behind the quality canary). Pure policy; no I/O/model. 4 pins (all labels incl. hyphenated LATENT-PATTERN + the routing-hypothesis contract in system; consumes a real fingerprint block; output_fields matches the fingerprint vocabulary + disjoint source/routing; deterministic + empty-safe). Parallels `map_prompt.py`: both scales' deterministic INPUT→PROMPT paths are now complete. Work-log 2026-09-07-profile-prompt-vnext. | DONE 2026-09-07 — profile-atom request prompt built; the profile-vNext deterministic input→prompt path is complete. GATED: S8 compiler tag-parse + worker switch (canary + fleet). |
| 11.146 | DOCUMENT-SEMANTIC-INDEX-V1 slice S5 — profile-vNext budget canary, LIVE (owner /goal authorized crossing the live gates 2026-09-07): `scripts/profile_vnext_canary.py` ran the adaptive fingerprint at 500/1000/1500/2000 on a 5-doc cinema cohort through the EXISTING `doc_profile` Groq pool (`workers.doc_profile_worker._pool_complete` — no new provider path, no second scheduler) — **20/20 `groq/compound` calls, 0 errors**, fleet idle on profiles (no shared-budget collision). Result: field + research-tag coverage SATURATES at 500 (16 fields / 7 tags), the no-first-400-bias gate is already met at 500 (late-structure 0.82 — the coverage surface spans the whole doc at every budget), and 1000-2000 nearly double input cost (969→1748 tok) for NO gain (slight regression at 2000: 14.4 fields / 5.6 tags). DECISION: `DEFAULT_BUDGET_TOKENS` 1000 → **500** (the plateau is the floor), pinned by `test_default_budget_is_canary_selected_floor`. Evidence: `docs/wiki/experiments/document-profile-vnext-canary-2026-09-07.{md,json}`. Reuses fingerprint + profile_prompt_vnext; read-only PG; no fleet-config/schema/live-stage change. Ledger §S7 → VERIFIED (canary passed). Work-log 2026-09-07-profile-vnext-canary. | DONE 2026-09-07 — S5 canary passed, budget=500 selected; first live gate crossed. Next: S8 (compiler tag-parse + worker switch to fingerprint/profile_prompt_vnext, dropping the major_concepts reader) — live stage + compiler, fleet. |
| 11.147 | DOCUMENT-SEMANTIC-INDEX-V1 slice S8 — profile/compiler wiring (owner /goal authorized live wiring 2026-09-07): crosses the profile/compiler gate REVERSIBLY. (1) `compiler.py` recognizes the 7 vNext research-index tags ADDITIVELY (canonical `LATENTPATTERN` is hyphen-free so `normalize`'s canonical rewrite doesn't re-break `_TAG_LINE`'s hyphen-split — the prompt label stays `LATENT-PATTERN`, plan §18; a line-start `LATENT-PATTERN`→`LATENTPATTERN` normalization bridges them; research tags in `Record`/`_LIST_ATTR`/dedup/`semantic_artifact`-when-present; never required/capped/scored; `COMPILER_VERSION` unchanged — a v3.x profile compiles BYTE-IDENTICALLY, no contract drift). (2) `doc_profile_worker.py` gains a vNext path behind the rollback switch `POLYMATH_DOC_PROFILE_VNEXT` (plan §28 `profile_generation`): when set it builds the fingerprint (budget 500) + `profile_prompt_vnext` and passes `want_terms=False` — **GAP-04 closed on the vNext path** (no `document_summaries.major_concepts` read); `contract()` is DISTINCT when on, IDENTICAL when off (flag-off is byte-identical → rollback is a config change, never a re-ingest); base surfaces still project unchanged, research tags land in the artifact (not yet projected). 60 profile/compiler determinism tests pass incl. a new vNext stage pin (fingerprint prompt used, research tags in artifact, projection ok, against real PG with fake pool/embedder/Qdrant) + a compiler research-tag + backward-compat pin. The S5 canary (11.146) already proved vNext generation LIVE. Ledger §S7 → wiring DONE reversible. Work-log 2026-09-07-s8-profile-compiler-wiring. | DONE 2026-09-07 — S8 wiring crossed reversibly (flag default OFF = byte-identical live path). GATED: owner flips `POLYMATH_DOC_PROFILE_VNEXT` ON after a self-retrieval qualification. Next: S7 Groq router live wiring. |
| 11.148 | DOCUMENT-SEMANTIC-INDEX-V1 slice S7a — Groq shared-budget accounting (owner /goal step 2, 2026-09-07): the producer of the `AccountState` that `groq_router.choose` (11.134) consumes — the "shared-budget accounting" gate before scaled parent-MAP generation. `AdaptiveLimiter.capacity_snapshot()` (read-only: remaining_rpd = spec.rpd − today's day_count, rolling_rpm/tpm_used from the token buckets, in_flight from the semaphore, locked_until=`_not_before`, breaker_open) exposes the enforcement layer's live headroom to the SELECTION layer without mutating it (no second scheduler). `shared/polymath_shared/document_profile/groq_accounts.py`: `account_states` aggregates an account's TWO model lanes (compound + compound-mini) into ONE shared `AccountState` — RPD = account budget − SUM(lane day usage) (the GROQ-ROUTING-POLICY-V1 §2 correction: both models draw down the one key's quota), RPM/TPM/in-flight sum, locked_until max, breaker any; `snapshot_from_registry` reads live snapshots. 6 pins (shared-budget aggregation, breaker/lock aggregate + deterministic account sort, unmapped-lane skip, live capacity_snapshot fields, registry snapshot → `choose` picks the least-loaded account). Additive + read-only — no selection behavior change (that is S7b, flag-gated). Work-log 2026-09-07-s7-shared-budget-accounting. | DONE 2026-09-07 — S7a accounting gate built + tested. Next S7b: flag-gated router wiring into the Groq compound/mini lane selection + config compound-mini lanes + a controlled live routing canary (merges with the parent-MAP map-infer of step 3). |
| 11.149 | DOCUMENT-SEMANTIC-INDEX-V1 — vNext profile self-retrieval qualification PASSED → flag ENABLED (owner /goal step 1, 2026-09-07): `scripts/profile_vnext_selfretrieval_canary.py` generated vNext profiles for all 67 cinema docs through the existing `doc_profile` Groq pool, projected them to a SEPARATE canary Qdrant collection (deleted after), and measured self-retrieval vs the production baseline on the SAME 67-doc competitor set. Result: **vNext top-1 0.933 vs baseline 0.860** (top-3 0.985 vs 0.995, median rank 1 both), 0 errors — the vNext profile retrieves BETTER; gate PASS. Per owner authorization, enabled the profile via the reversible flag `POLYMATH_DOC_PROFILE_VNEXT=1` (set in gitignored `.env`; effective on the next fleet/supervisor reload — the running fleet stays on the legacy path until then; rollback = unset). Evaluation-only: no production profile collection / live worker / chunks / authoritative state touched; existing profiles untouched (backfill is a later controlled step). Evidence: `docs/wiki/experiments/profile-vnext-selfretrieval-2026-09-07.{md,json}`. Ledger §S7 profile → qualified + enabled (reversible). Work-log 2026-09-07-vnext-profile-qualified-enabled. | DONE 2026-09-07 — step 1 complete: vNext profile qualified (0.933 > 0.860) + enabled via reversible flag. Next: S7b routing + parent-MAP activation (step 3). |
| 11.150 | DOCUMENT-SEMANTIC-INDEX-V1 step 3 — parent-MAP LIVE activation on a tiny cohort (owner /goal step 3, 2026-09-07): `scripts/parent_map_canary.py` ran `build_parent_skeletons → map_prompt → groq/compound-mini → compile_maps → run_document_mapping` on 3 cinema docs, writing durable 0054 maps (the controlled activation; reversible via `--cleanup`). LIVE result: Circumplex 20/20, Multistage Pipeline 11/11, Affective Movement 19/19 — each complete (0 unresolved), restart-idempotent (2nd run re-inferred NOTHING, same active set), durable in the 0054 tables; the `map_canary` limiter lane halved once on a rate event and recovered (AIMD enforcement). 50 parents mapped. Complete mapping + restart idempotency + durable SQL state proven LIVE; partial recovery + missing-alias repair proven deterministically in S9 (`test_doc_parent_map_worker`); shared-budget accounting in S7a (`test_groq_accounts`). Touches ONLY the 0054 tables for the named docs (no chunks/profiles/retrieval/Qdrant). Evidence: `docs/wiki/experiments/parent-map-canary-2026-09-07.json`. Work-log 2026-09-07-parent-map-live-activation. | DONE 2026-09-07 — parent-MAP pipeline proven LIVE end-to-end on a 3-doc cohort; 50 durable maps. Next step 4: project these maps to the contract-scoped Qdrant collection + reconcile (Postgres count == projected count) + purge/rebuild. |
| 11.151 | DOCUMENT-SEMANTIC-INDEX-V1 step 4 — parent-MAP projection canary: reconciliation + purge/rebuild (owner /goal step 4, 2026-09-07): `scripts/parent_map_projection_canary.py` projected the step-3 cohort's active 0054 maps to the contract-scoped Qdrant collection `polymath_document_parent_maps_embed_e794ec4cab197a3f` via `parent_map_projection` (S10) + the embedder, then verified the §14 gate. LIVE result: Circumplex 20 maps→20 points, Multistage 11→11, Affective Movement 19→19 — **reconcile==True for each** (Postgres active-map count == projected point count), purge→0, rebuild→restored. 50 routing points now exist alongside the 50 durable maps. Additive contract-named collection only (chunk/profile/summary collections untouched). Evidence: `docs/wiki/experiments/parent-map-projection-canary-2026-09-07.json`. Work-log 2026-09-07-parent-map-projection-canary. | DONE 2026-09-07 — parent-MAP scale proven LIVE end-to-end (generation → durable SQL → projection → reconciliation → purge/rebuild) on a 3-doc cohort. All step 1-4 E2E gates PASS. Next (step 5): projection-receipt persistence + S7b router-into-map-infer, then a controlled backfill → shadow → dual-read → cutover (owner-gated sequence). |
| 11.152 | DOCUMENT-SEMANTIC-INDEX-V1 slice S7b — live Groq lane routing (owner /goal step 2 completion, 2026-09-07): wires `groq_router.choose` over the live limiter registry for the Groq compound/compound-mini lanes. `LimiterRegistry.get_lane` (read-only existing-lane lookup); `shared/polymath_shared/document_profile/groq_routing.py::route(pin, work_class, est_total_tokens)` reads each pin lane's `capacity_snapshot`, aggregates via `groq_accounts.account_states` (S7a shared budget), `choose`s, and `select_lane` maps the decision back to the pin lane (monotonic clock; fresh-pool fallback to full-budget placeholders so first calls spread). Reversible: `POLYMATH_GROQ_ROUTER` off (default) → callers keep their rotation. Config: `map_groq1..6` (groq/compound-mini, DEDICATED, api_key_env `GROQ_API_KEY_{i}` — SHARES each account's key with `profile_groq{i}` → one shared account budget across both models) + `stage_pins.doc_parent_map` + six limiter rows. The map lanes are dedicated (general extraction untouched) + dormant (no ticket producer) until a backfill routes to them. 6 pins; no scheduler added (limiter stays enforcement). Work-log 2026-09-07-s7b-groq-routing. | DONE 2026-09-07 — S7 complete (accounting S7a + routing S7b, reversible); compound-mini lanes share the profile accounts' budget. Next: wire `route` into the map infer for a controlled cinema backfill (spread across the 6 accounts). |
| 11.153 | DOCUMENT-SEMANTIC-INDEX-V1 step 5 (start) — controlled routed parent-MAP backfill (owner /goal step 5, 2026-09-07): `scripts/parent_map_backfill.py` — per doc, `run_document_mapping` with a ROUTED compound-mini infer (`route_groq` picks the least-loaded `map_groq` account) + projection, idempotent + resumable. LIVE 8-doc cinema slice: **routed spread WORKS — `distinct_accounts_used: 6`** (map_groq1..6 = 30/1/1/2/2/2; the router spreads + routes around 429-locked accounts) after fixing the single-account-pinning bug (`route_groq` read lanes under provider `cloud` not `llm_cloud` → `get_lane` always missed → no spread; FIXED). Mechanism proven end-to-end at cohort scale (4/8 fully mapped+projected). Rate-limit finding: the 4 largest docs (Bruce 297 / Blain 264 / Breakthrough 90 / Anatomy 71 parents) did NOT fully map in one run — the shared Groq accounts are heavily loaded today (67 profiles + 67 qualification + prior maps), so compound-mini 429s and large docs complete over time; a full backfill is RPM/RPD-paced + resumable (surfaced before any mass run, exactly the point of a controlled backfill). Touches only the 0054 tables + parent-map Qdrant collection for the cohort. Evidence: `docs/wiki/experiments/parent-map-backfill-2026-09-07.json`. Work-log 2026-09-07-parent-map-controlled-backfill. | DONE 2026-09-07 — routed backfill mechanism proven + spread verified (6 accounts); full backfill is rate-limit-paced/resumable (infra-constrained today). Next (step 5 back half): retrieval-runtime SHADOW route (vNext profile→parent-map→child, no ranking effect) → dual-read → cutover; then gated retirement. |
| 11.154 | DOCUMENT-SEMANTIC-INDEX-V1 step 5 — S8 shadow runtime route (owner /goal step 5 `…→ shadow →…`, 2026-09-07): the vNext routing path (global profile → ONE filtered parent-map search → child deepening) run as a SHADOW and measured against the current child lane, with **no production rank effect** (structural — additive module + read-only canary; candidate_engine, the query path, ranking, and QUERY_READY are untouched). New pure store-abstracted module `shared/polymath_shared/document_profile/shadow_route.py` (`shadow_route()→ShadowReceipt` = the §17 fields + latency + graceful `degraded`; the same module later backs S9 dual-read) + `scripts/shadow_route_canary.py` (reuses `rrf_rank` — the qualified nominator — a `doc_id`-filtered `routing`-vector map search, and a `parent_id`-filtered child search) + `tests/determinism/test_shadow_route.py` (6/6, fakes-only). LIVE (read-only, 36 probes, 6 mapped cinema docs): document nomination **1.000**, parent-resolve-into-source-doc **0.972**, mean overlap 0.665, median 143 ms. **Headline finding: shadow coverage tracks parent-map backfill completeness in near-lockstep** — the 4 fully-mapped docs 0.80–1.00 overlap, the 2 partial docs Blain (88/264=33%) 0.242 and Anatomy (11/103=11%) 0.017; nomination + resolve hold across ALL docs. The gap is unmapped parents (closed by the paced backfill 11.153), not routing quality; the k-width ceiling on large docs is covered by S9 dual-read UNIONING rather than replacing. Evidence: `docs/wiki/experiments/shadow-route-2026-09-07.{json,md}`. Work-log 2026-09-07-shadow-route. | DONE 2026-09-07 — S8 shadow qualifies structurally (no rank effect); routing substrate sound, coverage = f(backfill completeness). Next (step 5): **S9 dual-read** — Hybrid CONSUMES the shadow candidates (union with the unrestricted direct child lane) behind a reversible flag, gated on frozen-eval non-regression + exact-lookup control. First slice that changes a live reader → separately gated + flagged before landing. |
| 11.155 | FINAL-RETRIEVAL-ROUTING-SYNTHESIS-V1 admitted as query-time plan-of-record (owner @king, 2026-09-07): the owner's FINAL RETRIEVAL/ROUTING/SYNTHESIS plan frozen verbatim into `docs/wiki/plans/FINAL-RETRIEVAL-ROUTING-SYNTHESIS-V1.md` (P0) with a P0–P14 phase-execution ledger + governing-laws digest + reconcile note vs the migration ledger. Owner authorized executing the FULL plan (all phases, GRAPH + WILDCARD). Query policy = INTENT × PROFILE-FIELD × TECHNIQUE × BUDGET; 3 public modes (HYBRID/GRAPH/WILDCARD); 10 primitives R1–R10; parent-MAP = universal localization; §53 hard laws (raw query + exact terms + global raw child always survive, profile nomination never hard-gates, latent/precision additive); intents derive from the EXISTING compiler (no new classifier LLM). The migration ledger (RETRIEVAL-MIGRATION-DEPENDENCY-V1) remains the safe-migration mechanics for this plan's P1 substrate. | DONE 2026-09-07 — plan-of-record frozen; P1 substrate built (registers 11.139–11.154); next P2 intent-policy. |
| 11.156 | S9 dual-read lane — DOCUMENT_PROFILE→PARENT_MAP→CHILD as an additive HYBRID lane (FINAL-PLAN P1.spine, owner /goal step 5, 2026-09-07): `candidate_engine` gains `LANE_E` — the shadow routing substrate consumed by the LIVE v2 chat engine, ADDITIVE + unioned LAST + behind `POLYMATH_CHAT_DUALREAD_ENABLED` (default OFF). `CandidateBudget.dualread_*` knobs; `retrieve_candidates`/`_retrieve_on` take an injected `dualread_search`; the lane E block mirrors lane D (nominator returns resolved parents → each deepened via `dense_search(CHILD,{doc_id,parent_id})`), receipted in `trace["dualread"]`. `chat_retrieval` builds the nominator closure (profile `profile_nominate` RRF → ONE filtered `search_parent_maps`; reusable read-only helpers added to the projection modules) + wires `POLYMATH_CHAT_DUALREAD_*`. **Gate PASS both halves:** flag OFF byte-identical (`chat_regression --check` 16 cases/131 rows/0 failing — union identical when lane E empty); flag ON exact-lookup control (`scripts/dualread_qualify.py` LIVE in-process over Qdrant: L `gold_in_union` 15/15→15/15 0 regressions, B 13/15→13/15 0 regressions, dual-read contributed 1 L + 6 B turns). 4 lane-E unit tests (off byte-identical + never-calls-nominator; on adds `SHADOW_DUALREAD` provenance; duplicate child merges; None-nominator inert). Obeys §53 (raw/exact/global-child survive; no hard gate). Evidence: `docs/wiki/experiments/dualread-qualify-2026-09-07.json`. Work-log 2026-09-07-s9-dualread-lane. | DONE 2026-09-07 — the profile→map→child spine is live-consumable (default off, reversible, exact-lookup preserved). Next: **P2 intent-policy** (`classify_intent(plan)→Intent` over the existing compiler → budget at the `shape_budget` seam). |
| 11.157 | FINAL-PLAN P2a — deterministic query-intent classifier (§5, 2026-09-07): `shared/polymath_shared/query_intent.py::classify_intent` maps the compiler's EXISTING signals (task_type + subquery qtype multiset + graph_useful + exact_terms + entities + response_type) + the `query_router` lexical families to ONE of the 10 canonical intents (EXACT/DEFINITION/MECHANISM/RELATIONSHIP/COMPARISON/PROCEDURE/APPLICATION/SYNTHESIS/RECALL/EXPLORATORY) — NO new classifier LLM. Ordered most-specific→general; EXACT needs an identifier-like exact term (distinguishing "AU21"→EXACT from bare "FACS"→DEFINITION); SYNTHESIS ("what do my books say…") outranks an embedded "why". `ChatPlan.intent` set at both construction sites (compile + fallback), surfaced in `plan_receipt`. OBSERVABILITY ONLY — nothing reads intent for budget/technique yet (P2b). Also fixed the FINAL-PLAN doc's missing `last_reviewed` frontmatter (wiki_worm CI gate that failed on 6b22a49). Proof: `test_query_intent.py` 7/7 incl. ALL 18 plan §15–§32 worked examples; compiler/engine/shadow tests green; no behavior change. Work-log 2026-09-07-p2a-query-intent. | DONE 2026-09-07 — the intent classifier is live + receipted, no behavior change. Next P2b: thread intent into `chat_retrieve_v2` + `intent→budget` at the `shape_budget` seam behind a reversible flag. |
| 11.158 | FINAL-PLAN P2b — intent→budget policy (§33, 2026-09-07): `query_intent.INTENT_POLICY` = one `IntentPolicy` row per intent (dualread, micro_latent, breadth, resolution_lift, graph); `apply_intent_policy(intent, budget)` applies the ACTIVE already-built knobs — the profile→map spine (`dualread_enabled`) + the latent micro-search (`latent_enabled`, §13) — per intent, leaving resolution_lift/graph/breadth forward-declared for P3/P6/P9. Wired at the ui.py v2 budget seam behind `POLYMATH_CHAT_INTENT_POLICY` (default OFF ⇒ byte-identical; explicit ✨ still wins latent). Decoupled from CandidateBudget via `dataclasses.replace`. Gate PASS: flag-off `chat_regression --check` 0 failing; flag-on live smoke (10 cinema L+B) 0 gold regressions, additive lanes activate per intent (EXPLORATORY/COMPARISON dualread 19–24 + latent 13–18; EXACT neither). `test_query_intent.py` 11/11. Work-log 2026-09-07-p2b-intent-policy. | DONE 2026-09-07 — intent now drives the additive depth lanes, reversibly. Next P3 Resolution Lift (reads `policy_for(intent).resolution_lift`). |
| 11.159 | FINAL-PLAN R4 PROFILE_ATOM substrate (owner directive "verify… do not collapse Profile Atoms into the global profile", 2026-09-08): verified R4 was UNFINISHED (compiler produces 10 atom kinds but they were never independently persisted/projected — only theories/concepts/seealso rode as multivectors inside the global doc-profile point). Built as its OWN primitive: migration `0055_document_profile_atoms` (Postgres authority §51; atom_id content-hash PK, atom_kind CHECK of the 10 canonical kinds, supersede-per-doc+contract), `document_profile/profile_atom.py` (extract/persist/read), `document_profile/profile_atom_projection.py` (one dense point per atom in `polymath_document_profile_atoms_<contract>`, §34; batched embed [sidecar limit 32]; §14 reconcile + purge + kind-filtered `search_atoms`), `scripts/profile_atom_canary.py`. LIVE cinema: **1847 atoms persisted + projected, reconcile TRUE (1847==1847)**. Additive (no existing store/reader touched); atoms are routing/expansion units, never factual evidence. GATED: full atom coverage (latent_pattern/boundary/tension/inversion/bridge/anchor/recallq = 0) depends on vNext profile regeneration. `test_profile_atom.py` 4/4. Evidence: `profile-atom-2026-09-08.json`. Work-log 2026-09-08-r4-profile-atom. | DONE 2026-09-08 — Profile Atom is a first-class persisted+projected primitive. Next: the R4 retrieval lane (atom→docs/parents→MAP→children, kind-selected per intent, behind a flag). |
| 11.161 | FINAL-PLAN R4 PROFILE_ATOM retrieval lane (§42, 2026-09-08): the atom lane wired into retrieval — the dualread nominator now, when `budget.atom_kinds` is non-empty, runs kind-filtered `profile_atom_projection.search_atoms` and UNIONS the atom-nominated docs into the SAME parent-MAP search (§42 convergence, §17 one search). `CandidateBudget.atom_kinds`/`atom_k`; `IntentPolicy.atom_kinds` per intent (§33: EXACT none, MECHANISM the 4 mechanism kinds, RELATIONSHIP +relational, EXPLORATORY all 10) set by `apply_intent_policy`. Atoms = routing/expansion only, never evidence; NOT collapsed into the global profile (own collection + search). Gate PASS: flag-off byte-identical (`chat_regression` 0 failing, atom_kinds empty default); LIVE `search_atoms` returns 8 hits nominating 3–6 docs/query (functional). GATED: measurable uplift needs more mapped docs (backfill) + richer atom kinds (vNext regen). Work-log 2026-09-08-r4-atom-lane. | DONE 2026-09-08 — the Profile Atom lane is live, additive, reversible. Next: R6 Resolution-Lift gatherer + probe. |
| 11.162 | FINAL-PLAN R6 Resolution-Lift gatherer (§10–§12, 2026-09-08): `resolution_lift_gather.py` — `gather_lift_candidates(query, doc_ids, corpus_id, sources, …)` reads the SOURCE-DERIVED precision surfaces (profile TERM/TOPIC via Qdrant payload, MAP semantic_hooks/exact_identifiers via `document_parent_maps`, profile-ATOM terminology via `document_profile_atoms`, heading paths via `chunks.heading_path`, canonical aliases via `concept_families`/`concept_aliases`) into LiftCandidates ranked by the §11 core (≤3). Pure over an injected `sources` (each read guarded); `LiveLiftSources` implements the live reads. No hardcoded vocab; lifted terms are hints, not evidence. `test_resolution_lift_gather.py` 4/4 + live smoke (Circumplex doc → source-derived MAP exact ids). GATED: corpus DF for rarity has no persisted index (ILIKE too slow) → `compute_df=False` default (rarity → mid). Work-log 2026-09-08-r6-lift-gatherer. | DONE 2026-09-08 — the gatherer is built + tested. Next R6b: the bounded ≤3 second-pass probe (lifted terms → sparse/dense probes via the candidate_engine seam, behind a flag). |
| 11.163 | FINAL-PLAN R6b Resolution-Lift probe lane (§12/§18/§50, 2026-09-08): `candidate_engine` lane F — the corpus's precise vocabulary probed as ORIGINAL children. Injected `lift_search` (chat_retrieval) profile-nominates docs → `gather_lift_candidates` (≤3 source-derived terms) → embeds them → dense-child-searches each → children tagged with the lifted term; the engine unions lane F LAST and the cross-encoder judges (§12). `CandidateBudget.resolution_lift_enabled`/`resolution_lift_children`; `apply_intent_policy` sets it from `policy.resolution_lift != off`. §10 quality gate `is_meaningful_term` drops bare-numeric discovered terms (page/figure refs). Gate PASS: flag-off byte-identical (`chat_regression` 0 failing); LIVE cinema — lane F probes 6 children/query from ≤3 lift terms, **union +6, 0 lost** (additive, exact-lookup preserved), terms now letter-bearing (IEEE, corpus codes). GATED: lift-term uplift is corpus-bound (needs DF index + semantic-term preference on noisy-identifier corpora). Work-log 2026-09-08-r6b-lift-probe. | DONE 2026-09-08 — Resolution Lift is live end-to-end (discover→probe→judge), additive + reversible. Next: P5 SEEALSO/BRIDGE fan-out; P6/P7 graph; P8 synthesis bundle; P9 breadth; P10–13 measure. |
| 11.164 | FINAL-PLAN P6 intent-conditioned graph assist (§37/§38, 2026-09-08): graph auto-attaches on a HYBRID turn for relational intents (RELATIONSHIP→policy.graph=auto) WITHOUT a 4th public mode (§2). `chat_retrieve_mode` gains `graph_assist`; `_with_graph_assist` replicates the GRAPH branch's qvec-capture + bounded `_attach_graph` while keeping `meta.mode=HYBRID`. Fed from `policy_for(_plan.intent).graph` at ui.py (gated behind intent policy, default off); ui surface gate relaxed to show assist facts. Neo4j fail-open (degrades cleanly). Gate PASS: flag-off byte-identical (`chat_regression` 0 failing); `test_chat_modes` 24/24 (auto attaches + keeps HYBRID; off/conditional don't); LIVE relational query attached 20 facts (12 surface + 8 card seeds), mode HYBRID, not degraded. Facts ride the side channel (P7 routes destinations→parent-MAP→children). Work-log 2026-09-08-p6-graph-assist. | DONE 2026-09-08 — graph is intent-conditioned + additive + safe. Next P7: destination→parent-MAP localization. |
| 11.165 | FINAL-PLAN P8 synthesis evidence-role bundle (§44–§47, 2026-09-08): every selected evidence chunk tagged with its synthesis ROLE from lane arrivals — `candidate_engine.synthesis_role` (DIRECT=direct lanes A/B/C; PRECISION=lift lane F only; RELATIONAL=graph-destination child `ARRIVAL_GRAPH_DEST`, P7; LATENT=latent/dual-read D/E only; a direct arrival always wins, §47). `chat_retrieve_v2` stamps `role` per evidence row + `meta.evidence_roles` counts. Additive (no ranking/selection change). Gate PASS: `chat_regression` 0 failing; `test_candidate_engine` 37/37; LIVE every row tagged, bundle present (DIRECT-dominated on the mapped cohort — diversifies as additive lanes win selection on the full corpus). Work-log 2026-09-08-p8-synthesis-roles. | DONE 2026-09-08 — synthesis role structure in place. Next P8b: synthesizer presents by role (DIRECT first, LATENT after, PRECISION labels, RELATIONAL provenance). |
| 11.166 | FINAL-PLAN P9 task-conditioned breadth (§48/§49/§61, 2026-09-08): `apply_intent_policy` sets `rerank_round_robin` from the intent's breadth — SINGLE_OK (EXACT/DEFINITION) turns the doc-fair round-robin OFF so one excellent source may dominate (§61); MULTI_PREFERRED/MULTI_REQUIRED keep it ON (evidence-earned, §49). Wired onto the EXISTING soft round-robin composer (fills remaining seats in fusion order) — NOT the reverted per-doc quota. Default-off (intent policy). Gate PASS: `test_query_intent` 13/13; `chat_regression` 0 failing. Work-log 2026-09-08-p9-breadth. | DONE 2026-09-08 — breadth is intent-conditioned without quotas. Remaining: P5 (SEEALSO fan-out, BRIDGE/ANCHOR gated on atom gen), P7 (graph dest→children), P8b (synthesizer by role), P10-13 (measurement, gated on backfill value), P14 (retirement, gated on zero-reader proof). |
| 11.167 | FINAL-PLAN production routing qualification (2026-09-08): `scripts/production_routing_qualify.py` runs the intent-aware stack (intent policy ON: dual-read + profile-atom + resolution-lift + micro-latent + breadth + graph assist) vs OFF over the frozen L (exact) + B (grounded) cinema fixtures via the live `chat_retrieve_v2`, comparing `gold_in_union`. **PASS: L 15/15→15/15, B 13/15→13/15, 0 regressions** — exact-lookup + grounded gold preserved with every additive lane on (§53). Non-regression (safety) proven; corpus-scale UPLIFT gated on parent-MAP backfill + vNext atom regen (P10–P13). Evidence: `production-routing-qualify-2026-09-08.json`. Work-log 2026-09-08-production-routing-qualify. | DONE 2026-09-08 — the full intent-aware routing stack is production-SAFE (non-regressive). Uplift measurement gated on backfill. |
| 11.168 | FINAL-PLAN ledger unification + continuation bootstrap (2026-09-08): made `FINAL-RETRIEVAL-ROUTING-SYNTHESIS-V1.md` the SINGLE living execution ledger (formal phase table P0–P14 + primitive table R1–R10 + DEFERRED register D-5…D-14 with dependencies; P10 leads with the non-regression PASS; work-logs carry a 'Ledger row' back-reference so the MD is the hub, register/work-logs are evidence not a parallel ledger). Continuation: new handoff snapshot `docs/wiki/reports/2026-09-08/` (README + DIRECTORY_MAP); CONTINUITY-REPORT new 2026-09-08 checkpoint + read-order repointed; DOCUMENT-SEMANTIC-INDEX START-HERE plan-of-record → FINAL plan (that plan = this phase's P1 substrate, still valid). Bootstrap verified: agent_preflight ok, main=a17e4d6 (whole phase landed), repo_guard + wiki_worm green. Docs-only. | DONE 2026-09-08 — the MD is the governance control point; a fresh session bootstraps from CONTINUITY-REPORT → 2026-09-08 snapshot → FINAL-PLAN ledger. |
| 11.169 | FINAL-PLAN data-regen: vNext profile + full-kind profile-atom regeneration (cinema, 2026-09-08). Re-armed all 67 cinema doc_profile tickets via the sanctioned `_emit_ticket_event` path (`backfill_document_profiles.py --rearm`, new flag) — era-safe (`worker_identity('doc_profile').semantic_bundle == cinema pin`, `compatible()=True`, no blue-green) — so the supervised slots regenerated under vNext (`POLYMATH_DOC_PROFILE_VNEXT=1`): 67/67 `vnext=true`, 0 invalid, avg quality 0.819, compiled profiles now carry all 16 vNext fields (relational + rediscovery). Then `profile_atom_canary.py --project` (fixed to purge-then-rebuild the §14 cache) re-extracted/persisted/projected **609 atoms across all 10 kinds** (ANCHOR 67 · LATENT_PATTERN 66 · CONCEPT 66 · RECALLQ 65 · THEORY 64 · BOUNDARY 60 · SEEALSO 59 · TENSION 58 · BRIDGE 54 · INVERSION 50), **reconcile PASS (609==609)**, queryable via `search_atoms`. UNBLOCKS D-5 (P5 fan-out) + D-12 (Wildcard frontier) — the relational atom kinds now exist. Evidence: `profile-atom-cinema-2026-09-08.json`. Work-log 2026-09-08-vnext-profile-atom-regen. | DONE 2026-09-08 — cinema is a complete vNext profile+atom generation; D-5/D-12 data gates cleared. Map-backfill (D-10 uplift coverage) still partial/paused (420/11,993), resumable. |
| 11.170 | Parent-MAP backfill bounded concurrency (2026-09-08): `parent_map_backfill.py --concurrency N` — per-doc work over a ThreadPoolExecutor (idempotent per-doc, own tx() pool connection, route_groq spreads, per-doc try/except so one failure never aborts the batch). Canary (5 docs, c=5) clean in 7.2 s. **CORRECTION same session:** at scale c>1 exposed `route_groq` single-account pinning (a c=3 pass put 633/640 map calls on `map_groq1`, +13 maps — worse than sequential); default reverted to 1, then RESTORED to 6 after the router fix (11.171). Work-log 2026-09-08-parent-map-backfill-concurrency. | DONE 2026-09-08 — the flag + per-doc resilience landed; the pinning it exposed is fixed in 11.171 so 6-way now spreads. |
| 11.171 | route_groq CONCURRENCY-SPREAD-V1 (2026-09-08): `groq_routing.route` gained a short-TTL (`RESERVATION_TTL_S=6 s`) in-process reservation — each pick recorded per account, pending picks injected as extra `in_flight` into the snapshot `groq_router.choose` sees, so a SIMULTANEOUS burst rotates across all six accounts via the existing `-in_flight` tie-break (previously N concurrent callers read the same fresh snapshot and all chose the lexicographically-smallest account). Verified: 60 concurrent `route()` → exactly 10/account (was 60 on one); sequential unaffected (picks decay past the TTL). `reset_reservations()` for isolation; `test_route_spreads_a_concurrent_burst_across_accounts` + `test_reservation_decays_…` (27/27 green). Process-local (coordinates one process's threads; cross-process fleet spread is separate + tolerated). Work-log 2026-09-08-parent-map-backfill-concurrency (Resolution). | DONE 2026-09-08 — unblocks a genuine ~6× parallel parent-MAP backfill within capacity; D-10 coverage still capacity-gated but consumed in parallel. |
| 11.172 | FINAL-PLAN P5 SEEALSO/BRIDGE fan-out — TERM branch (2026-09-08). Additive engine lane G (`SEEALSO_FANOUT`, `candidate_engine.py`): the intent's RELATIONAL atom texts (SEEALSO/BRIDGE/ANCHOR/TENSION/INVERSION — routing-inferred pointers) probe ORIGINAL children via GLOBAL dense child search (§21 TERM/vocab-probe branch — coverage-INDEPENDENT of the parent-MAP). Atom ROUTES (never evidence, §63/§64), source children PROVE, cross-encoder judges; role = LATENT (§46/§47; RELATIONAL reserved for Neo4j). `fanout_search` in `chat_retrieval.py`; `IntentPolicy.seealso_fanout` on RELATIONSHIP + EXPLORATORY. Default-off, unioned last, flag-off byte-identical (`chat_regression` 131/0). Tests: 2 lane-G determinism pins + retrieval subset 220 green. LIVE (RELATIONSHIP/cinema): 24 fan-out children from 6 relational atoms, judged, 2 survive as LATENT (398 ms). Work-log 2026-09-08-p5-seealso-fanout. | DONE 2026-09-08 (TERM branch) — the relational atoms now route to adjacent source children end-to-end. ENTITY branch → P7; DOCUMENT branch → parent-MAP coverage; corpus-scale uplift → D-10. |
| 11.173 | FINAL-PLAN P7 graph destination as JUDGED evidence — D-7 RESOLVED (2026-09-08). Additive engine lane H (`candidate_engine.py`): query entity-card seeds → Neo4j hop-1 (`graph_expand`, reused from P6) → destination entities (fact subject/object) → their DOCUMENTS (`SELECT doc_id FROM mentions`) → ORIGINAL children, unioned BEFORE the cross-encoder and tagged `ARRIVAL_GRAPH_DEST` ⇒ RELATIONAL role (§46 — the bucket that was always 0). Clears D-7's graph-before-judge requirement WITHOUT a pipeline reorder: `_attach_graph` (P6 post-judge fact display) untouched; lane H is the pre-judge judged path. `graph_dest_search` in `chat_retrieval.py` (fail-open at every store); enabled where the intent's graph budget is auto/strong (RELATIONSHIP). Default-off, unioned last, flag-off byte-identical (`chat_regression` 131/0). Tests: lane-H determinism pin + retrieval subset green. LIVE (RELATIONSHIP/cinema): 8 destination docs, judged; evidence_roles DIRECT 12 / RELATIONAL 1 / LATENT 2 — RELATIONAL populated 0→1 (2.4 s, Neo4j reached). Work-log 2026-09-08-p7-graph-destination. | DONE 2026-09-08 (doc-level) — graph now supplies JUDGED source-attested RELATIONAL evidence. §39 parent-MAP localization (doc→parent) → coverage; P11 GRAPH uplift → density+coverage (D-11); 2.4 s latency tuning. |
| 11.174 | FINAL-PLAN P8b synthesizer presents by role — D-8b resolved for the LLM path (2026-09-08). The retrieval per-chunk role (DIRECT/PRECISION/RELATIONAL/LATENT, from `synthesis_role`) rides the bundle as `evidence_roles`; `_grounded_messages` (shared grounded-prompt assembly for every chat backend), behind `POLYMATH_CHAT_SYNTH_ROLES`, STABLE-groups the [S#] block by role (DIRECT first), labels each tag, and prepends the §47 role guidance (LATENT/RELATIONAL never substitute for DIRECT). AVOIDS D-8b's blocker: role presentation is in the LLM prompt path only — `answer_synthesis.grounded_answer` (the deterministic claim system) is untouched. Default-off byte-identical (`chat_regression` 131/0); the [S#] tag→locator legend is unchanged, so citations are unaffected. Test: `test_p8b_role_aware_presentation_…`. Work-log 2026-09-08-p8b-synthesis-roles. | DONE 2026-09-08 (LLM path) — the chat synthesizer now presents grounded evidence by role. Deterministic /ask path by role + answer-quality uplift (thin roles on current coverage) are follow-ups. |
| 11.175 | RETRIEVAL-MIGRATION S11-proper — vNext readiness as a first-class semantic_readiness verdict (2026-09-08). `semantic_readiness.vnext_readiness(conn, corpus, documents)` + a `vnext` field on `semantic_completion`: VNEXT_COMPLETE/INCOMPLETE/NOT_STARTED computed from the durable parent-MAP substrate (eligible parents via `document_region.NOISY_ROLES` vs active maps + exclusions, §19 floor `unresolved==0`) AND vNext-profile coverage (`doc_profile` artifacts with `vnext=true`). Additive — the legacy `verdict` is unchanged; fail-open (missing substrate/read error → NOT_STARTED); does NOT redefine the frozen `query_ready` control contract. This is the readiness the QUERY_READY flip (S13) + cutover (S14) gate on — the control plane now SEES per-corpus vNext completeness. Test: `test_s11proper_vnext_readiness_…` (COMPLETE/INCOMPLETE/NOT_STARTED/fail-open) + readiness suite green. LIVE: cinema VNEXT_INCOMPLETE (11993 elig / 551 mapped / 368 excl / 11074 unresolved; profiles 67/67); ecom VNEXT_INCOMPLETE (0/1346, 0/10). Work-log 2026-09-08-s11-vnext-readiness-verdict. | DONE 2026-09-08 — readiness authority is vNext-aware. S13 (QUERY_READY-requires-vNext) reads it; the flip + cutover stay coverage-gated (VNEXT_INCOMPLETE until coverage). |
| 11.176 | RETRIEVAL-MIGRATION S12 — FIRST corpus driven to VNEXT_COMPLETE end-to-end (2026-09-08). `d7-h1-test` (3 docs, 78 eligible parents) taken through the whole data-regen chain: capacity canary (26/26 parents, 0 err, spread across 3 Groq accounts via CONCURRENCY-SPREAD-V1) → full parent-MAP backfill (78/78 mapped, 0 unresolved, projected) → 3/3 vNext doc profiles (supervised `profile_worker`, POLYMATH_DOC_PROFILE_VNEXT=1, quality p50 0.823) → 30 Profile Atoms across all 10 kinds (reconciled 30==30). `semantic_completion('d7-h1-test').vnext` = **VNEXT_COMPLETE** (pending []) — the FIRST corpus to flip INCOMPLETE→COMPLETE, proving the S11-proper §19 floor fires exactly and the migration substrate is autonomously completable. NOT a cutover (S13/S14 = owner QUERY_READY flip, BE-AWARE §7 — untaken on any corpus); NOT a real-corpus quality result (d7 is a synthetic 3-doc test corpus with no P10 fixture set — uplift needs cinema at coverage). Sanctioned map/profile lanes only (~12 routed calls), no paid fallback. Work-log 2026-09-08-d7-first-vnext-complete. | DONE 2026-09-08 — mechanism proven end-to-end on one corpus. cinema (real, fixtured) cutover still gated: 551/11,993 mapped (multi-session RPD campaign) + owner QUERY_READY flip. |
| 11.177 | RETRIEVAL-MIGRATION S12 backfill — account-spread FIXED, large-doc yield GATED (2026-09-08). While advancing cinema parent-MAP coverage 551→704, two defects were diagnosed under the "fix the gate, don't bypass" law. (1) **FIXED:** `parent_map_backfill.py` `_routed_infer` routed via `route()`, whose capacity view is BLIND for these map lanes in a dedicated process (unregistered limiter lanes → tied placeholders → the 6 s reservation expires during real calls → lexical-first pin, measured 649/657 on map_groq1). Replaced with explicit ROUND-ROBIN across the six distinct-account endpoints (BACKFILL-SPREAD-V1); each keeps its own AIMD limiter. Live-verified even 110/110/110/109/109/109; test `test_parent_map_backfill_spread.py` (10/account; empty-pool raises). (2) **GATED (root cause captured):** the residual coverage wall is large-doc batch OVER-SIZING — `map_batches.plan_batches` cuts batches by a fixed alias COUNT (~40, from the avg density's OUTPUT tokens) with NO INPUT-token cap, so a big doc's large parent chunks overflow the endpoint: on a 469-parent doc, 20 aliases→maps, 40→EMPTY (even at max_tokens 8000), 60→HTTP_413; endpoints otherwise OK. NOT capacity/concurrency/pinning/decline/parse. **Fix (its own migration-safe slice):** pack batches by estimated INPUT tokens (`estimate_input_tokens`) + bump `BATCH_PLANNER_VERSION` (re-batch; maps persist by map_hash) + assert a large fresh doc reaches unresolved==0. Work-log 2026-09-08-backfill-spread-and-bigdoc-yield. | DONE (spread) 2026-09-08; large-doc yield FIXED in 11.178. |
| 11.178 | RETRIEVAL-MIGRATION S12 — large-doc batch OVER-SIZING FIXED (map-batches-v2, 2026-09-08). The cinema campaign's real wall: docs ≥~430 parents mapped ≈0 (Ken Dancyger 2/430, Hey Whipple 0/469). Root cause (corrected from the initial input-size hypothesis, AFTER capturing a batch): `map_batches.plan_batches` capped batches by a token-envelope alias COUNT (`mapping_only_capacity`=60), but `groq/compound-mini` reliably returns a COMPLETE structured map only for SMALL batches — measured 10-15 aliases 100% (10/10, 15/15 ×3), 20 mostly, ≥25 flakily EMPTY/partial despite a tiny prompt (~16 KB at 60). The token envelope was never the ceiling; the model's structured-output reliability is. **Fix:** `MAP_RELIABILITY_CAP=15` in `mapping_only_capacity` + `BATCH_PLANNER_VERSION`→`map-batches-v2` (batch_hash changes → large docs re-batch into 15s; maps persist by map_hash; resumable; small docs unchanged). Tests: `test_map_batches` pins updated (capacity==15; 40→[15,15,10]; 60→4×15; 150→10×15), 24 planner-dependent tests green. **Qualified live:** Hey Whipple 0→75/469 in one capacity-limited pass (~20× better maps/call); 15-batches 100% reliable under fresh capacity. Work-log 2026-09-08-backfill-spread-and-bigdoc-yield. | DONE 2026-09-08 — cinema large-doc mapping no longer batch-size-blocked. **SUPERSEDED re: the capacity claim by 11.184 (FORENSIC HOLD 2026-09-08/09):** "completing all docs is purely capacity-gated" is now DISPUTED — backfill STOPPED pending the Groq Parent-MAP forensic audit; not to be resumed on a quota reset. |
| 11.179 | FINAL-PLAN P10 rerun + routing lanes QUALIFIED LIVE on cinema (2026-09-08). The additive lanes — previously landed default-off, only single-query-smoke-tested — are now SYSTEMATICALLY qualified through the real `chat_retrieve_v2` (read-only). Non-regression: L 15/15→15/15, B 13/15→13/15, 0 regressions. Beyond that the intent lanes EXPAND the candidate union in EVERY query (L 69.5→109.5 +58%, B 90.9→147.8 +63%); on RELATIONSHIP queries all four fire + contribute SOURCE-CHILD candidates — P5 seealso_fanout 24 (6 relational atom routers), P7 graph_dest 8 (Neo4j-attested dest docs, 2.4 s), F resolution_lift 6 (lifted terms), E dual-read 22-24 (838 parent-maps); union +60-75%, gold preserved, candidates all `routing_child` (atoms/graph ROUTE, children PROVE, §63/§64 held). **Correction:** P5/P7/F are parent-MAP-coverage-INDEPENDENT (atom/graph→global child), so they qualify on cinema NOW regardless of the coverage wall; only E dual-read reach scales with coverage. **All three compositions qualified live:** HYBRID (the four lanes above), **WILDCARD** (`chat_retrieve_mode` — divergent sweep 45/42 latent_candidates → 3 bridges on the separate wildcard lane, P13), **GRAPH** (15–20 source-attested Neo4j facts via 8 entity-card seeds, predicates PRODUCES/REQUIRES, P11) — facts/bridges ride side channels, never evidence. Evidence `docs/wiki/experiments/routing-lanes-qualify-cinema-2026-09-08.json`. Work-log 2026-09-08-routing-lanes-live-qualify. | DONE 2026-09-08 — HYBRID/WILDCARD/GRAPH all past 'default-off wiring' to live-contributing (union breadth) + invariant-preserved (P10/P11/P13). **Judge-survival + VALUE DEMONSTRATED:** on exact/QA L/B the relational candidates expand the union but are out-competed by DIRECT hits (final evidence all DIRECT — correct when cinema has direct answers); BUT on cross-doc/exploratory queries where direct is SPARSE, they SURVIVE the cross-encoder into final evidence (editing↔fermentation 5 LATENT, hydrology↔composition 3 LATENT, maintenance↔cinematography 2 RELATIONAL per query) — value shown exactly where direct falls short, invariant-preserved. **P8b synthesis presentation also operationalized** on real multi-role evidence (`_grounded_messages`, 10 DIRECT + 5 LATENT): roles ON → DIRECT-first regroup (0 DIRECT-after-LATENT) + per-[S#] role labels + §47 guidance; roles OFF byte-identical; citations stable. Remaining (D-10, narrowed): a FORMAL answer-quality number (LLM-judge/rubric on a relational fixture) needs a chat model (capacity) — retrieval-side value + synthesis presentation are now live-qualified. |
| 11.181 | FINAL-PLAN D-10 answer-quality uplift MEASURED (2026-09-08). The plan's "measurable uplift" is now quantified on the ANSWER, not just retrieval: over the FREE Ollama `gemma4:31b-cloud` (`_ollama_generate`, no owner spend), 5 relational cinema queries generated with lanes ON (intent policy) vs OFF (default budget). A blind A/B LLM-judge (randomized order, "which better explains the connection") prefers the lanes-ON answer **4/5**; the ON answer objectively CITES relational-role evidence (LATENT/RELATIONAL/PRECISION `[S#]`) in **3/5** — evidence the DIRECT-only default could not supply (colour→film-music "coloristic effects" etc.). Consistent with the objective cross-encoder-survival result (11.179). Caveat recorded: small sample + LLM-judge, not a human rubric. Evidence `docs/wiki/experiments/d10-answer-quality-cinema-2026-09-08.json`. | DONE 2026-09-08 — answer-side uplift measured (ON 4/5 judge, 3/5 relational-cite). Residual: scale the fixture + a human/larger-model rubric for a tight number. Retrieval + synthesis value are now BOTH live-qualified. |
| 11.182 | SILICONFLOW-EXTRACTION-LANES-V1 ACTIVE + qualified (owner 2026-09-08). The three SiliconFlow accounts (siliconflow1/2/3 @ Qwen/Qwen3-8B) added in 2981a7d are now LIVE — owner pasted SILICONFLOW_API_KEY_1/2/3 into `.env`; assistant never wrote the keys. Canary `scripts/siliconflow_extraction_canary.py` PASS 3/3: each account authenticates independently, runs a real extraction, returns valid JSON with 8 entities + 8 relations, `thinking_off_ok=true` (the THINKING-CONTROL-V1 `enable_thinking:false` fix works — a thinking-on Qwen3-8B returns empty). `dedicated:false` → they shard the unpinned `extract` ring only (graph extraction), each its own AIMD limiter lane / account balance. ~14 s/extraction (SiliconFlow cloud + 8B) = added ring capacity. Running extract workers pick the lanes up on restart. Owner reminded to rotate the keys (in-transcript). | DONE 2026-09-08 — 3 graph-extraction accounts live + canary-qualified. Adds extraction throughput/graph density (relevant to P11 GRAPH). Optional follow-up: pin `extract` to SiliconFlow if the owner wants it primary rather than shared. |
| 11.183 | SILICONFLOW-EXTRACTION-LANES-V1 fire-at-max-rate concurrency config (owner 2026-09-08: "configure the concurrency/rate … so it fires at its max rate"). MEASURED the real edge before setting seeds (limiter.yaml rule: raise a cap only with a measured reason). Single-account probes: SiliconFlow exposes **NO rate-limit headers** (`use_headers` is a no-op) and returns **NO 429/Retry-After even at 48 trivial-concurrent** — there is no rate limit to sync against; the ONLY failure mode is ReadTimeout under heavy-request latency saturation. Heavy bursts (prod system prompt, `max_tokens=2000`, `enable_thinking:false`): N=16 → 16/16 clean; **N=24 → 24/24 clean (~32 req/min, highest CLEAN)**; N=32 → 30/32 (2 ReadTimeouts, still no 429). So each lane is `kind: concurrency` (NOT rate — a rate/token bucket would be fiction), ceiling `max: 24` (highest measured-clean/account), seed `init: 12` (climb, don't cold-open at the edge in a slow provider window), `min: 2`, AIMD halves on timeout/503 and climbs +1/8-clean back to 24. **No `family`** → the 3 accounts stay fully independent (a backoff on one never cools the others — the point of 3 separate balances). 3 × 24 = **72 aggregate concurrent graph-extraction slots**. Integrated live qual (client → AdaptiveLimiter → SiliconFlow, N=12): 12/12 ok, 0 errors, limiter climbed 12→15 on clean successes. Guard `test_siliconflow_lanes_are_independent_concurrency` pins kind/family/seed/ceiling + asserts no rate buckets (9 green). Commit a1b94ae. | DONE 2026-09-08 — SiliconFlow lanes now fire at measured max concurrency (24/account, 72 aggregate), self-tuning via AIMD, no 429 storms. Residual: the 3-account-simultaneous edge (backend sharing) is untested — AIMD backs each lane off independently if it appears; raise `max` only with a new measured burst. |
| 11.184 | RETRIEVAL-MIGRATION S12 — cinema Parent-MAP backfill STOPPED under a FORENSIC HOLD (owner directive 2026-09-08/09). **Status correction, not new implementation.** The prior session's conclusion — a `+0`-parent / `0`-errored_docs backfill pass PROVED the six Groq accounts had exhausted daily RPD (recorded in 11.178, S12, CONTINUITY-REPORT) — is **DISPUTED** and downgraded from proven provider fact to unverified inference. It was never reconciled end-to-end against provider truth: Groq rate-limit headers (`x-ratelimit-*-requests`), limiter admission (`LIMITER_REFUSED` must mean ZERO HTTP request, never provider consumption), actual HTTP dispatch counts (vs `lane_counter` endpoint-selections), `MappingOutcome.errors` (can accumulate while `parent_map_backfill.py` reports `errored_docs=0`), retry waste (`run_document_mapping(max_attempts=3)`), compiler yield, and persisted maps. Cinema coverage ≈**1254/11,993** (verified live). **Owner boundary:** DO NOT resume the backfill on a quota reset; DO NOT spend provider quota to gather evidence; architecture FROZEN (ParentSkeleton → plaintext MAP DSL → deterministic `map_compiler` → durable maps → projection; no JSON mode / function-calling / compiler bypass; chunker untouched; no autonomous QUERY_READY flip). **Next session = the Groq Parent-MAP forensic audit** (establish provider RPD truth is observable + reconciles with local `day_count`; refusal/error accounting correct; retry waste quantified; request→valid-map efficiency measured; benchmark MAP batch sizes 15/20/30/40/60 under the DSL+compiler) → acceptance gate → bounded canary → owner review → only then backfill. Handoff: `docs/wiki/reports/2026-09-08/START-HERE.md` (+ CONTINUATION_REPORT / BE_AWARE / UNFINISHED_WORK / DEPENDENCY_MAP / GROQ-FORENSIC-AUDIT). Incidental observation (this session, not audit evidence): a stopped re-run showed several docs `mapped=N/N` yet `complete=False` (e.g. Anatomy for Sculptors 71/71) — an accounting discrepancy the audit should examine. | HOLD 2026-09-08/09 — backfill STOPPED by owner; RPD-exhaustion claim DISPUTED; forensic audit is the gate before any resume. No provider spend authorized during handoff. |
| 11.180 | RETRIEVAL-MIGRATION S16(c) — legacy-reader census benign auto-classification (2026-09-08). Pre-stages the zero-reader retirement proof by shrinking the manual review queue. `legacy_dependency_census.py::_role` gains two BENIGN classes after the writer/producer/reader precedence: `comment` (a `#` line) and `label` (the symbol inside a STRING LITERAL — a name/event-type/dict-key/lane-label/constant value, matched only BETWEEN two quotes, so a bare identifier stays `reference`). A genuine read carries a reader verb (SELECT/FROM/get_/read_/fetch/load_/query/route/.get) caught first, so a quoted SQL string stays `reader` — verified by test pins. Result: runtime 334, **benign 114 auto-classified, unclassified 216→102** (−53%). Residual 102 = docstring prose + genuine references, resolved at the coverage+cutover pass (they read the legacy summaries vNext replaces). `test_legacy_dependency_census` updated (benign + safety assertions), 9 green. | DONE 2026-09-08 — retirement census queue halved; S16 zero-reader proof is now a smaller, coverage-gated pass. Genuine-reader retirement still gated on S14 cutover (owner QUERY_READY flip). |
| 11.185 | GROQ-MAP-CONTROL-PLANE-REPAIR-V1 — the offline forensic repair of the Parent-MAP control plane (owner directive 2026-09-09). Turns an ambiguous accounting system into an instrumented conservation chain; NO MAP architecture change (ParentSkeleton → plaintext MAP DSL → deterministic `map_compiler` → durable maps → projection FROZEN; no JSON/schema/function-calling; compiler + chunker untouched); NO provider spend; backfill STAYS STOPPED. **Historical cause now MEASURED (read-only census):** cinema durable state = 1,064 batches, 15,773 claims, **95.2% on zero-yield batches**, terminal `last_error` **`LIMITER_REFUSED` 926** vs HTTP_429 11 / HTTP_413 3 / empty-200 1. A `LIMITER_REFUSED` dispatches ZERO HTTP (`client.py:425`, pre-`_chat`), so the disputed `+0/0-errors` pass was a LOCAL refusal cascade — provider RPD exhaustion is CONTRADICTED, not proven. **Defects fixed, each with a failing→passing regression:** (A) `_sync_headers` fed `x-ratelimit-*-requests` (Groq DAILY budget) into the per-MINUTE `_rpm` bucket → now drives a distinct provider-RPD representation (conservative epoch reconciliation) + a `PROVIDER_RPD` admission gate; (B) success-path headers were dropped → `_chat` returns them, `complete_one` feeds `record_success` (provider RPD observable on 2xx); (E) `errored_docs=0` hid internal failures → `MappingOutcome` + `summarize()` surface refusals/HTTP/empty/invalid, PASS requires all-complete AND zero-internal; (G) `lane_counter` counted SELECTIONS → LANE SELECTION and PROVIDER HTTP DISPATCH are now separate counters; (H) all 12 Groq lanes shared `family: groq` → `family: groq_acct_N` (six isolated circuits, per the routing policy of record); (F/Phase 11) a local refusal / dispatched fault now DEFERS (resumable) instead of spinning `max_attempts` — only PARTIAL retries (§18.4 repair). `admit()` returns a reasoned `LimiterDecision` (acquire() kept as a bool wrapper). **Offline replay reconciles** (selections==admitted+refused; dispatches==admitted==2xx+429+transport; refusals 0 HTTP; persisted≤compiler-valid). New tests: `test_limiter_control_plane` (17), `test_groq_account_isolation` (2), `test_offline_conservation_replay` (1) + extended worker/backfill suites; adjacency 195/195 green; `repo_guard` ok. **UNVERIFIED (needs the bounded live probe):** real Groq per-account daily quota + compound/compound-mini sharing (config "RPD 250" is an assumption, never header-reconciled); WHICH gate produced each historical refusal is UNOBSERVABLE with the old telemetry. Work-log 2026-09-09-groq-map-control-plane-repair. | DONE (offline repair) 2026-09-09 — control plane is an instrumented, testable conservation chain. **Live-probe gate OPEN:** Phase 1/18 quota-topology probe + Phase 19 batch benchmark are the only remaining gate items and need a bounded, owner-authorized provider budget. Backfill STOPPED until: bounded probe → benchmark → bounded canary → owner review. |
| 11.160 | FINAL-PLAN R6 Resolution-Lift ranking core (§11, 2026-09-08): `shared/polymath_shared/resolution_lift.py` — `LiftCandidate` + the §11 specificity ranking (`score_candidate` weighting source-prior/locality/rarity-idf/canonicality/identifier-form/semantic-support; `rank_lift_candidates` filters non-specific terms, dedupes, caps at ≤3 per §14; `specificity_beyond` enforces "a lift, not a restatement"; `is_identifier_like`). Pure + deterministic; "vocabulary discovers, source chunks prove" (§12). `test_resolution_lift.py` 5/5 incl. the plan's punched-face → AU21/Action Unit/FACS example. GATHERER (read the corpus surfaces → LiftCandidates) + the bounded second-pass probe are the next slice; corpus DF for rarity is not persisted (compute on the fly). Work-log 2026-09-08-r4-profile-atom. | DONE 2026-09-08 — the ranking core is built + tested. Next: the gatherer + probe wiring (R6) via the candidate_engine second-pass seam. |
| 11.186 | RAG-PIPELINE-FINISH-V1 — finish the fresh-document pipeline so a new upload reaches VNEXT_COMPLETE (owner /goal 2026-09-09, runbook `RAG_PIPELINE_FINISH_PLAN.md` adopted from PR #2). **Phases 0–14 landed OFFLINE, gate GREEN (178 passed/1 skipped), ZERO provider spend, forensic hold intact.** Central finding (Phase 1): `doc_parent_map` (pMAP) was NOT auto-minted (backfill-only), so a fresh upload could never satisfy the vNext readiness floor `unresolved_eligible_parents==0`. Built: **P2** LANE-REGISTRY-V1 (explicit account/model registry + `lane_inventory.py`, credential-sharing audit, no secrets); **P3** vendored rate-limit seed + EFFECTIVE-CAPACITY-V1 precedence (observed>config>seed>unknown); **P4** functional-pool drain assessment + `pool_lane_health`; **P5** DOCUMENT-GROUNDING-CONTEXT-V1 (deterministic ~50-100-tok CPU-only document grounding, 11 doc-class fixtures); **P6** grounded pMAP prompt (map-prompt-v2 + grounding_hash binds batch identity; grounding=None byte-identical → frozen cinema identities unchanged); **P7** lane-qualified pMAP batch cap (`map_batch_cap`/`pmap_pool_batch_cap`, target 60 preserved); **the SPINE** — `doc_parent_map_stage_worker` (in-run cross-lane failover over the six map_groq accounts; first healthy lane finishes a batch, all-dark defers TRANSIENT no-attempt) minted like parent_enrichment by the FLAG-GATED `auto_map_parents_on_chunks` scheduler phase (OUTSIDE STAGE_DAG; NON_BLOCKING); **P12** CANONICAL-DOCUMENT-STATUS-V1 (per-doc aggregate + ordered blockers); **P14** offline gate + `rag_pipeline_canary.py` harness. FROZEN: ParentSkeleton → plaintext MAP DSL → deterministic compiler → durable maps → projection; chunker untouched; STAGE_DAG unchanged. **HOLD-SAFE:** `POLYMATH_DOC_PARENT_MAP_ENABLED` default OFF ⇒ no tickets minted ⇒ byte-identical, and `POLYMATH_DOC_PARENT_MAP_CORPUS` scopes the canary so cinema is never re-mapped. Work-logs: `2026-09-09-rag-*` (admission, phase2..7, pmap-stage-wiring, phase12, phase14, phase15-canary-live). **PHASE 15 EXECUTED LIVE + PASSED (2026-09-09):** fleet restarted with the pMAP slot + fleet-autopilot demand lane, `POLYMATH_DOC_PARENT_MAP_ENABLED=1` scoped to `rag-canary` (cinema untouched); `rag_pipeline_canary.py --passes 3` → **3 consecutive passes 215.9s / 169.3s / 107.7s** (all < 240s, each `probe_ok=True`, `ZQX-*` fact cited from `child_evidence`). Live bugs found+fixed: pMAP projection embed contract, doc-resolution race, per-doc vnext_ready, doc_profile-early, probe child_evidence parsing. | **GOAL ACHIEVED 2026-09-09** — fresh-document pipeline COMPLETE + PROVEN LIVE: `/upload` → auto-minted grounded pMAP (Groq compound-mini) + early doc_profile → per-doc `vnext_ready` < 4 min → source-grounded retrieval; 3/3 consecutive canaries. Transient canary flags clear on the next normal fleet restart. **POST-CANARY (offline, commits 1ad74bc…87fe75e):** Phase 16 pMAP packing sanity (60-parent planning target-60 + persistence, provider-free); Phase 18 Files/status UI on CANONICAL-DOCUMENT-STATUS-V1 (GET /documents/{id}/status + map_active + FilesView MapBadge/StatusPanel, npm build green); new-uploads-only production guard `POLYMATH_DOC_PARENT_MAP_SINCE` (enable globally for fresh uploads only, historical never swept); Phase 17 read-only cinema dry-run (10,176 unresolved, ungrounded maps) with the owner-gated unblock documented. Full offline gate 156 passed/1 skipped. **REMAINING = owner-gated only:** production pMAP enable (spend), the forensic-hold Groq probe + cinema reconciliation (Phase 17), and the Phase B doc_profile DAG cutover. |
| 11.187 | OPERATIONAL-UI-V1 — surface the control-plane health the pipeline already computes, as a MINIMAL extension of the existing Polymath UI (owner brief 2026-09-09; supersedes the placeholder "11.199 (pending)" the backend work-log named before this row existed). Design law: FILES = "is this document healthy?", CONTROL PLANE = "is the machinery healthy?", CHAT unchanged. **Backend contract (§11, ONE authority the UI renders, never recomputes):** `document_status.py` — FIX run-resolution to THIS document's own `chunked.v1` run (was the corpus's newest → wrong graph/projection/elapsed for any multi-doc corpus); `detail=True` adds the diagnostic-drawer sections (graph extraction, projections, elapsed, pMAP efficiency + model); `corpus_document_summaries()` bounded N+1-free batch for the Files columns; `control_plane_status.py` (CONTROL-PLANE-STATUS-V1) — corpus summary + four functional pools (GRAPH_EXTRACTION/DOCUMENT_PROFILE/PMAP/CHAT) with `limiter_refused` (0 HTTP, LOCAL) distinguished from actual HTTP 429, and `pool_lanes_detail()` model→account lanes exposing the env NAME only (never a secret). Endpoints (read-only): `/documents/{id}/status` (detail), `/documents/summary`, `/control_plane`, `/control_plane/pool/{function}`, `/control_plane/predicates` (top-N on demand). **Frontend (Slice 2, this commit):** Files columns File/Type/Size/Added/Parents/pMAP/Graph/Profile/Ready — an under-mapped doc renders RED (`st-failed` when unresolved>0); the row drawer is the CANONICAL-DOCUMENT-STATUS-V1 diagnostic (DOCUMENT / GRAPH EXTRACTION / DOCUMENT PROFILE / PMAP / PROJECTIONS / READINESS) with exact counts + the exact ordered blockers from the backend. Tests: `test_control_plane_status` (batch vnext_ready rule, four pools, refused≠429, secret-free lanes) + `test_document_status*` green; `npm run build` green; live-verified on 7200 — `/documents/summary` rag-canary 10 docs 12 ms (exact DocSummary shape), cinema 67 docs 620 ms (53 render RED, e.g. 1319 eligible/0 mapped/1316 unresolved), `/documents/{id}/status` fully populated. **Slice 3 (Control Plane screen):** the existing "Fleet" rail slot repurposed into "Control Plane" (§10 — no nav added/removed, the fleet board preserved as a drill-down) rendering CONTROL-PLANE-STATUS-V1: corpus summary + the four functional pools, each drilling into FUNCTION→MODEL→ACCOUNT/KEY lanes (env NAME only, never a secret) + GRAPH predicate distribution; limiter_refused shown DISTINCT from HTTP 429 (§4); pMAP maps-per-request efficiency + per-lane batch-15/target-60 (§6). Browser-verified live at :5173/ui (proxy→:7200). **Slice 4 (Chat §9):** minimal — the three SETTABLE selectors already exist (Query Type = retrieval mode, Chat Model = synthesizer, Reasoning = mode dropdown, default `none` = lightest, no auto-escalation path in code); Intent is compiler-DERIVED with no override contract, so it is surfaced READ-ONLY as a `⌖ intent` badge on the answer (from the existing chat_plan receipt), NOT a fake selector (§9 "if backend supports it" → it does not; §14). Browser-verified: a live turn rendered `HYBRID · ⌖ comparison · GENERATED · deepseek-v4-flash · 15 chunks`. **Slice 5 (acceptance + durability):** §13 acceptance checklist run against the live backend — **16/16 PASS**; one consistency FIX it surfaced (the document-status DETAIL graph block now returns `relations`, matching the Files column — one authority). The three status suites are 9/9 green; the full `tests/determinism` has 2 PRE-EXISTING failures unrelated to this change (a cinema `you`-endpoint fact from 2026-09-05 + a live `/chat/stream` synthesis variance test), neither touching this surface. Guards ok; CONTINUITY checkpoint written. Work-logs `-backend-counters` + `-files-drawer` (S2) + `-control-plane` (S3) + `-chat-intent` (S4) + `-acceptance` (S5). | DONE 2026-09-09 — backend contract + Slices 2/3/4/5 complete & proven live (16/16 acceptance). No pipeline/architecture change; read-only; no API-key value rendered; limiter_refused kept distinct from HTTP 429; pMAP 15 shown as the per-lane qualified batch (target 60 named), not a global cap; forensic hold intact. Owner-gated remainders unchanged (production pMAP enable, Groq forensic probe + cinema reconciliation, Phase B DAG cutover). |
| 11.188 | CHAT-RETRIEVAL-RUNTIME-AUDIT — verification only (no code change), owner question "is the chat query mapped how I planned?". **Finding (runtime truth):** the `FINAL-RETRIEVAL-ROUTING-SYNTHESIS-V1` INTENT×FIELD×TECHNIQUE×BUDGET routing is BUILT (P2–P8b) but **flag-gated OFF** — `intent_policy_enabled()` reads `POLYMATH_CHAT_INTENT_POLICY` (default off, `chat_retrieval.py:141`), which is UNSET in `.env`, fleet/run scripts, `settings.py`, and the running orchestrator process env. Gate `ui.py:2597` `_ip = _ip_on() and _plan.intent` → False ⇒ `default_budget()`, `graph_assist="off"`, no roles. Live `/chat/stream` receipt: `intent=SYNTHESIS, graph_useful=True` yet `mode HYBRID · counts {} · additive-lane counts NONE · evidence_roles None` — intent CLASSIFIED, not ROUTED. So live chat = baseline HYBRID (dense+sparse+document-profile → cross-encoder → synthesis). Endpoint split confirmed: `/chat`+`/chat/stream` = chat-retrieval-v2 (policy present-but-off); `/retrieve`+`/ask` = hybrid-retrieval-v1 (no policy). This RECONCILES the plan's own ledger (P2–P8b "DONE default-off"; uplift D-10/D-11 GATED on parent-MAP coverage, cinema behind the forensic hold). The op-UI `⌖ intent` badge (11.187) shows the classification honestly — it is inert for routing. Work-log `2026-09-09-chat-retrieval-runtime-audit.md`. | DONE (verification) 2026-09-09 — runtime truth recorded; no default changed, no spend. **Next: read-only A/B** (`POLYMATH_CHAT_INTENT_POLICY` off vs on, throwaway probe on rag-canary) before any owner-gated default flip. `FINAL-RETRIEVAL-ROUTING-SYNTHESIS-V1` = ACTIVE query-time AUTHORITY; `RETRIEVAL-MIGRATION-DEPENDENCY-V1` = MIGRATION AUTHORITY (retirement gate). |
| 11.189 | U1-INTENT-ROUTING-AB — the read-only qualification of `POLYMATH_CHAT_INTENT_POLICY` (UNFINISHED_WORK U-1), owner directive 2026-09-09. **Evidence collection, not cutover; no code/config/default changed; zero external provider spend; baseline fleet untouched; forensic hold intact.** Two proofs on `rag-canary` (probe corpus, `query_ready=true`). **Proof A** (retrieval primitive, reuses `scripts/production_routing_qualify.py` methodology — same query/corpus/engine, `default_budget()` vs `apply_intent_policy(classify_intent(q), default_budget())` → `chat_retrieve_v2`, isolated by EXPLICIT budget; ephemeral rag-canary queries, frozen L/B fixtures NOT touched): additive lanes are **0 in every OFF arm**; ON activates ONLY the lanes each intent permits — EXACT→dualread+resolution_lift; EXPLORATORY→+seealso_fanout(24)+latent_rescue(8); RELATIONSHIP→+seealso_fanout(24)+graph_dest(8)+latent_rescue(3); union additive (grows/holds), selected=15 constant, ZQX fact chunk preserved, `regression_any=False`; roles stayed all-DIRECT. **Proof B** (full `/chat` runtime, a throwaway `uvicorn orchestrator.main:app` on `:7299` differing from the running baseline `:7200` by EXACTLY `POLYMATH_CHAT_INTENT_POLICY=1` — `POLYMATH_AUTOPILOT=0`, pMAP-mint unset; `synthesizer=deterministic-template-v3` = no LLM; SEQUENTIAL, one shared reranker/GPU): OFF = base lanes only, additive 0, graph_facts 0; ON = `dualread 16 · resolution_lift 6 · seealso_fanout 24 · graph_dest 8 · latent_rescue 4 · graph-assist 7 facts`, arrivals gain SHADOW_DUALREAD/SEEALSO_FANOUT/GRAPH_DEST, funnel 57→63, selected=15, `degraded=[]`. **Finding:** intent routing is PROVEN mechanically + at the live runtime + non-regressive, but **candidate activation ≠ final-evidence uplift** — additive candidates never survive rerank into the final 15 with PRECISION/RELATIONAL/LATENT roles on this homogeneous 10-doc corpus. Architecture OBSERVED (not changed): `synthesis_role` assigned AFTER selection (`chat_retrieval.py:447`); composer reserves relevance/diversity/sparse/aspect/fill with NO role-reserved seat (`candidate_engine.py:1131`) → the cross-encoder stays sole selection authority. Contract test `tests/determinism/test_u1_intent_routing_contract.py` (23 green, provider-free; pins OFF-inert / ON-only-permitted / base-preserved / additive-union / no-role-reserved-slot; encodes no rag-canary ids or live counts). U-1 result: IMPLEMENTATION PROVEN · LIVE WIRING PROVEN · CANDIDATE CONTRIBUTION PROVEN · NON-REGRESSION PROVEN · FINAL-EVIDENCE UPLIFT NOT PROVEN · PRODUCTION DEFAULT OFF/GATED. **The master cutover plan `PRODUCTION-RAG-MIGRATION-CUTOVER-V1.md` is NOT YET MATERIALIZED** — U-1 proves one migration uncertainty, not the full production dependency archaeology; it must NOT be authored/marked authoritative until a dedicated planning slice produces it through exhaustive bidirectional dependency closure. Evidence `docs/wiki/experiments/u1-intent-routing-ab-2026-09-09/`; work-log `2026-09-09-u1-intent-routing-ab-closeout.md`; U-1 also recorded in the `RETRIEVAL-MIGRATION-DEPENDENCY-V1.md` execution ledger. | COMPLETE / QUALIFIED 2026-09-09 — U-1 done. `LIVE DEFAULT POLYMATH_CHAT_INTENT_POLICY=OFF`; `PRODUCTION ENABLEMENT=GATED`. **Next slice = the MASTER PRODUCTION MIGRATION PLANNING** (bidirectional archaeology → the cutover plan + closure gate), NOT U-2 execution: U-2 (Groq Parent-MAP forensic closure) is a known dependency but the archaeology may surface an earlier prerequisite. Do NOT resume cinema pMAP backfill merely to obtain the uplift test; the forensic hold (11.184/11.185) stands. |
| 11.190 | PRODUCTION-RAG-MIGRATION-CUTOVER-V1 — the cutover/retirement execution authority around the ALREADY-BUILT final retrieval engine, materialized via bidirectional archaeology against HEAD `bbe956d` (owner /goal 2026-09-09). **Planning + archaeology only — no retrieval architecture change, no ranking/composition change, no default flip, no producer stop, no retirement.** Consolidates the query-time authority (`FINAL-RETRIEVAL-ROUTING-SYNTHESIS-V1`) + the retirement S-slice ledger (`RETRIEVAL-MIGRATION-DEPENDENCY-V1`) into one dependency-ordered plan with an endpoint→engine map, a per-component disposition matrix (KEEP/MIGRATE READER/STOP WRITER/RETIRE/DELETE-LATER, each FILE:SYMBOL + gate), a feature-flag inventory, and a dependency-closure gate (each non-zero category carries a named blocker). **Key archaeology findings (FILE:SYMBOL-verified):** (1) the final engine is ALREADY the default — `chat_retrieval_flag()` defaults `v2` (`chat_retrieval.py:107`), `/chat`+`/chat/stream` run `chat_retrieve_v2` for all modes (`ui.py:2522`); (2) the final engine is legacy-state-free — `candidate_engine`/`chat_retrieval`/`pass1`/`compiler_context` read NONE of parent_enrichment/retrieval_summaries/parent_summaries/summary_jobs/document_summaries; (3) `routing_*` summary lanes are FINAL (KEEP), not legacy; (4) still-live legacy producers = `auto_enrich_on_chunks` (`scheduler.py:235`, re-mints parent_enrichment) + the summary worker; (5) `/retrieve`,`/ask`,evidence,graph,wildcard,MCP-retrieve = v1 KEPT lower-level/raw-evidence contracts, not Chat bypasses to migrate; (6) final defaults v2/vNext/GROQ_ROUTER already ON, INTENT_POLICY + SYNTH_ROLES OFF. **Master blocker:** every cutover step past "readers use the final engine" is transitively gated on U-2 (cinema forensic hold → coverage → S14 cutover → S16 reader-migration / S15+S17 producer-stop / D-14 retirement); INTENT_POLICY default flip needs U-1 uplift → also U-2. No safe non-owner-gated retirement/producer/reader-migration step exists at HEAD. Work-log `2026-09-09-production-cutover-plan.md`. | COMPLETE (planning slice) 2026-09-09 — cutover authority materialized; phases 0–1 DONE + SAFE (archaeology + confirmation the final engine is the default legacy-free production runtime). **Next action = OWNER GATE:** U-2 bounded Groq forensic probe (spend authorization); everything downstream blocks on it. Do NOT resume cinema pMAP backfill to obtain coverage. |
| 11.191 | RETRIEVE-ENGINE-MIGRATION-V1 — MIGRATE LEGACY READER (owner correction 2026-09-10: reader migration ≠ delete state; the former is U-2-INDEPENDENT). `/retrieve` single-corpus HYBRID now rides the FINAL `chat_retrieve_mode` core by default, behind `POLYMATH_RETRIEVE_ENGINE` (v1|v2, default v2; `v1` = rollback = legacy `hybrid_fast_retrieve`). Response contract byte-shape-identical — **parity A/B (read-only, local, no spend)** proved identical top-level keys (evidence/meta/query/selected_documents/selected_sections/trace) for v1 vs final on `rag-canary` (covered) + `ecom-meta-v1` + `cinema` (legacy, query_ready), v2 non-empty + no error ⇒ shape-safe AND **coverage-independent** (final base lanes are standard projections, additive lanes flag-off; runs coverage-free). v2 returns 15 rows vs v1's 10 (richer default, not a regression). MCP `retrieve` (default mode HYBRID) inherits the migration. **Unchanged (not migrated):** FAST (multi-corpus — final core is single-corpus), GRAPH + WILDCARD (v1 `graph_retrieve` returns a NESTED documents/sections shape ≠ final flat shape → PENDING PARITY), the default/LEGACY `retrieval_summaries` path (DELETE STATE — U-2/cutover-gated), `/ask` (distinct composite). No legacy state deleted, no producer stopped, no provider spend; open runs=0 at edit (fence-safe); running orchestrator picks up the default on next restart. Contract guard `tests/determinism/test_retrieve_engine_migration.py` (5 green); evidence `docs/wiki/experiments/retrieve-engine-migration-2026-09-10/`; work-log `2026-09-10-retrieve-engine-migration.md`. | DONE 2026-09-10 — first reader migrated onto the one retrieval core, reversibly, independent of U-2. Follow-ups: `/retrieve` GRAPH/WILDCARD parity, `evidence.py` HYBRID (same pattern), then `/ask`; DELETE of the v1 path + legacy summaries stays U-2/cutover-gated. |
| 11.192 | GROQ-MAP-FORENSIC-PROBE-V1 — U-2 bounded Groq Parent-MAP forensic probe (owner-authorized 2026-09-10, under the existing forensic limits). New instrument `scripts/groq_map_forensic_probe.py`: CINEMA-FREE (synthetic parents, NO corpus/DB/map write), production `complete_one` path with `max_attempts=1` (no retry loop), reads the 11.185 limiter `state()` before/after to observe the conservation chain, `compile_maps` for yield. 17 `groq/compound-mini` requests total (~3 RPD/account). **Findings:** (1) provider RPD OBSERVABLE + NOT exhausted — all 6 accounts ~246–248 remaining of ~250 (`x-ratelimit-remaining-requests` on success; config "RPD 250" now MEASURED); the disputed "accounts exhausted RPD" inference (11.178) is CONTRADICTED; (2) local↔provider RPD reconcile — `day_count` +1 per dispatch, dispatched==requests, RPD tracks dispatch, 0 LIMITER_REFUSED in-probe; with 11.185's census (926 historical LIMITER_REFUSED = 0 HTTP) the `+0/0-errors` pass was a LOCAL refusal cascade, not exhaustion; (3) batch 15/20/30/40/60 = yield 1.0 on SYNTHETIC parents (best case; real-parent 20–60 reliability — the 11.178 concern — still needs a real-parent benchmark, so cap 15 stands); (4) no retry waste. **Acceptance gate substantially MET** (only the real-parent batch benchmark remains). **Cinema-finish estimate (owner review):** ~10,739 unresolved → ~716 req at cap-15 (or ~179 at batch 60) vs ~1,500 req/day capacity ⇒ < 1 day. Respects the forensic hold (cinema untouched). Evidence `docs/wiki/experiments/u2-groq-map-forensic-probe-2026-09-10/`; audit result appended to `docs/wiki/reports/2026-09-08/GROQ-FORENSIC-AUDIT.md`; work-log `2026-09-10-u2-groq-map-forensic-probe.md`. | DONE (bounded probe) 2026-09-10 — forensic hold's premise (RPD exhaustion) DISPROVEN; accounting reconciles. **OWNER-REVIEW GATE:** resuming cinema pMAP (bounded canary → full backfill) awaits owner review of these findings + estimate; do NOT resume on a quota reset. Remaining audit item = a real-parent (non-cinema) batch benchmark before raising MAP_RELIABILITY_CAP. |
| 11.193 | PROVIDER-LANE-REASSIGNMENT-V1 — owner-directed API-env reconfiguration 2026-09-10 (config only; no code/retrieval-architecture change; INERT until `boot_polymath.sh`; running fleet unchanged; cinema forensic hold respected). New provider→function topology in `config/cloud_providers.json` (stage_pins + providers) + `config/extraction_models/limiter.yaml` (seeds): **graph extraction** = ALL Google (`gemini1–6`+`gemini1b–6b`) + `nvidia`,`nvidia2` + `siliconflow1–3` (unpinned ring); **doc_profile** = `profile_groq1` (GROQ_KEY_1, `groq/compound`) + OpenRouter `mistral-small-2603` (KEY_2) fallback; **pMAP** = `map_groq2–6` (GROQ_KEY_2–6, `compound-mini`) + NEW `map_fallback_openrouter` (`mistral-small-2603`, KEY_3, MAP DSL) — **de-shares the 6 Groq accounts** (KEY_1=doc_profile only, KEY_2–6=pMAP only; was shared via `groq_acct_N`); **chat compiler** = a chat-model fallback ring `compiler_alibaba_qwen` (Alibaba token-plan compatible-mode `qwen3.8-flash`) → `compiler_alibaba_deepseek` (`deepseek-v4-flash-0731`) → `compiler_ollama_gemma` (local Ollama `gemma4:31b-cloud`) → `compiler_alt` (OpenRouter backstop), freeing all Google; **parent_enrichment** (legacy) = openrouter only. Disabled the superseded duplicate lanes (`compiler1–4`, `profile_groq2–6`, `profile_fallback_gemini1/2`, `map_groq1`). **Endpoints pre-verified through the real `LLMExtractionClient`** (Alibaba qwen/deepseek clean JSON; Ollama gemma JSON; **OpenCode EXCLUDED — Cloudflare 1010 blocks the raw client**, usable only via the synthesizer's litellm). Validation: all 4 pins resolve to ACTIVE endpoints (0 dark), graph ring correct, `bundle_integrity` READY (config not in the 8-file semantic bundle), `repo_guard` ok. Local gitignored `.env` `OLLAMA_API_KEY` dummy (daemon ignores it; needed so the no-auth lane counts as active). Work-log `2026-09-10-provider-lane-reassignment.md`. | DONE (config, validated) 2026-09-10 — takes effect on the next fleet bounce (owner runs `boot_polymath.sh`). Caveats: doc_profile on 1 Groq key ≈ 1–2 profiles/min + no Groq redundancy (OR fallback covers outage, not throughput); the pMAP OpenRouter fallback's actual MAP-DSL yield is unvalidated (last-resort lane); Ollama compiler lane needs the local daemon up (ring fails over otherwise). |
| 11.194 | ORPHANED-DEDICATED-LANES-V1 — config fix found by the post-bounce verification of 11.193 (owner-authorized fleet bounce 2026-09-10). **Defect:** after the lane reassignment, five enabled + credentialed + ACTIVE lanes (`gemini5`, `gemini5b`, `gemini6`, `gemini6b`, `nvidia`) carried `"dedicated": true` while belonging to NO `stage_pins` group. Per DEDICATED-V1 (`llm_extraction/pool.py:68-70`,`:293-294`) a dedicated endpoint serves ONLY its pinned stages and never joins the general extraction sharding — so those five lanes served **zero stages**: dead capacity, not a display artifact (the same flag drives `select_endpoint_for_stage`). They had been dedicated to the `profile_fallback_gemini1/2` lanes that 11.193 disabled; the flag was not cleared with them. Live proof: `/control_plane/pool/GRAPH_EXTRACTION` returned 8 Google lanes, not the 12 that 11.193 declared. **Why 11.193's validation missed it:** it checked that all four PINS resolve to active endpoints (0 dark) — an un-pinned dedicated lane is invisible to a pin-side check, and `unreachable_pins()` deliberately skips `dedicated_unpinned` (`lane_registry.py:322`). **Fix:** `"dedicated": false` on the five lanes. **Proof (offline, provider-free):** `build_registry()` → `GRAPH_EXTRACTION` = 18 lanes (`gemini1–6`, `gemini1b–6b`, `nvidia`, `nvidia2`, `primary`, `siliconflow1–3`); enabled+active lanes resolving to `dedicated_unpinned` = NONE; `unreachable_pins()` = NONE; the remaining 12 `dedicated_unpinned` entries are all `enabled=false` (superseded `compiler1–4`, `profile_groq2–6`, `profile_fallback_gemini1/2`, `map_groq1`). No provider/key/model added — owner's 'convergence, not topology' (2026-09-10) respected. Work-log `2026-09-10-orphaned-dedicated-lanes.md`. | DONE (config, proven) 2026-09-10 — restores the ring 11.193 already declared. **Inert until the next `boot_polymath.sh`.** Open gaps: graph throughput NOT measured (no ingestion running — any future third-lane decision needs a measured ring number, not a lane count); no guard yet prevents a lane being `dedicated` + pinned to nothing. |
| 11.195 | FRONTEND-V2-CONTRACT-INVENTORY-V1 (F0) — inventory of the backend contracts Frontend V2 consumes, measured against the LIVE orchestrator (39 routes, bundle `f0db5412e473820e`), owner directive 2026-09-10 ("the application itself is now the priority", F0–F12). **Inventory only — no endpoint added, changed or deprecated; no provider spend** (the `/chat/stream` exercise used `deterministic-template-v3`). **Readiness triad mapped to authorities:** CONTROL = `/health/pipeline` + `/control_plane` + `/fleet` + `/ready`; SEMANTIC = `/semantic_readiness?corpus_id=` `verdict` (`semantic-readiness-v1`); VNEXT = the same call's `vnext{verdict,parents{eligible,mapped,excluded,unresolved},pending[]}` + per-document `/documents/summary` `vnext_ready` and `/documents/{id}/status?detail=true` blockers. **The legacy boolean is disproven with data:** `cinema` returns `query_ready: true` while SEMANTIC/VNEXT are INCOMPLETE with 10,176 unresolved parents and **14 of 67** documents `vnext_ready` — `rag-canary` returns COMPLETE/COMPLETE, 10/10, 0 unresolved. Owner capabilities 1–6 and 9 are SERVED (notably `answer.retrieval.arrivals` = per-chunk lane provenance + `lane_sizes` funnel + `graph_fact_count`/`graph_seeds`/`graph_bounds`, so Profile/Atom/pMAP routing and graph contribution are inspectable today). **Five GAPS recorded rather than papered over:** GAP-1 no single CONTROL-READY verdict (client composition = recomputation, forbidden by 11.187's design law); GAP-2 no retrieval-comparison contract (N `/chat/stream` calls share no compiled plan or seed ⇒ the diff confounds mode with variance, N synthesis spends) ⇒ F6 blocked; GAP-3 no answer-REVIEW contract (`synthesizer` re-answers; nothing reviews answer A against the evidence A cited) ⇒ F7 blocked; GAP-4 `control_plane.summary.processing` has no age qualifier — cinema shows `processing: 64` for runs last touched 2026-09-07, so V2 would paint dormant runs as live; GAP-5 `/documents/summary` unpaginated (67 docs ≈600 ms today). Work-log `2026-09-10-frontend-v2-f0-contract-inventory.md`. | **F0 COMPLETE 2026-09-10.** **F1 BLOCKED — the approved greenfield design document does not exist** in `docs/wiki/plans/`, `docs/wiki/reports/` or `~/Downloads` (searched); the owner's F0–F12 + capability list settles F0 but not F1's information architecture / navigation / design system. GAP-1…GAP-5 are unstarted backend work. Reference corpus = `rag-canary` (only VNEXT_COMPLETE corpus); `cinema` is the honesty fixture (53 blocked documents must render blocked, never green). Existing `frontend/` NOT modified. |
| 11.196 | U2-FORENSIC-CLOSURE-AUDIT-V1 — owner directive 2026-09-10: clear the cinema forensic hold ONLY if the DURABLE evidence closes all seven accounting questions ("do not clear merely because ~247/250 looks plausible"). **Read-only audit of durable state; ZERO provider requests; cinema untouched; no historical row rewritten.** **VERDICT = `U-2 FORENSIC HOLD: NOT CLEARED`.** **Criterion 6 (maps returned vs persisted) was CLOSED HERE, not by the probe** — the probe wrote nothing to the DB by design, so it could never have answered it. Parent-level: **2,312 maps returned → 2,312 with a persisted ACTIVE row, 0 batches short**; the naive batch-level Δ735 is batch RE-IDENTITY (`grounding_hash` rebinds `batch_id` on re-run) against dedupe on `(doc_id,parent_id,map_contract)` — proven by active rows 1,577 == distinct triples 1,577. **Criterion 5 is stronger than assumed: REAL-parent yield = 1.000** across all 134 `done` batches (2,212 expected → 2,212 returned, 16.5/batch); the 937 `partial` batches' 0.005 is the LOCAL refusal cascade (14,617 attempts, 0 HTTP), not model failure. **Criteria 1 + 7 FAIL on a bounded, quantified basis (D-1):** of 926 terminally-`LIMITER_REFUSED` batches, **3 carry a `raw_response_hash` with `valid_count=0`** — real provider consumption booked as a LOCAL refusal, breaking the `LIMITER_REFUSED ⇒ zero consumption` invariant that 11.187's control plane depends on; one hash is `e3b0c44298fc1c14…` = SHA-256 of the EMPTY STRING (empty completion body). All three are `expected_count=60` on REAL parents (2026-09-08) — independently corroborating the 11.178 partial-map concern and the decision to keep `MAP_RELIABILITY_CAP` at 15. Magnitude is 3 of 1,077 batches (~3 requests vs ~1,500/day capacity): it moves NEITHER the RPD conclusion NOR the cinema-finish estimate, but the owner set completeness (not plausibility) as the bar, so waiving it is an OWNER decision, not the agent's. **Second finding (D-2): 6 batches frozen on leases that expired 2026-09-09 04:54Z** (`doc-parent-map-worker-v1`), holding 90 parents (5×HTTP_429, 1×LIMITER_REFUSED) — real owed work no resumption will pick up until the leases are reaped BY PINNED BATCH ID (never a status sweep). Also recorded: C3 weak spot — `provider_rpd_remaining_before` is null in every probe row, so the per-call RPD delta is inferred from call order, never observed on both sides. Evidence `docs/wiki/experiments/u2-forensic-closure-audit-2026-09-10/` (README + `closure-audit.json`); work-log `2026-09-10-u2-forensic-closure-audit.md`. | **HOLD REMAINS 2026-09-10.** The hold's original PREMISE (six Groq accounts RPD-exhausted) stays DISPROVEN — nothing here weakens 11.192. **No bounded cinema canary was run**: the owner gated the canary on the hold clearing first. Path to closure (owner picks): (1) fix D-1 forward — classify terminal state by whether HTTP was dispatched so a dispatched-but-empty completion books as `EMPTY_COMPLETION`, not `LIMITER_REFUSED` (worker code ⇒ stale-bundle fence ⇒ bounce; fleet idle, window cheap); (2) reap D-2's six expired leases by pinned batch id; (3) never rewrite the 3 historical rows (forensic evidence); (4) re-run this audit expecting 0 violations, OR clear the hold explicitly accepting D-1 as a known bounded exception. |
| 11.197 | DORMANT-BACKLOG-CLASSIFICATION-V1 — owner standing order 2026-09-10 ("classify by generation/function/corpus FIRST; no blind requeue, no blind cancellation"). **Read-only; NOTHING requeued, NOTHING cancelled.** **70 open runs:** cinema 63 `reconciling` = **HELD** (real documents, blocked behind the cinema forensic hold, not stalled workers) + 1 `intake` **ORPHANED** (no document, attempt 3 exhausted); d7-h1-test 6 = 3 HELD + 3 **ORPHANED**. **NONE is SUPERSEDED** — tested by joining each open run's `source_name` against `query_ready` runs in the same corpus: `also_query_ready = 0` for both corpora, so these are the ONLY runs for their documents and cancelling one abandons a document. **253 pending tickets split decisively: 107 (42%) are owed to a RETIRING subsystem** — `corpus_summary` 41 + `document_summary` 39 + `parent_summary` 27, which `PRODUCTION-RAG-MIGRATION-CUTOVER-V1` §68 marks STOP WRITER → RETIRE (S15→S17); **a blanket requeue would spend provider quota producing state the cutover schedules for deletion.** `vocabulary` (41) is **NOT** legacy — it is read by the FINAL core (`candidate_engine.py`, `chat_retrieval.py`, `chat_plan.py`; Resolution Lift's corpus-vocabulary precision input) ⇒ VALID OWED; the remaining 105 (`verify_projections` 27, `compile_objects` 27, `project_canonical` 21, `project_neo4j` 9, `canonicalize` 9, `profile_document` 4, `project_qdrant` 4, `extract` 4) are final-path VALID OWED. **9 failed tickets:** 3 d7-h1-test `intake` = **ORPHANED, unrepairable by design** (`LEGACY_EVENT_UNRECOVERABLE intake.v1: missing ['source_name'] after adapter recovery` — a retry re-fails deterministically; same 3 runs that have no document); 1 cinema `intake` ORPHANED (attempts exhausted); 5 cinema `project_qdrant` = **VALID OWED and the cheapest real repair in the backlog** (local projection, zero provider spend). `archived_at`/`archived_reason` are NULL on all 9 — the archive path exists and is unused. Feeds GAP-4 (the control plane counts these 70 runs with no age qualifier ⇒ cinema renders `processing: 64` for runs last touched 2026-09-07). Work-log `2026-09-10-dormant-backlog-classification.md`. | CLASSIFICATION COMPLETE 2026-09-10 — **no mutation**. Owner-gated dispositions, all pinned by ticket id (never a status sweep): (1) leave the 107 legacy-summary tickets until S15 STOP WRITER, then cancel them AS retirement, not requeue; (2) the 5 `project_qdrant` failures are the safe first repair; (3) the 41 vocabulary + 105 tail tickets unblock WITH cinema, not before; (4) archive the 4 orphaned intakes with a reason rather than retrying. Open gap: the cinema `intake` failure receipt was NOT read — that it is the 11.121 near-duplicate refusal is a hypothesis (the source name is the Sound Design twin), not evidence. |
| 11.198 | LANE-CONTRACT-TESTS-REALIGNED-V1 — owner directive 2026-09-10 workstream A: inventory every provider lane, gate `enabled + dedicated + unreachable = 0`, run config/limiter tests, verify live. **No provider, key, model, pin or flag changed; no restart required.** **GATE MET = 0.** Full 46-lane inventory (lane · provider · account · model · enabled · dedicated · function · stage pin · fallback tier · reachable): the 12 remaining `dedicated_unpinned` lanes are ALL `enabled=false`/`disabled` — exactly the lanes 11.193 superseded (`compiler1–4`, `map_groq1`, `profile_groq2–6`, `profile_fallback_gemini1/2`); **none was disabled by this slice to satisfy the gate**. `unreachable_pins()` NONE. Credential audit: every `dedicated_unpinned` account-sharing pairs only with DISABLED lanes (no contention); the sole real cross-function sharing is 3 OpenRouter keys between a permanent function's LAST-RESORT fallback and legacy `parent_enrichment`. **`parent_enrichment` is NOT a permanent function** (legacy stage pin only) — permanent functions are exactly GRAPH_EXTRACTION 18 · DOCUMENT_PROFILE 2 · PMAP 6 · CHAT 4, matching the live `/control_plane/pool/*` allocation exactly. **Attribution measured, not asserted** (two throwaway worktrees + `-p no:randomly`, since pytest-randomly made the failure set look unstable): 6 of these tests already failed at `3c36c4e` (pre-reassignment); **`5adb0f5` (11.193) introduced exactly 2** (`test_config_has_map_lanes_sharing_the_profile_keys`, `test_profile_pool_is_pinned_and_isolated_in_config`) — i.e. the lane reassignment SHIPPED WITH 2 RED TESTS IT NEVER RAN — and **`820ceeb` (11.194) introduced ZERO**. Realigned 8 assertions across 4 files to the owner-approved allocation: de-shared Groq keys (KEY_1 = doc_profile only, KEY_2–6 = pMAP only), doc_profile pin = `profile_groq1` + `profile_fallback_openrouter`, pMAP pin = `map_groq2–6` + `map_fallback_openrouter`, compound-mini lanes 6→5, batch cap over the 5 capped Groq lanes with the uncapped OpenRouter last-resort asserted explicitly. Assertions were rewritten to the NEW contract, never weakened — the isolation test is STRONGER than the sharing test it replaces (adds a general invariant: no enabled lane pair may share an account across two permanent functions). AGENTS.md §2.8 (tests immutable) considered and recorded as not applying: §2.8 stops an agent bending a test to hide an implementation failure; here the implementation is owner-directed and correct and the owner asked for these tests to be run. Limiter + runtime-config + control-plane-v2: **38 passed**. Live: 13 workers / ONE bundle `f0db5412e473820e` / 0 quarantined / `/ready` true / `/retrieve` = `chat-retrieval-v2` 15 rows / `/chat` = `chat-retrieval-v2`. Work-log `2026-09-10-lane-contract-tests-realigned.md`. | DONE 2026-09-10 — gate 0, live == declared. **One PRE-EXISTING unrelated failure left RED deliberately** so the attribution baseline stays honest: `test_document_profile_stage::test_worker_writes_the_profile_...` asserts `TITLE:` while the context builder now emits `IDENTITY:… · format: markdown` (DOCUMENT-GROUNDING-CONTEXT-V1 drift; fails at `3c36c4e` too) — needs its own slice. Open gap: no guard yet asserts the gate itself (a lane can still be `dedicated` + pinned to nothing without any check failing). |
| 11.199 | FRONTEND-V2-PLAN + F1 — owner directive 2026-09-10 workstream B ("the design is approved; it just is not on disk"). **`docs/wiki/plans/FRONTEND-V2-PLAN.md` persisted as an ACTIVE authority** (scope: greenfield replacement · backend_architecture: FROZEN · old_frontend: LEGACY/rollback reference), capturing navigation (Chat/Files/Graph │ Control Plane/Settings), Chat controls (corpus · Hybrid/Graph/Wildcard · intent · model · reasoning · streaming, **VECTOR not public**), the Query Inspector's FIVE distinct lane states (ENABLED/FIRED/ENTERED UNION/SURVIVED RERANK/USED IN ANSWER — "a lane that FIRED did not thereby affect the answer"), the Evidence Inspector rule (**Profiles/Atoms/pMAP explain WHY A SOURCE WAS FOUND; they are routing artifacts, never factual evidence**), Compare Retrieval, evaluation-only Answer Review, the Files readiness TRIAD, source-attested-only Graph, the function-first Control Plane, phases F0–F12, and the qualification corpora (`rag-canary` green path, `cinema` honesty fixture). **F1 SHIPPED:** new `frontend-v2/` app (React 19 + TS strict + Vite 6) — typed contract mirrors, ONE backend client with a streaming SSE reader, the readiness module, design system, shell + navigation, Overview screen. `npm run build` (tsc strict + vite) GREEN; **browser-verified live** against the running orchestrator (`SEMANTIC_COMPLETE` / `VNEXT_COMPLETE` / 50-of-50 parents / real semantic counts for `rag-canary`; navigation routes). The legacy `frontend/` is UNTOUCHED and shares no code. **GAP-6 DISCOVERED BY F1:** `/health/pipeline` reports `state: DEGRADED`, `stalls_open: 282` on a COMPLETELY IDLE fleet (`queued_tickets: 0`, `blocked_workers: 0`, `live_workers: 13`, `medic_actions_15m: []`); the 282 decompose exactly into the dormant backlog (`PENDING_ON_PREDECESSOR×221` + `PENDING_ADVANCE_BLOCKED×32` + `RUN_SETTLED_NOT_PROMOTED×29`) ⇒ **CONTROL READY cannot read green until the backlog is dispositioned** — the primary health verdict is pinned by history, not by current activity (same class as GAP-4). Also proven in passing: a missing dev-proxy entry made `/ready` 404 and the triad rendered UNKNOWN rather than defaulting green — the designed honesty behaviour. `scripts/repo_guard.py`: `frontend-v2/dist/` added to IGNORED_PREFIXES (vite build output, git-ignored + content-hashed — the same rationale as the existing `node_modules` entry; the V2 SOURCE is declared in the scaffold TREE). Work-log `2026-09-10-frontend-v2-plan-and-f1.md`. | **F1 DONE 2026-09-10.** Next F2 (Chat) → F3 → F4 → F5. **F6 BLOCKED on GAP-2** (no compare contract: N calls share no compiled plan or seed ⇒ the diff confounds mode with variance) and **F7 BLOCKED on GAP-3** (no answer-review contract). GAP-1/GAP-4/GAP-6 are unstarted backend work. No frontend test yet (F11). Policy unchanged: INTENT_POLICY OFF, SYNTH_ROLES OFF, pMAP auto-mint = rag-canary only. |
| 11.200 | U2-PERSISTENCE-CANARY-V1 — owner-authorized 2026-09-10 workstream C: ONE bounded persistence-capable pMAP canary, because 11.192 deliberately persisted nothing and so could not close `provider response → MappingOutcome → compiler → durable pMAP → projection`. New instrument `scripts/u2_persistence_canary.py` (dry-run by default; `--execute` mints EXACTLY ONE ticket for ONE pinned run; **never a backfill**; `POLYMATH_DOC_PARENT_MAP_CORPUS` untouched at `rag-canary`). **Spend: 1 Groq request.** Target = the smallest unmapped cinema document `doc_6a5301e0043db44f…` (5 eligible parents, one batch), whose before-state was the historical failure in miniature: 2 stale `partial` batches, `valid_count=0`, `raw_response_hash NULL`, 33 local attempts ⇒ NEVER DISPATCHED. **THE INVARIANT RECONCILES:** requested 5 = eligible 5 (excluded 0) → **1 HTTP dispatch** → `compiler_complete 1` with partial/invalid/empty/429/failures/limiter_refusals ALL 0 → 5 compiled → **5 PERSISTED** (5 new map_ids) → **5 PROJECTED** (Qdrant count) → `parents_mapped 5`, `unresolved 0`, 1 attempt, 0 retry waste. **No unexplained parents, no unexplained requests, no silent loss** — the persistence question this canary was authorized to answer is ANSWERED AFFIRMATIVELY. **BUT TWO DEFECTS. D-3 (NEW, material):** the worker then raised `DOC_PARENT_MAP_INCOMPLETE: unresolved=0 partial=2 refusals=0 http_fail=0` and the ticket RE-ARMED — `outcome.complete` is False because `batches_partial=2` counts the two STALE NEVER-DISPATCHED batch rows, while `unresolved=0` proves every eligible parent IS mapped; `_is_transient_incomplete()` is False so it is a hard RuntimeError, not a transient hold (`doc_parent_map_stage_worker.py:256-261`, `doc_parent_map_worker.py:406-408`). **A successful document reports FAILURE** — at cinema scale a resumed backfill would report failures on documents it actually completed, a strong candidate explanation for the historical `+0 parents / 0 errored_docs` confusion this hold began with. Blast radius measured ZERO (watched 90 s: no re-dispatch; the autopilot had re-parked the slot). **D-4:** `llm_controller_state` has NO row for ANY pMAP lane (`map_groq2…6`, `map_fallback_openrouter`) before or after — the dispatch incremented no DURABLE `day_count`; 11.192's "+1 per dispatch" was IN-PROCESS only. No request is lost (batch row + artifact account for it) but the limiter is not a usable local RPD ledger for pMAP. Cleanup: the canary's OWN ticket closed BY PINNED TICKET ID (never a sweep) with `archived_reason` recording D-3 and `last_error_note` preserved; 0 ready/leased tickets anywhere; the 5 maps + 5 projections KEPT (real verified work). Evidence `docs/wiki/experiments/u2-persistence-canary-2026-09-10/`; work-log `2026-09-10-u2-persistence-canary.md`. | **`U-2 FORENSIC HOLD: ACTIVE (NOT CLEARED)` 2026-09-10.** **THE EXACT BROKEN TRANSITION:** `MappingOutcome.complete` (and therefore the ticket outcome and stage receipt) is computed from STALE BATCH ROWS rather than from UNRESOLVED ELIGIBLE PARENTS — the stage's own verdict contradicts its verified result. Cinema work STOPPED and reported, per the owner's non-reconciliation instruction. Path to clearing: (1) fix D-3 — make completeness a function of `unresolved_parent_ids` (already computed and correct) or exclude `raw_response_hash IS NULL` rows from the partial tally [worker code ⇒ stale-bundle fence ⇒ one bounce, fleet idle so the window is cheap]; (2) fix D-1 forward + reap D-2's 6 leases by pinned batch id (11.196); (3) persist pMAP limiter counters (D-4); (4) re-run this canary on a second unmapped cinema document expecting all six checks PASS; (5) only THEN the bounded cinema resumption sequence. |
| 11.201 | FRONTEND-V2 F2/F3 + the F5 lane-inspector core — owner directive 2026-09-10 workstream B. Chat with corpus · Hybrid/Graph/Wildcard · intent · model · reasoning · streaming, plus the Query Inspector's five lane states. **Frontend only; no backend contract changed; no Python policy reimplemented in TypeScript.** `npm run build` green. **Browser-verified on a REAL turn** (`rag-canary`, "what is the ZQX fact", HYBRID, backend-default model): phases streamed visibly (`scope › scope_ok › compile › retrieve › retrieve_done › assemble › assemble_done › generate …`), grounded answer with `[S1]…[S11]` citations, badges `HYBRID · ⌖ EXACT · chat-retrieval-v2`, 29.3 s. **The inspector earned its place on the first turn:** `global_dense_child` 50→50→15→15, `hierarchical` 24→24→13→13, `global_sparse_child` 40→40→12→12, but **`section_summary` FIRED 24 → survived 0 → used 0** and **`entity_card` FIRED 8 → survived 0 → used 0** — 32 candidates from two lanes reaching the answer in ZERO rows, i.e. the plan's "a lane that FIRED did not thereby affect the answer" RENDERED rather than inferred (funnel: retrieved 51 · union 51 · pre_rerank 32 · post_rerank 32 · selected 15 · cited 13 · graph_facts 0). Derivation documented, not guessed: FIRED=`lane_sizes`, ENTERED UNION=`funnel.lane_counts`, SURVIVED=`final_detail[].arrivals`, USED=`legend[].tag` ∩ arrivals; the backend names one lane three ways (`hierarchical_children`/`hierarchical`/`HIERARCHICAL_ROUTE`) so the alias map is written down in `LaneTable.tsx`, and an unreported lane renders `—` (not 0 — unknown and zero are different claims). **VECTOR is NOT offered** (backend primitive, plan §2); **Intent is a DISABLED "Auto (classified)" control** with the reason stated, because no override contract exists and INTENT_POLICY is OFF — a real limitation shown as a limitation, never a fake selector. Rejected mid-build after checking a real receipt: "`arrivals` is the union" — it has exactly 15 entries matching `selected`, so it is the FINAL selection's provenance; getting it backwards would have made every lane look like it survived. Work-log `2026-09-10-frontend-v2-f2-f3-chat.md`. | F2 + F3 DONE 2026-09-10; F5's lane core shipped inside the Chat inspector. Next F4 (evidence inspector — `chunks`/`legend`/`final_detail.rerank_score` are already in the receipt), then F8/F9/F10. **F6 still blocked on GAP-2, F7 on GAP-3.** Open: answer text renders pre-wrap not markdown; Enter-to-send unverified under synthetic key events (Send button is the proven path); no frontend test yet (F11). |
| 11.202 | COMPLETION-TRUTH-AND-RPD-DURABILITY-V1 (D-3 + D-4) — owner directive 2026-09-10. **D-3 at `workers/workers/doc_parent_map_worker.py::MappingOutcome.complete`:** completion authority is now CURRENT DURABLE STATE (`eligible − mapped − excluded == unresolved == 0`) — `return not self.unresolved_parent_ids`. The `batches_partial == 0` term is REMOVED: redundant for a LIVE partial (its parents stay unresolved, so the first clause already returns False) and WRONG for a STALE one. Proven by evaluating BOTH predicates on the canary's exact fixture: OLD → False (raises `DOC_PARENT_MAP_INCOMPLETE`, ticket re-arms), NEW → True; the INCOMPLETE direction unchanged. `batches_partial` stays on the outcome/receipt as a diagnostic. **D-4 in `shared/polymath_shared/llm_extraction/limiter.py` — ROOT CAUSE, not the symptom:** the only controller-store attach site was `workers/workers/llm_provider.py:300`, and the pMAP stage worker uses the SHARED `LLMExtractionClient` directly and NEVER imports `workers.llm_provider`, so that process attached no store, `_on_change` stayed None, and every pMAP dispatch was accounted in memory only — which is exactly why `llm_controller_state` held rows for `gemini*`/`openrouter*` and ZERO for `map_groq*` after thousands of dispatches. Fixed by `LimiterRegistry.ensure_store()` called from `lane()`/`budget()`, so durability is a property of the REGISTRY and every consumer (extract, pMAP, doc_profile, chat compiler) gets it with no per-caller wiring to forget; plus `last_dispatch_at` + a dispatch-dirty flag + `_persist_rpd_if_due()` flushed outside the lock, coalesced at `RPD_PERSIST_MIN_INTERVAL_S=1.0` (`flush_rpd()` forces) so the durable row is a LOWER BOUND on today's dispatches, never an over-count. **No schema change, no second accounting system, no provider/lane/flag change.** **LIVE PROOF — 3 post-fix bounded canaries (2 Groq requests total):** canary 2 re-ran the SAME document that had failed (5 eligible, 5 mapped, same 2 stale rows) → ticket `done`, **ZERO dispatch**, watched 60 s with no re-arm and no re-dispatch; canary 3 (9 parents) → 1 dispatch → 9 compiled → **9 persisted → 9 projected**, `done` at attempt 0; canary 4 (10 parents) → **`reconciles: true`, ALL SIX CHECKS PASS** (10→1→10→10→10). **D-4 acceptance:** `llm_controller_state` now holds `llm_cloud[map_groq5] · day=2026-09-11 · day_count=1 · last_dispatch_at=2026-09-11T04:45:46Z` — the first durable pMAP row ever — and **restart continuity is proven with ZERO spend**: a brand-new `LimiterRegistry` (what a restarted worker gets) restores `day_count` and `last_dispatch_at` from durable state, not the yaml seed. Reconciliation needs no new table: lane = the row key, account/function = the LANE-REGISTRY join the control plane already performs, day/window + dispatch count + rate-limit state + last update = the row; no secret stored. Tests: `test_doc_parent_map_completion_truth.py` (6) + `test_rpd_durability.py` (5), plus limiter/lane/pMAP suites green (26 + 42 + 85). Two fence-safe bounces (0 ready/leased each). Work-log `2026-09-10-d3-d4-completion-truth-and-rpd-durability.md`. | DONE 2026-09-10 — both defects closed and proven live. Open and NOT fixed here: **D-1** (3 historical batches with real consumption booked as `LIMITER_REFUSED` — explained and bounded, but a future dispatched-but-empty batch would be misbooked the same way and corrupt the refused-vs-429 distinction DURING a backfill; one-line classification fix, recommended before a FULL cinema backfill) and **D-2** (6 batches frozen on leases expired 2026-09-09 04:54Z, holding 90 parents). |
| 11.203 | FRONTEND-V2 F4/F5/F8/F9/F10 — owner directive 2026-09-10. **F4 Evidence Inspector:** document · section/parent · child · exact text · rerank score · provenance · citation, with FINAL-selection rows separated from unselected candidates and rows whose provenance is entirely summary/card lanes tagged **ROUTING** (plan §4: Profiles/Atoms/pMAP explain why a source was FOUND; never factual evidence). **F5 complete Query Trace:** requested vs executed mode (drift highlighted) · classified intent (labelled INERT because INTENT_POLICY is OFF) · retrieval version · latency · degradation · lane table · compiler plan. **F8 Files:** the triad plus per-document vNext with the backend's own counts. **F10 Control Plane:** four function cards, `limiter_refused` (LOCAL, 0 HTTP) kept visually DISTINCT from `http_429` (a real spent request), `parent_enrichment` named a legacy stage pin not a fifth pool, ENV NAMES only. **F9 Graph:** real predicate distribution + an explicit statement of what is NOT served. **Live proof:** Files on the honesty fixture `cinema` reads *67 documents, 17 vNext-ready, 50 BLOCKED*, VNEXT INCOMPLETE with 1,473/11,993 mapped and **10,152 unresolved** — and the arithmetic reconciles against this session's canaries (1,449+24=1,473; 10,176−24=10,152); **nothing manufactured green**. Control Plane states *"DEGRADED for a historical reason, not a live one — 282 open stall episodes with 0 queued tickets and 0 blocked workers are DORMANT records of the migration backlog"*, lists the causes (`PENDING_ON_PREDECESSOR×221`/`PENDING_ADVANCE_BLOCKED×32`/`RUN_SETTLED_NOT_PROMOTED×29`) and says they are NOT cleared because clearing them to green the badge would destroy the backlog evidence; `processing` carries its GAP-4 caveat. Graph on `rag-canary`: `ACTS_ON` 16 (24.6%) · `RELATED_TO` 11 (16.9%) · `REQUIRES` 10 · `CAUSES` 7 (`RELATED_TO` well under the 50% junk floor). **Two bugs found by RUNNING it rather than assuming:** `/control_plane` returns `pools` as a MAP with TOP-LEVEL counters (the guessed list-shape crashed the screen — `pools.map is not a function`) and a React key warning in the evidence table; both fixed against the live response and the contract type corrected. **GAP-7 recorded:** there is NO graph route in the API — no entity search, relationship browse or supporting-chunk fetch — so F9 is partial by necessity; rendering an entity browser the backend cannot attest would contradict "only source-attested relationships may appear as canonical truth". Work-log `2026-09-10-frontend-v2-f4-f5-f8-f9-f10.md`. | DONE 2026-09-10 — F1–F5, F8, F9(partial), F10 shipped. **F6 blocked on GAP-2, F7 on GAP-3, F9 completion on GAP-7.** F11/F12 unbuilt. Legacy `frontend/` untouched throughout. |
| 11.204 | **U-2 FORENSIC HOLD: CLEARED** (2026-09-10) — the owner's five-criterion release gate evaluated on the SHIPPED code. **Persistence chain PASS** (proven four times through the real production path: 5→5→5, 9→9→9, 10→10→10, 11→11→11, compiled = persisted = projected every time). **D-3 completion truth PASS** (the SAME document with the SAME 2 stale never-dispatched rows now completes with ZERO dispatch, ticket `done`, 60 s watch with no re-arm and no re-dispatch; 6 regression cases pin both directions). **D-4 durable accounting PASS** (`llm_controller_state` carries the first pMAP rows ever — `map_groq3`, `map_groq5`, each `day`+`day_count`+`last_dispatch_at`; restart continuity proven with ZERO spend by a fresh registry restoring the count instead of the yaml seed; 5 tests). **Bounded post-fix canary PASS** (canary 5, 11 parents, `reconciles: true`, ALL SIX checks including `limiter_matches_dispatch`). **No unexplained accounting PASS.** Total forensic spend across the entire U-2 effort: 17 probe requests + 4 canary requests. Gate + documented STOP CONDITIONS recorded in `docs/wiki/experiments/u2-persistence-canary-2026-09-10/RELEASE-GATE.md`. | CLEARED — bounded cinema resumption is now permitted under the 7 documented stop conditions, scoped by pinned run/document ids (`POLYMATH_DOC_PARENT_MAP_CORPUS` stays `rag-canary`; cinema work is minted explicitly, never by broadening auto-mint). **CARRY FORWARD, NOT FIXED: D-1** (3 batches with real consumption booked `LIMITER_REFUSED`; the CLASSIFIER is unchanged, so a dispatched-but-empty batch during a backfill would be misbooked the same way and corrupt the refused-vs-429 signal an operator watches — recommended one-line fix BEFORE a full backfill) and **D-2** (6 batches frozen on leases expired 2026-09-09 04:54Z holding 90 parents; reap by PINNED BATCH ID). `MAP_RELIABILITY_CAP` stays 15 — real-parent reliability above 15 is unmeasured and the 3 historical dispatched-but-empty batches were ALL `expected_count=60` on real parents, which is evidence AGAINST raising it. |
| 11.206 | FRONTEND-V2 F6/F7/F9/F11 + three ADDITIVE contracts — owner directive 2026-09-11. Closes GAP-2, GAP-3, GAP-7. **No retrieval/ranking/readiness/provider policy changed; nothing on the chat path altered; no writer added.** **GRAPH-BROWSE-V1** (`GET /graph/entities`, `GET /graph/entity/{id}/relationships`) — the SMALLEST read-only graph API: corpus-scoped, bounded (≤100), and **source-attested by construction** (Neo4j holds topology but carries NO corpus and NO provenance, so a relationship is served only when Postgres `evidence(fact_id,doc_id,chunk_id)` ties it to a document in THIS corpus; unattested ones are withheld and COUNTED as `dropped_unattested` so the absence is visible). **COMPARE-RETRIEVAL-V1** (`POST /compare`) — one question, several modes, **retrieval only**, arms SEQUENTIAL (the reranker is one shared GPU lane; parallel support passes measured p50 12s→31s, so parallel arms would make latencies meaningless), and arms differ ONLY by mode — no knobs forwarded, or it stops being a comparison of modes. VECTOR refused 422 (backend primitive, never a product mode). **ANSWER-REVIEW-V1** (`POST /review`) — evaluation only: question + answer + citations + the cited evidence → grounding/correctness/completeness/citation-support/retrieval-adequacy + unsupported claims + missing evidence + verdict; performs NO retrieval and judges the answer handed in (the distinction from `synthesizer`, which merely re-answers). **Live proof:** `/compare` on rag-canary — HYBRID 412ms · GRAPH 508ms · WILDCARD 819ms, each evidence=15 docs=6 union=51, and ALL THREE selected the SAME documents (on a small homogeneous corpus the modes genuinely converge — the UI states that as a RESULT, not a bug). `/graph/entities` on cinema returned `laban movement analysis` (32 mentions/10 docs) and its relationship `-USES-> bayesian models` WITH the attesting document and passage. **F11: 9/9 integration tests green against the live backend**, including the legacy-boolean case (cinema `query_ready: true` while VNEXT INCOMPLETE — it SKIPS itself once cinema completes so it cannot rot into a false assertion), the FIRED-vs-SURVIVED trace fields, source attestation, and both refusals. Three guessed things were caught by running them: `chat_retrieve_v2` takes no `latent`; `_run_synthesizer` does not exist (the reviewer now uses the real `litellm.completion` + `_litellm_credentials` path with `max_tokens=2000`); and `chat_retrieve_mode` returns the `/retrieve` contract, so lane/funnel data lives under `trace`. Work-log `2026-09-11-frontend-v2-f6-f7-f9-f11.md`. | DONE 2026-09-11 — the required user flow is complete: ask → choose Hybrid/Graph/Wildcard → inspect query trace → inspect evidence → inspect graph contribution → compare modes → review with an evaluator model. F12 (real browser E2E) next. Open: `/compare` compares RETRIEVAL not answers (synthesis would cost N calls — a deliberate omission); the reviewer's own reliability is unmeasured; graph entity search ranks by mention count, not semantics. |
| 11.207 | PMAP-SCALE-OUT-V1 — found while running the owner-authorized cinema backfill 2026-09-11. The single `doc_parent_map` slot was chosen on an EXPLICIT assumption ("the worker's own infer closure fails over across the six map_groq accounts in-run, so one slot drains the pool") that the POOL is the constraint. **Measured otherwise: 40 dispatches in ~70 minutes against 5 lanes × 2 rpm = 10/min available ⇒ ~6% provider utilisation** — a worker maps ONE DOCUMENT at a time and pays skeleton-build, batch planning and projection-embed serially between dispatches, so the WORKER was the constraint. Added `doc_parent_map2/3/4` + an autopilot rule of one worker per open `doc_parent_map` ticket capped at FOUR (matching the existing `extract` cap-3 and `doc_profile` cap-6 rules). **Result: 52 SUCCESS batches in 4 minutes vs 103 in the preceding 30 — 3.4/min → ~13/min, ≈4×**; cinema mapped 2,473 → 2,840 in ~3 min. Safe by construction: batch tickets are LEASED PER DOCUMENT so concurrent workers cannot double-map, four concurrent documents still fit the lane budget, and autopilot parks all four when no pMAP ticket is open (idle cost unchanged). **`MAP_RELIABILITY_CAP` untouched at 15; lane topology, auto-mint scope and every retrieval path untouched.** Work-log `2026-09-11-pmap-scale-out.md`. | DONE 2026-09-11 — fleet shape only. Rejected: raising the cap (owner-forbidden and contra-indicated), adding lanes (utilisation was 6%), and keeping one slot on the strength of a comment whose assumption the measurement disproved. Open: cap 4 sits inside 5×2 rpm but is not tuned; throughput measured on mid-size documents. |
| 11.209 | PRODUCTION-CONFORMANCE-AUDIT-V1 — a RE-FIRABLE audit framework (owner mission 2026-09-12): discover what exists, prove what works, prove what is safe to retire. Authority `docs/wiki/plans/PRODUCTION-CONFORMANCE-AUDIT-V1.md`; logic in `shared/polymath_shared/conformance/` (discovery · classify · assess · evidence · attempts · report), orchestration in `scripts/audit_polymath.py`. **Nothing about today's topology is hardcoded** — providers, models, accounts, lanes, workers, routes and tables are rediscovered every run from config + Postgres + the running processes + the code graph; a first run discovered 46 lanes / 7 providers / 15 models / 43 routes / 16 workers / 46 tables without being told any of them. **`NOT_TESTED` and `UNKNOWN` are RED by construction** (`classify.GREEN` holds exactly one state) so absence of evidence can never render as success — the first `--no-spend` run classified 177 components: 69 WORKING_PROVEN, 31 CONFIGURED_IDLE, 14 LEGACY_REQUIRED, 16 RETIRE_CANDIDATE, **47 NOT_TESTED**. **PROVIDER-ATTEMPT-LEDGER-V1 (migration 0056, `llm_provider_attempts`)** closes the gap this framework was built around: measured 2026-09-11, a pMAP run recorded SUCCESS on every batch while issuing **181 HTTP 429s (145 in one 10-min window, an 82% rejection rate)** — in-run cross-lane failover retries a refused lane on the next and only the FINAL outcome reached `document_parent_map_batches`, so provider pressure was invisible to every durable counter, to the control plane, AND to the backfill's own stop conditions. One row per ATTEMPT (lane · provider · model · account ENV NAME · ordinal · limiter admission · dispatch · status · Retry-After · error · latency · success), written at `LLMExtractionClient.complete_one` — the single seam seeing admission, lane, status and Retry-After together; fail-soft (a missing table logs once and the call proceeds) and **no credential is ever stored**. `attempts.reconcile()` reports `PROVIDER_PRESSURE` / `FAILOVER_ATTEMPTS` / `HIDDEN_429` as COUNTS. Retirement follows the existing law (eliminate readers → stop writers → prove no re-mint → remove code → delete state after a rollback window); `--prove-unused` runs the STATIC half and prints that static proof alone is NOT sufficient. Ambiguous references count as READERS deliberately — over-counting readers only delays a retirement, under-counting enables a wrong deletion. 12 agnosticism tests (`test_conformance_agnostic.py`) prove the framework against a SYNTHETIC vendor topology (acme/zeta) with added lanes, renamed models, a removed provider, a disabled lane and reordered fallbacks, plus a negative test that no current vendor token appears in the framework source. `artifacts/audit/` is git-ignored (run artifacts, same rationale as build output). | **SLICES A–C DONE 2026-09-12.** Honest status: L0/L1 proven; **L2–L5 NOT BUILT** — `contract_qualified`/`pipeline_qualified`/`e2e_qualified` emit `NOT_TESTED` rather than a guess; no retirement removal performed (no component yet satisfies the full proof); Control Plane integration and a CI job not built. First run also caught a live defect: **runtime bundle NOT uniform** (3 hashes) because adding the conformance package to `shared/` changed the execution fingerprint — a fleet bounce is due before pipeline work. |

## §13 Enrichment lane scheduling & isolation (rev 6, 2026-08-30)

Authoritative section added to the migration plan (§13). Design decided:

| # | Detail | Status |
|---|---|---|
| 13.1 | External, in compile_objects (NOT native to extract) | DECIDED |
| 13.2 | Dedicated `llm_enrich` lane, separate provider/key — own AIMD/buckets/breaker, zero interaction with baseline cloud lane | DECIDED (provider/key choice pending owner) |
| 13.3 | ARM-ON-DRAIN: enrichment tickets arm per corpus when extract tickets all done | design fixed |
| 13.4 | BASELINE PREEMPTS: no enrichment claims while any extract ticket is ready/leased | design fixed |
| 13.5 | LOCAL ASSIST: manual, owner-deployed, post-drain only (GPU window belongs to baseline) | design fixed |
| 13.6 | Contract: parent-semantic-compiler-v1, bounded 1/3/3/4 = 8 latent vectors/parent; gate = coverage + refs⊆input + malformed-reject; (parent, source_hash, contract) keyed; STALE on diff | design fixed |
| 13.7 | SWEEP = re-arm compile_objects corpus-wide; DIFF = source_hash compare → STALE → regenerate | design fixed |
| 13.8 | Implementation: enrichment compiler inside compile_objects worker; no new stage, no new subsystem | build pending |

## Completion tally (normative details)

- DONE: 34 · DEVIATED: 9 · SUPERSEDED: 8 · PARTIAL: 17 · **MISSING: 24**

## Priority order to close MISSING/PARTIAL (goal mode, post-GO)

1. 4.3.8 per-item output budget scaling + 4.9.1 uniform packing (speed + shape)
2. 4.9.6 client-side /infer_batch wiring (batch-40 density) + 4.9.4 curve spike → lock volume config
3. 3.3 ingest_cli (run/status/resume/cancel/report) — the control surface
4. 1.6/4.7.4 extractor_version stamping + parent-level resume
5. 4.4.4/4.4.5 corpus_entities + entity_links migration (owner directive 5)
6. 4.3.6 digest → parent routing cards (corpus mapping layer wiring)
7. 4.3.3/4.3.4/4.4.6/4.4.7 latent + prompt seeds (quality profile)
8. 5.1/5.3/5.4 eval set + benchmark gate + MRR harness
9. ~~4.6 graph modes~~ REJECTED by owner 2026-08-30
