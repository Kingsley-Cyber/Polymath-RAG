---
title: "OWNER BACKLOG — work the owner has parked, in the owner's words, with the state it was left in"
last_reviewed: 2026-09-07
last_touched: 2026-09-06
status: living
---

# OWNER BACKLOG

Items the owner has explicitly deferred ("we won't do it now"). Each entry names the decision needed, the state on the day it was parked, and where the evidence lives. Nothing here is started without the owner saying so.

| # | parked | item | state when parked | pointers |
|---|---|---|---|---|
| B1 | 2026-09-06 | **Near-duplicate guard at intake (DUPLICATE-DOCUMENT-GUARD layer 3).** Shingle + MinHash containment of the normalised text against the corpus's existing documents; ≥ ~0.9 containment refused as "near-duplicate of X" with the pair in a receipt; explicit override to keep both; the Files tab reports a refused duplicate as "already in corpus (same bytes / same text / near-duplicate of X)" instead of "failed: 409"; a folder drop ends with a summary (N new, N exact, N near). Touches ingestion (intake worker) — owner go required. | **DONE 2026-09-07** (register 11.121): the v3.3 containment design ported as layer 3 (exact shingles, no MinHash — 2.7 s for all 67 cinema documents); the real Sound Design twin, rebuilt from the spool 8 bytes apart, is refused at 100 % containment naming the survivor; `keep both` override; Files tab wording + amber `duplicate` pill + folder-drop summary; replay exemption found on the first live proof and fixed before commit. | `orchestrator/orchestrator/api/ui.py` (upload guard), `workers/workers/intake_worker.py` (layer 2), `frontend/src/components/FilesView.tsx` (state wording) |
| B2 | 2026-09-06 | **Delete the Sound Design duplicate in `cinema`.** Two files, 1,067,157 vs 1,067,149 bytes, 255 sections each, different hashes ("… Cinema.md" and "… Cinema (1).md"); one copy to go via the documents API, no re-ingest. Deferred so the owner's mode comparisons run on an unchanged corpus. | **DONE 2026-09-06** (register 11.111): "(1)" copy removed via DOCUMENT-DELETE-V1; 68 → 67 documents; surviving copy intact; no re-ingest. Follow-up 11.114: the delete had left the derived Neo4j subgraph behind (a doc_id-keyed Chunk delete that matched nothing; no Document / Fact / Evidence pruning; receipts skipped silently) — endpoint fixed, graph reconciled, lifecycle test green. | documents API; corpus `cinema`; work-log 2026-09-06-b2-sound-design-twin |
| B3 | 2026-09-06 | **Corpus shaping for `cinema`** (owner is weighing it): the owner's own `handbook.html` (802 sections) into its own corpus, and/or a document-kind tag (book / paper / manual / handbook) with a scope filter in the UI. | **CLOSED 2026-09-07 — owner chose C** ("leave my handbook alone"): the handbook stays in `cinema` as it is; no tag, no filter, no move. Document-fair judging (B11) and the ADJACENT aspect (B9) are what keeps it from crowding. Register 11.119 records the proposal and the decision. | receipts of 2026-09-06 19:04 (VECTOR control); per-document competition table offered after the owner's HYBRID / GRAPH / WILDCARD runs |
| B4 | 2026-09-06 | **Rotate the OpenCode and Alibaba Model Studio keys** (both were pasted in chat). | Keys live in `.env` only; never in the repo. | `.env` (owner-held) |
| B5 | 2026-09-06 | **bge-reranker-v2-m3 comparison** against Qwen3-Reranker-0.6B (owner: "hold off on model change"). | Judge fast path shipped (11.100); no comparison run. | `scripts/rerank_judge_bench.py` |
| B6 | 2026-09-06 | **v33 database salvage assessment** (summaries / entity extractions mapped to documents). | Five `polymath_v33_*` Docker volumes kept; software removed. | `~/polymath-v33-archive`, Docker volumes |
| B7 | 2026-09-06 | **Receipt whitelist: add `generation`** (finish_reason / max_tokens) to the stored query receipt — one line in `shared/polymath_shared/query_receipts.py`; needs a fence round + respawn, so parked until the owner pauses testing. | **DONE 2026-09-06** (register 11.112): `generation` on the receipt whitelist; both routes' stored receipts carry it. | 11.106 work-log; work-log 2026-09-06-b7-generation-receipt |
| B8 | 2026-09-06 | **GRAPH evidence hygiene (GRAPH-EVIDENCE-HYGIENE-V1).** (a) A graph fact's provenance chunk becomes an [S#] evidence row only if it passes the same judge floor as everything else — otherwise the fact stays in a facts block without a tag; (b) judged evidence first in the legend, graph rows after and labelled `graph` (as WILDCARD labels bridges `divergent`); (c) the legend dedupes by chunk id; (d) table-of-contents / index regions (the materializer's noise roles) are filtered at the candidate lanes. Chat-runtime work (evidence_assembly + ui legend + candidate lanes), not ingestion; it changes GRAPH's recorded floors in the regression manifest, so it ships with a frozen-plan re-measure. | **DONE 2026-09-06** (register 11.115): claim rows out of the legend, one tag per chunk, lexical index-page filter at the union; 10 GRAPH turns: rows 21.5 → 15, duplicates 41 → 0, unjudged 61 → 0, index pages 5 → 0, tags held; replay bounds held. | `shared/polymath_shared/evidence_assembly.py` ("graph claims first"), `orchestrator/orchestrator/api/ui.py` `_evidence_legend`, `shared/polymath_shared/candidate_engine.py` lanes, `document_region.py` NOISY_ROLES; receipt 19:19:21 GRAPH |
| B9 | 2026-09-06 | **Adjacent-knowledge exposure (BREADTH-V1).** The pipeline is precision-first: dense + lexical lanes match wording, the judge ranks 24 candidates against the compiled queries, the composer diversifies within the judged set. Owner wants answers that also surface what the corpus knows next door (Laban / Bartenieff / Stage Combat for a fight question). Proposed: (a) an ADJACENT aspect in the compiler — a domain-neutral reformulation of the resolved request ("how a body communicates force and intent") with reserved seats and its own coverage line; (b) a per-document cap inside the judged prefix (e.g. ≤ 8 of 24 pairs from one document) so a six-candidate book gets scored; (c) receipts: `adjacent` arrivals and seats, so exposure is measurable. The latent-abstraction lane that would serve the same goal is B12 (this row keeps the compiler aspect and the judge-prefix cap). Chat-runtime work (compiler + candidate engine + composer); frozen-plan re-measure (B / M) before promotion. | **DONE 2026-09-06** (register 11.117): ADJACENT aspect (prescriptive rule; permissive wording got 0 / 18) — synthesis plans 0 → 6 / 9 with it, final-set documents 7.5 → 9 per turn and 33 → 43 distinct over ten turns, tags 8.5 → 15, compiler +0.3 s; factual questions untouched (precision 1.0). | `chat_compiler` aspects, `candidate_engine.select_evidence` (judge prefix, slots), receipts 19:04 / 19:09 / 19:19 |
| B10 | 2026-09-06 | **Summary pipeline value — two ablations (SUMMARY-ABLATION-V1).** (1) Prompt without section / document summary rows (10 questions, same model): prompt chars, generation time, citation precision, answer shape — does anything change besides a smaller prompt? (2) Retrieval without the summary-routed `hierarchical` lane on the frozen fixture B: gold-in-union, hit@10, survival. Decide from the numbers whether summaries stay as prompt rows, as a routing lane only, or neither at retrieval time; the upstream cost of producing them (enrichment, §3.23) is the owner's separate ledger. | **DONE 2026-09-06 (measurement; register 11.118)**: ablation 1 = B11 (rows out: −31 % prompt, precision 1.0, 0 citations lost); ablation 2 (ABC vs BC, 10 frozen plans): identical recall at every stage — the summary-routed lane found nothing the child lanes did not; recommend confirming on the 30-plan fixture + L before retiring the lane; producing summaries is the owner's enrichment ledger. | `evidence_assembly.assemble_evidence_bundle` (section / document rows), `candidate_engine` lanes, `scripts/chat_baseline.py` arms; funnel `arrivals` in receipts |
| B11 | 2026-09-06 | **EVIDENCE-DIET-V1 — smaller, honest prompt; fair judging** (owner: document summaries are a waste of injected rows; keep parent context; more books should survive fairly). Three steps, each gated: (1) **document-summary rows out of the prompt** (they stay a routing input pending B10) — legend, Sources panel and receipts count passages only; (2) **section rows folded into breadcrumbs**: each chunk's legend line carries "book › chapter › section" from its parent instead of a standalone section row with its own tag; (3) **document round-robin judging**: fill the judged prefix (24, re-measure 32 with the fast judge) by taking each surfaced document's best fused candidate, then second-best, … with a per-document cap, so survival is decided by the cross-encoder's relevance score rather than by a document's headcount in the union; receipts `judged_docs`, `capped_out`, per-document union → judged → final → cited. Composer dominance guard stays. GATES: prompt chars ↓ with citation precision and answers-with-tags unchanged on the 10-question probe (steps 1–2); frozen fixture B gold-in-union / hit@10 / survival not below the recorded floors and documents-judged per turn ↑ (step 3). | **IMPLEMENTED 2026-09-06** (register 11.113): steps 1–2 MET (prompt 41.6k → 28.9k chars, passages only, breadcrumbs, precision 1.0); step 3 shipped ON at 32 fair seats — every manifest floor held, documents judged per turn 5 → 9, survival-given-union below its P1.c value (B 0.852 → 0.778, L 1.0 → 0.933) recorded; rollback `POLYMATH_CHAT_RERANK_ROUND_ROBIN=0`. | `evidence_assembly.assemble_evidence_bundle` (document / section rows), `ui._evidence_legend` + legend lines, `candidate_engine.select_evidence` (judge prefix order), `chat_regression_manifest` floors |
| B12 | 2026-09-06 | **WILDCARD that works, and the latent layer as a composable toggle (LATENT-COMPOSITION-V1).** Owner: "it died … I do like how I can toggle it with other retrieval. It should work." Two parts. (1) **WILDCARD finish under load**: the divergent sweep runs at T=0 but its finish (two-hop validation through the judge, novelty) shares the contended judge and times out — give the finish its own small budget on the fast judge (fp16 / 384 tokens; one batched call for all candidate parents instead of one round trip each), and when even that budget is missed ship the top bridges labelled `unverified` instead of dropping them (receipted `wildcard.partial`), so the mode always delivers what it found. (2) **✨ latent as a v2 lane, not a v1 fallback**: port `latent_rescue` into the v2 composition as a fourth lane (abstraction / transfer vectors nominate parents; their ORIGINAL children compete through the same judge, bounded and receipted) so the toggle combines with VECTOR / HYBRID / GRAPH without abandoning the compiled queries, judge slots and coverage lines; retire the v1 drop-down. Gates: WILDCARD bridges on ≥ 80 % of frozen-plan B turns under enrichment load with 0 bridges in the evidence list (the P1.e invariant); ✨ turns stay on `chat-retrieval-v2` with a `latent` lane in `arrivals`; B / M floors held. Supersedes the lane half of B9 (B9 keeps the compiler ADJACENT aspect and the judge-prefix cap). | **DONE 2026-09-06** (register 11.116): finish budget 4 s + ≤ 8 pairs per parent + unverified bridges (labelled, receipted); ✨ = lane D on the v2 engine; 10-turn replay under load: bridges 10 / 10 turns (5 verified + 25 unverified, 6 finish timeouts), 0 in evidence; live ✨ turn on chat-retrieval-v2 with 15 latent candidates; B floors unchanged with the lane on; WILDCARD +4.6 s / lane D +2.9 s p50 under load recorded. | `orchestrator/orchestrator/api/chat_retrieval.py` `_retrieve_wildcard` (`divergent_finish`, `WILDCARD_FINISH_GRACE_S`), `shared/polymath_shared/divergent.py`, `shared/polymath_shared/hybrid.py` `latent_rescue` (v1), `ui.py` `_v2_mode` (the ✨ / v1 gate), register 11.97 |
| B13 | 2026-09-07 | **SECTION-ROUTING-V1 (owner decision 2026-09-07: "document summary should also be removed from search; it should fall on parent or section for routing").** The hierarchical lane routes by section summaries (plus the child and entity-card votes) only; the document-summary vector search is off by default (`hierarchy_route_documents`, `POLYMATH_CHAT_HIERARCHY_ROUTE_DOCUMENTS=1` restores it); receipts `route_kinds`; breadth stays with B11's document-fair judged prefix (the "doc filter" the owner asked about — already implemented) and the composer's diversity seats. Gate: frozen fixtures B and L, 10 plans, document vote on vs off interleaved — recall floors held, one fewer search per turn, documents judged / final per turn held or up. | **DONE 2026-09-07** (register 11.120): document vote off by default; B and L recall identical with it off, final-set documents equal or +1, L MRR 0.742 → 0.775, one fewer search per turn. | `candidate_engine.py` lane A (`hierarchy_route_documents`), `chat_m_replay.py` (judged_docs / final_docs receipts) |
| B14 | 2026-09-07 | **ABSTRACTION-LADDER-V1 (owner proposal 2026-09-07, agreed).** Replace the compiler's flat query-type list with an abstraction ladder as its vocabulary: L0 literal (today's PRIMARY / EXAMPLE / ENTITY), L1 procedure (PROCEDURE), L2 mechanism (MECHANISM / CAUSAL), **L3 framework / model (missing today — step 1)**, L4 universal principle and L5 meta (held: only when the corpus map says the corpus holds theory, one slot), L6 analogical (ADJACENT / BRIDGE). Levels per TASK, not per turn: factual QA = L0 + L1; synthesis / create = L0 + L2 + L3 + one L6 — still `MAX_QUERIES` 4, because every aspect takes seats from the literal question inside the same 32 fair judge seats. High rungs search the latent layer (latent_abstraction / latent_transfer, the corpus side's own ladder) and bring back the original passages, as lane D already does, not the child lanes. GATE per rung (the constant lesson: gold-in-union will not move): seats taken → survived the judge → **cited by the model** on the synthesis fixture, ten questions, one rung at a time; a rung cited on fewer than ~⅓ of synthesis turns earns nothing and is dropped. Cost per rung ≈ 0.2–0.3 s (its searches + judge pairs within the cap). Receipts: per-level arrivals / judged / final / cited. | **QUEUED 2026-09-07** — owner: "i agree with you"; step 1 = L3 alone; chat-runtime work (compiler prompt + `validate_plan`, candidate engine lane routing for high rungs), nothing under §3.23. | `shared/polymath_shared/chat_plan.py` (QUERY_TYPES, ADJACENT rule, `validate_plan`), `candidate_engine.py` lane D (`latent_search`), `scripts/chat_m_replay.py` (per-level receipts), B9 work-log (the ADJACENT precedent: 33 → 43 distinct documents) |
| B15 | 2026-09-07 | **BRIDGE-HOP-V1 — GRAPH hop-2 in the corpus's own language (owner proposal 2026-09-07: "the bridge is manufactured in the corpus's own language").** Round 1 wins → the entities evidenced in the winning passages → keep those with fact evidence in a document OUTSIDE the round-1 set, drop hubs (df > ~6 of N), canonical join for surface variants → ≤ 8 bridge terms → round 2 = their evidence passages in the outside documents (≤ 12), judged against "<term> — <question>" and labelled `bridge` in the legend; seed-starvation fix in `_selected_surfaces` (question words filled all 12 slots); `graph` seeds / facts on the receipt whitelist. MEASURED 2026-09-07 (punch question, GRAPH live): seeds = 12 question words + 8 camera-ish cards; 20 facts from VES 7 / Fight Choreography 6 / Cinematic Motion 4 / others 3, 0 from any Laban book, 0 tagged; the bridge entities exist (`effort` in 11 books incl. Stage Combat Arts, Fight Choreography, Laban Workbook, Bartenieff; `weight`, `timing`, `rhythm`, `breath support`, `intention`, `footwork`) but were never seeds. GATE: the punch question cites ≥ 1 Laban-family passage; bridge rows cited on ≥ ⅓ of 10 synthesis turns; literal precision 1.0; GRAPH wall Δ ≤ +1 s; manifest case 11 held. | **RE-SCOPED 2026-09-07 after pre-build measurement** (punch question, three corpus-supervised sources from the 15 winning passages): fact-evidenced entities → 2 terms (`exercise`, `reaction shot`); mentions → 6 terms, the one real bridge `impact` → Timing for Animation ("in a game with physical contact like combat, the animator must be conscious of the placement…"); latent abstractions of the 4 winning sections → top-6 neighbours all cinematography books, 0 movement-family. The winners are camera-craft passages, so every hop FROM them deepens the camera frame; Laban is reachable only from a different starting point (B16 titles → compiler, B14 L3). B15 stays as a modest deterministic hop over MENTIONS (≤ 8 terms, ≤ 12 outside passages, judged against "<term> — <question>", ≤ +1 s) with an honest gate: an outside-book `bridge` row cited on ≥ ⅓ of 10 synthesis turns; the "must reach Laban" gate moves to B16. Order becomes **B16 → B14 (L3) → B15**. | `orchestrator/orchestrator/api/chat_retrieval.py` `_attach_graph`, `orchestrator/orchestrator/api/graph.py` `_selected_surfaces`, `orchestrator/orchestrator/api/retrieve.py` `graph_expand_or_502`, `shared/polymath_shared/query_receipts.py` whitelist, `docs/wiki/plans/IMPLEMENTATION-TODO-2026-09-07.md` §1 |
| B16 | 2026-09-07 | **COMPILER-CORPUS-CONTEXT-V1 — the compiler sees the library's TITLES before it writes queries (owner design 2026-09-07: "titles of the books, not summaries … top 40 or something, dynamic").** Today the compiler prompt carries only `CORPUS IN SCOPE: <id>`; it has never seen a title, so its ADJACENT query is a domain-neutral guess. Change: a `RELEVANT BOOKS IN THE LIBRARY:` block of document TITLES only (extension and hash suffixes stripped), pulled live from `documents` on every turn (no cache → no staleness). Dynamic top-N, default 40 (`POLYMATH_CHAT_COMPILER_TITLES_TOP_N`, 0 = off): a corpus with ≤ N documents injects every title; above N, **rank documents by content, not title words (owner refinement 2026-09-07: "top section summaries per document, backward-mapped to the top-k documents")** — embed the question ONCE, dense-search the section summaries (lane A's own route), `aggregate_documents_n` → top-k documents → their titles; the vector is handed to retrieval so nothing is embedded twice (compile already runs serially before retrieval, so the net cost is one Qdrant search ≈ 50 ms, not an extra embed). Deterministic given the index; title-word overlap only as the fallback when the embedder is dark. Prompt rule: "use the library's own terminology when it fits; the PRIMARY query stays the raw message". Bias guardrails already in force: exactly one PRIMARY = the raw message; every aspect has bounded judge seats. Complements B15 (this aims before retrieval; B15 corrects after round one). GATE (10 synthesis questions incl. the punch question, titles on vs off interleaved): the punch plan carries ≥ 1 query in corpus terms and arrivals from a Laban-family book > 0; title-driven rows cited on ≥ ⅓ of turns; distinct final-set documents ≥ B9's 43 / 10 turns; compiler wall Δ ≤ +0.3 s at ≤ ~1 k extra prompt tokens; literal precision 1.0. Receipts: `compiler.titles` {n_injected, n_corpus, top_n}. | **IMPLEMENTED 2026-09-07** (register 11.122): titles-only block, ranked by content, dense default (the only ranker that put the Laban Workbook in front of the compiler for the punch question — it then wrote a Laban query and the Workbook was cited; off and sparse never got there); sparse 80 ms / dense 1.3–2.2 s under load (latency gate missed, recorded); factual questions unchanged; synthesis fixture: distinct cited documents 24 → 31 with sparse, dense arm partial (owner stopped it to test by hand). Hygiene done: orphan summary + duplicate removed, 2,754 orphan routing points of a pre-B2 delete removed. Order now B14 → B15. | `shared/polymath_shared/chat_plan.py` `user_prompt` / `compile_plan`, `orchestrator/orchestrator/api/ui.py` `_compile_chat_plan` (title fetch), `scripts/chat_m_replay.py` (titles arm), `docs/wiki/plans/IMPLEMENTATION-TODO-2026-09-07.md` |
| B17 | 2026-09-07 | **LEAN-PROMPT-V1 (owner 2026-09-07: "every query the model is injected a large amount of context").** Measured: standing instructions 17,037 chars ≈ 4,300 tokens per turn — grounding 2,670, the v3.3 style layer **10,921**, presentation 2,926, study/authority/date 520 — against ≈ 6,500 chars of passages (15 rows × 73-word children). Plan: trim the style layer to what the grounding and presentation contracts do not already say (target system prompt ≤ 7 k chars), keep the abstention / citation / completeness laws verbatim, receipt `prompt_contract` bump; gate = ten-question loop (fixture M + the punch question): citation precision 1.0, tags held, words per answer ≤ the presentation-v2 targets, no abstention regressions. | **QUEUED 2026-09-07** — after the owner's manual test; the length rule (11.124) is already in. | `orchestrator/orchestrator/api/polymath_style.py`, `ui._llm_system_prompt`, `scripts/presentation_probe.py` |
| B18 | 2026-09-07 | **DOCUMENT-PROFILE-V1 (owner architecture 2026-09-07 — plan of record `docs/wiki/plans/DOCUMENT-PROFILE-V1.md`).** Every admitted document gets one cheap document-level enrichment pass → a compiled Document Retrieval Profile (ONE, SUMMARY, TOPIC 3–5, TERM 3–5, Q 3–7, SEARCH 3–7, THEORY 1–5, CONCEPT 1–5, SEEALSO 2–5) → artifact with the receipt chain (content hash → input hash → raw response hash → compiled hash → projection hash) → named dense vectors (title, identity, theme) + multivectors (questions[], searches[], theories[], concepts[], seealso[]) in a separate collection → **a document is not QUERY_READY until the profile artifact and vectors exist** (tolerant minimum contract: core + hook + vectors; counts never required). Isolated `profile.enrich` API pool (free tier → cheap fallback → dead-letter). Retrieval: a document-profile lane that boosts and deepens (never gates); SEEALSO out of normal answers. DO NOT touch chunk vectors, chunk ids, parent/child identity, graph receipts. | **IN PROGRESS 2026-09-07** — steps 1–4 DONE (11.125, 11.126): compiler v3, lean context builder, `doc_profile` stage + isolated pool + projection to its own collection, control-plane wiring (phase A, non-blocking), 24 tests; nothing live yet. Aims raised 2026-09-07 to 10 / 10 / 15 / 15 / 10 / 10 / 10 (11.127); pool order = six dedicated keys rotated by run → Gemini → OpenRouter (lanes wired once the owner names provider / model / env var names). Next: step 5 backfill (mint tickets for existing runs, boot the fleet with the new slot, 67 pool calls — owner go on spend), step 6 retrieval lane, then phase B (QUERY_READY requires the profile). | `shared/polymath_shared/document_profile/{compiler,prompt}.py`, plan doc, `~/Downloads/rag_compiler_final (1).py` (owner's original) |
