---
title: "WORK LOG — P9 task-conditioned breadth (intent → doc-fairness)"
change_id: BREADTH-POLICY-V1
date: 2026-09-08
owner: worker (intent-gated composition tuning, default-off)
last_reviewed: 2026-09-08
last_touched: 2026-09-08
status: complete
register: 11.166
package: shared/polymath_shared/query_intent.py, tests/determinism/test_query_intent.py
architecture_impact: "P9 (§48/§49/§61): task-conditioned breadth wired onto the EXISTING doc-fair composer (round-robin cap that already fills remaining seats in fusion order — §49-compliant, no hard quota). `apply_intent_policy` now sets `rerank_round_robin` from the intent's breadth: SINGLE_OK (EXACT/DEFINITION) turns doc-fairness OFF so one excellent source may dominate (§61); MULTI_PREFERRED/MULTI_REQUIRED keep it ON (evidence-earned diversity). Gated behind the intent policy (default off ⇒ current behavior). Does NOT reintroduce the reverted per-doc quota — it only toggles the existing soft round-robin."
---

> **Ledger row:** `FINAL-RETRIEVAL-ROUTING-SYNTHESIS-V1.md` phase **P9** — this work-log is the EVIDENCE for that ledger row (the MD phase table is the control point; this does not duplicate it).

# WORK LOG — P9 task-conditioned breadth

## Contract

FINAL-RETRIEVAL-ROUTING-SYNTHESIS-V1 §48/§49/§61: breadth is task-conditioned
(SINGLE_OK / MULTI_PREFERRED / MULTI_REQUIRED) and diversity is EVIDENCE-EARNED — never an
artificial quota. The composer already does a soft doc-fair round-robin that fills remaining
seats in fusion order; P9 makes that fairness intent-conditioned.

Owner: `worker`. Verifier: `test_query_intent.py` + `chat_regression --check`. Rollback:
intent policy off (default) — the composer's current behavior is unchanged.

## Changes

- **`query_intent.py`:** `apply_intent_policy` sets `budget.rerank_round_robin = (breadth !=
  "SINGLE_OK")` — SINGLE_OK lets one source dominate; MULTI_* keep the doc-fair round-robin.

## Proof

- **Unit:** `test_query_intent.py` — EXACT/DEFINITION → round-robin off; MECHANISM/COMPARISON
  → on. 13/13.
- **Flag OFF byte-identical:** `chat_regression --check` → `131 rows, 0 failing`.

## Rejected claims

- **Not** a quota / not the reverted per-doc cap: only the existing soft round-robin toggles;
  the composer still fills seats in fusion order (§49). Global raw child recall is untouched.

## Open contract gaps

- MULTI_REQUIRED (comparison) currently maps to the same doc-fair round-robin as
  MULTI_PREFERRED; a stronger "ensure ≥2 documents" guarantee for comparison is a possible
  refinement (must stay evidence-earned, §49).
