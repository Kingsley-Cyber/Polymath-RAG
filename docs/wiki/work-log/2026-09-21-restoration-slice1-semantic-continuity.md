---
change_id: RESTORATION-SLICE-1-SEMANTIC-CONTINUITY
owner: "@king"
date: 2026-09-21
status: complete
architecture_impact: "ADAPTER_RUNTIME extended additively. A DERIVED read-only OpportunitySemanticViewV1 (no table, no second ledger) reaches agent steps through the existing opt-in `materials` channel (`config.show` path `semantics.*`) and DOMAIN_OPERATION steps in memory (`config.inputs` path `context.semantics.*`). AdapterStepV1, its four-field `context.hypotheses` and every Trail payload are unchanged. HypothesisStateV1 gains two OPTIONAL id arrays. REVISE applies or refuses every `changes` key. The readable / citable evidence caps are unchanged; a bounded share inside them is reserved for later retrieval passes. Manifest ecommerce.product_research 0.2.0. Not merged, not deployed."
last_reviewed: 2026-09-21
---

# Restoration Slice 1 — semantic continuity (reference §8)

## Contract
Owner build reference `docs/migration/SEMANTIC_TRANSDUCTION_RESTORATION_REFERENCE.md` §8, exit condition §8.6: rich state → `OpportunitySemanticViewV1` → a reasoning consumer without semantic collapse; tests §8.1 (1–8); a run-5-shaped fixture proves later-pass rows become READABLE; REVISE applies or loudly refuses every `changes` key. Locked decision §3.2: the view is derived, never stored. Branch `restoration/semantic-continuity` in worktree `../pmv4-semantic-continuity`, off `production` @ `1d9f695`. No Trail behaviour changes in this slice.

## Changes
- NEW `shared/polymath_shared/adapter/semantic_view.py` — pure. `build(current, outputs, order, step_outputs)` joins, BY ID, the ledger's newest revision with the run's own outputs read by key (`primitives`, `latent_structures`, `population_leads` / `community_leads`, `bridges`, `physical_jobs`, `mechanisms`, `product_concepts`, `priors`, `territories`) and with admitted field evidence of every loop pass (the evidence boundary's own `_index_rows`, one reader of that shape). Blocks: `hypothesis · origin · semantics · knowledge · transduction · bridge · jobs · mechanisms · concepts · trail · field_evidence · missing`. Primitive families attach through SHARED CITED EVIDENCE IDS (never word overlap); `transferable_invariants` / `shared_predicates` are run-level; latent structures carry their basis (`DECLARED` | `SHARED_EVIDENCE`). Missing values are listed in `missing`, never fabricated. Bounded (400-char text, 12-item lists). Projections: `agent_projection`, `query_projection`, `product_reality_projection`, and `trail_projection` — CLOSED at Trail's four wire fields.
- `service.py`: `_materials` scope gains `semantics` (built only when a `show` path asks); `advance` hands an automatic step that names a `context.semantics…` input an IN-MEMORY copy of the step with `context.semantics` (the stored step and `_payload_for`'s input are untouched → the `extra="forbid"` trap is not triggered); `_apply_theta` passes the run's known origin ids to the ledger; `_context_refs` uses the later-pass reservation. `manifest.py`: `config.show` may start with `semantics.`.
- `hypotheses.py`: ORIGIN LINKAGE — optional `lead_ids[]`, `latent_structure_ids[]` stored as ids only (GENERATE, REVISE, SPLIT children inherit); an id the run did not produce is refused. REVISE: scalars, `assumptions`, `falsifiers`, origin ids REPLACE; `knowledge_gaps` UPSERT by `gap_id` or normalised question (stable gap identity; close, never delete); `contradictions` APPEND and must cite evidence the step may cite; ANY other `changes` key rejects the transition with a typed message. `contracts.py` `ORIGIN_ID_FIELDS`; `transitions._cited_ids` skips them (they are lineage ids, not evidence citations — found by the acceptance test: the `*_ids` citation convention rejected them).
- `contracts/adapter/v1/hypothesis_state.schema.json`: two OPTIONAL properties; `required` unchanged → every stored revision stays valid.
- `evidence_boundary.py`: `knowledge_passes` (a pass = a run of ADJACENT outputs carrying a `rows` / `graph_rows` key; a row belongs to the newest pass that returned it) + `reserve_recent` (≤ 40 % of a knowledge class's slots for later passes, newest first; baseline order kept; single pass ⇒ identical result). `hydrate` marks such rows `retrieval_pass` and reports a sibling `allocation` block; `coverage` is byte-identical. Caps unchanged (60 readable, 200 citable); field evidence keeps its floor.
- `config/adapters/ecommerce.product_research.json` → `adapter_version` 0.2.0: `C_hypotheses` is shown `primitives` (incl. `transferable_invariants`) and its proposal schema gains the origin ids; `C_bridge`, `G_mechanisms`, `K_revise`, `N_jobs`, `N_concepts` are shown `hypothesis_semantics` (exactly five consumers — not "everything everywhere"); the three transition schemas enumerate what `changes` may carry (closed). Stage order untouched.
- Tests: NEW `tests/determinism/test_adapter_semantic_view.py` (30) + fixture manifest `tests/fixtures/adapter_semantic_view/fixture.semantic_continuity.json`. `test_adapter_ecommerce_product_research_e2e.py`: ONE assertion updated — it used `G_mechanisms` as its example of a step WITHOUT `config.show`; that step now opts in by design, so the assertion checks what it is shown instead (the opt-in law itself stays proven on two fixture manifests).

## Proof
UNIT_PROVEN + WORKTREE_INTEGRATION_PROVEN for `shared/` (executed path asserted: `test_the_code_under_test_is_this_checkout`). EXECUTED in the worktree, no database (`POLYMATH_PG_DSN` unset, in-memory store double), no network: new file 30 / 30; 19 DB-free adapter / ledger / contract suites 190 / 190; the ADAPTER_RUNTIME + MCP_SURFACE impact list 70 / 70. §8.1 1–8 each have a named test; §8.6 = `test_acceptance_*` (a run through `service.start / advance / next_step / submit`: the revising step receives statement, population, task, context, mechanism, friction, gaps, invariant, origin lead + structure, bridge and TARGETED rows while the first pass alone holds 150 rows; the domain consumer sees revision 1 and both gaps; the stored step has no `semantics` key and `context.hypotheses` is still four fields). Run-5 shape: 121 + 30 + 107 + 9 rows → 60 readable, later passes present (before: 0 at all 20 steps). Guards 0 / 0 / 0 / READY. NOT proven: anything live — `MERGED`, `DEPLOYED`, `LIVE_PATH_PROVEN` wait for the owner's merge + bounce (manifest edits are inert until then). No `workers/` file changed: the worker's existing `context.*` path resolution carries `context.semantics` (that resolution is live code; its use with this key is first exercised after the merge).

## Rejected claims
- "The hypothesis context was widened." It was not: `adapter_step.schema.json` is untouched; the view travels beside the step.
- "Origin is inferred." Only DECLARED ids are origin; shared-evidence structures are labelled as such.
- "Later evidence is readable in production now." Not until merged and bounced; and delta D-a (run 5's boundary calls returned 0 rows) is a separate, undiagnosed retrieval fact — this slice makes later rows readable WHEN they exist.
- "REVISE can change anything." `status`, support, priors, evidence ids and unknown keys are refused.

## Open contract gaps
- ADAPTER_RUNTIME: UPDATED (additive; spec `adapter_step.schema.json` unchanged; `hypothesis_state` + manifest 0.2.0 extended) — tests above green.
- MCP_SURFACE: TESTED_UNCHANGED — `adapter_next` gains nothing new in shape: `materials` and `evidence` already existed as sibling keys; `evidence.allocation` and `rows[].retrieval_pass` are additive; parity / server / principal suites green.
- Owed live (after the owner's merge + bounce): L1 `adapter_next` at `G_mechanisms` shows `hypothesis_semantics`; L2 a proposal with `lead_ids` persists and an invented id is rejected; L3 a REVISE with `changes.knowledge_gaps` lands in `adapter_hypotheses.state`; L4 at a post-`F_plan` step `evidence.allocation.later_pass_rows > 0`.
- `trail.product_discovery` is unchanged (no `show`, no origin ids in its schemas); the ledger and boundary changes reach it and are backward compatible (BOTH).
