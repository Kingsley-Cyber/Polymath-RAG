---
change_id: SESSION-C-P6-V1
owner: governance
date: 2026-08-31
status: complete (measurement; GO/NO-GO is the owner's)
architecture_impact: none (eval suite + results)
last_reviewed: 2026-08-31
---

# WORK LOG — SESSION C (P6 latent-transfer qualification, 20 cases)

## Contract
SESSION-ROADMAP.md Session C: >=20 authored cases, HYBRID latent off vs
on, the roadmap metric set headlined by nomination -> child survival.
Cases AI-drafted per the owner's established delegation pattern; each
query uses vocabulary ABSENT from the source while its principle exists
in the corpus (forbidden_shortcuts document the lexical giveaways).

## Changes
- cases.yaml v1: 20 cross-domain cases over the live corpus (AWS +
  Learning SQL) — lifecycle-as-aging, joins-as-ledgers, IAM-as-doorman,
  autoscaling-as-kitchen, normalization-as-address-books, etc.
- Harness v2: measures nomination/survival/admission (from the Session
  B diagnostics), unique gain, displacement, per-kind attribution,
  latency medians, failure count; writes LATENT-TRANSFER-P6-RESULTS.md.

## Proof (live, 20/20 cases, 0 failures)
- **Survival rate 78%** (60 nominated -> 47 with >=1 surviving ORIGINAL
  child; 55 latent children in final evidence). The roadmap's own
  calibration: ~8% = noise, ~55% = meaningful. 78% clears decisively.
- Unique evidence gained: 3.0/case. Displacement: 3.0/case —
  symmetric BY CONSTRUCTION at fixed evidence budget (latent hits
  replace the weakest baseline items; the reranker arbitrates).
- Attribution: abstraction 55 / transfer 27 nominations — BOTH kinds
  well above the 5% kill line; no kind dropped, no latent_query split
  indicated yet.
- Latency: median delta +20 ms (1985 -> 1877 median; free in noise).

## Rejected claims
- "Displacement 3.0/case means latent is pushing out good evidence" —
  not established: at a FIXED evidence budget every gained item
  displaces one; the reranker chose the survivors. Whether the trade
  is net-positive is exactly the owner's GO judgment (and the answer
  quality on the reach probes says yes).

## Owner gate (open)
GO = set latent_retrieval_enabled=true (HYBRID default-on). The switch
is one settings flip; nothing in this session enables it.

## Open contract gaps
- FalseAnalogyRate needs labeled negatives — follow-up suite if the
  owner wants it quantified beyond per-case eyeballing.
