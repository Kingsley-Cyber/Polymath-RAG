---
change_id: RESTORATION-SLICE-2-RESEARCH-FIDELITY
owner: "@king"
date: 2026-09-21
status: complete
architecture_impact: "ADAPTER_RUNTIME extended additively; engine (adapters/ecommerce) gains a semantic query compiler. Per-hypothesis gaps from every legitimate source reach Trail's gaps.compile with an owner and a stable id; an unowned gap is a typed refusal at submit time; research.plan compiles queries from each hypothesis's own semantic state, binds Trail's template slots per hypothesis and never searches governance text; a need compiled by a DOMAIN_OPERATION step reaches the evidence boundary (an agent-written one never does); field records read the current ledger state. Two thin worker call sites changed. Trail, its wire models and the receipt contract are untouched. Manifest 0.3.0. Not merged, not deployed; the deployed Hermes skill copy is unchanged until the owner's redeploy."
last_reviewed: 2026-09-21
---

# Restoration Slice 2 — hypothesis-specific research fidelity (reference §9)

## Contract
Owner build reference §9, exit §9.7: H1 gap → H1 semantic query → H1 harness intent, H2 likewise; no unresolved `{slot}`; governance phrases never searched literally; hypothesis ids survive; an unowned gap is a typed refusal on Polymath's side; `K_questions`' need reaches `K_retrieve`. Branch `restoration/research-fidelity` (worktree `../pmv4-research-fidelity`) STACKED on Slice 1 `98a0384`. Trail stays the authority for role, stage, source classes, freshness, independence and budget.

## Changes
- NEW `shared/polymath_shared/adapter/research_gaps.py` (pure): `harvest` merges ledger gaps (status open) · a non-Trail step's top-level `knowledge_gaps` · the agent's `open_gaps` · the bridge's `gaps` · the newest TRAIL output's gate gaps — deduplicated per (hypothesis, question), each `{gap_id, hypothesis_id, question, evidence_role, origin}`; ids are the ledger's or `gap_<hid8>_<origin><sha8(question)>` → STABLE across rounds (audit L18); the agent's `open_gaps` are no longer shadowed by `L_judge`'s (L8). A gap without a live owner is REFUSED (`GAP_OWNER_MISSING` / `GAP_OWNER_NOT_LIVE`) and never sent, so Trail's first-hypothesis fallback is unreachable from this path (L13). `trail_gap` is CLOSED at `ResearchKnowledgeGapV1`'s four fields; `origin` never crosses the wire.
- `service.py`: `submit` refuses a top-level `knowledge_gaps` / `open_gaps` entry without a live `hypothesis_id` (typed, the step stays issued); the in-memory `context.semantics` gains `research_gaps`; any automatic step opts in by naming a `context.semantics…` path anywhere in `config` (DOMAIN_OPERATION inputs, EXTERNAL_OPERATION `gaps_from`); `advance` resolves a domain-compiled need (below) and hands it to the executor in memory (`_compiled_need`).
- `evidence_boundary.py`: `domain_compiled_need(config, outputs, trusted_steps)` — ONE narrow exception: `source: outputs.<step>.<key>` is honoured only when `<step>` is a manifest DOMAIN_OPERATION step (deterministic domain code). `original_needs(..., compiled_need=)` takes the already-resolved STRING; its signature still has no `outputs` and the evidence executor still never reads a step output — both pre-existing law tests pass UNTOUCHED (first attempt passed `outputs` into `original_needs` and broke them; redesigned).
- `workers/adapter_step_worker.py` (thin, two call sites): `gaps.compile` sends `RG.trail_gap_payload(context.semantics)` when the step has `config.gaps_from` (else the previous behaviour, byte for byte); `exec_evidence` passes `compiled_need=step.get("_compiled_need")`.
- NEW `adapters/ecommerce/python/query_semantics.py` (pure): `gap_query` (who + doing + the gap's own words + role focus, ≤ 8 terms; a `trail_gate` gap contributes NO words — it is served by its hypothesis's vocabulary; governance terms are dropped even when a reasoning step echoes them; no semantic state → the agent's question or the statement, never governance text), `falsifier_query` (one contradiction-seeking search per hypothesis), `bind_template` (`{activity} {task} {friction_family} {population} {context}` from the ledger; `{product_territory}` from a territory NAME when Trail returns one — Slice 4; an unbindable slot is returned as `missing`). `executors.channel_queries(..., short=)` accepts the compiled string.
- `binding.py`: `research.plan` with a `semantics` input → `_semantic_research_plan`: bound per-hypothesis variants of every slotted Trail intent (`q-complaint:<hid8>`, governance fields intact), per-gap channel intents compiled through the gap's OWN hypothesis, `intent_index[{intent_id, hypothesis_id, gap_id, origin, query, basis}]`, `planned.unresolved_slots`, typed refusals `RESEARCH_PLAN_UNBOUND` / `RESEARCH_PLAN_UNBOUND_SLOT`; without `semantics` the operation is unchanged. `population.evidence_cards`: friction family and population from the CURRENT ledger state (REVISE / SPLIT safe, audit L17); `community` = stated community → the researched lead's name → the linked hypothesis's population → host only as a last resort (`community_basis` reports which); `workaround` = the claim (what the person does), the quote stays in `quote_ref`. `joined` keeps its three counters.
- Manifest 0.3.0: `H_gaps.config.gaps_from`; `H_plan` inputs `semantics` + `research_gaps`; `J_cards` input `semantics`; `K_revise.open_gaps[]` requires `hypothesis_id, question, evidence_role`; `I_research` is shown `intent_index`. `K_retrieve.config.source = outputs.K_questions.need` was already there — it now resolves.
- Tests: NEW `tests/determinism/test_adapter_research_fidelity.py` (15) + fixture `tests/fixtures/adapter_research_fidelity/fixture.research_fidelity.json`.

## Proof
EXECUTED in the worktree, no database, no network: new file 15 / 15 (the binding is run OUT OF PROCESS through its own JSON protocol — the worker's door — so no `workers` import); DB-free adapter suites + Slice 1 = 220 / 220; the engine's own suite under the Hermes venv with a temp loop DB 609 / 609. `shared/` + engine: UNIT_PROVEN + WORKTREE_INTEGRATION_PROVEN. `workers/adapter_step_worker.py`: IMPLEMENTED only (two call sites; under pytest in a worktree `workers` resolves to MAIN, so no test here counts for it) — live qualification owed. Guards 0 / 0 / 0 / READY.

## Rejected claims
- "Polymath now maps the registry." It does not: it binds Trail's OWN templates with values the ledger already holds; template choice, roles and budget stay Trail's.
- "The first-hypothesis fallback is fixed." It is unreachable from the governed EPR path; removing it at source is Slice 4 (Trail).
- "An agent can now steer retrieval." Only a DOMAIN_OPERATION step's output is ever trusted; the law tests that forbid it still pass unchanged.
- "`{product_territory}` is bound." Not until Trail returns a territory name (Slice 4); until then it is reported per hypothesis in `unresolved_slots`, never sent.

## Open contract gaps
- ADAPTER_RUNTIME: UPDATED (additive) — tests green. MCP_SURFACE: TESTED_UNCHANGED (no tool or payload shape changed; `materials.values.intent_index` is a new opt-in value).
- DEPLOYED SKILL COPY: after the merge the owner's block must include `scripts/deploy_ecommerce_skill.py` (engine files changed: `binding.py`, `executors.py`, new `query_semantics.py`) and its parity receipt; until then the Hermes copy lacks the compiler (the governed worker uses THIS repo's `adapters/ecommerce`, so governed runs are unaffected).
- Owed live: L5 `H_gaps` payload carries ledger + bridge + agent gaps with stable ids; L6 `H_plan.intent_index` per hypothesis, zero `{` in the harness action; L7 `K_retrieve.needs` = `K_questions.need` from round 1 on; L8 `J_cards.community_basis.host == 0` on a run with leads.
- `S_gaps` (supply stage) keeps the previous gap payload on purpose (not in this slice's scope). `trail.product_discovery`: unchanged (no opt-in).
