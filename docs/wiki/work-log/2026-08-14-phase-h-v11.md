---
change_id: phase-h-v11-boundary-corpus
owner: governance
date: 2026-08-14
last_reviewed: 2026-08-14
last_touched: 2026-08-14
status: complete
architecture_impact: none
---

# Phase H v1.1: boundary corpus construction, freeze, and rerun

## Contract

Build an additive Corpus v1.1 that exercises the lexical-semantic arm
boundary (resource-expanded positives, class-breadth traps, polysemy
contrast sets, passive-with-parse, ARG1→ARG2, assertion controls,
alignment-gap and structural cohorts), freeze it with hashes, and rerun
the existing Phase H harness on it — with ZERO production extraction
changes.

## Changes

- `eval/gold/relations_v1.1.yaml`: 33 items, authored independently of
  compiler output, with per-item coverage provenance recorded from the
  vendored tables. v1.0 untouched (control corpus).
- Harness (additive only): `--gold` corpus selection; frozen parse
  passthrough for PASSIVE_PARSE items; authored cohort passthrough;
  union-based transition accounting (absent side = CORRECT_ABSTENTION);
  oriented-fact recording fixed (harness defect — passive inversion was
  recorded as surface order; the compiler was correct).
- Tests: corpus v1.0 byte-identity, v1.1 freeze hash, required-cohort
  coverage, artifacts-vs-manifest consistency, oriented-fact recording
  regression, union accounting.

## Proof

- v1.1 verdict: REJECT — Δcorrect=+1, Δincorrect=+4, Δmissed=-4
  (35 units: baseline 25/0/8, hybrid 26/4/4; precision 1.000 → 0.867).
- 6 measured improvements (all VerbNet class-expansion driven:
  created/acquired/uses), 3 class-breadth traps fired exactly as
  designed (founded x2, similar_to), 2 hybrid-only suppressions via the
  FN anchor filter (developed), 1 spurious oblique-pairing edge.
- Polysemy: no sense-disambiguation value measured — every correct
  polysemy outcome was already correct in the baseline.
- Composed-only cohort: n=0 by resource topology (every composed pb→fn
  roleset also has a direct pb→vn mapping in this contract).
- v1.0 control rerun reproduces its frozen verdict (29/2/2, delta 0).
- Determinism: v1.1 baseline sha256 6b2dbac8…, hybrid a40546d5… across
  three runs each.
- 71+ unit tests, 12 integration, 3 guards green.

## Rejected claims

- No production extraction change: the two named mechanisms (expanded-
  trigger roleset constraint; composed-FN anchor filter) are recorded
  as candidate experiments, NOT applied.
- No corpus edits after authoring; freeze hash 3ee7065a…

## Open contract gaps

- The two named mechanisms become the next measured extraction
  experiments (E1): (1) class-expanded triggers require resolved
  roleset compatibility; (2) the FN anchor filter must not exclude on
  composed-only frame mismatch. Each needs its own before/after delta
  on the frozen v1.1 corpus.
- Sense-disambiguation value of PB/FN remains unproven — a dedicated
  polysemy experiment on a larger contrast set is required.
