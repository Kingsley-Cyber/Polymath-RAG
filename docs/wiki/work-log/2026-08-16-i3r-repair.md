---
change_id: i3r-repair
owner: governance
date: 2026-08-16
last_reviewed: 2026-08-16
last_touched: 2026-08-16
status: complete
architecture_impact: extraction policy (rule pack v1.2.0 default), durable
  mentions schema (migration 0009/0010), verifier orphan semantics,
  query_ready revocation path
---

# Work Log: 2026-08-16 — I3R repository-realigned repair

## Contract

Authorized I3R repair of the concrete defects established by R0/I3, in
the mandated order, with one commit per repair boundary. Frozen:
GLiNER model/revision/threshold, profile routing, embedding model,
retrieval-summary-v2, FAST/HYBRID/GRAPH, RRF, G3, E5 contracts, I3
documents/gold/questions. No new learned models, no parser
requirement, no tuning after gold freeze.

## Changes

- I3R-R1 `8a0e89f`: typed trigger contract (EvidenceSpan
  lexical_class/predicate_id/match_source; compiler tests only the
  authorizing arm; legacy untyped spans unchanged), bounded verb-form
  matching (s/es/d/ed/ing + e-drop/y-drop/doubling), rule pack v1.2.0
  as production default (uses noun triggers → multiword constructions;
  founded Organization-object signatures only).
- I3R-R2 `a3ea99f` + `8507efa`: trigger-scoped argument frames,
  predicate-region boundaries, bounded single-sided entity lists,
  double-list fail-closed, referential gate for the association frame,
  type-compatible nearest-slot selection.
- I3R-R3 `f7bc691`: bounded definite-description resolver (head match
  / unique-org), alias-only identity, two-pass extraction with doc
  entity history.
- I3R-R4 `bd79c1b`: mentions table (migration 0009) persisting every
  accepted GLiNER proposal; factless referential entities durable;
  graph stays fact-driven.
- I3R-R5 `296eb7e`: verifier orphan semantics (deletion requires no
  authoritative source), in-flight projection keeping + degraded
  re-entry, invalidate_corpus_projections (in-place attempt updates),
  census neo4j receipt gaps.
- I3R-R6/R7 `27b8716`: exact-evidence-v1 offsets (migration 0010),
  real GLiNER pin in the extract manifest, unresolved-pin repo guard.

## Proof

- Q1 frozen locks 50/3/3 both arms; E3B gates baseline unchanged.
- I3 rerun: FP 8→0 (facts), mentions 0→84, durable factless entities
  74, reconstruction hash_equal, retrieval 30/30, negatives 13/13,
  replay/order/concurrency/interrupt/versioning all pass.
- R5 live integration test: query_ready → invalidation → census
  re-drive → reconvergence with receipts restored.
- Full suites: 243 unit passed / 42 skipped; integration 37 passed +
  1 live-pipeline-required test (passes with the pipeline up) +
  1 skipped; guards green.

## Rejected claims

- No production acceptance: I3 is a repair regression; I4 (fresh
  untouched holdout) is required before declaring production ingestion
  complete.
- The I3 fact recall limitation (0/3) is reported as a frozen
  signature/discovery boundary (GLiNER Organization typing of Okta,
  Technology-subject uses signature, coordinated-VP subject), not
  repaired by weakening precision.

## Open contract gaps

- I4 fresh heterogeneous holdout (authorization required).
- Whether the uses Technology-subject signature and GLiNER label
  typing for identity-provider products should change is a future
  frozen-contract decision, not an I3R item.
