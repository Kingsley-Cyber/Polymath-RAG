---
change_id: CONSOLIDATION-MIGRATION-PHASE12-REAL-ECOMMERCE-E2E
owner: "@king"
date: 2026-09-21
status: complete
architecture_impact: "none in this slice (evidence + records). Embedded Trail is now the live mode (`.env`: POLYMATH_TRAIL_MODE=embedded + a durable POLYMATH_TRAIL_STORE); the three defects the runs found were fixed in their own slices (11.381 – 11.383)."
last_reviewed: 2026-09-21
---

# Consolidation migration — Phase 12: the first complete REAL ecommerce run

## Contract
Owner direction 2026-09-21 "finish core functionality": real corpus knowledge, real domain reasoning, real external evidence, real product reality, real sourcing, real Trail governance, through the hosted endpoint as a non-admin
principal; the harness executes the SYSTEM's directives and does not repair them silently; capability coverage in the owner's vocabulary; the dossier reviewed as a product artifact. Corpus for this run: `cinema` (owner's instruction).
`EXECUTION_PLAN.md` Phase 12. Decision `AUTO_DECISIONS.md` M-023.

## Changes
- Embedded Trail activated in production at a quiescent point (nothing leased, 0 open adapter runs): two non-secret `.env` lines, clean drain + boot, 13 worker types healthy on one bundle, the live adapter worker carries the mode and
  the store path. Targeted governed check through the worker's own client: a real `registry.project` (12 priors, snapshot `trs-da9942986e52`), identical replay from the durable store after a simulated restart, a typed
  `SNAPSHOT_MISMATCH` refusal, one audit row persisted. The external Trail daemon is not running and is not needed.
- Acceptance principal `prn_accept_e2e` (profile `friend`, corpus `cinema`, adapter `ecommerce.product_research`; key in a 0600 file, never printed). To be revoked after acceptance.
- Five real runs (table + capability coverage: `docs/migration/PARITY_MATRIX.md`; text-free summary: `eval/consolidation_e2e/2026-09-21-real-ecommerce-e2e.json`). Run 5 COMPLETED: 78 accepted steps, 3 research rounds, 5 Trail admissions,
  judgement ×3, territory, qualification ×2, score refusal `HARD_GATE_UNMET`, 5 concepts × 2 variations, real competitors with prices, 4 supplier leads with printed price / MOQ, governed dossier rendered by the engine's renderer.
- Agent-host conduct, recorded because it shapes what the evidence means: run 1's hypotheses and population names were verbose and its gaps were nested, which produced useless compiled queries — shown, not hidden; from run 2 the agent
  wrote plain community names and emitted top-level gaps (the adapter's own inputs), and the system's chain produced usable directives. Runs 3 – 5 replayed this agent's own earlier answers with ids remapped and every directed query
  re-executed for real; observations are the same real sources read earlier that day. Product-reality templates were executed both literally and with the adapter-supplied product terms; both are in the tool trace.

## Proof
- Evidence class REAL_INPUT_EXECUTED for every capability listed as such in the parity matrix; vantage = the host through the public hostname (NOT external-machine proof).
- Live database: run 5 `completed`, `owner_principal_id = prn_accept_e2e`; admissions 8 / 20 · 0 / 6 · 0 / 0 · 5 / 9 · 9 / 9 with Trail's reasons; Trail audit store 42 operations.
- Dossier review against the owner's list: present — problem, populations, lived clusters + situations, corpus evidence packets, admitted / rejected field observations with reasons, hypotheses with revisions, contradiction, 5 concepts
  + variations, supplier leads with price / MOQ / URL, score refusals, registry snapshot, unresolved, audit. MISSING — a section of real existing products (names, brands, prices, links), Trail's qualification gate results, clickable links.

## Rejected claims
- "The product is accepted." No: the run used `cinema`, not the commerce corpus; host vantage only; a positive Trail score was not reached; the dossier has three product-artifact gaps; `commerce-v1` has two failed extractions.
- "Four failed runs mean the design is wrong." Three were boundary defects between Polymath and Trail that scripted inputs could not reach (fixed, pinned); one was a correct typed refusal caused by the harness driver.
- "Trail's refusal is a bug." It is deterministic and specific: one more competitor review analysis and a supply risk review are missing for the strongest hypothesis; twelve observations were stale beyond a 14-day policy.

## Open contract gaps
- None changed here. Findings recorded for their owners (parity matrix): query compiler quality · ledger gaps not feeding `gaps.compile` · evidence display cap · non-citable `doc:` / `fact:` rows · hidden vocabularies in shown schemas ·
  no polarity in the receipt · product-reality templates keyed on territory names · interpretation objective vs validator · dossier gaps · any other Trail request-validation failure at an admission step still ends a run. **DEFERRED**,
  each non-blocking for a governed outcome.
