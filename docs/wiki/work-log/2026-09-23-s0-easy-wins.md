---
change_id: DOCUMENT-RAG-S0-EASY-WINS
owner: "@king"
date: 2026-09-23
status: complete
architecture_impact: "shared/ code on branch fix/document-rag-easy-wins (NOT merged, NOT live). Three fixes: the compiler's raw-HTTP reasoning placement (E1), bridge-compiler labels use real atom text (E2), and the resolution lift off for every intent (E8, owner decision D3). Merge + bounce wait for the owner's word."
last_reviewed: 2026-09-23
---

# DOCUMENT-RAG-COMPLETION-V1 · S0 — easy wins E1, E2, E8

## Contract
The owner, 2026-09-23: "Are there easy fixes we can make now to lessen the load for easy wins and then pass off in a new
context window…" The plan of record (`docs/wiki/plans/DOCUMENT-RAG-COMPLETION-V1.md` §9 S0) names E1, E4, E8 and E2. E8 is
owner decision D3: "switch off resolution lift".

## Changes
- **E1:** `shared/polymath_shared/reasoning_policy.py` `apply_chat_completions`. The chat compiler posts with raw `httpx`,
  which never unpacks an `extra_body` key, so provider passthrough fields now go at the TOP level of the JSON body. DeepSeek's
  `thinking: {"type": "disabled"}` reached the server as an ignored literal `extra_body` field. Qwen is unchanged
  (`thinking_budget: 300` top-level, REASONING-BOUNDARY-V1).
- **E2:** `shared/polymath_shared/bridge_integration.py` `concepts_from_nominations`. The label precedence is now atom text →
  title → source name → surface → doc id; before, the atom KIND ("THEORY") won.
- **E8:** `shared/polymath_shared/query_intent.py` `INTENT_POLICY`: `resolution_lift="off"` for all 10 intents. The previous
  values are kept in a comment for the repair; `apply_intent_policy` therefore sets `resolution_lift_enabled=False`.
- **Tests (added, none edited):**
  - `test_reasoning_policy.py::test_compiler_raw_http_body_carries_switches_top_level` captures the exact JSON handed to httpx
    for the DeepSeek and Qwen compiler lanes;
  - `test_bridge_integration.py::test_concept_label_is_the_atom_text_not_its_kind_name`, a live-shaped nomination.
  - Also a lint fix inside my own new test (instance `headers`).
- NOT done in S0: E4 (the WILDCARD atom-frontier receipt). It is carried to the next session with E3 / E5 / E6 / E7.

## Proof
- **Red first:**
  - the E1 test failed on the unfixed code: "a literal extra_body key is never unpacked on a raw HTTP call", with the body
    showing `extra_body`;
  - the E2 test failed on the unfixed code.
  - Both pass after the fix.
- **Import origins** (worktree, PYTHONPATH recipe): `polymath_shared` → `pmv4-easy-wins/shared/polymath_shared/__init__.py`.
- **Suites** (`-k "not test_live_"`): test_bridge_integration, test_bridge_compiler, test_latent_selection,
  test_profile_scout, test_reasoning_policy (19 / 19), test_u1_intent_routing_contract, test_candidate_engine,
  test_resolution_lift and test_resolution_lift_gather: 142 passed.
- test_chat_modes: 26 passed, 1 failed on BOTH the branch and production `1c01dc8`. The failure is
  `test_wildcard_sweep_overlaps_the_core…` (`sweep_done_before_core is False`, line 327): pre-existing, not caused by S0.
- Proof level: UNIT_PROVEN (`shared/`, executed path = worktree). Not merged, not deployed. The live effect (Alibaba compiler
  lane fallbacks, lift absent from the funnel) is measured after merge + bounce.
- Guards: see the commit.

## Rejected claims
- "E1 changes the Qwen policy": it does not. Disabling Qwen's compiler thinking entirely would conflict with the governing
  test `test_qwen_chat_completions_uses_thinking_budget_not_effort` (REASONING-BOUNDARY-V1). That is an owner decision,
  reported and not taken.
- "E1 proves the Alibaba lanes will stop falling back": it proves what we send. The fallback rate is measured after the
  bounce (hypothesis promotion rule).
- "E8 deletes the lift": the lane code and its tests remain; only the intent policy turns it off.

## Open contract gaps
The contract-impact hook flagged QUERY_INTENT [live] with 11 transitive contracts. The impacted suites were run in the
worktree without `POLYMATH_PG_DSN`: 109 passed, 2 failed. Both failures fail identically on production `1c01dc8`, so they are
pre-existing:
- `test_chat_runtime::test_compiler_on_drives_the_same_retrieval_decision_on_both_routes`;
- `test_query_receipts::test_all_three_query_handlers_and_read_surfaces_are_wired`.

Dispositions:
- QUERY_INTENT: UPDATED. The lift is off in `INTENT_POLICY`; `test_u1_intent_routing_contract` derives its expectations
  from the policy and stays green.
- `apply_chat_completions` wire placement, and the bridge-integration labels: UPDATED (tests added).
- TESTED_UNCHANGED (their suites green, apart from the two pre-existing failures): CANDIDATE_ENGINE, QUERY_PLANNER,
  SUBQUERY_PROVENANCE, RETRIEVAL_RECEIPT, EVIDENCE_PACKET, RESOLUTION_STATE, PROFILE_YIELD_RECEIPT, MCP_SURFACE
  (`test_mcp_server_v2`).
- ACCEPTANCE: DEFERRED. `tests/integration/test_cross_domain_routing.py` skips on production (7 skipped) and cannot be
  collected under the worktree recipe (it imports `orchestrator.orchestrator…`).
- ADAPTER_RUNTIME and EVIDENCE_BOUNDARY_API: DEFERRED. Not run: the adapter / MCP-principals / hosted-acceptance suites share
  the fleet Postgres or hosted services (the isolation rule). Run them in the qualification window after merge.
- DEFERRED: E4, E3, E5, E6, E7 (next session, plan §9).
- BLOCKED: merge into `production` + bounce, on the owner's word.
