__pycache__/
*.pyc
candidates/*
!candidates/.gitkeep
registry/compiled/
exports/
# field-evidence ledger: Reddit authors + verbatim quotes — local only, never in this PUBLIC repo (it lives in Polymath's field-evidence corpus)
registry/research_evidence.csv
# maintenance patches are review artifacts; applying + committing them is the promotion
registry/patches/
state/
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal
